---
phase: 07-onboarding-rollen
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - mobile/src/navigation/AppNavigator.tsx
  - mobile/src/screens/admin/VolunteerManagementScreen.tsx
  - backend/src/modules/admin/admin.router.ts
  - backend/src/modules/admin/admin.service.ts
  - backend/src/modules/admin/admin.repository.ts
  - backend/src/app.ts
autonomous: true
requirements:
  - ROLE-01
  - ROLE-02
  - ROLE-03
  - ROLE-04

must_haves:
  truths:
    - "Volunteer sieht nur Items-Tab und Neu-Tab (kein Dashboard, kein StoreInfo, keine Kampagnen, keine Badges)"
    - "Admin sieht alle Tabs: Dashboard, Items, Neu, StoreInfo, Kampagnen, Badges, Volunteer-Verwaltung"
    - "Admin kann neuen Volunteer-Account erstellen (POST /api/admin/users)"
    - "Admin kann Liste aller Volunteers sehen (GET /api/admin/users)"
    - "Nicht-Admins erhalten 403 auf /api/admin/* Endpunkte"
  artifacts:
    - path: "mobile/src/navigation/AppNavigator.tsx"
      provides: "AdminTabs und VolunteerTabs klar getrennt (role-basiert)"
      contains: "AdminTabs"
    - path: "mobile/src/screens/admin/VolunteerManagementScreen.tsx"
      provides: "Admin-Screen zum Anzeigen und Erstellen von Volunteer-Accounts"
      exports: ["VolunteerManagementScreen"]
    - path: "backend/src/modules/admin/admin.router.ts"
      provides: "GET /api/admin/users und POST /api/admin/users"
      exports: ["adminRouter"]
    - path: "backend/src/modules/admin/admin.service.ts"
      provides: "Business-Logik für Volunteer-Erstellung und Listing"
      exports: ["listVolunteers", "createVolunteer"]
    - path: "backend/src/modules/admin/admin.repository.ts"
      provides: "DB-Queries: User-Liste nach Rolle filtern"
      exports: ["findUsersByRole", "createUser"]
  key_links:
    - from: "AppNavigator.tsx"
      to: "authStore.user.role"
      via: "useAuthStore hook — role === 'admin' → AdminTabs, role === 'volunteer' → VolunteerTabs"
      pattern: "role === 'admin'"
    - from: "admin.router.ts"
      to: "requireRole('admin')"
      via: "authenticateToken + requireRole Middleware"
      pattern: "requireRole.*admin"
    - from: "VolunteerManagementScreen.tsx"
      to: "POST /api/admin/users"
      via: "fetch mit Bearer Token aus authStore"
      pattern: "api/admin/users"
---

<objective>
Rollen-Trennung zwischen Admin und Volunteer: VolunteerTabs zeigt nur Items-bezogene Tabs, AdminTabs zeigt alles inklusive neuer Volunteer-Verwaltung. Backend-Endpunkte für Volunteer-Erstellung und -Listing durch Admin.

Purpose: Volunteer sieht kein Dashboard und keine Konfiguration (per D-Entscheidung); Admin kann Volunteer-Accounts anlegen (ROLE-04).
Output: Getrennte Tab-Navigator-Komponenten für Admin/Volunteer, VolunteerManagementScreen, neues admin-Backend-Modul.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-onboarding-rollen/07-01-SUMMARY.md

<interfaces>
<!-- Aus backend/src/middleware/auth.ts -->
```typescript
export function authenticateToken(req: Request, res: Response, next: NextFunction): void;
// Setzt req.user: AuthUser = { sub: string, storeId: string, role: 'admin'|'volunteer'|'visitor' }
```

<!-- Aus backend/src/middleware/requireRole.ts -->
```typescript
export function requireRole(...roles: AuthUser['role'][]): (req, res, next) => void;
// Gibt 403 zurück wenn req.user.role nicht in roles enthalten
```

<!-- Aus backend/src/modules/auth/auth.repository.ts -->
```typescript
export async function createUser(data: {
  username: string;
  email: string;
  passwordHash: string;
  storeId: string;
  role?: 'admin' | 'volunteer' | 'visitor';
}): Promise<User>;

export async function findUserByEmail(email: string): Promise<User | null>;
```

<!-- Aus backend/src/modules/auth/auth.types.ts -->
```typescript
export interface AuthUser {
  sub: string;
  storeId: string;
  role: 'admin' | 'volunteer' | 'visitor';
}
```

<!-- Aus mobile/src/theme/index.ts -->
```typescript
export const colors = { primary, primaryLight, background, surface, text, textSecondary, border, error, white, ... };
export const fonts = { regular, medium, semiBold, bold }; // WorkSans-*
export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 };
export const borderRadius = { sm: 8, md: 12, lg: 16, full: 999 };
```

<!-- Aus mobile/src/navigation/AppNavigator.tsx — aktueller VolunteerTabs (nach Plan 01 unverändert) -->
```typescript
// VolunteerTabs enthält aktuell: Dashboard, Items, Neu, StoreInfo, Kampagnen, Badges
// → In Plan 02: umbenennen zu AdminTabs, neues VolunteerTabs mit nur Items+Neu
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppNavigator — AdminTabs und VolunteerTabs sauber trennen</name>
  <files>mobile/src/navigation/AppNavigator.tsx</files>
  <action>
Die bestehende `VolunteerTabs`-Funktion zu `AdminTabs` umbenennen und einen neuen `VolunteerManagementScreen`-Tab hinzufügen. Danach eine echte, eingeschränkte `VolunteerTabs`-Funktion erstellen.

**1. VolunteerTabs → AdminTabs umbenennen + VolunteerManagement-Tab hinzufügen:**

```typescript
import VolunteerManagementScreen from '../screens/admin/VolunteerManagementScreen';

function AdminTabs() {
  return (
    <Tab.Navigator screenOptions={{ /* gleiche Options wie bisher */ }}>
      <Tab.Screen name="Dashboard" component={DashboardScreen} options={{ title: 'Dashboard' }} />
      <Tab.Screen name="Items" component={ItemListScreen} options={{ title: 'Kleidung' }} />
      <Tab.Screen name="Neu" component={ItemCreateScreen} options={{ title: 'Neu anlegen' }} />
      <Tab.Screen name="StoreInfo" component={StoreInfoScreen} options={{ title: 'Store-Info' }} />
      <Tab.Screen name="Kampagnen" component={CampaignListScreen} options={{ title: 'Kampagnen' }} />
      <Tab.Screen name="Badges" component={BadgeLevelsScreen} options={{ title: 'Badges' }} />
      <Tab.Screen name="Volunteers" component={VolunteerManagementScreen} options={{ title: 'Team' }} />
    </Tab.Navigator>
  );
}
```

**2. Neues VolunteerTabs — nur Items und Neu:**

```typescript
function VolunteerTabs() {
  return (
    <Tab.Navigator
      screenOptions={{
        headerShown: true,
        tabBarActiveTintColor: colors.primary,
        tabBarInactiveTintColor: colors.textSecondary,
        tabBarStyle: { backgroundColor: colors.surface, borderTopColor: colors.border },
        tabBarLabelStyle: { fontFamily: fonts.medium, fontSize: 11 },
        headerStyle: { backgroundColor: colors.primary },
        headerTintColor: colors.white,
        headerTitleStyle: { fontFamily: fonts.bold, fontSize: 17 },
      }}
    >
      <Tab.Screen name="Items" component={ItemListScreen} options={{ title: 'Kleidung' }} />
      <Tab.Screen name="Neu" component={ItemCreateScreen} options={{ title: 'Neu anlegen' }} />
    </Tab.Navigator>
  );
}
```

**3. AuthenticatedStack anpassen:**

```typescript
function AuthenticatedStack({ role }: { role: string | undefined }) {
  return (
    <Stack.Navigator screenOptions={{ headerShown: false, ... }}>
      {role === 'visitor' ? (
        <Stack.Screen name="Tabs" component={VisitorTabs} />
      ) : role === 'admin' ? (
        <Stack.Screen name="Tabs" component={AdminTabs} />
      ) : (
        // volunteer (und unbekannte Rollen als Fallback)
        <Stack.Screen name="Tabs" component={VolunteerTabs} />
      )}
      <Stack.Screen name="Privacy" component={PrivacyScreen} options={{ headerShown: true, title: 'Datenschutz' }} />
      <Stack.Screen name="CampaignCreate" component={CampaignCreateScreen} options={{ headerShown: true, title: 'Neue Kampagne' }} />
      <Stack.Screen name="BadgeLevels" component={BadgeLevelsScreen} options={{ headerShown: true, title: 'Badge-Stufen' }} />
    </Stack.Navigator>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript-Fehler = 0; AdminTabs hat 7 Tabs (Dashboard + Items + Neu + StoreInfo + Kampagnen + Badges + Team); VolunteerTabs hat 2 Tabs (Items + Neu); AuthenticatedStack routet role==='admin' zu AdminTabs, role==='volunteer' zu VolunteerTabs</done>
</task>

<task type="auto">
  <name>Task 2: Backend admin-Modul (Volunteer-Listing + Erstellung)</name>
  <files>backend/src/modules/admin/admin.repository.ts, backend/src/modules/admin/admin.service.ts, backend/src/modules/admin/admin.router.ts, backend/src/app.ts</files>
  <action>
Neues Backend-Modul `backend/src/modules/admin/` mit drei Dateien. Verzeichnis anlegen.

**admin.repository.ts:**
```typescript
import { db } from '../../db/client';
import { users } from '../../db/schema';
import { eq } from 'drizzle-orm';

export async function findUsersByStoreAndRole(storeId: string, role: 'volunteer' | 'visitor' | 'admin') {
  return db
    .select({
      id: users.id,
      username: users.username,
      email: users.email,
      role: users.role,
      createdAt: users.createdAt,
    })
    .from(users)
    .where(eq(users.storeId, storeId) && eq(users.role, role));
  // Hinweis: Drizzle-ORM and()-Syntax: import { and } from 'drizzle-orm'
  // Korrekte Schreibweise: .where(and(eq(users.storeId, storeId), eq(users.role, role)))
}

export async function createUser(data: {
  username: string;
  email: string;
  passwordHash: string;
  storeId: string;
  role: 'volunteer';
}) {
  const result = await db.insert(users).values(data).returning({
    id: users.id,
    username: users.username,
    email: users.email,
    role: users.role,
    createdAt: users.createdAt,
  });
  return result[0];
}
```

Wichtig: `and` aus `drizzle-orm` importieren für kombinierte WHERE-Bedingungen.

**admin.service.ts:**
```typescript
import bcrypt from 'bcrypt';
import * as adminRepo from './admin.repository';

const BCRYPT_ROUNDS = 12;

export async function listVolunteers(storeId: string) {
  return adminRepo.findUsersByStoreAndRole(storeId, 'volunteer');
}

export interface CreateVolunteerBody {
  username: string;
  email: string;
  password: string;
}

export async function createVolunteer(body: CreateVolunteerBody, storeId: string) {
  const passwordHash = await bcrypt.hash(body.password, BCRYPT_ROUNDS);
  return adminRepo.createUser({
    username: body.username,
    email: body.email,
    passwordHash,
    storeId,
    role: 'volunteer',
  });
}
```

**admin.router.ts:**
```typescript
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
import * as adminService from './admin.service';

const router = Router();

const CreateVolunteerSchema = z.object({
  username: z.string().min(3).max(50),
  email: z.string().email(),
  password: z.string().min(8),
});

// GET /api/admin/users?role=volunteer
router.get('/users', authenticateToken, requireRole('admin'), async (req: Request, res: Response) => {
  const storeId = req.user!.storeId;
  const volunteers = await adminService.listVolunteers(storeId);
  res.json({ users: volunteers });
});

// POST /api/admin/users
router.post('/users', authenticateToken, requireRole('admin'), async (req: Request, res: Response) => {
  const body = CreateVolunteerSchema.parse(req.body);
  const storeId = req.user!.storeId;
  const volunteer = await adminService.createVolunteer(body, storeId);
  res.status(201).json({ user: volunteer });
});

export default router;
```

**app.ts — adminRouter einbinden:**
Zeile nach dem letzten `app.use('/api/badges', badgesRouter);` hinzufügen:
```typescript
import adminRouter from './modules/admin/admin.router';
// ...
app.use('/api/admin', adminRouter);
```

Nach Backend-Änderungen: Deploy via `git add backend/src/modules/admin/ backend/src/app.ts && git commit -m "feat(07-02): admin modul volunteer verwaltung" && git push`, dann auf Server: `ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn && git pull && docker compose build backend && docker compose up -d backend"`. Test: `curl -sk https://xn--plietsche-plnn-rsb.de/api/admin/users -H "Authorization: Bearer TOKEN"` → 403 ohne Admin-Token, 200 mit Admin-Token.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript-Fehler = 0; 3 neue Dateien in backend/src/modules/admin/; app.ts importiert adminRouter; GET /api/admin/users gibt 403 ohne Admin-JWT, 200 mit Admin-JWT; POST /api/admin/users erstellt Volunteer-Account mit role='volunteer'</done>
</task>

<task type="auto">
  <name>Task 3: VolunteerManagementScreen für Admin</name>
  <files>mobile/src/screens/admin/VolunteerManagementScreen.tsx</files>
  <action>
Neuer Screen `mobile/src/screens/admin/VolunteerManagementScreen.tsx`.

**Funktionalität:**
- Lädt beim Mount die Volunteer-Liste via `GET /api/admin/users` (Bearer Token aus authStore)
- Zeigt Liste der Volunteers: Name, E-Mail, Datum der Erstellung
- Formular am Ende der Seite zum Erstellen eines neuen Volunteer-Accounts: Felder Username, E-Mail, Passwort
- Absendelogik: `POST /api/admin/users` mit den Formulardaten → bei Erfolg wird die Liste neu geladen und das Formular geleert
- Fehler werden via `Toast.show({ type: 'error', text1: '...' })` aus `react-native-toast-message` angezeigt

**Layout:**
```typescript
import React, { useEffect, useState } from 'react';
import { View, Text, FlatList, TextInput, TouchableOpacity, StyleSheet, Alert } from 'react-native';
import Toast from 'react-native-toast-message';
import { useAuthStore } from '../../store/authStore';
import { colors, fonts, spacing, borderRadius } from '../../theme';

// API-Basis-URL aus bestehendem Pattern (import aus api-client oder direkt)
const API_BASE = 'https://xn--plietsche-plnn-rsb.de/api';
```

**State:**
```typescript
interface VolunteerUser {
  id: string;
  username: string;
  email: string;
  role: string;
  createdAt: string;
}

const [volunteers, setVolunteers] = useState<VolunteerUser[]>([]);
const [loading, setLoading] = useState(false);
const [username, setUsername] = useState('');
const [email, setEmail] = useState('');
const [password, setPassword] = useState('');
const [creating, setCreating] = useState(false);
```

**loadVolunteers-Funktion:**
```typescript
async function loadVolunteers() {
  setLoading(true);
  try {
    const token = useAuthStore.getState().token;
    const res = await fetch(`${API_BASE}/admin/users`, {
      headers: { Authorization: `Bearer ${token}` },
    });
    if (!res.ok) throw new Error('Laden fehlgeschlagen');
    const data = await res.json();
    setVolunteers(data.users);
  } catch (e) {
    Toast.show({ type: 'error', text1: 'Fehler beim Laden der Volunteers' });
  } finally {
    setLoading(false);
  }
}
```

**createVolunteer-Funktion:**
```typescript
async function handleCreate() {
  if (!username.trim() || !email.trim() || password.length < 8) {
    Toast.show({ type: 'error', text1: 'Alle Felder ausfüllen (Passwort mind. 8 Zeichen)' });
    return;
  }
  setCreating(true);
  try {
    const token = useAuthStore.getState().token;
    const res = await fetch(`${API_BASE}/admin/users`, {
      method: 'POST',
      headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' },
      body: JSON.stringify({ username: username.trim(), email: email.trim(), password }),
    });
    if (!res.ok) {
      const err = await res.json();
      throw new Error(err.error ?? 'Fehler beim Erstellen');
    }
    Toast.show({ type: 'success', text1: 'Volunteer-Account erstellt' });
    setUsername(''); setEmail(''); setPassword('');
    loadVolunteers();
  } catch (e: any) {
    Toast.show({ type: 'error', text1: e.message ?? 'Fehler beim Erstellen' });
  } finally {
    setCreating(false);
  }
}
```

**JSX-Struktur:**
- Outer `ScrollView` mit `backgroundColor: colors.background`, `padding: spacing.md`
- Abschnitt 1 "Team-Mitglieder" (Überschrift `fonts.bold`, 18px): Liste der Volunteers als `FlatList` (oder einfache Map in ScrollView), jede Zeile: Name in `fonts.semiBold`, E-Mail in `fonts.regular`, `color: colors.textSecondary`
- Abschnitt 2 "Neues Mitglied anlegen": TextInputs mit `borderWidth: 1`, `borderColor: colors.border`, `borderRadius: borderRadius.sm`, `padding: spacing.sm`, `fontFamily: fonts.regular`. Passwort-Input mit `secureTextEntry`. Submit-Button mit `backgroundColor: colors.primary`, `borderRadius: borderRadius.sm`, Text "Anlegen" in `fonts.bold`, `color: colors.white`.
- `useEffect(() => { loadVolunteers(); }, [])` im Mount

Export: `export default function VolunteerManagementScreen()`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript-Fehler = 0; Screen lädt Volunteer-Liste; Formular erstellt neuen Account via POST /api/admin/users; Erfolgs-/Fehlermeldungen via Toast; Liste wird nach Erstellung neu geladen</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Mobile → Backend /api/admin/* | Nur Admin-JWT darf diese Endpunkte erreichen |
| Backend → DB (Users-Tabelle) | Admin kann nur Users innerhalb seiner storeId verwalten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Elevation of Privilege | POST /api/admin/users | mitigate | requireRole('admin') Middleware blockiert Volunteer/Visitor (403); JWT ist Server-signiert, nicht manipulierbar |
| T-07-02-02 | Tampering | Store-Isolation bei Volunteer-Erstellung | mitigate | storeId kommt aus req.user!.storeId (JWT, Server-seitig) — nicht aus Request-Body; Volunteer wird automatisch zur richtigen Store zugeordnet |
| T-07-02-03 | Information Disclosure | GET /api/admin/users | mitigate | requireRole('admin') + storeId-Filter im Query — Admin sieht nur eigene Store-Volunteers |
| T-07-02-04 | Elevation of Privilege | Client-seitige Tab-Sichtbarkeit | accept | VolunteerTabs rendert nur Items+Neu; selbst wenn Client manipuliert wird, schützen Backend-Endpunkte mit requireRole |
| T-07-02-05 | Spoofing | Passwort-Übertragung im Create-Volunteer-Request | mitigate | HTTPS (TLS) auf xn--plietsche-plnn-rsb.de; Passwort wird mit bcrypt(12) gehasht, nie im Klartext gespeichert |
</threat_model>

<verification>
1. TypeScript (Backend): `cd backend && npx tsc --noEmit` — 0 Fehler
2. TypeScript (Mobile): `cd mobile && npx tsc --noEmit` — 0 Fehler
3. Backend Deploy + Smoke-Test:
   - `curl -sk https://xn--plietsche-plnn-rsb.de/api/admin/users` → 401 (kein Token)
   - `curl -sk https://xn--plietsche-plnn-rsb.de/api/admin/users -H "Authorization: Bearer VOLUNTEER_TOKEN"` → 403
   - `curl -sk https://xn--plietsche-plnn-rsb.de/api/admin/users -H "Authorization: Bearer ADMIN_TOKEN"` → 200 `{ users: [...] }`
   - `curl -sk -X POST https://xn--plietsche-plnn-rsb.de/api/admin/users -H "Authorization: Bearer ADMIN_TOKEN" -H "Content-Type: application/json" -d '{"username":"testvolunteer","email":"tv@test.de","password":"secure123"}' ` → 201
4. Manuell (App): Login als Admin → "Team"-Tab sichtbar → Volunteer-Liste wird geladen → neuer Account anlegen funktioniert
5. Manuell (App): Login als Volunteer → nur "Kleidung" und "Neu anlegen" Tabs sichtbar, kein Dashboard
</verification>

<success_criteria>
- AdminTabs zeigt 7 Tabs; VolunteerTabs zeigt 2 Tabs (Items + Neu)
- role === 'admin' → AdminTabs, role === 'volunteer' → VolunteerTabs (per AppNavigator)
- GET /api/admin/users: 401 ohne Token, 403 für Volunteer-Token, 200 für Admin-Token
- POST /api/admin/users erstellt User mit role='volunteer' in gleicher storeId wie Admin
- VolunteerManagementScreen zeigt Liste + Formular, Toast-Feedback bei Erfolg/Fehler
- TypeScript (backend + mobile) kompiliert fehlerfrei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/07-onboarding-rollen/07-02-SUMMARY.md` erstellen mit:
- Was implementiert wurde
- Datei-Änderungen
- Deploy-Schritte (git push + SSH server pull/build)
- Curl-Test-Ergebnisse
</output>
