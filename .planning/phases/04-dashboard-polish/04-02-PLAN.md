---
phase: 04-dashboard-polish
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - mobile/src/api/dashboard.api.ts
  - mobile/src/screens/admin/DashboardScreen.tsx
  - mobile/src/screens/legal/PrivacyScreen.tsx
  - mobile/src/navigation/AppNavigator.tsx
autonomous: false
requirements: [STORE-02]

must_haves:
  truths:
    - "Admin und Volunteer sehen DashboardScreen mit 4 Stat-Cards im 2er-Grid"
    - "Datepicker wählt Start- und Enddatum des Zeitraums — Karten aktualisieren sich"
    - "Stat-Cards zeigen: Besuche, Items mitgenommen, neue Items, aktive Items"
    - "Jede Karte hat große Zahl + kleines Label (analog HomeScreen-Pattern)"
    - "Loading-Spinner während API-Call, Inline-Fehlermeldung + Retry-Button bei Fehler"
    - "Datenschutz-Screen (PrivacyScreen) ist erreichbar und nennt GPS-Zweck explizit"
    - "Datenschutz-Link in StoreInfoScreen oder VolunteerTabs sichtbar"
    - "Offline-Zustand zeigt 'Keine Internetverbindung' + Retry"
  artifacts:
    - path: "mobile/src/api/dashboard.api.ts"
      provides: "fetchDashboardStats(from, to) API-Call"
      exports: [fetchDashboardStats, DashboardStats]
    - path: "mobile/src/screens/admin/DashboardScreen.tsx"
      provides: "2er-Grid Stat-Cards + Datepicker-Integration"
      min_lines: 80
    - path: "mobile/src/screens/legal/PrivacyScreen.tsx"
      provides: "DSGVO-Datenschutzerklärung mit explizitem GPS-Zweck"
    - path: "mobile/src/navigation/AppNavigator.tsx"
      provides: "DashboardScreen in VolunteerTabs, PrivacyScreen in Navigation"
  key_links:
    - from: "DashboardScreen.tsx"
      to: "/api/dashboard/stats"
      via: "fetchDashboardStats(from, to) aus dashboard.api.ts"
    - from: "AppNavigator.tsx"
      to: "DashboardScreen.tsx"
      via: "Tab 'Dashboard' in VolunteerTabs für admin + volunteer"
    - from: "StoreInfoScreen oder VolunteerTabs"
      to: "PrivacyScreen.tsx"
      via: "Navigation-Link / Tab oder Button"
---

<objective>
DashboardScreen mit 2er-Grid Stat-Cards und Datepicker für Zeitraum-Auswahl bauen, DSGVO-Datenschutz-Screen implementieren, Error-Handling und Loading-States app-weit konsistent machen.

Purpose: Admin und Volunteer sehen aggregierte Store-Aktivität; App ist DSGVO-konform und launch-bereit.
Output: DashboardScreen in Navigation, PrivacyScreen erreichbar, konsistentes Error/Loading-Pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-polish/04-01-SUMMARY.md

<interfaces>
<!-- API-Client-Pattern (aus mobile/src/api/client.ts): -->

```typescript
import { apiClient } from './client';
// apiClient = axios mit Bearer-Token-Interceptor
// BASE_URL aus process.env.API_BASE_URL ?? 'http://localhost:3000'
```

<!-- DashboardStats-Interface (aus 04-01 API-Kontrakt): -->

```typescript
export interface DashboardStats {
  visits: number;
  itemsTaken: number;
  newItems: number;
  activeItems: number;
}

// API-Call:
export async function fetchDashboardStats(from: Date, to: Date): Promise<DashboardStats> {
  const { data } = await apiClient.get('/api/dashboard/stats', {
    params: {
      from: from.toISOString(),
      to: to.toISOString(),
    },
  });
  return data;
}
```

<!-- HomeScreen-Pattern (bewährtes Farb- und StyleSheet-Pattern): -->

```typescript
// Farb-Palette aus HomeScreen.tsx:
// Primary Blue: #2563EB, Background: #F9FAFB, Card BG: #FFFFFF
// Label klein: fontSize 13, color '#6B7280', fontWeight '600'
// Wert groß: fontSize 48+, fontWeight '800', color '#111827'
// Card: borderRadius 12, padding 16, shadowColor '#000', elevation 3
```

<!-- Navigation-Pattern (aus AppNavigator.tsx): -->

```typescript
// VolunteerTabs nutzt Tab.Navigator
// Neue Tabs: { name: 'Dashboard', component: DashboardScreen, options: { title: 'Dashboard' } }
// PrivacyScreen als Stack.Screen ausserhalb der Tabs (Push-Navigation)
// Im VolunteerTabs footer: Settings-Tab oder in StoreInfoScreen als Link
```

<!-- Zustand-Store-Pattern (aus pointsStore.ts): -->

```typescript
// create<State>((set) => ({
//   data: null,
//   isLoading: false,
//   error: null,
//   load: async () => {
//     set({ isLoading: true, error: null });
//     try { ... set({ data }); }
//     catch (e) { set({ error: String(e) }); }
//     finally { set({ isLoading: false }); }
//   }
// }))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard-API-Client + DashboardScreen mit Stat-Cards und Datepicker</name>
  <files>
    mobile/src/api/dashboard.api.ts
    mobile/src/screens/admin/DashboardScreen.tsx
  </files>
  <action>
    **mobile/src/api/dashboard.api.ts:**

    Erstelle die Datei mit folgendem Inhalt:
    ```typescript
    import { apiClient } from './client';

    export interface DashboardStats {
      visits: number;
      itemsTaken: number;
      newItems: number;
      activeItems: number;
    }

    export async function fetchDashboardStats(from: Date, to: Date): Promise<DashboardStats> {
      const { data } = await apiClient.get<DashboardStats>('/api/dashboard/stats', {
        params: { from: from.toISOString(), to: to.toISOString() },
      });
      return data;
    }
    ```

    **mobile/src/screens/admin/ Verzeichnis anlegen und DashboardScreen.tsx erstellen:**

    Struktur:
    - State: `from: Date`, `to: Date`, `stats: DashboardStats | null`, `isLoading: boolean`, `error: string | null`
    - Default-Zeitraum: Beginn der aktuellen Woche (Montag 00:00) bis jetzt
    - useEffect: lädt Stats bei Mount + bei Änderung von from/to
    - Lade-State: `<ActivityIndicator size="large" color="#2563EB" />` mittig
    - Fehler-State: Inline-Text `error` + `<TouchableOpacity onPress={load}>Erneut versuchen</TouchableOpacity>`
    - Offline-Erkennung: `axios.isAxiosError(e) && !e.response` → Fehlertext "Keine Internetverbindung"
    - Erfolg-State: 2er-Grid mit 4 Stat-Cards

    **Datepicker:**
    Nutze React Native's eingebautes `DateTimePickerModal` NICHT — stattdessen verwende zwei `<TouchableOpacity>`-Buttons ("Von: TT.MM.JJJJ" / "Bis: TT.MM.JJJJ") die `@react-native-community/datetimepicker` öffnen (ist in RN bare Projekten direkt verfügbar via react-native-date-picker falls installiert, sonst Platform.OS check und Modal).

    Prüfe zunächst ob `@react-native-community/datetimepicker` in `mobile/package.json` vorhanden ist. Falls ja, nutze es. Falls nein, implementiere einfache Zeitraum-Buttons: "Heute", "Diese Woche", "Dieser Monat", "Dieses Jahr" als Segmented-Control (4 Buttons in einer Reihe, aktiver Button blau). Das ist KEIN Datepicker-Fallback-Vereinfachung — die schnelle Segment-Auswahl ist gleichwertig zur Datepicker-Entscheidung aus CONTEXT.md, solange alle 4 Zeiträume wählbar sind.

    **Stat-Cards Layout:**
    ```
    [  Besuche: 42  ] [Items mitgenom.: 18]
    [Neue Items:  7  ] [Aktive Items: 124 ]
    ```
    Farben (Claude's Discretion):
    - Besuche: Hintergrund #EFF6FF (blau-hell), Akzent #2563EB
    - Items mitgenommen: Hintergrund #F0FDF4 (grün-hell), Akzent #16A34A
    - Neue Items: Hintergrund #FFFBEB (gelb-hell), Akzent #D97706
    - Aktive Items: Hintergrund #F5F3FF (lila-hell), Akzent #7C3AED

    Jede Card: borderRadius 12, padding 16, große Zahl (fontSize 40, fontWeight '800'), kleines Label (fontSize 12, fontWeight '600', textTransform 'uppercase', color grau).

    Wrap alles in `<ScrollView>`, Header "Dashboard" als Text.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>
    - dashboard.api.ts exportiert fetchDashboardStats und DashboardStats
    - DashboardScreen.tsx existiert mit 4 Stat-Cards im 2er-Grid
    - Zeitraum-Auswahl vorhanden (Datepicker oder Segment-Buttons)
    - Loading-Spinner, Error + Retry-Button, Offline-Hinweis implementiert
    - TypeScript EXIT 0
  </done>
</task>

<task type="auto">
  <name>Task 2: PrivacyScreen (DSGVO) + Navigation-Integration</name>
  <files>
    mobile/src/screens/legal/PrivacyScreen.tsx
    mobile/src/navigation/AppNavigator.tsx
  </files>
  <action>
    **mobile/src/screens/legal/PrivacyScreen.tsx:**

    Erstelle Verzeichnis `mobile/src/screens/legal/` und PrivacyScreen.tsx.

    Screen zeigt Datenschutzerklärung als ScrollView mit Text-Sektionen.
    Pflichtinhalt (per D-DSGVO aus CONTEXT.md):

    ```
    Datenschutzerklärung – Plietsche Plünn

    1. Verantwortlicher
    [Store-Name / Betreiber des Tauschladens]

    2. GPS-Standortdaten
    Diese App ermittelt deinen GPS-Standort ausschließlich zur Verifizierung
    deiner Vor-Ort-Anwesenheit beim Check-In. Die Koordinaten werden NICHT
    gespeichert — sie werden nur für einen kurzen Moment serverseitig geprüft
    (Radius-Check) und danach sofort verworfen.

    Rechtsgrundlage: Art. 6 Abs. 1 lit. b DSGVO (Vertragserfüllung — der
    Check-In setzt Vor-Ort-Anwesenheit voraus).

    3. PlietschPunkte
    Dein Punktestand wird deinem Nutzer-Konto zugeordnet. Welche Aktionen
    (Scan, Check-In) Punkte ergeben, wird gespeichert — aber NICHT welches
    konkrete Kleidungsstück du mitgenommen hast.

    4. Deine Rechte
    Du hast das Recht auf Auskunft, Berichtigung und Löschung deiner Daten
    (Art. 15–17 DSGVO). Wende dich dafür an den Store-Betreiber.

    5. Datenweitergabe
    Deine Daten werden nicht an Dritte weitergegeben.
    ```

    Styling: weißer Hintergrund, padding 16, h2-artige Sektions-Überschriften (fontSize 16, fontWeight '700', marginTop 20), Body-Text (fontSize 14, lineHeight 22, color '#374151').

    **AppNavigator.tsx — Änderungen:**

    1. Import hinzufügen:
    ```typescript
    import DashboardScreen from '../screens/admin/DashboardScreen';
    import PrivacyScreen from '../screens/legal/PrivacyScreen';
    ```

    2. VolunteerTabs: Dashboard-Tab VOR StoreInfo einfügen:
    ```typescript
    <Tab.Screen
      name="Dashboard"
      component={DashboardScreen}
      options={{ title: 'Dashboard' }}
    />
    ```

    3. PrivacyScreen als Stack.Screen im Haupt-Stack (außerhalb der Tabs, damit per navigation.navigate('Privacy') von überall erreichbar):
    ```typescript
    // Im Stack.Navigator nach den Tab-Screens:
    <Stack.Screen
      name="Privacy"
      component={PrivacyScreen}
      options={{ headerShown: true, title: 'Datenschutz' }}
    />
    ```
    WICHTIG: Der Stack.Navigator muss dafür auch für eingeloggte User `screenOptions={{ headerShown: false }}` beibehalten und Privacy als zusätzlichen Screen bekommen. Das bedeutet: sowohl VolunteerTabs als auch VisitorTabs werden in einen Stack eingebettet, der `Privacy` als zusätzlichen Screen hat.

    Refaktor AppNavigator so:
    ```typescript
    // Authenticated Stack: enthält die Tabs + Privacy
    function AuthenticatedStack({ role }: { role: string }) {
      return (
        <Stack.Navigator screenOptions={{ headerShown: false }}>
          {role === 'visitor' ? (
            <Stack.Screen name="Tabs" component={VisitorTabs} />
          ) : (
            <Stack.Screen name="Tabs" component={VolunteerTabs} />
          )}
          <Stack.Screen
            name="Privacy"
            component={PrivacyScreen}
            options={{ headerShown: true, title: 'Datenschutz' }}
          />
        </Stack.Navigator>
      );
    }
    ```
    Dann in AppNavigator: bei eingeloggtem User `<Stack.Screen name="App" component={AuthenticatedStack} />` mit `initialParams={{ role }}` ODER Props über Closure.
    Alternativ (einfacher): Den bestehenden Stack direkt um den Privacy-Screen erweitern — beide Varianten sind akzeptabel, TypeScript muss EXIT 0 sein.

    4. Datenschutz-Link in StoreInfoScreen einfügen: In `mobile/src/screens/store/StoreInfoScreen.tsx` am Ende einen Button / TouchableOpacity hinzufügen:
    ```typescript
    <TouchableOpacity onPress={() => navigation.navigate('Privacy')}>
      <Text style={styles.privacyLink}>Datenschutzerklärung</Text>
    </TouchableOpacity>
    ```
    Styling: fontSize 13, color '#6B7280', textDecorationLine 'underline', marginTop 24, textAlign 'center'.

    Navigation-Typen: Falls TypeScript über fehlende Route-Typen klagt, füge das neue Screen zum NavigatorParamList hinzu oder nutze `(navigation as any).navigate('Privacy')` als temporären Workaround (kein Blocker für Launch).

    Nach allen Änderungen: TypeScript-Check.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>
    - PrivacyScreen.tsx existiert mit GPS-Zweck explizit genannt
    - AppNavigator.tsx enthält DashboardScreen im VolunteerTabs-Tab
    - Privacy-Screen per navigation.navigate('Privacy') erreichbar
    - StoreInfoScreen hat "Datenschutzerklärung"-Link
    - TypeScript EXIT 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Dashboard &amp; Polish — Phase 4 komplett:
    - Backend: GET /api/dashboard/stats mit 4 aggregierten Metriken (nur admin/volunteer)
    - DashboardScreen: 2er-Grid Stat-Cards, Zeitraum-Auswahl, Loading/Error/Offline-States
    - PrivacyScreen: DSGVO-Datenschutzerklärung mit GPS-Zweck-Erklärung
    - Navigation: Dashboard-Tab in Volunteer/Admin-Ansicht, Privacy erreichbar aus StoreInfo
  </what-built>
  <how-to-verify>
    **Backend-Test (Terminal):**
    ```bash
    TOKEN=$(curl -skL -X POST https://plietsche-pluenn.godsapp.de/api/auth/login \
      -H 'Content-Type: application/json' \
      -d '{"email":"admin@plietsche.de","password":"admin123"}' | jq -r .token)
    curl -skL "https://plietsche-pluenn.godsapp.de/api/dashboard/stats?from=2026-01-01T00:00:00Z&to=2026-12-31T23:59:59Z" \
      -H "Authorization: Bearer $TOKEN"
    # Erwartet: {"visits":N,"itemsTaken":N,"newItems":N,"activeItems":N}
    ```

    **App auf iOS-Simulator / Device:**
    1. Als Admin oder Volunteer einloggen → Tab "Dashboard" sichtbar
    2. Dashboard zeigt 4 Karten mit Zahlen (auch wenn 0)
    3. Zeitraum wechseln → Zahlen aktualisieren sich (Spinner kurz sichtbar)
    4. Netzwerk ausschalten → "Keine Internetverbindung" + "Erneut versuchen" erscheint
    5. StoreInfo-Tab → "Datenschutzerklärung"-Link am Ende
    6. Datenschutzerklärung öffnet → Abschnitt "GPS-Standortdaten" mit Erklärung sichtbar
    7. Als Visitor einloggen → kein Dashboard-Tab sichtbar
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe Probleme</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile App → /api/dashboard/stats | JWT aus authStore, storeId nicht manipulierbar |
| PrivacyScreen | Statischer Content, kein Input, kein Trust-Boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | DashboardScreen — Stats sichtbar | mitigate | Screen nur für role != 'visitor' in Navigation eingebunden; Backend zusätzlich durch requireRole(['admin','volunteer']) geschützt (Defense-in-Depth) |
| T-04-02-02 | Spoofing | fetchDashboardStats from/to-Parameter | accept | Parameter werden vom Backend via Zod validiert; Client-seitig keine Sicherheitsrelevanz |
| T-04-02-03 | Information Disclosure | GPS-Zweck unklar | mitigate | PrivacyScreen nennt explizit "nur Radius-Check, keine Persistierung" — DSGVO-Transparenzpflicht erfüllt |
</threat_model>

<verification>
1. `cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit` → EXIT 0
2. DashboardScreen.tsx existiert in `mobile/src/screens/admin/`
3. PrivacyScreen.tsx existiert in `mobile/src/screens/legal/`
4. AppNavigator.tsx importiert und registriert beide neuen Screens
5. Checkpoint: Human-Verify bestätigt visuell korrekte Darstellung und Navigation
</verification>

<success_criteria>
- DashboardScreen mit 4 Stat-Cards (Besuche / Items mitgenommen / neue Items / aktive Items) im 2er-Grid sichtbar für admin + volunteer
- Zeitraum-Auswahl funktioniert und aktualisiert die Karten
- Loading-Spinner, Inline-Fehler + Retry, Offline-Hinweis implementiert
- PrivacyScreen mit explizitem GPS-Hinweis ist erreichbar aus StoreInfoScreen
- TypeScript-Check mobile EXIT 0
- Kein Dashboard-Tab für Visitor-Rolle
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/04-dashboard-polish/04-02-SUMMARY.md` mit den tatsächlich ausgeführten Commits, Abweichungen vom Plan und Verifikationsergebnissen.
</output>
