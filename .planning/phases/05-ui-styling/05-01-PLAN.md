---
phase: 05-ui-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/navigation/AppNavigator.tsx
  - mobile/src/screens/visitor/ScanScreen.tsx
  - mobile/src/screens/visitor/CheckInScreen.tsx
  - mobile/src/screens/visitor/PointsHistoryScreen.tsx
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
  - STYLE-05

must_haves:
  truths:
    - "Tab-Bar zeigt Theme-Farben (aktiv: colors.primary #27b092, inaktiv: colors.textSecondary)"
    - "Navigation-Header zeigt Theme-Farben (backgroundColor: colors.primary, tintColor: white, Work Sans Bold)"
    - "ScanScreen verwendet fontFamily: fonts.regular/semiBold auf allen sichtbaren Texten"
    - "CheckInScreen verwendet theme-Farben statt hardcodiertem #2563EB und fontFamily Work Sans"
    - "PointsHistoryScreen verwendet theme-Farben und fontFamily Work Sans, Punkte-Zahl in colors.primary"
    - "P² Logo-Platzhalter auf LoginScreen ist bereits vorhanden (P² Text, logoText Style)"
  artifacts:
    - path: "mobile/src/navigation/AppNavigator.tsx"
      provides: "Styled Tab-Bar und Header"
      contains: "tabBarActiveTintColor.*colors.primary"
    - path: "mobile/src/screens/visitor/ScanScreen.tsx"
      provides: "Gestyled ScanScreen mit theme fonts"
      contains: "fontFamily.*fonts"
    - path: "mobile/src/screens/visitor/CheckInScreen.tsx"
      provides: "Gestyled CheckInScreen mit theme colors + fonts"
      contains: "colors, fonts, spacing, borderRadius.*theme"
    - path: "mobile/src/screens/visitor/PointsHistoryScreen.tsx"
      provides: "Gestyled PointsHistoryScreen mit theme colors + fonts"
      contains: "colors, fonts.*theme"
  key_links:
    - from: "AppNavigator.tsx"
      to: "theme/index.ts"
      via: "import { colors, fonts } from '../theme'"
      pattern: "colors\\.primary.*tabBarActiveTintColor"
    - from: "CheckInScreen.tsx"
      to: "theme/index.ts"
      via: "import { colors, fonts, spacing, borderRadius } from '../../theme'"
      pattern: "LinearGradient.*colors\\.gradientColors"
---

<objective>
Tab-Bar, Navigation-Header und Visitor-Screens (Scan, CheckIn, Punkte-Historie) auf
durchgängiges Theme umstellen: Work Sans, Gradient-Farben, theme/index.ts als einzige
Farbquelle.

Purpose: Diese Screens sind der primäre Besucher-Bereich. Konsistentes Styling hier ist
der sichtbarste Teil des v1.1 Polish.

Output:
- AppNavigator mit gestyletem Tab-Bar (primary/textSecondary) und Navigation-Header (primary bg, white text, Work Sans)
- ScanScreen: fontFamily Work Sans auf allen Texten, theme-Farben auf Permission- und Error-States
- CheckInScreen: theme-Farben ersetzen hardcodierten #2563EB, LinearGradient-Button, Work Sans
- PointsHistoryScreen: theme-Farben, Work Sans, Punkte in colors.primary statt #2563EB
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ui-styling/05-CONTEXT.md

<interfaces>
<!-- Vollständiger Theme-Export — alle Werte für diesen Plan relevant -->
<!-- Aus: mobile/src/theme/index.ts -->

```typescript
export const colors = {
  gradientStart: '#27b092',
  gradientMid: '#79c4b0',
  gradientEnd: '#80b4e2',
  gradientColors: ['#27b092', '#79c4b0', '#80b4e2'] as const,
  gradientLocations: [0, 0.51, 1] as const,
  primary: '#27b092',
  primaryLight: '#79c4b0',
  background: '#F9FAFB',
  surface: '#FFFFFF',
  text: '#111827',
  textSecondary: '#6B7280',
  textLight: '#9CA3AF',
  border: '#E5E7EB',
  error: '#EF4444',
  success: '#10B981',
  white: '#FFFFFF',
};

export const fonts = {
  regular: 'WorkSans-Regular',
  medium: 'WorkSans-Medium',
  semiBold: 'WorkSans-SemiBold',
  bold: 'WorkSans-Bold',
};

export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 };
export const borderRadius = { sm: 8, md: 12, lg: 16, full: 999 };
```

<!-- LoginScreen Referenz-Pattern für Gradient-Button und Header -->
<!-- Aus: mobile/src/screens/auth/LoginScreen.tsx -->
```typescript
// Gradient-Header Pattern:
<LinearGradient
  colors={[...colors.gradientColors]}
  locations={[...colors.gradientLocations]}
  start={{ x: 0, y: 0 }}
  end={{ x: 1, y: 1 }}
  style={styles.header}
>

// Gradient-Button Pattern:
<TouchableOpacity style={[styles.button, loading && styles.buttonDisabled]} onPress={...} disabled={...}>
  <LinearGradient
    colors={[...colors.gradientColors]}
    locations={[...colors.gradientLocations]}
    start={{ x: 0, y: 0 }}
    end={{ x: 1, y: 0 }}
    style={styles.buttonGradient}
  >
    <Text style={styles.buttonText}>Label</Text>
  </LinearGradient>
</TouchableOpacity>

// buttonGradient style: { padding: 14, alignItems: 'center' }
// buttonText style: { color: colors.white, fontSize: 16, fontFamily: fonts.semiBold }
// button style: { borderRadius: borderRadius.sm, overflow: 'hidden', marginBottom: spacing.md }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppNavigator Tab-Bar und Navigation-Header stylen</name>
  <files>mobile/src/navigation/AppNavigator.tsx</files>
  <action>
Theme-Import am Anfang hinzufügen:
```typescript
import { colors, fonts } from '../theme';
```

VolunteerTabs und VisitorTabs Tab.Navigator mit screenOptions stylen:
```typescript
screenOptions={{
  tabBarActiveTintColor: colors.primary,
  tabBarInactiveTintColor: colors.textSecondary,
  tabBarStyle: {
    backgroundColor: colors.surface,
    borderTopColor: colors.border,
  },
  tabBarLabelStyle: {
    fontFamily: fonts.medium,
    fontSize: 11,
  },
  headerStyle: {
    backgroundColor: colors.primary,
  },
  headerTintColor: colors.white,
  headerTitleStyle: {
    fontFamily: fonts.bold,
    fontSize: 17,
  },
}}
```

Den äußeren Stack.Navigator (AuthenticatedStack) und den Root-Stack-Navigator brauchen kein Header-Styling — headerShown: false bleibt erhalten.

Der PrivacyScreen Stack.Screen hat bereits `options={{ headerShown: true, title: 'Datenschutz' }}`. Diesem Stack.Navigator ebenfalls screenOptions hinzufügen:
```typescript
screenOptions={{
  headerShown: false,
  headerStyle: { backgroundColor: colors.primary },
  headerTintColor: colors.white,
  headerTitleStyle: { fontFamily: fonts.bold, fontSize: 17 },
}}
```

Keine Funktionalitätsänderungen — nur screenOptions ergänzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - AppNavigator.tsx importiert colors und fonts aus theme
    - Tab-Bar zeigt tabBarActiveTintColor: colors.primary und tabBarInactiveTintColor: colors.textSecondary
    - Header-Hintergrund ist colors.primary, Text ist white, fontFamily fonts.bold
    - TypeScript kompiliert ohne Fehler
  </done>
</task>

<task type="auto">
  <name>Task 2: ScanScreen, CheckInScreen und PointsHistoryScreen auf Theme umstellen</name>
  <files>
    mobile/src/screens/visitor/ScanScreen.tsx,
    mobile/src/screens/visitor/CheckInScreen.tsx,
    mobile/src/screens/visitor/PointsHistoryScreen.tsx
  </files>
  <action>
**ScanScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing, borderRadius } from '../../theme';
import LinearGradient from 'react-native-linear-gradient';
```

Styles ersetzen — hardcodierte Werte durch theme ersetzen:
- `center`: `backgroundColor: colors.background`
- `scanFrame`: `borderColor: colors.white`, `borderRadius: borderRadius.md`
- `hint`: `fontFamily: fonts.regular`, `color: colors.white`
- `errorBanner`: `backgroundColor: colors.error`, `borderRadius: borderRadius.sm`, `padding: spacing.md`
- `errorText`: `fontFamily: fonts.semiBold`, `color: colors.white`
- `permissionText`: `fontSize: 16, fontFamily: fonts.medium, color: colors.text, marginBottom: spacing.md`
- `permissionBtn`: Ersetzen durch Gradient-Button (LinearGradient Wrapper wie im Login-Pattern). Der "Erlauben"-Button bekommt Gradient-Style.

Permission-Button-JSX anpassen (von einfachem Text zu styledtem TouchableOpacity mit LinearGradient):
```tsx
<TouchableOpacity style={styles.permissionButton} onPress={...}>
  <LinearGradient
    colors={[...colors.gradientColors]}
    locations={[...colors.gradientLocations]}
    start={{ x: 0, y: 0 }}
    end={{ x: 1, y: 0 }}
    style={styles.permissionButtonGradient}
  >
    <Text style={styles.permissionButtonText}>Erlauben</Text>
  </LinearGradient>
</TouchableOpacity>
```
Styles dafür:
```typescript
permissionButton: { borderRadius: borderRadius.sm, overflow: 'hidden' },
permissionButtonGradient: { paddingHorizontal: spacing.xl, paddingVertical: 14, alignItems: 'center' },
permissionButtonText: { color: colors.white, fontSize: 16, fontFamily: fonts.semiBold },
```

---

**CheckInScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing, borderRadius } from '../../theme';
import LinearGradient from 'react-native-linear-gradient';
```

Styles vollständig ersetzen:
- `center`: `backgroundColor: colors.background, padding: spacing.lg`
- `title`: `fontSize: 24, fontFamily: fonts.bold, color: colors.text, marginBottom: spacing.sm, textAlign: 'center'`
- `subtitle`: `fontSize: 14, fontFamily: fonts.regular, color: colors.textSecondary, textAlign: 'center', marginBottom: spacing.lg`
- `btn`: Entfernen — stattdessen Gradient-Button wie Login-Pattern
- `btnText`: Entfernen — in buttonText umbenennen
- `stepper`: `flexDirection: 'row', alignItems: 'center', marginVertical: spacing.lg`
- `stepBtn`: `width: 48, height: 48, borderRadius: borderRadius.full, backgroundColor: colors.border, justifyContent: 'center', alignItems: 'center'`
- `stepBtnText`: `fontSize: 24, fontFamily: fonts.bold, color: colors.text`
- `stepCount`: `fontSize: 48, fontFamily: fonts.bold, color: colors.text, marginHorizontal: spacing.xl`
- `successIcon`: `fontSize: 64, color: colors.success, marginBottom: spacing.sm`
- `points`: `fontSize: 32, fontFamily: fonts.bold, color: colors.primary, marginVertical: spacing.sm`
- `errorText`: `fontSize: 16, fontFamily: fonts.regular, color: colors.error, textAlign: 'center', marginBottom: spacing.md`
- `scanFrame`: `width: 240, height: 240, borderWidth: 2, borderColor: colors.white, borderRadius: borderRadius.md, backgroundColor: 'transparent'`
- `hint`: `color: colors.white, marginTop: spacing.md, fontSize: 14, fontFamily: fonts.regular, textAlign: 'center', paddingHorizontal: spacing.xl`

Alle JSX-Stellen mit `<TouchableOpacity style={styles.btn}` auf Gradient-Button-Pattern umstellen (wie Login). Betrifft: GPS-Step, Stepper-Step "Einchecken", Success-Step "Fertig", Error-Step "Nochmal versuchen".

---

**PointsHistoryScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing } from '../../theme';
```

Styles ersetzen:
- `container`: `backgroundColor: colors.background`
- `header`: `fontSize: 18, fontFamily: fonts.bold, color: colors.text, padding: spacing.md`
- `row`: `backgroundColor: colors.surface, borderBottomColor: colors.border` (borderBottomWidth: 1 bleibt)
- `source`: `fontSize: 15, fontFamily: fonts.semiBold, color: colors.text`
- `date`: `fontSize: 13, fontFamily: fonts.regular, color: colors.textLight, marginTop: 2`
- `points`: `fontSize: 18, fontFamily: fonts.bold, color: colors.primary` (war #2563EB)
- `empty`: `fontFamily: fonts.regular, color: colors.textSecondary, fontSize: 16, textAlign: 'center'`

RefreshControl tintColor auf colors.primary setzen:
```tsx
<RefreshControl refreshing={isRefreshing} onRefresh={() => load(true)} tintColor={colors.primary} />
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ScanScreen.tsx importiert colors, fonts, spacing, borderRadius aus theme (grep bestätigt: `from '../../theme'`)
    - CheckInScreen.tsx importiert colors, fonts, spacing, borderRadius aus theme, kein hardcodiertes #2563EB mehr
    - PointsHistoryScreen.tsx importiert colors, fonts aus theme, Punkte-Farbe ist colors.primary
    - TypeScript kompiliert ohne Fehler
    - `grep -r "2563EB" mobile/src/screens/visitor/` findet keine Treffer mehr
    - `grep -r "fontFamily.*fonts\." mobile/src/screens/visitor/` findet Einträge in allen 3 Dateien
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-Only | Reine Styling-Phase, keine neuen Datenflüsse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | StyleSheet-Werte | accept | Styling-Änderungen haben keinen Sicherheitseinfluss; keine Daten verarbeitet |
</threat_model>

<verification>
Nach Ausführung beider Tasks:

```bash
# Kein hardcodiertes #2563EB in Visitor-Screens mehr
grep -r "2563EB" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/

# fontFamily Work Sans in allen Visitor-Screens
grep -rn "fontFamily.*fonts\." /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/

# theme-Import in AppNavigator
grep "from '../theme'" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/navigation/AppNavigator.tsx

# Tab-Bar primary color
grep "tabBarActiveTintColor" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/navigation/AppNavigator.tsx

# TypeScript clean
cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit
```
</verification>

<success_criteria>
- AppNavigator: Tab-Bar und Header sind mit theme-Farben gestyled (colors.primary, fonts.bold)
- ScanScreen: fontFamily Work Sans auf allen Texten, Gradient-Permission-Button, theme-Farben
- CheckInScreen: Alle hardcodierten Blau-Werte durch theme ersetzt, Gradient-Buttons, Work Sans
- PointsHistoryScreen: Work Sans, Punkte in colors.primary, theme-Farben
- TypeScript: `cd mobile && npx tsc --noEmit` ohne Fehler
- Keine hardcodierten Hex-Farben (#2563EB, #F9FAFB, #111827 etc.) mehr in den 4 geänderten Dateien
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-ui-styling/05-01-SUMMARY.md` erstellen mit:
- Welche Screens umgestellt wurden
- Welche Pattern-Entscheidungen getroffen wurden (z.B. Gradient-Buttons vs. solid)
- TypeScript-Status
</output>
