---
phase: 05-ui-styling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/screens/items/ItemCreateScreen.tsx
  - mobile/src/screens/items/ItemListScreen.tsx
  - mobile/src/screens/admin/DashboardScreen.tsx
  - mobile/src/screens/store/StoreInfoScreen.tsx
  - mobile/src/screens/auth/RegisterScreen.tsx
  - mobile/src/screens/legal/PrivacyScreen.tsx
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-04

must_haves:
  truths:
    - "ItemCreateScreen verwendet fontFamily Work Sans und colors.primary statt #2563EB auf Chips/Submit"
    - "ItemListScreen verwendet fontFamily Work Sans und colors.primary statt #2563EB auf Chips"
    - "DashboardScreen verwendet fontFamily Work Sans, colors.primary im Segment-Control, StatCards mit theme-abgeleiteten Farben"
    - "StoreInfoScreen verwendet fontFamily Work Sans, Gradient-Header statt #2563EB solid"
    - "RegisterScreen ist vollständig auf theme umgestellt (wie LoginScreen ohne Logo-Block)"
    - "PrivacyScreen verwendet fontFamily Work Sans auf allen Texten"
  artifacts:
    - path: "mobile/src/screens/items/ItemCreateScreen.tsx"
      provides: "Gestyled Item-Erstellen Screen"
      contains: "colors, fonts.*theme"
    - path: "mobile/src/screens/items/ItemListScreen.tsx"
      provides: "Gestyled Item-Liste Screen"
      contains: "colors, fonts.*theme"
    - path: "mobile/src/screens/admin/DashboardScreen.tsx"
      provides: "Gestyled Dashboard Screen"
      contains: "colors, fonts.*theme"
    - path: "mobile/src/screens/store/StoreInfoScreen.tsx"
      provides: "Gestyled Store-Info Screen mit Gradient-Header"
      contains: "LinearGradient.*gradientColors"
    - path: "mobile/src/screens/auth/RegisterScreen.tsx"
      provides: "Vollständig gestyltes Register-Screen"
      contains: "colors, fonts, spacing, borderRadius.*theme"
    - path: "mobile/src/screens/legal/PrivacyScreen.tsx"
      provides: "Gestyled Privacy Screen mit Work Sans"
      contains: "fonts.*theme"
  key_links:
    - from: "RegisterScreen.tsx"
      to: "LoginScreen.tsx (Referenz)"
      via: "Gleicher Gradient-Button, gleiche Input-Styles, gleiche Link-Text-Farbe"
      pattern: "LinearGradient.*gradientColors"
    - from: "StoreInfoScreen.tsx"
      to: "theme/index.ts"
      via: "LinearGradient Header statt backgroundColor: '#2563EB'"
      pattern: "LinearGradient.*gradientColors"
---

<objective>
Volunteer/Admin-Screens (ItemCreate, ItemList, Dashboard, StoreInfo) sowie RegisterScreen
und PrivacyScreen auf durchgängiges Theme umstellen: Work Sans, theme-Farben aus
mobile/src/theme/index.ts, keine hardcodierten Hex-Werte mehr.

Purpose: Nach Plan 01 (Navigation + Visitor-Screens) vervollständigt dieser Plan das
visuelle Theme auf allen verbleibenden Screens. Das Ergebnis ist STYLE-04 (alle Screens
konsistent) vollständig erfüllt.

Output:
- 6 Screens vollständig auf theme umgestellt
- Kein hardcodiertes #2563EB, #2d6a4f oder andere nicht-theme Hex-Farben
- Work Sans durchgängig auf allen Screens
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ui-styling/05-CONTEXT.md

<interfaces>
<!-- Vollständiger Theme-Export -->
<!-- Aus: mobile/src/theme/index.ts -->

```typescript
export const colors = {
  gradientStart: '#27b092',
  gradientMid: '#79c4b0',
  gradientEnd: '#80b4e2',
  gradientColors: ['#27b092', '#79c4b0', '#80b4e2'] as const,
  gradientLocations: [0, 0.51, 1] as const,
  primary: '#27b092',
  primaryLight: '#79c4b0',
  background: '#F9FAFB',
  surface: '#FFFFFF',
  text: '#111827',
  textSecondary: '#6B7280',
  textLight: '#9CA3AF',
  border: '#E5E7EB',
  error: '#EF4444',
  success: '#10B981',
  white: '#FFFFFF',
};

export const fonts = {
  regular: 'WorkSans-Regular',
  medium: 'WorkSans-Medium',
  semiBold: 'WorkSans-SemiBold',
  bold: 'WorkSans-Bold',
};

export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32 };
export const borderRadius = { sm: 8, md: 12, lg: 16, full: 999 };
```

<!-- LoginScreen Referenz-Pattern (bereits vollständig gestyled) -->
<!-- Aus: mobile/src/screens/auth/LoginScreen.tsx -->
```typescript
// Input-Style Pattern:
input: {
  backgroundColor: colors.surface,
  borderWidth: 1,
  borderColor: colors.border,
  borderRadius: borderRadius.sm,
  padding: 14,
  marginBottom: spacing.md,
  fontSize: 16,
  fontFamily: fonts.regular,
  color: colors.text,
},

// Gradient-Button Pattern:
button: { borderRadius: borderRadius.sm, overflow: 'hidden', marginBottom: spacing.md },
buttonDisabled: { opacity: 0.6 },
buttonGradient: { padding: 14, alignItems: 'center' },
buttonText: { color: colors.white, fontSize: 16, fontFamily: fonts.semiBold },

// Link-Text Pattern:
linkText: { textAlign: 'center', color: colors.primary, fontSize: 14, fontFamily: fonts.medium },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ItemCreateScreen und ItemListScreen auf Theme umstellen</name>
  <files>
    mobile/src/screens/items/ItemCreateScreen.tsx,
    mobile/src/screens/items/ItemListScreen.tsx
  </files>
  <action>
**ItemCreateScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing, borderRadius } from '../../theme';
import LinearGradient from 'react-native-linear-gradient';
```

Styles ersetzen:
- `container`: `backgroundColor: colors.background`
- `content`: `padding: spacing.md, paddingBottom: 40`
- `label`: `fontSize: 14, fontFamily: fonts.semiBold, color: colors.textSecondary, marginTop: spacing.md, marginBottom: 6`
- `input`: `borderWidth: 1, borderColor: colors.border, borderRadius: borderRadius.sm, padding: 12, fontSize: 16, fontFamily: fonts.regular, color: colors.text, backgroundColor: colors.surface`
- `chipRow`: `flexDirection: 'row', flexWrap: 'wrap', gap: 8`
- `chip`: `paddingHorizontal: 12, paddingVertical: 6, borderRadius: borderRadius.full, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface`
- `chipSelected`: `backgroundColor: colors.primary, borderColor: colors.primary`
- `chipText`: `fontSize: 14, fontFamily: fonts.regular, color: colors.textSecondary`
- `chipTextSelected`: `color: colors.white, fontFamily: fonts.medium`
- `colorPreview`: Bleibt wie ist (dynamische backgroundColor, nur borderColor auf colors.border)
- `colorPicker`, `hueSlider`, `sizeScroll`: Keine Änderung nötig

Submit-Button von `backgroundColor: '#2563EB'` auf Gradient-Button umstellen:
```tsx
<TouchableOpacity
  style={[styles.submitButton, isLoading && styles.submitButtonDisabled]}
  onPress={handleSubmit}
  disabled={isLoading}
>
  <LinearGradient
    colors={[...colors.gradientColors]}
    locations={[...colors.gradientLocations]}
    start={{ x: 0, y: 0 }}
    end={{ x: 1, y: 0 }}
    style={styles.submitButtonGradient}
  >
    {isLoading ? (
      <ActivityIndicator color={colors.white} />
    ) : (
      <Text style={styles.submitButtonText}>Item anlegen</Text>
    )}
  </LinearGradient>
</TouchableOpacity>
```

Styles:
- `submitButton`: `borderRadius: borderRadius.sm, overflow: 'hidden', marginTop: spacing.lg`
- `submitButtonDisabled`: `opacity: 0.6`
- `submitButtonGradient`: `padding: 16, alignItems: 'center'`
- `submitButtonText`: `color: colors.white, fontSize: 16, fontFamily: fonts.bold`

Placeholder-Text-Farbe auf `colors.textLight` vereinheitlichen (war `#9CA3AF`).

---

**ItemListScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing, borderRadius } from '../../theme';
```

Styles ersetzen:
- `container`: `backgroundColor: colors.background`
- `searchInput`: `margin: spacing.md, padding: 10, borderWidth: 1, borderColor: colors.border, borderRadius: borderRadius.sm, fontSize: 16, fontFamily: fonts.regular, color: colors.text, backgroundColor: colors.surface`
- `filterRow`: `paddingHorizontal: spacing.md, marginBottom: 4`
- `chip`: `paddingHorizontal: 12, paddingVertical: 6, borderRadius: borderRadius.full, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface, marginRight: spacing.sm`
- `chipSelected`: `backgroundColor: colors.primary, borderColor: colors.primary`
- `chipText`: `fontSize: 13, fontFamily: fonts.regular, color: colors.textSecondary`
- `chipTextSelected`: `color: colors.white, fontFamily: fonts.medium`
- `itemRow`: `backgroundColor: colors.surface, borderBottomColor: colors.border` (rest bleibt)
- `itemTitle`: `fontSize: 15, fontFamily: fonts.semiBold, color: colors.text`
- `itemMeta`: `fontSize: 13, fontFamily: fonts.regular, color: colors.textSecondary, marginTop: 2`
- `statusBadge`: `paddingHorizontal: 8, paddingVertical: 3, borderRadius: borderRadius.md, backgroundColor: '#D1FAE5'` (success-tint, behalten)
- `statusTaken`: `backgroundColor: '#FEE2E2'` (error-tint, behalten)
- `statusText`: `fontSize: 12, fontFamily: fonts.medium, color: colors.textSecondary`
- `emptyText`: `fontFamily: fonts.regular, color: colors.textSecondary, fontSize: 16, textAlign: 'center'`

Placeholder-Farbe im TextInput auf `colors.textLight` setzen.

RefreshControl tintColor auf `colors.primary` setzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ItemCreateScreen.tsx: `grep "from '../../theme'" mobile/src/screens/items/ItemCreateScreen.tsx` findet import
    - ItemCreateScreen.tsx: `grep "2563EB" mobile/src/screens/items/ItemCreateScreen.tsx` findet keine Treffer
    - ItemListScreen.tsx: `grep "from '../../theme'" mobile/src/screens/items/ItemListScreen.tsx` findet import
    - ItemListScreen.tsx: `grep "2563EB" mobile/src/screens/items/ItemListScreen.tsx` findet keine Treffer
    - TypeScript kompiliert ohne Fehler
  </done>
</task>

<task type="auto">
  <name>Task 2: DashboardScreen, StoreInfoScreen, RegisterScreen und PrivacyScreen auf Theme umstellen</name>
  <files>
    mobile/src/screens/admin/DashboardScreen.tsx,
    mobile/src/screens/store/StoreInfoScreen.tsx,
    mobile/src/screens/auth/RegisterScreen.tsx,
    mobile/src/screens/legal/PrivacyScreen.tsx
  </files>
  <action>
**DashboardScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing, borderRadius } from '../../theme';
```

Styles ersetzen:
- `container`: `backgroundColor: colors.background`
- `content`: `padding: spacing.md`
- `title`: `fontSize: 24, fontFamily: fonts.bold, color: colors.text, marginBottom: spacing.md`
- `segmentRow`: `flexDirection: 'row', backgroundColor: colors.border, borderRadius: borderRadius.md, padding: 3, marginBottom: spacing.lg`
- `segmentButton`: `flex: 1, paddingVertical: 8, alignItems: 'center', borderRadius: borderRadius.sm`
- `segmentButtonActive`: `backgroundColor: colors.primary` (war #2563EB)
- `segmentText`: `fontSize: 13, fontFamily: fonts.semiBold, color: colors.textSecondary`
- `segmentTextActive`: `color: colors.white, fontFamily: fonts.semiBold`
- `errorText`: `fontSize: 15, fontFamily: fonts.regular, color: colors.error, textAlign: 'center', marginBottom: spacing.md`
- `retryButton`: `backgroundColor: colors.primary, borderRadius: borderRadius.sm, paddingHorizontal: spacing.lg, paddingVertical: 10`
- `retryText`: `color: colors.white, fontFamily: fonts.semiBold, fontSize: 14`
- `card`: `borderRadius: borderRadius.md, ...` (Struktur bleibt, borderRadius durch theme)
- `cardValue`: `fontSize: 40, fontFamily: fonts.bold, marginBottom: 4` (color bleibt accentColor prop)
- `cardLabel`: `fontSize: 12, fontFamily: fonts.semiBold, color: colors.textSecondary, textTransform: 'uppercase'`

ActivityIndicator color auf `colors.primary` setzen (war `#2563EB`).

StatCard-Farben: Die bgColor/accentColor-Props in DashboardScreen bleiben als hardcodierte Werte — diese semantischen Tint-Farben (EFF6FF, F0FDF4 etc.) sind bewusste Design-Entscheidungen für die Kategorie-Karten, nicht globale Primärfarben. Nur der Segment-Control und Retry-Button verwenden colors.primary.

---

**StoreInfoScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing, borderRadius } from '../../theme';
import LinearGradient from 'react-native-linear-gradient';
```

Header-Bereich: `backgroundColor: '#2563EB'` durch LinearGradient ersetzen:
```tsx
<LinearGradient
  colors={[...colors.gradientColors]}
  locations={[...colors.gradientLocations]}
  start={{ x: 0, y: 0 }}
  end={{ x: 1, y: 1 }}
  style={styles.header}
>
  <Text style={styles.storeName}>{store?.name ?? 'Laden'}</Text>
</LinearGradient>
```

Styles ersetzen:
- `container`: `backgroundColor: colors.background`
- `header`: `padding: spacing.lg, alignItems: 'center'` (backgroundColor entfernen — LinearGradient kümmert sich darum)
- `storeName`: `fontSize: 24, fontFamily: fonts.bold, color: colors.white`
- `section`: `backgroundColor: colors.surface, marginHorizontal: spacing.md, marginTop: spacing.md, borderRadius: borderRadius.sm, padding: spacing.md, borderWidth: 1, borderColor: colors.border`
- `sectionTitle`: `fontSize: 13, fontFamily: fonts.semiBold, color: colors.textSecondary, marginBottom: 6, textTransform: 'uppercase'`
- `sectionContent`: `fontSize: 16, fontFamily: fonts.regular, color: colors.text, lineHeight: 24`
- `privacyLink`: `fontSize: 13, fontFamily: fonts.regular, color: colors.textSecondary, textDecorationLine: 'underline', marginTop: spacing.lg, textAlign: 'center', marginBottom: spacing.md`
- `errorText`: `fontFamily: fonts.regular, color: colors.error, fontSize: 16, textAlign: 'center', padding: spacing.lg`

ActivityIndicator ohne Farbe lassen (wird automatisch theme-nah aussehen).

---

**RegisterScreen.tsx:**

RegisterScreen vollständig auf LoginScreen-Pattern umstellen. Struktur: Gradient-Header-Block + Form-Block.

Theme- und LinearGradient-Import:
```typescript
import { colors, fonts, spacing, borderRadius } from '../../theme';
import LinearGradient from 'react-native-linear-gradient';
```

JSX-Struktur:
```tsx
<View style={styles.container}>
  <LinearGradient
    colors={[...colors.gradientColors]}
    locations={[...colors.gradientLocations]}
    start={{ x: 0, y: 0 }}
    end={{ x: 1, y: 1 }}
    style={styles.header}
  >
    <Text style={styles.title}>Registrieren</Text>
    <Text style={styles.subtitle}>Erstelle deinen Plietsche-Plünn-Account</Text>
  </LinearGradient>

  <View style={styles.form}>
    <TextInput style={styles.input} placeholder="Benutzername (min. 3 Zeichen)" ... />
    <TextInput style={styles.input} placeholder="E-Mail" ... />
    <TextInput style={styles.input} placeholder="Passwort (min. 8 Zeichen)" ... />

    <TouchableOpacity style={[styles.button, loading && styles.buttonDisabled]} ...>
      <LinearGradient
        colors={[...colors.gradientColors]}
        locations={[...colors.gradientLocations]}
        start={{ x: 0, y: 0 }}
        end={{ x: 1, y: 0 }}
        style={styles.buttonGradient}
      >
        {loading ? <ActivityIndicator color={colors.white} /> : <Text style={styles.buttonText}>Konto erstellen</Text>}
      </LinearGradient>
    </TouchableOpacity>

    <TouchableOpacity onPress={() => navigation.goBack()}>
      <Text style={styles.linkText}>Zurück zum Login</Text>
    </TouchableOpacity>
  </View>
</View>
```

Styles (analog LoginScreen):
```typescript
container: { flex: 1, backgroundColor: colors.background },
header: { paddingTop: 60, paddingBottom: 40, alignItems: 'center' },
title: { fontSize: 28, fontFamily: fonts.bold, color: colors.white, marginBottom: spacing.xs },
subtitle: { fontSize: 14, fontFamily: fonts.regular, color: 'rgba(255,255,255,0.8)' },
form: { flex: 1, padding: spacing.lg, marginTop: -20, backgroundColor: colors.background, borderTopLeftRadius: borderRadius.lg, borderTopRightRadius: borderRadius.lg },
input: { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, borderRadius: borderRadius.sm, padding: 14, marginBottom: spacing.md, fontSize: 16, fontFamily: fonts.regular, color: colors.text },
button: { borderRadius: borderRadius.sm, overflow: 'hidden', marginBottom: spacing.md },
buttonDisabled: { opacity: 0.6 },
buttonGradient: { padding: 14, alignItems: 'center' },
buttonText: { color: colors.white, fontSize: 16, fontFamily: fonts.semiBold },
linkText: { textAlign: 'center', color: colors.primary, fontSize: 14, fontFamily: fonts.medium },
```

Alle placeholder-Farben auf `colors.textLight` setzen.

---

**PrivacyScreen.tsx:**

Theme-Import hinzufügen:
```typescript
import { colors, fonts, spacing } from '../../theme';
```

Styles ersetzen:
- `container`: `backgroundColor: colors.surface`
- `content`: `padding: spacing.md, paddingBottom: 40`
- `mainTitle`: `fontSize: 18, fontFamily: fonts.bold, color: colors.text, marginBottom: spacing.lg`
- `sectionTitle`: `fontSize: 16, fontFamily: fonts.bold, color: colors.text, marginTop: spacing.lg, marginBottom: spacing.sm`
- `bodyText`: `fontSize: 14, fontFamily: fonts.regular, lineHeight: 22, color: colors.textSecondary, marginBottom: spacing.sm`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - DashboardScreen.tsx: `grep "from '../../theme'" mobile/src/screens/admin/DashboardScreen.tsx` findet import
    - StoreInfoScreen.tsx: `grep "LinearGradient" mobile/src/screens/store/StoreInfoScreen.tsx` findet Import und Verwendung
    - RegisterScreen.tsx: `grep "2d6a4f" mobile/src/screens/auth/RegisterScreen.tsx` findet keine Treffer
    - PrivacyScreen.tsx: `grep "fontFamily.*fonts\." mobile/src/screens/legal/PrivacyScreen.tsx` findet Einträge
    - TypeScript kompiliert ohne Fehler
    - `grep -r "2563EB" mobile/src/screens/admin/ mobile/src/screens/store/ mobile/src/screens/auth/RegisterScreen.tsx` findet keine Treffer
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-Only | Reine Styling-Phase, keine neuen Datenflüsse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | StyleSheet-Werte | accept | Styling-Änderungen haben keinen Sicherheitseinfluss; keine Daten verarbeitet |
</threat_model>

<verification>
Nach Ausführung beider Tasks:

```bash
# Kein hardcodiertes #2563EB in Items/Admin/Store/Auth(Register) mehr
grep -r "2563EB" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/items/
grep -r "2563EB" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/admin/
grep -r "2563EB" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/store/
grep "2563EB" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/auth/RegisterScreen.tsx

# Kein altes #2d6a4f mehr in RegisterScreen
grep "2d6a4f" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/auth/RegisterScreen.tsx

# fontFamily Work Sans in allen geänderten Screens
grep -rn "fontFamily.*fonts\." /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/items/
grep -rn "fontFamily.*fonts\." /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/admin/
grep -n "fontFamily.*fonts\." /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/store/StoreInfoScreen.tsx
grep -n "fontFamily.*fonts\." /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/auth/RegisterScreen.tsx
grep -n "fontFamily.*fonts\." /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/legal/PrivacyScreen.tsx

# Gradient in StoreInfoScreen und RegisterScreen
grep "LinearGradient" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/store/StoreInfoScreen.tsx
grep "LinearGradient" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/auth/RegisterScreen.tsx

# TypeScript clean
cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit
```
</verification>

<success_criteria>
- ItemCreateScreen: Gradient-Submit-Button, theme-Chips, Work Sans, kein #2563EB
- ItemListScreen: theme-Chips, Work Sans, kein #2563EB
- DashboardScreen: theme-Segment-Control (colors.primary), Work Sans, kein #2563EB
- StoreInfoScreen: LinearGradient-Header statt solid #2563EB, Work Sans
- RegisterScreen: vollständig wie LoginScreen gestyled (Gradient-Header, Gradient-Button, theme-Inputs), kein #2d6a4f
- PrivacyScreen: Work Sans auf allen Texten
- TypeScript: `cd mobile && npx tsc --noEmit` ohne Fehler
- Alle 6 Dateien haben `from '../../theme'` oder `from '../theme'` Import
</success_criteria>

<output>
Nach Abschluss `.planning/phases/05-ui-styling/05-02-SUMMARY.md` erstellen mit:
- Welche Screens umgestellt wurden
- Besonderheiten (StoreInfoScreen Gradient-Header, RegisterScreen Login-Pattern)
- TypeScript-Status
- Gesamtstatus Phase 05 (beide Plans abgeschlossen)
</output>
