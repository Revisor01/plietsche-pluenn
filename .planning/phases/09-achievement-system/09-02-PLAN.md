---
phase: 09-achievement-system
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - mobile/src/api/badges.api.ts
  - mobile/src/screens/visitor/BadgeOverviewScreen.tsx
  - mobile/src/screens/visitor/HomeScreen.tsx
  - mobile/src/navigation/AppNavigator.tsx
autonomous: false
requirements:
  - ACH-08
  - ACH-09
  - ACH-12

must_haves:
  truths:
    - "Besucher sieht eine Badge-Uebersichtsseite mit allen Badges, gruppiert nach Kategorie"
    - "Erreichte Badges sind hervorgehoben, ausstehende zeigen Fortschrittsbalken"
    - "Fortschrittstext lautet 'X von Y' (z.B. '72 von 100 geholt')"
    - "Homescreen zeigt hoechstes erreichtes Badge statt altem Punkte-Level"
    - "Toast erscheint wenn ein neues Badge freigeschaltet wird"
    - "Badge-Uebersichtsseite ist vom HomeScreen erreichbar"
  artifacts:
    - path: "mobile/src/screens/visitor/BadgeOverviewScreen.tsx"
      provides: "Badge-Uebersichtsseite gruppiert nach Kategorie"
      exports: ["default BadgeOverviewScreen"]
    - path: "mobile/src/api/badges.api.ts"
      provides: "fetchMyAchievements, Achievement-Typen"
      exports: ["fetchMyAchievements", "Achievement", "AchievementWithProgress"]
  key_links:
    - from: "mobile/src/screens/visitor/HomeScreen.tsx"
      to: "BadgeOverviewScreen"
      via: "TouchableOpacity navigiert zu BadgeOverview"
      pattern: "BadgeOverview"
    - from: "mobile/src/screens/visitor/ScanScreen.tsx oder CheckInScreen.tsx"
      to: "Toast"
      via: "newAchievements in API-Response loest Toast aus"
      pattern: "Toast.show"
---

<objective>
Badge-Uebersichtsseite fuer Besucher implementieren, Homescreen-Badge-Anzeige auf das neue Achievement-System umstellen, und Toast-Benachrichtigung bei neuem Badge.

Purpose: Besucher sehen ihren Achievement-Fortschritt und werden motiviert durch Toast-Feedback bei neuen Badges.
Output: BadgeOverviewScreen, aktualisierter HomeScreen, Toast-Integration in Scan + CheckIn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-achievement-system/09-CONTEXT.md
@.planning/phases/09-achievement-system/09-01-SUMMARY.md

<interfaces>
<!-- Neue API-Typen aus Plan 01 (badges.types.ts) -->
```typescript
export type TriggerType =
  | 'items_brought' | 'items_taken' | 'visits'
  | 'streak_weeks' | 'season_items_brought' | 'season_items_taken' | 'milestone';

export type Tier = 'bronze' | 'silber' | 'gold' | 'custom';
export type Season = 'fruehling' | 'sommer' | 'herbst' | 'winter';

export interface Achievement {
  id: string;
  storeId: string;
  name: string;
  description: string;
  iconName: string;    // Font Awesome 5 icon_name, kein Emoji
  triggerType: TriggerType;
  triggerValue: number;
  tier: Tier;
  season: Season | null;
  sortOrder: number;
}

export interface AchievementWithProgress extends Achievement {
  progress: number;
  completed: boolean;
  completedAt: Date | null;
}
```

<!-- Neue Backend-Endpunkte (Plan 01) -->
// GET /api/badges/achievements -> AchievementWithProgress[]
// Gibt alle Achievements des Stores mit Fortschritt des eingeloggten Users zurueck

<!-- Bestehende Mobile-Infrastruktur -->
// react-native-toast-message: bereits installiert, Toast-Aufruf in AppNavigator.tsx: <Toast />
// react-native-vector-icons/FontAwesome5: bereits installiert
// colors, fonts, spacing, borderRadius aus '../../theme'
// apiClient aus '../../api/client'

<!-- Scan-Response (nach Plan 01) -->
// scanItemQr response: { points, title, totalPoints, campaign, newAchievements: Achievement[] }
// processCheckin response: { points, itemPoints, totalPoints, campaign, newAchievements: Achievement[] }

<!-- TierFarben (per Discretion) -->
// bronze: '#CD7F32', silber: '#A8A9AD', gold: '#FFD700', custom: colors.primary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: badges.api.ts erweitern + BadgeOverviewScreen erstellen</name>
  <files>
    mobile/src/api/badges.api.ts,
    mobile/src/screens/visitor/BadgeOverviewScreen.tsx
  </files>

  <read_first>
    - mobile/src/api/badges.api.ts (bestehende Funktionen behalten, neue anhaengen)
    - mobile/src/screens/visitor/HomeScreen.tsx (Theme-Patterns uebernehmen)
  </read_first>

  <action>
**1. mobile/src/api/badges.api.ts — Neue Typen und Funktion anhaengen** (bestehende Exporte behalten):

```typescript
export type TriggerType =
  | 'items_brought' | 'items_taken' | 'visits'
  | 'streak_weeks' | 'season_items_brought' | 'season_items_taken' | 'milestone';

export type Tier = 'bronze' | 'silber' | 'gold' | 'custom';
export type Season = 'fruehling' | 'sommer' | 'herbst' | 'winter';

export interface Achievement {
  id: string;
  name: string;
  description: string;
  iconName: string;
  triggerType: TriggerType;
  triggerValue: number;
  tier: Tier;
  season: Season | null;
  sortOrder: number;
}

export interface AchievementWithProgress extends Achievement {
  progress: number;
  completed: boolean;
  completedAt: string | null;
}

export async function fetchMyAchievements(): Promise<AchievementWithProgress[]> {
  const { data } = await apiClient.get<AchievementWithProgress[]>('/api/badges/achievements');
  return data;
}
```

**2. mobile/src/screens/visitor/BadgeOverviewScreen.tsx erstellen:**

Vollstaendige Implementierung:
- ScrollView mit Kategorien-Sektionen
- Kategorien-Mapping: items_brought->"Bringer", items_taken->"Holer", visits->"Besucher", streak_weeks->"Streaks", season_items_brought|season_items_taken->"Saison", milestone->"Meilensteine"
- Pro Badge: Icon (FontAwesome5), Name, Tier-Farbe, Fortschrittsbalken, Text "X von Y [Einheit]"
- Einheit per triggerType: items_brought->"gebracht", items_taken->"geholt", visits->"Besuche", streak_weeks->"Wochen", season*->"Teile", milestone->""
- Erreichte Badges: voller Farbhintergrund mit tier-Farbe, Icon in Weiss, completedAt-Datum
- Ausstehende Badges: grauer Hintergrund, gedimmtes Icon, Fortschrittsbalken
- Tier-Farben: bronze='#CD7F32', silber='#A8A9AD', gold='#FFD700', custom=colors.primary
- Ladeindikator waehrend fetchMyAchievements
- Fehlertext + Retry-Button bei Fehler

Kategorie-Gruppen-Reihenfolge: Bringer, Holer, Besucher, Streaks, Saison, Meilensteine

```typescript
import React, { useEffect, useState, useCallback } from 'react';
import { View, Text, StyleSheet, ScrollView, ActivityIndicator, TouchableOpacity } from 'react-native';
import Icon from 'react-native-vector-icons/FontAwesome5';
import { fetchMyAchievements, type AchievementWithProgress } from '../../api/badges.api';
import { colors, fonts, spacing, borderRadius } from '../../theme';

const TIER_COLORS = { bronze: '#CD7F32', silber: '#A8A9AD', gold: '#FFD700', custom: colors.primary };

const CATEGORY_MAP: Record<string, string> = {
  items_brought: 'Bringer',
  items_taken: 'Holer',
  visits: 'Besucher',
  streak_weeks: 'Streaks',
  season_items_brought: 'Saison',
  season_items_taken: 'Saison',
  milestone: 'Meilensteine',
};

const UNIT_MAP: Record<string, string> = {
  items_brought: 'gebracht',
  items_taken: 'geholt',
  visits: 'Besuche',
  streak_weeks: 'Wochen',
  season_items_brought: 'Teile',
  season_items_taken: 'Teile',
  milestone: '',
};

const CATEGORY_ORDER = ['Bringer', 'Holer', 'Besucher', 'Streaks', 'Saison', 'Meilensteine'];

export default function BadgeOverviewScreen() {
  const [achievements, setAchievements] = useState<AchievementWithProgress[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const load = useCallback(async () => {
    setIsLoading(true);
    setError(null);
    try {
      const data = await fetchMyAchievements();
      setAchievements(data.sort((a, b) => a.sortOrder - b.sortOrder));
    } catch {
      setError('Badges konnten nicht geladen werden');
    } finally {
      setIsLoading(false);
    }
  }, []);

  useEffect(() => { void load(); }, [load]);

  if (isLoading) {
    return <View style={styles.centered}><ActivityIndicator size="large" color={colors.primary} /></View>;
  }
  if (error) {
    return (
      <View style={styles.centered}>
        <Text style={styles.errorText}>{error}</Text>
        <TouchableOpacity style={styles.retryButton} onPress={load}>
          <Text style={styles.retryText}>Erneut versuchen</Text>
        </TouchableOpacity>
      </View>
    );
  }

  // Gruppieren nach Kategorie
  const grouped: Record<string, AchievementWithProgress[]> = {};
  for (const a of achievements) {
    const cat = CATEGORY_MAP[a.triggerType] ?? 'Sonstige';
    if (!grouped[cat]) grouped[cat] = [];
    grouped[cat].push(a);
  }

  const completedCount = achievements.filter((a) => a.completed).length;

  return (
    <ScrollView style={styles.container} contentContainerStyle={styles.content}>
      <View style={styles.header}>
        <Text style={styles.headerTitle}>Meine Badges</Text>
        <Text style={styles.headerSubtitle}>{completedCount} von {achievements.length} erreicht</Text>
      </View>

      {CATEGORY_ORDER.filter((cat) => grouped[cat]?.length > 0).map((category) => (
        <View key={category} style={styles.section}>
          <Text style={styles.sectionTitle}>{category}</Text>
          {grouped[category].map((achievement) => {
            const tierColor = TIER_COLORS[achievement.tier] ?? colors.primary;
            const unit = UNIT_MAP[achievement.triggerType] ?? '';
            const progressPercent = Math.min(100, Math.round((achievement.progress / achievement.triggerValue) * 100));

            return (
              <View
                key={achievement.id}
                style={[
                  styles.badgeCard,
                  achievement.completed && { borderColor: tierColor, borderWidth: 2 },
                ]}
              >
                <View style={[styles.iconContainer, { backgroundColor: achievement.completed ? tierColor : colors.border }]}>
                  <Icon
                    name={achievement.iconName || 'trophy'}
                    solid
                    size={22}
                    color={achievement.completed ? '#fff' : colors.textSecondary}
                  />
                </View>
                <View style={styles.badgeContent}>
                  <View style={styles.badgeHeader}>
                    <Text style={[styles.badgeName, achievement.completed && { color: tierColor }]}>
                      {achievement.name}
                    </Text>
                    {achievement.completed && (
                      <Icon name="check-circle" solid size={16} color={tierColor} />
                    )}
                  </View>
                  <Text style={styles.badgeDescription}>{achievement.description}</Text>
                  {!achievement.completed && (
                    <>
                      <View style={styles.progressTrack}>
                        <View style={[styles.progressFill, { width: `${progressPercent}%` as any, backgroundColor: tierColor }]} />
                      </View>
                      <Text style={styles.progressText}>
                        {achievement.progress} von {achievement.triggerValue}{unit ? ` ${unit}` : ''}
                      </Text>
                    </>
                  )}
                  {achievement.completed && achievement.completedAt && (
                    <Text style={styles.completedAt}>
                      Erreicht am {new Date(achievement.completedAt).toLocaleDateString('de-DE')}
                    </Text>
                  )}
                </View>
              </View>
            );
          })}
        </View>
      ))}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: colors.background },
  content: { padding: spacing.md, paddingBottom: 40 },
  centered: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: spacing.lg },
  header: { marginBottom: spacing.lg },
  headerTitle: { fontSize: 24, fontFamily: fonts.bold, color: colors.text },
  headerSubtitle: { fontSize: 14, fontFamily: fonts.regular, color: colors.textSecondary, marginTop: 4 },
  section: { marginBottom: spacing.lg },
  sectionTitle: { fontSize: 16, fontFamily: fonts.bold, color: colors.text, marginBottom: spacing.sm, textTransform: 'uppercase', letterSpacing: 0.5 },
  badgeCard: {
    flexDirection: 'row',
    backgroundColor: colors.surface,
    borderRadius: borderRadius.md,
    padding: spacing.md,
    marginBottom: spacing.sm,
    borderWidth: 1,
    borderColor: colors.border,
    alignItems: 'flex-start',
  },
  iconContainer: {
    width: 44,
    height: 44,
    borderRadius: borderRadius.sm,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: spacing.md,
  },
  badgeContent: { flex: 1 },
  badgeHeader: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginBottom: 2 },
  badgeName: { fontSize: 15, fontFamily: fonts.semiBold, color: colors.text, flex: 1, marginRight: spacing.xs },
  badgeDescription: { fontSize: 12, fontFamily: fonts.regular, color: colors.textSecondary, marginBottom: spacing.xs },
  progressTrack: { height: 5, backgroundColor: colors.border, borderRadius: borderRadius.full, overflow: 'hidden', marginBottom: 4 },
  progressFill: { height: 5, borderRadius: borderRadius.full },
  progressText: { fontSize: 11, fontFamily: fonts.regular, color: colors.textSecondary },
  completedAt: { fontSize: 11, fontFamily: fonts.regular, color: colors.textSecondary, marginTop: 2 },
  errorText: { fontSize: 15, fontFamily: fonts.regular, color: colors.error, textAlign: 'center', marginBottom: spacing.md },
  retryButton: { backgroundColor: colors.primary, borderRadius: borderRadius.sm, paddingHorizontal: spacing.lg, paddingVertical: 10 },
  retryText: { color: '#fff', fontFamily: fonts.semiBold, fontSize: 14 },
});
```
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - BadgeOverviewScreen.tsx existiert und importiert korrekt
    - AchievementWithProgress-Typ in badges.api.ts exportiert
    - fetchMyAchievements() ruft /api/badges/achievements auf
    - Kein einziges Emoji in der Datei
    - TypeScript: npx tsc --noEmit gibt keine Fehler
  </acceptance_criteria>

  <done>BadgeOverviewScreen und API-Typen kompilieren fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen Badge-Update + Toast-Integration + Navigation</name>
  <files>
    mobile/src/screens/visitor/HomeScreen.tsx,
    mobile/src/navigation/AppNavigator.tsx
  </files>

  <read_first>
    - mobile/src/screens/visitor/HomeScreen.tsx (badge-Anzeige auf neues Achievement-System umstellen)
    - mobile/src/navigation/AppNavigator.tsx (BadgeOverviewScreen hinzufuegen, BadgeLevelsScreen bleibt)
    - mobile/src/screens/visitor/ScanScreen.tsx (Toast nach Scan einfuegen)
    - mobile/src/screens/visitor/CheckInScreen.tsx (Toast nach Check-In einfuegen)
  </read_first>

  <action>
**1. HomeScreen.tsx — Badge-Anzeige auf Achievement umstellen:**

Ersetze die badge-Anzeige auf dem Homescreen. Statt BadgeProgressResponse (currentLevel/nextLevel/pointsToNext) wird das hoechste erreichte Achievement angezeigt.

Importiere `fetchMyAchievements, AchievementWithProgress` aus `../../api/badges.api`.
Entferne `fetchMyBadge` Import (oder behalte ihn fuer Abwaertskompatibilitaet, nutze ihn aber nicht mehr im Render).

Neuer State:
```typescript
const [topAchievement, setTopAchievement] = useState<AchievementWithProgress | null>(null);
const [achievementCount, setAchievementCount] = useState<{ completed: number; total: number } | null>(null);
```

Im useEffect: `fetchMyAchievements()` aufrufen, hoechstes completed Achievement (nach sortOrder absteigend) als `topAchievement` setzen, completed/total Anzahl berechnen.

Badge-Block im Render ersetzen durch:
```tsx
{topAchievement && (
  <View style={styles.badgeLabel}>
    <Icon name={topAchievement.iconName || 'trophy'} solid size={18} color={colors.white} style={{ marginRight: 6 }} />
    <Text style={styles.badgeLabelText}>{topAchievement.name}</Text>
  </View>
)}
{achievementCount && (
  <Text style={styles.pointsToNextLabel}>
    {achievementCount.completed} von {achievementCount.total} Badges erreicht
  </Text>
)}
```

"Badges ansehen"-Button unterhalb des Punkte-Cards hinzufuegen (TouchableOpacity mit navigation.navigate('BadgeOverview')):
```tsx
<TouchableOpacity style={styles.badgesButton} onPress={() => navigation.navigate('BadgeOverview' as never)}>
  <Icon name="medal" solid size={14} color={colors.primary} style={{ marginRight: 6 }} />
  <Text style={styles.badgesButtonText}>Alle Badges ansehen</Text>
</TouchableOpacity>
```

Style dazu:
```typescript
badgesButton: {
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'center',
  paddingVertical: 10,
  backgroundColor: colors.surface,
  borderRadius: borderRadius.md,
  borderWidth: 1,
  borderColor: colors.border,
  marginBottom: spacing.lg,
},
badgesButtonText: {
  fontSize: 14,
  fontFamily: fonts.semiBold,
  color: colors.primary,
},
```

HomeScreen braucht `navigation` Prop. Typ: `import type { NativeStackNavigationProp } from '@react-navigation/native-stack';` oder nutze `useNavigation()` Hook:
```typescript
import { useNavigation } from '@react-navigation/native';
const navigation = useNavigation();
```

**2. AppNavigator.tsx — BadgeOverviewScreen als Stack-Screen hinzufuegen:**

Import hinzufuegen:
```typescript
import BadgeOverviewScreen from '../screens/visitor/BadgeOverviewScreen';
```

In AuthenticatedStack, im Stack.Navigator nach dem `BadgeLevels` Screen:
```tsx
<Stack.Screen
  name="BadgeOverview"
  component={BadgeOverviewScreen}
  options={{ headerShown: true, title: 'Meine Badges' }}
/>
```

**3. Toast bei neuem Badge — ScanScreen.tsx und CheckInScreen.tsx:**

Lies zuerst beide Screens. In beiden Screens: nach dem API-Call wenn `newAchievements` in der Response enthalten sind, zeige einen Toast pro Badge:

```typescript
import Toast from 'react-native-toast-message';

// Nach erfolgreichem Scan/CheckIn:
if (result.newAchievements && result.newAchievements.length > 0) {
  result.newAchievements.forEach((achievement: { name: string; iconName: string }) => {
    Toast.show({
      type: 'success',
      text1: 'Neues Badge freigeschaltet!',
      text2: achievement.name,
      visibilityTime: 3000,
      position: 'top',
    });
  });
}
```

Passe den Typ des API-Response-Objekts in den entsprechenden API-Funktionen an:
- In `mobile/src/api/scan.api.ts` (oder scan.api.ts, Datei zuerst lesen): Response-Typ um `newAchievements?: Array<{ name: string; iconName: string }>` erweitern
- In `mobile/src/api/checkin.api.ts`: ebenso
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - HomeScreen zeigt hoechstes erreichtes Badge (oder nichts wenn noch keins erreicht)
    - HomeScreen zeigt "X von Y Badges erreicht"
    - "Alle Badges ansehen" Button navigiert zu BadgeOverviewScreen
    - Toast erscheint nach Scan/CheckIn wenn newAchievements nicht leer ist
    - BadgeOverviewScreen ist per Navigation erreichbar
    - TypeScript: npx tsc --noEmit gibt keine Fehler
    - Kein einziges Emoji in den geaenderten Dateien
  </acceptance_criteria>

  <done>HomeScreen zeigt Achievement-Fortschritt, Toast funktioniert, Navigation zu BadgeOverview eingerichtet</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visuelle Verifikation Badge-Uebersichtsseite</name>
  <what-built>
    Badge-Uebersichtsseite (BadgeOverviewScreen), aktualisierter HomeScreen mit Achievement-Anzeige, Toast bei neuem Badge.
    Plan 01 muss abgeschlossen und Default-Badges geseedet sein.
  </what-built>
  <how-to-verify>
    1. App starten (ios Simulator oder Device)
    2. Als Visitor einloggen
    3. HomeScreen: Pruefe ob "X von Y Badges erreicht" angezeigt wird (kein Emoji)
    4. "Alle Badges ansehen" antippen: Badges-Uebersichtsseite oeffnet sich
    5. Badges-Seite: Pruefe Kategorien (Bringer, Holer, Besucher, Streaks, Saison, Meilensteine)
    6. Pruefe Fortschrittsbalken und "X von Y"-Text bei ausstehenden Badges
    7. Check-In oder Scan ausfuehren: wenn Badge freigeschaltet, Toast erscheint oben
    8. Pruefe: kein einziges Emoji sichtbar in der gesamten Badge-Anzeige
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe Probleme</resume-signal>
  <action>Manueller Verifikationsschritt — kein Code-Aenderungen erforderlich</action>
  <verify><automated>echo checkpoint</automated></verify>
  <done>Benutzer hat "approved" eingegeben</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile -> /api/badges/achievements | Visitor liest eigene Achievements — JWT erforderlich |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Info Disclosure | BadgeOverviewScreen | accept | Zeigt nur eigene Daten (userId aus JWT auf Backend-Seite) |
| T-09-07 | Spoofing | Toast-Anzeige | accept | Toast basiert auf Server-Response, kein Client-seitiger Trigger |
</threat_model>

<verification>
- cd mobile && npx tsc --noEmit gibt 0 Fehler
- BadgeOverviewScreen.tsx existiert und rendert korrekt
- fetchMyAchievements() ist in badges.api.ts exportiert
- Navigation 'BadgeOverview' in AppNavigator registriert
- HomeScreen importiert fetchMyAchievements (nicht mehr fetchMyBadge)
- Kein Emoji in mobile/src/screens/visitor/BadgeOverviewScreen.tsx (grep -r "[🏆🔥⭐]" gibt nichts)
</verification>

<success_criteria>
- Besucher sieht alle Badges gruppiert nach 6 Kategorien
- Fortschrittsanzeige: "X von Y [Einheit]" fuer ausstehende Badges
- Erreichte Badges hervorgehoben mit Tier-Farbe
- HomeScreen: hoechstes Badge + Gesamtfortschritt-Zaehler
- Toast bei neuem Badge nach Scan oder Check-In
- Keine Emojis sichtbar
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/09-achievement-system/09-02-SUMMARY.md` erstellen.
</output>
