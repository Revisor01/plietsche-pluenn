---
phase: 09-achievement-system
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - mobile/src/screens/admin/AchievementAdminScreen.tsx
  - mobile/src/navigation/AppNavigator.tsx
autonomous: false
requirements:
  - ACH-10

must_haves:
  truths:
    - "Admin-Tab zeigt AchievementAdminScreen statt BadgeLevelsScreen"
    - "Admin sieht alle Achievements des Stores in einer Liste"
    - "Admin kann ein neues Achievement mit Name, Icon, Trigger-Typ, Trigger-Wert, Tier, Saison erstellen"
    - "Admin kann ein bestehendes Achievement bearbeiten"
    - "Admin kann ein Achievement loeschen (mit Bestaetigung)"
    - "Default-Badges koennen per Button geseedet werden"
    - "Kein Emoji sichtbar — alle Icons sind FA5 icon_name Strings"
  artifacts:
    - path: "mobile/src/screens/admin/AchievementAdminScreen.tsx"
      provides: "Admin-CRUD-Screen fuer Achievements"
      exports: ["default AchievementAdminScreen"]
  key_links:
    - from: "mobile/src/navigation/AppNavigator.tsx"
      to: "AchievementAdminScreen"
      via: "AdminTabs Badges-Tab ersetzt BadgeLevelsScreen durch AchievementAdminScreen"
      pattern: "AchievementAdminScreen"
    - from: "AchievementAdminScreen"
      to: "/api/badges/achievements"
      via: "fetchAllAchievements, createAchievement, updateAchievement, deleteAchievement"
      pattern: "achievements"
---

<objective>
Admin Badge-Verwaltung: AchievementAdminScreen mit CRUD fuer Achievements, Default-Badge-Seed-Button, Loeschen mit Bestaetigung. Ersetzt BadgeLevelsScreen im Admin-Tab.

Purpose: Admin kann eigene Badges erstellen/bearbeiten und die Default-Badge-Seed-Funktion ausfuehren, ohne auf Punkte-Schwellen beschraenkt zu sein.
Output: AchievementAdminScreen, aktualisierte Navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-achievement-system/09-CONTEXT.md
@.planning/phases/09-achievement-system/09-01-SUMMARY.md

<interfaces>
<!-- Admin API-Endpunkte (aus Plan 01) -->
// GET  /api/badges/achievements/all  -> Achievement[]  (Admin-Token)
// POST /api/badges/achievements      -> Achievement    (Admin-Token)
// PATCH /api/badges/achievements/:id -> Achievement    (Admin-Token)
// DELETE /api/badges/achievements/:id -> 204           (Admin-Token)
// POST /api/badges/achievements/seed -> { seeded, achievements }  (Admin-Token, einmalig)

<!-- Achievement-Typ (aus badges.api.ts nach Plan 01) -->
```typescript
export interface Achievement {
  id: string;
  name: string;
  description: string;
  iconName: string;          // FA5 icon_name, kein Emoji
  triggerType: TriggerType;  // items_brought | items_taken | visits | streak_weeks | season_items_brought | season_items_taken | milestone
  triggerValue: number;      // Schwellenwert (z.B. 10, 25, 50, 100)
  tier: Tier;                // bronze | silber | gold | custom
  season: Season | null;     // fruehling | sommer | herbst | winter | null
  sortOrder: number;
}
```

<!-- Picker-Optionen fuer triggerType -->
// items_brought -> 'Teile gebracht'
// items_taken   -> 'Teile geholt'
// visits        -> 'Check-Ins'
// streak_weeks  -> 'Streak (Wochen)'
// season_items_brought -> 'Saisonteile gebracht'
// season_items_taken   -> 'Saisonteile geholt'
// milestone     -> 'Meilenstein'

<!-- Picker-Optionen fuer tier -->
// bronze -> 'Bronze'
// silber -> 'Silber'
// gold   -> 'Gold'
// custom -> 'Individual'

<!-- Mobile-Infrastruktur -->
// Kein Emoji. FA5 icon_name Strings.
// colors, fonts, spacing, borderRadius aus '../../theme'
// apiClient aus '../../api/client'
// Alert aus 'react-native' fuer Loeschen-Bestaetigung
// KEIN @react-native-picker/picker installiert — Picker als ScrollView mit Auswahl-Buttons implementieren
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin API-Funktionen + AchievementAdminScreen erstellen</name>
  <files>
    mobile/src/api/badges.api.ts,
    mobile/src/screens/admin/AchievementAdminScreen.tsx
  </files>

  <read_first>
    - mobile/src/api/badges.api.ts (Admin-Funktionen anhaengen, bestehende behalten)
    - mobile/src/screens/admin/BadgeLevelsScreen.tsx (UI-Pattern und StyleSheet-Konventionen)
  </read_first>

  <action>
**1. mobile/src/api/badges.api.ts — Admin-Funktionen anhaengen:**

```typescript
// Admin: alle Achievements des Stores
export async function fetchAllAchievements(): Promise<Achievement[]> {
  const { data } = await apiClient.get<Achievement[]>('/api/badges/achievements/all');
  return data;
}

export interface CreateAchievementInput {
  name: string;
  description: string;
  iconName: string;
  triggerType: TriggerType;
  triggerValue: number;
  tier: Tier;
  season: Season | null;
  sortOrder: number;
}

export async function createAchievement(body: CreateAchievementInput): Promise<Achievement> {
  const { data } = await apiClient.post<Achievement>('/api/badges/achievements', body);
  return data;
}

export async function updateAchievement(id: string, body: Partial<CreateAchievementInput>): Promise<Achievement> {
  const { data } = await apiClient.patch<Achievement>(`/api/badges/achievements/${id}`, body);
  return data;
}

export async function deleteAchievement(id: string): Promise<void> {
  await apiClient.delete(`/api/badges/achievements/${id}`);
}

export async function seedAchievements(): Promise<{ seeded: number; achievements: Achievement[] }> {
  const { data } = await apiClient.post<{ seeded: number; achievements: Achievement[] }>('/api/badges/achievements/seed');
  return data;
}
```

**2. mobile/src/screens/admin/AchievementAdminScreen.tsx erstellen:**

Vollstaendige Implementierung mit:

**State:**
- `achievements: Achievement[]` — geladene Liste
- `isLoading: boolean`
- `error: string | null`
- `editingId: string | null` — null = Neu-Formular, string = Edit-Modus
- Formular-State: `formName`, `formDescription`, `formIconName`, `formTriggerType`, `formTriggerValue`, `formTier`, `formSeason`, `formSortOrder`
- `isSaving: boolean`
- `isSeeding: boolean`

**Layout:**
- FlatList mit Achievement-Karten oben
- Formular am Ende (ListFooterComponent) fuer Neu/Bearbeiten
- "Default-Badges laden" Button wenn Liste leer

**Achievement-Karte:**
- Icon (FA5), Name, triggerType-Label, triggerValue, Tier-Badge
- "Bearbeiten"-Button (fuellt Formular, setzt editingId)
- "Loeschen"-Button (Alert.alert Bestaetigung)

**Formular:**
- TextInput fuer Name (required), Description, Icon-Name
- Trigger-Typ Auswahl: horizontale ScrollView mit Buttons (kein Picker/Dropdown)
- Trigger-Wert: TextInput numeric
- Tier Auswahl: 4 Buttons nebeneinander (bronze/silber/gold/custom)
- Saison Auswahl: 5 Buttons (fruehling/sommer/herbst/winter/keine)
- Speichern-Button: erstellt oder aktualisiert
- "Abbrechen"-Button wenn im Edit-Modus

**Trigger-Typ Labels:**
```typescript
const TRIGGER_LABELS: Record<TriggerType, string> = {
  items_brought: 'Teile gebracht',
  items_taken: 'Teile geholt',
  visits: 'Check-Ins',
  streak_weeks: 'Streak (Wo.)',
  season_items_brought: 'Saison gebr.',
  season_items_taken: 'Saison geholt',
  milestone: 'Meilenstein',
};
```

**Tier-Farben** (gleich wie BadgeOverviewScreen):
```typescript
const TIER_COLORS = { bronze: '#CD7F32', silber: '#A8A9AD', gold: '#FFD700', custom: colors.primary };
```

**Formular-Defaults fuer neues Achievement:**
```typescript
const resetForm = () => {
  setFormName('');
  setFormDescription('');
  setFormIconName('trophy');
  setFormTriggerType('visits');
  setFormTriggerValue('10');
  setFormTier('custom');
  setFormSeason(null);
  setFormSortOrder('0');
  setEditingId(null);
};
```

**handleSave:**
```typescript
const handleSave = async () => {
  if (!formName.trim()) { Alert.alert('Fehler', 'Name ist erforderlich'); return; }
  const val = parseInt(formTriggerValue, 10);
  if (isNaN(val) || val < 1) { Alert.alert('Fehler', 'Trigger-Wert muss mindestens 1 sein'); return; }
  setIsSaving(true);
  try {
    const body: CreateAchievementInput = {
      name: formName.trim(),
      description: formDescription.trim(),
      iconName: formIconName.trim() || 'trophy',
      triggerType: formTriggerType,
      triggerValue: val,
      tier: formTier,
      season: formSeason,
      sortOrder: parseInt(formSortOrder, 10) || 0,
    };
    if (editingId) {
      const updated = await updateAchievement(editingId, body);
      setAchievements((prev) => prev.map((a) => a.id === editingId ? updated : a));
    } else {
      const created = await createAchievement(body);
      setAchievements((prev) => [...prev, created].sort((a, b) => a.sortOrder - b.sortOrder));
    }
    resetForm();
  } catch {
    Alert.alert('Fehler', 'Badge konnte nicht gespeichert werden');
  } finally {
    setIsSaving(false);
  }
};
```

**handleSeed:**
```typescript
const handleSeed = async () => {
  Alert.alert('Default-Badges laden', '23 Standard-Badges fuer alle Kategorien werden angelegt. Bestehende Badges werden nicht ueberschrieben.', [
    { text: 'Abbrechen', style: 'cancel' },
    { text: 'Laden', onPress: async () => {
      setIsSeeding(true);
      try {
        const result = await seedAchievements();
        setAchievements(result.achievements.sort((a, b) => a.sortOrder - b.sortOrder));
      } catch {
        Alert.alert('Fehler', 'Default-Badges konnten nicht geladen werden');
      } finally {
        setIsSeeding(false);
      }
    }},
  ]);
};
```

Kein einziges Emoji. Alle Icons sind FA5 icon_name Strings. Alle Texte auf Deutsch.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - AchievementAdminScreen.tsx existiert und kompiliert fehlerfrei
    - Admin-API-Funktionen in badges.api.ts exportiert
    - Kein Emoji in der Datei (grep -r "[🏆🔥⭐🌟]" gibt nichts)
    - TypeScript: npx tsc --noEmit gibt keine Fehler
  </acceptance_criteria>

  <done>AchievementAdminScreen und Admin-API-Funktionen kompilieren fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: Navigation umstellen — AdminTabs auf AchievementAdminScreen</name>
  <files>
    mobile/src/navigation/AppNavigator.tsx
  </files>

  <read_first>
    - mobile/src/navigation/AppNavigator.tsx (AdminTabs Badges-Tab und BadgeLevels Stack-Screen aendern)
  </read_first>

  <action>
**AppNavigator.tsx aendern:**

1. Import anpassen:
```typescript
// ALT:
import BadgeLevelsScreen from '../screens/admin/BadgeLevelsScreen';
// NEU (zusaetzlich):
import AchievementAdminScreen from '../screens/admin/AchievementAdminScreen';
// BadgeLevelsScreen Import BEHALTEN (wird noch als Stack-Screen gebraucht fuer Abwaertskompatibilitaet)
```

2. In `AdminTabs` den Badges-Tab-Screen ersetzen:
```tsx
// ALT:
<Tab.Screen
  name="Badges"
  component={BadgeLevelsScreen}
  options={{ title: 'Badges', ... }}
/>

// NEU:
<Tab.Screen
  name="Badges"
  component={AchievementAdminScreen}
  options={{
    title: 'Badges',
    tabBarIcon: ({ focused, color, size }) => (
      <Icon name="trophy" solid={focused} size={size} color={color} />
    ),
  }}
/>
```

3. Den `BadgeLevels` Stack.Screen in `AuthenticatedStack` auf `AchievementAdmin` umbenennen:
```tsx
// ALT:
<Stack.Screen
  name="BadgeLevels"
  component={BadgeLevelsScreen}
  options={{ headerShown: true, title: 'Badge-Stufen' }}
/>

// NEU:
<Stack.Screen
  name="AchievementAdmin"
  component={AchievementAdminScreen}
  options={{ headerShown: true, title: 'Badge-Verwaltung' }}
/>
```

BadgeLevelsScreen Import kann nach dieser Aenderung entfernt werden, wenn er nirgends mehr genutzt wird. Pruefe vorher ob noch andere Stellen BadgeLevelsScreen referenzieren.

Sicherstellen dass `BadgeOverview` Stack.Screen aus Plan 02 bereits vorhanden ist — falls nicht, ebenfalls einfuegen:
```tsx
<Stack.Screen
  name="BadgeOverview"
  component={BadgeOverviewScreen}
  options={{ headerShown: true, title: 'Meine Badges' }}
/>
```
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - AdminTabs Badges-Tab zeigt AchievementAdminScreen (nicht mehr BadgeLevelsScreen)
    - TypeScript: npx tsc --noEmit gibt keine Fehler
    - Kein Emoji in AppNavigator.tsx
  </acceptance_criteria>

  <done>Navigation umgestellt, kein TypeScript-Fehler</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visuelle Verifikation AchievementAdminScreen</name>
  <what-built>
    AchievementAdminScreen mit CRUD fuer Badges, Default-Seed-Button, und aktualisierte Navigation.
  </what-built>
  <how-to-verify>
    1. Als Admin einloggen
    2. Badges-Tab oeffnen: AchievementAdminScreen soll erscheinen (nicht mehr die alte Punkte-Stufen-Liste)
    3. "Default-Badges laden" tippen (wenn Liste leer): 23 Badges werden geladen
    4. Badge in der Liste antippen "Bearbeiten": Formular fuellt sich mit Badge-Daten
    5. Name aendern und Speichern: Badge wird in der Liste aktualisiert
    6. Badge loeschen: Bestaetigung erscheint, nach Bestaetigung aus Liste entfernt
    7. Neues Badge erstellen: Trigger-Typ waehlen, Wert eingeben, Speichern
    8. Pruefe: kein Emoji sichtbar im gesamten Screen
  </how-to-verify>
  <resume-signal>Tippe "approved" oder beschreibe Probleme</resume-signal>
  <action>Manueller Verifikationsschritt — kein Code-Aenderungen erforderlich</action>
  <verify><automated>echo checkpoint</automated></verify>
  <done>Benutzer hat "approved" eingegeben</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mobile -> /api/badges/achievements (mutierend) | Admin-Only — requireRole('admin') auf Backend |
| mobile -> /api/badges/achievements/seed | Admin-Only Einmalfunktion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Elevation | AchievementAdminScreen | mitigate | Endpunkte haben requireRole('admin') auf Backend-Seite; Client-seitiger Screen ist nur fuer Admins sichtbar (role-basiertes Routing) |
| T-09-09 | Tampering | Achievement-Seed | accept | Seed ist idempotent (prueft ob bereits geseedet), kein Datenverlust |
| T-09-10 | DoS | Bulk-Delete | accept | Einzelnes Loeschen pro Alert-Bestaetigung, kein Bulk-Loeschen |
</threat_model>

<verification>
- cd mobile && npx tsc --noEmit gibt 0 Fehler
- AchievementAdminScreen.tsx existiert
- AdminTabs Badges-Tab importiert AchievementAdminScreen
- fetchAllAchievements, createAchievement, updateAchievement, deleteAchievement, seedAchievements alle exportiert
- Kein Emoji in neuen/geaenderten Dateien
</verification>

<success_criteria>
- Admin kann Achievements erstellen, bearbeiten, loeschen
- Default-Seed-Button laedt 23 Badges
- Navigation umgestellt: Badges-Tab zeigt AchievementAdminScreen
- Alle Badges ohne Emoji dargestellt
- TypeScript kompiliert ohne Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/09-achievement-system/09-03-SUMMARY.md` erstellen.
</output>
