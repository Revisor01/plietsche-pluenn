---
phase: 06-engagement-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/campaigns/campaigns.types.ts
  - backend/src/modules/campaigns/campaigns.repository.ts
  - backend/src/modules/campaigns/campaigns.service.ts
  - backend/src/modules/campaigns/campaigns.router.ts
  - backend/src/modules/scan/scan.service.ts
  - backend/src/modules/checkin/checkin.service.ts
  - backend/src/app.ts
  - mobile/src/api/campaigns.api.ts
  - mobile/src/screens/admin/CampaignListScreen.tsx
  - mobile/src/screens/admin/CampaignCreateScreen.tsx
  - mobile/src/navigation/AppNavigator.tsx
autonomous: true
requirements:
  - CAMP-01
  - CAMP-02
  - CAMP-03
  - CAMP-04

must_haves:
  truths:
    - "Admin kann Kampagne mit Titel, Beschreibung, Start/Ende, Multiplikator erstellen"
    - "Kampagnen-Liste zeigt alle Kampagnen des Stores im Admin"
    - "Aktive Kampagne multipliziert Punkte bei Item-Scan und Check-In automatisch"
    - "Aktiver Kampagnen-Banner erscheint auf dem Visitor-Homescreen"
    - "Punkte-Engine berechnet: basePoints * multiplier (ganzzahlig gerundet)"
  artifacts:
    - path: "backend/src/db/schema.ts"
      provides: "campaigns-Tabelle (id, store_id, title, description, multiplier, starts_at, ends_at, created_at)"
      contains: "export const campaigns"
    - path: "backend/src/modules/campaigns/campaigns.router.ts"
      provides: "CRUD-Endpunkte POST/GET/PATCH/DELETE /api/campaigns"
      exports: ["default router"]
    - path: "mobile/src/screens/admin/CampaignListScreen.tsx"
      provides: "Kampagnen-Liste für Volunteer/Admin-Tab"
    - path: "mobile/src/screens/admin/CampaignCreateScreen.tsx"
      provides: "Formular zum Erstellen einer Kampagne"
  key_links:
    - from: "scan.service.ts + checkin.service.ts"
      to: "campaigns.repository.getActiveCampaign()"
      via: "async call vor awardPoints"
      pattern: "getActiveCampaign"
    - from: "mobile/HomeScreen"
      to: "/api/campaigns?active=true"
      via: "fetchActiveCampaigns in campaigns.api.ts"
      pattern: "fetchActiveCampaigns"
---

<objective>
Kampagnen-Backend: Tabelle, CRUD-API und Multiplikator-Logik in der Punkte-Engine. Plus Admin-Screens zum Verwalten.

Purpose: Admins können zeitlich begrenzte Aktionen anlegen, die automatisch Punkte multiplizieren — ohne Codeänderung.
Output: /api/campaigns REST-API, Multiplikator in scan.service + checkin.service, CampaignList + CampaignCreate Screens im Volunteer-Tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

KRITISCHE CONSTRAINTS:
- Docker NICHT lokal verfügbar
- Schema-Änderungen per: ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c '...'"
- Backend rebuild: ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn && git pull && cd backend && docker build -t plietsche-backend:latest . && docker stop plietsche-backend && docker rm plietsche-backend" dann Container aus Compose neu starten
- Testen: curl -skL https://xn--plietsche-plnn-rsb.de/api/...
- Store UUID: 6a0fb86e-d795-4fd3-87fa-a21143f830b3
- TypeScript prüfen: cd mobile && npx tsc --noEmit

<interfaces>
<!-- Bestehende Muster für die Implementierung -->

Aus backend/src/db/schema.ts:
```typescript
import { pgTable, uuid, text, timestamp, integer, real } from 'drizzle-orm/pg-core';
// Alle Tabellen folgen diesem Muster: uuid PK, storeId FK, timestamps
export const stores = pgTable('stores', { id: uuid('id').primaryKey().defaultRandom(), ... });
```

Aus backend/src/modules/auth/auth.router.ts (Muster für neue Router):
```typescript
import { Router, type Request, type Response } from 'express';
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
// Express 5: async Fehler propagieren automatisch — kein try/catch nötig
router.post('/', authenticateToken, requireRole('admin'), async (req, res) => { ... });
```

Aus backend/src/modules/scan/scan.service.ts (wo Multiplikator eingebaut wird):
```typescript
const settings = await getStoreSettings(storeId);
const pointsPerScan = settings.pointsPerScan;
// HIER: Kampagnen-Multiplikator abrufen und anwenden
await scanRepo.awardPoints(userId, storeId, pointsPerScan, 'item_scan');
```

Aus backend/src/modules/checkin/checkin.service.ts (wo Multiplikator eingebaut wird):
```typescript
const basePoints = settings.pointsPerCheckin;
const itemPoints = Math.min(itemCount, settings.maxItemsPerCheckin) * settings.pointsPerItem;
const totalPoints = await awardPoints(userId, storeId, basePoints + itemPoints, 'checkin');
// HIER: Kampagnen-Multiplikator auf (basePoints + itemPoints) anwenden
```

Aus backend/src/app.ts (Router einhängen):
```typescript
import campaignsRouter from './modules/campaigns/campaigns.router';
app.use('/api/campaigns', campaignsRouter);
```

Aus mobile/src/api/client.ts:
```typescript
export const apiClient = axios.create({ baseURL: 'https://xn--plietsche-plnn-rsb.de', ... });
// Token wird automatisch per Interceptor gesetzt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + Backend (Tabelle, Repository, Service, Router)</name>
  <files>
    backend/src/db/schema.ts
    backend/src/modules/campaigns/campaigns.types.ts
    backend/src/modules/campaigns/campaigns.repository.ts
    backend/src/modules/campaigns/campaigns.service.ts
    backend/src/modules/campaigns/campaigns.router.ts
    backend/src/app.ts
  </files>
  <read_first>
    - backend/src/db/schema.ts (Schema erweitern — bestehende Struktur)
    - backend/src/modules/items/items.router.ts (Router-Muster mit requireRole)
    - backend/src/modules/items/items.service.ts (Service-Muster)
    - backend/src/app.ts (Router einhängen)
  </read_first>
  <action>
1. In backend/src/db/schema.ts: `campaigns`-Tabelle hinzufügen:
```typescript
import { boolean } from 'drizzle-orm/pg-core'; // falls noch nicht importiert
export const campaigns = pgTable('campaigns', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  title: text('title').notNull(),
  description: text('description'),
  multiplier: real('multiplier').notNull().default(1.0),
  startsAt: timestamp('starts_at').notNull(),
  endsAt: timestamp('ends_at').notNull(),
  createdAt: timestamp('created_at').defaultNow(),
});
```

2. campaigns.types.ts erstellen:
```typescript
export interface CreateCampaignBody {
  title: string;
  description?: string;
  multiplier: number;
  startsAt: string; // ISO string
  endsAt: string;
}
export interface UpdateCampaignBody {
  title?: string;
  description?: string;
  multiplier?: number;
  startsAt?: string;
  endsAt?: string;
}
```

3. campaigns.repository.ts erstellen:
```typescript
import { db } from '../../db/client';
import { campaigns } from '../../db/schema';
import { eq, and, lte, gte } from 'drizzle-orm';
import type { CreateCampaignBody, UpdateCampaignBody } from './campaigns.types';

export async function getActiveCampaign(storeId: string) {
  const now = new Date();
  const result = await db.select().from(campaigns)
    .where(and(
      eq(campaigns.storeId, storeId),
      lte(campaigns.startsAt, now),
      gte(campaigns.endsAt, now),
    ))
    .limit(1);
  return result[0] ?? null;
}

export async function listCampaigns(storeId: string) {
  return db.select().from(campaigns)
    .where(eq(campaigns.storeId, storeId))
    .orderBy(campaigns.startsAt);
}

export async function createCampaign(storeId: string, data: CreateCampaignBody) {
  const result = await db.insert(campaigns).values({
    storeId,
    title: data.title,
    description: data.description ?? null,
    multiplier: data.multiplier,
    startsAt: new Date(data.startsAt),
    endsAt: new Date(data.endsAt),
  }).returning();
  return result[0];
}

export async function updateCampaign(id: string, storeId: string, data: UpdateCampaignBody) {
  const set: Record<string, unknown> = {};
  if (data.title !== undefined) set.title = data.title;
  if (data.description !== undefined) set.description = data.description;
  if (data.multiplier !== undefined) set.multiplier = data.multiplier;
  if (data.startsAt !== undefined) set.startsAt = new Date(data.startsAt);
  if (data.endsAt !== undefined) set.endsAt = new Date(data.endsAt);
  const result = await db.update(campaigns).set(set)
    .where(and(eq(campaigns.id, id), eq(campaigns.storeId, storeId)))
    .returning();
  return result[0] ?? null;
}

export async function deleteCampaign(id: string, storeId: string) {
  await db.delete(campaigns)
    .where(and(eq(campaigns.id, id), eq(campaigns.storeId, storeId)));
}
```

4. campaigns.service.ts erstellen (dünne Schicht über Repository):
```typescript
import * as repo from './campaigns.repository';
import type { CreateCampaignBody, UpdateCampaignBody } from './campaigns.types';

export const getActiveCampaign = (storeId: string) => repo.getActiveCampaign(storeId);
export const listCampaigns = (storeId: string) => repo.listCampaigns(storeId);
export const createCampaign = (storeId: string, data: CreateCampaignBody) => repo.createCampaign(storeId, data);
export const updateCampaign = (id: string, storeId: string, data: UpdateCampaignBody) => repo.updateCampaign(id, storeId, data);
export const deleteCampaign = (id: string, storeId: string) => repo.deleteCampaign(id, storeId);
```

5. campaigns.router.ts erstellen:
```typescript
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
import * as campaignsService from './campaigns.service';

const router = Router();

const CreateSchema = z.object({
  title: z.string().min(1).max(200),
  description: z.string().optional(),
  multiplier: z.number().min(1).max(10),
  startsAt: z.string().datetime(),
  endsAt: z.string().datetime(),
});

const UpdateSchema = CreateSchema.partial();

// GET /api/campaigns — alle Kampagnen des Stores (Admin + Volunteer zum Anzeigen)
router.get('/', authenticateToken, requireRole('volunteer', 'admin'), async (req: Request, res: Response) => {
  const result = await campaignsService.listCampaigns(req.user!.storeId);
  res.json(result);
});

// GET /api/campaigns/active — aktive Kampagne (auch für Visitor: ohne requireRole)
router.get('/active', authenticateToken, async (req: Request, res: Response) => {
  const campaign = await campaignsService.getActiveCampaign(req.user!.storeId);
  res.json(campaign ?? null);
});

// POST /api/campaigns — nur Admin
router.post('/', authenticateToken, requireRole('admin'), async (req: Request, res: Response) => {
  const body = CreateSchema.parse(req.body);
  const campaign = await campaignsService.createCampaign(req.user!.storeId, body);
  res.status(201).json(campaign);
});

// PATCH /api/campaigns/:id — nur Admin
router.patch('/:id', authenticateToken, requireRole('admin'), async (req: Request, res: Response) => {
  const body = UpdateSchema.parse(req.body);
  const campaign = await campaignsService.updateCampaign(req.params.id, req.user!.storeId, body);
  if (!campaign) { res.status(404).json({ error: 'Kampagne nicht gefunden' }); return; }
  res.json(campaign);
});

// DELETE /api/campaigns/:id — nur Admin
router.delete('/:id', authenticateToken, requireRole('admin'), async (req: Request, res: Response) => {
  await campaignsService.deleteCampaign(req.params.id, req.user!.storeId);
  res.status(204).send();
});

export default router;
```

6. In backend/src/app.ts: Router einhängen (per CAMP-01):
```typescript
import campaignsRouter from './modules/campaigns/campaigns.router';
// nach den bestehenden Router-Importen:
app.use('/api/campaigns', campaignsRouter);
```

7. SQL auf Server ausführen:
```bash
ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"
CREATE TABLE IF NOT EXISTS campaigns (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id),
  title TEXT NOT NULL,
  description TEXT,
  multiplier REAL NOT NULL DEFAULT 1.0,
  starts_at TIMESTAMPTZ NOT NULL,
  ends_at TIMESTAMPTZ NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);
\""
```
  </action>
  <verify>
    <automated>
# Nach git push + Docker rebuild auf Server:
curl -skL -X POST https://xn--plietsche-plnn-rsb.de/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@test.de","password":"test1234"}' | grep -o '"token":"[^"]*"' > /tmp/token.txt

TOKEN=$(cat /tmp/token.txt | sed 's/"token":"//;s/"//')

# Kampagne erstellen
curl -skL -X POST https://xn--plietsche-plnn-rsb.de/api/campaigns \
  -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' \
  -d '{"title":"Test-Aktion","multiplier":2,"startsAt":"2026-01-01T00:00:00Z","endsAt":"2030-12-31T23:59:59Z"}'
# Erwartung: 201 mit id, title, multiplier

# Aktive Kampagne abrufen
curl -skL https://xn--plietsche-plnn-rsb.de/api/campaigns/active \
  -H "Authorization: Bearer $TOKEN"
# Erwartung: JSON mit multiplier: 2
    </automated>
  </verify>
  <acceptance_criteria>
    - POST /api/campaigns erstellt Kampagne, gibt 201 + vollständiges Objekt zurück
    - GET /api/campaigns/active gibt aktive Kampagne (starts_at <= now <= ends_at) zurück
    - GET /api/campaigns listet alle Kampagnen des Stores
    - PATCH/DELETE funktionieren für Admin
    - Visitor-Token kann GET /api/campaigns/active aufrufen
  </acceptance_criteria>
  <done>campaigns-Tabelle in DB, alle 5 Endpunkte funktionieren, Router in app.ts eingehängt</done>
</task>

<task type="auto">
  <name>Task 2: Multiplikator in Punkte-Engine (scan + checkin)</name>
  <files>
    backend/src/modules/scan/scan.service.ts
    backend/src/modules/checkin/checkin.service.ts
  </files>
  <read_first>
    - backend/src/modules/scan/scan.service.ts
    - backend/src/modules/checkin/checkin.service.ts
    - backend/src/modules/campaigns/campaigns.repository.ts (getActiveCampaign — aus Task 1)
  </read_first>
  <action>
1. scan.service.ts — Multiplikator einbauen (per CAMP-02, CAMP-04):
```typescript
import * as scanRepo from './scan.repository';
import { getStoreSettings } from '../points/points.repository';
import { getActiveCampaign } from '../campaigns/campaigns.repository';

export async function scanItemQr(qrToken: string, userId: string, storeId: string) {
  const item = await scanRepo.findItemByQrToken(qrToken, storeId);
  if (!item) throw Object.assign(new Error('Unbekannter QR-Code'), { statusCode: 404 });
  if (item.status === 'taken') throw Object.assign(new Error('Dieses Teil wurde bereits mitgenommen'), { statusCode: 409 });
  if (item.createdBy && item.createdBy === userId) throw Object.assign(new Error('Du hast dieses Teil eingestellt'), { statusCode: 403 });

  const settings = await getStoreSettings(storeId);
  const campaign = await getActiveCampaign(storeId);
  const multiplier = campaign?.multiplier ?? 1;
  const basePoints = settings.pointsPerScan;
  const pointsAwarded = Math.round(basePoints * multiplier);

  await scanRepo.markItemTaken(item.id);
  const totalPoints = await scanRepo.awardPoints(userId, storeId, pointsAwarded, 'item_scan');

  return { points: pointsAwarded, title: item.title, totalPoints, campaign: campaign ? { title: campaign.title, multiplier } : null };
}
```

2. checkin.service.ts — Multiplikator einbauen (per CAMP-02, CAMP-04):
Nur die processCheckin-Funktion am Ende ändern:
```typescript
import { getActiveCampaign } from '../campaigns/campaigns.repository';
// ...innerhalb processCheckin, nach getStoreSettings:
const campaign = await getActiveCampaign(storeId);
const multiplier = campaign?.multiplier ?? 1;
const basePoints = settings.pointsPerCheckin;
const itemPoints = Math.min(itemCount, settings.maxItemsPerCheckin) * settings.pointsPerItem;
const pointsAwarded = Math.round((basePoints + itemPoints) * multiplier);
const totalPoints = await awardPoints(userId, storeId, pointsAwarded, 'checkin');
return { points: Math.round(basePoints * multiplier), itemPoints: Math.round(itemPoints * multiplier), totalPoints, campaign: campaign ? { title: campaign.title, multiplier } : null };
```

Multiplier: Math.round() sicherstellt ganzzahlige Punkte. Kein Multiplikator = 1.0 = keine Änderung.
  </action>
  <verify>
    <automated>
# Vorher-Nachher-Test: aktive Kampagne mit multiplier=2 setzen, dann scannen/einchecken
# Erwartung: doppelte Punkte im Response-Objekt
# Test via Backend-Log oder direkter API-Aufruf mit bekanntem QR-Token
cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - scan.service.ts und checkin.service.ts importieren getActiveCampaign
    - Wenn aktive Kampagne (multiplier=2) existiert: zurückgegebene points = Math.round(base * 2)
    - Wenn keine aktive Kampagne: Verhalten identisch zum vorherigen Stand
    - Response-Objekt enthält campaign-Feld (null oder { title, multiplier })
    - TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>
  <done>Beide Services multiplizieren Punkte mit aktivem Kampagnen-Multiplikator, campaign-Feld im Response</done>
</task>

<task type="auto">
  <name>Task 3: Admin-Screens (CampaignList + CampaignCreate) + Kampagnen-API</name>
  <files>
    mobile/src/api/campaigns.api.ts
    mobile/src/screens/admin/CampaignListScreen.tsx
    mobile/src/screens/admin/CampaignCreateScreen.tsx
    mobile/src/navigation/AppNavigator.tsx
  </files>
  <read_first>
    - mobile/src/navigation/AppNavigator.tsx (VolunteerTabs-Struktur und Stack-Muster)
    - mobile/src/screens/admin/DashboardScreen.tsx (Admin-Screen-Muster)
    - mobile/src/api/items.api.ts (API-Muster mit apiClient)
    - mobile/src/theme/index.ts (colors, fonts, spacing, borderRadius)
  </read_first>
  <action>
1. mobile/src/api/campaigns.api.ts erstellen:
```typescript
import { apiClient } from './client';

export interface Campaign {
  id: string;
  storeId: string;
  title: string;
  description?: string;
  multiplier: number;
  startsAt: string;
  endsAt: string;
  createdAt: string;
}

export async function fetchActiveCampaign(): Promise<Campaign | null> {
  const { data } = await apiClient.get<Campaign | null>('/api/campaigns/active');
  return data;
}

export async function fetchCampaigns(): Promise<Campaign[]> {
  const { data } = await apiClient.get<Campaign[]>('/api/campaigns');
  return data;
}

export async function createCampaign(body: {
  title: string;
  description?: string;
  multiplier: number;
  startsAt: string;
  endsAt: string;
}): Promise<Campaign> {
  const { data } = await apiClient.post<Campaign>('/api/campaigns', body);
  return data;
}

export async function deleteCampaign(id: string): Promise<void> {
  await apiClient.delete(`/api/campaigns/${id}`);
}
```

2. CampaignListScreen.tsx erstellen:
- FlatList mit allen Kampagnen (Titel, Multiplikator als "2x", Start-Ende-Datum)
- Aktive Kampagne visuell hervorheben (grüner Rand oder Badge "Aktiv")
- Löschen-Button pro Kampagne (Alert zur Bestätigung)
- FAB oder Header-Button "+" zum Navigieren zu CampaignCreateScreen
- Leere State: "Keine Kampagnen" mit Hinweistext
- Styling mit colors.surface, borderRadius.md, fonts.semiBold für Multiplikator

3. CampaignCreateScreen.tsx erstellen:
- Formular: Titel (TextInput), Beschreibung (optional, TextInput multiline), Multiplikator (TextInput numeric, Hinweis "z.B. 2 für doppelte Punkte"), Startdatum (TextInput ISO oder DatePicker-freundlich, Format: YYYY-MM-DDTHH:mm:ssZ), Enddatum
- Validierung: Titel nicht leer, Multiplikator >= 1 und <= 10, Enddatum nach Startdatum
- Submit-Button: "Kampagne erstellen"
- Bei Erfolg: zurück zu CampaignListScreen navigieren + Liste neu laden

4. AppNavigator.tsx — CampaignScreens in VolunteerTabs einbinden (per CAMP-03 der Admin-Zugang):
- In VolunteerTabs einen neuen Tab "Kampagnen" (CampaignListScreen) hinzufügen
- CampaignCreateScreen als Stack-Screen (nicht Tab) hinter CampaignListScreen
- Da VolunteerTabs eine Tab.Navigator-Struktur ist: CampaignList als Tab, Create über Stack navigieren
- AuthenticatedStack: Neuen Stack.Screen "CampaignCreate" hinzufügen (headerShown: true, title: 'Neue Kampagne')
- Import: CampaignListScreen und CampaignCreateScreen

Hinweis: CampaignListScreen und CampaignCreateScreen liegen in mobile/src/screens/admin/ — Volunteer-Tab zeigt diesen Bereich ja eh schon (Dashboard). Der Tab "Kampagnen" ist im Volunteer-Tab-Navigator.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript-Kompilierung ohne Fehler
    - CampaignListScreen rendert Liste (FlatList) oder Leere-State-Meldung
    - CampaignCreateScreen validiert Eingaben vor API-Call
    - AppNavigator.tsx importiert und registriert beide neuen Screens
    - Kampagne mit startsAt <= now <= endsAt wird als "Aktiv" markiert
  </acceptance_criteria>
  <done>Beide Admin-Screens erstellt, in Navigation eingebaut, TypeScript sauber</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor→API | Visitor-Token kann /api/campaigns/active lesen (gewollt) aber nicht POST/PATCH/DELETE |
| Admin→API | Admin-Token kann Kampagnen anlegen/ändern/löschen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Elevation | POST /api/campaigns | mitigate | requireRole('admin') — Volunteer und Visitor erhalten 403 |
| T-06-01-02 | Tampering | PATCH /api/campaigns/:id | mitigate | where storeId = req.user.storeId — Admin kann nur eigenen Store ändern |
| T-06-01-03 | Information | GET /api/campaigns/active | accept | Kampagnen-Info ist öffentlich sichtbar (Banner-Konzept), kein PII |
| T-06-01-04 | Denial | multiplier=999 | mitigate | Zod-Schema: max(10) verhindert extreme Multiplikatoren |
</threat_model>

<verification>
Backend-Endpunkte:
- `curl -skL https://xn--plietsche-plnn-rsb.de/api/campaigns/active -H "Authorization: Bearer $VISITOR_TOKEN"` → 200 (null oder Campaign-Objekt)
- `curl -skL -X POST https://xn--plietsche-plnn-rsb.de/api/campaigns -H "Authorization: Bearer $VISITOR_TOKEN" ...` → 403
- `curl -skL -X POST https://xn--plietsche-plnn-rsb.de/api/campaigns -H "Authorization: Bearer $ADMIN_TOKEN" ...` → 201

Punkte-Multiplikator:
- Aktive 2x-Kampagne erstellen, dann Item scannen → Response.points = base * 2
- Keine aktive Kampagne → Response.points = base (unverändert)
</verification>

<success_criteria>
1. Kampagnen-Tabelle in PostgreSQL vorhanden
2. POST/GET/PATCH/DELETE /api/campaigns funktionieren (Admin-geschützt)
3. GET /api/campaigns/active gibt aktive Kampagne zurück (Visitor-Token akzeptiert)
4. scan.service.ts und checkin.service.ts multiplizieren Punkte mit campaign.multiplier
5. CampaignList und CampaignCreate Screens im Volunteer-Tab erreichbar
6. TypeScript: npx tsc --noEmit ohne Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-engagement-features/06-01-SUMMARY.md` erstellen
</output>
