---
phase: 06-engagement-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/items/items.repository.ts
  - backend/src/modules/items/items.service.ts
  - backend/src/modules/items/items.router.ts
  - backend/src/modules/items/items.types.ts
  - mobile/src/api/items.api.ts
  - mobile/src/screens/visitor/HomeScreen.tsx
autonomous: true
requirements:
  - SHOW-01
  - SHOW-02
  - SHOW-03

must_haves:
  truths:
    - "Volunteer/Admin kann Item als Showcase markieren (PATCH /api/items/:id/showcase)"
    - "GET /api/items/showcase liefert bis zu 6 aktive Showcase-Items (älteste zuerst raus bei mehr)"
    - "Visitor-Homescreen zeigt Showcase-Sektion mit Item-Karten (Titel, Kategorie, Größe, Farbe)"
    - "Showcase-Sektion ist nur sichtbar wenn mindestens 1 Item vorhanden"
    - "Keine Reservierung — nur Anzeige (kein Button außer optionalem Info-Hinweis)"
  artifacts:
    - path: "backend/src/db/schema.ts"
      provides: "showcase Boolean-Spalte in items-Tabelle"
      contains: "isShowcase"
    - path: "backend/src/modules/items/items.repository.ts"
      provides: "getShowcaseItems() + setShowcase()"
      exports: ["getShowcaseItems", "setShowcase"]
    - path: "mobile/src/screens/visitor/HomeScreen.tsx"
      provides: "Showcase-Sektion über Welcome-Text"
      contains: "ShowcaseSection"
  key_links:
    - from: "HomeScreen.tsx"
      to: "/api/items/showcase"
      via: "fetchShowcaseItems in items.api.ts"
      pattern: "fetchShowcaseItems"
    - from: "ItemListScreen (Volunteer)"
      to: "PATCH /api/items/:id/showcase"
      via: "toggleShowcase in items.api.ts"
      pattern: "toggleShowcase"
---

<objective>
Showcase-Flag auf Items: Backend-Spalte, Abfrage + Toggle-Endpunkt, Visitor-Homescreen Showcase-Sektion.

Purpose: Ehrenamtliche können interessante Items als "Schaufenster" markieren — Besucher sehen einen Vorgeschmack auf den Laden.
Output: showcase-Spalte in items, GET /api/items/showcase + PATCH /api/items/:id/showcase, Showcase-Sektion im Visitor-Homescreen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

KRITISCHE CONSTRAINTS:
- Docker NICHT lokal verfügbar
- Schema-Änderungen per: ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c '...'"
- Backend rebuild: ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn && git pull && cd backend && docker build -t plietsche-backend:latest . && docker stop plietsche-backend && docker rm plietsche-backend" dann Container neu starten
- Testen: curl -skL https://xn--plietsche-plnn-rsb.de/api/...
- Store UUID: 6a0fb86e-d795-4fd3-87fa-a21143f830b3
- TypeScript prüfen: cd mobile && npx tsc --noEmit

<interfaces>
Aus backend/src/db/schema.ts (items-Tabelle erweitern):
```typescript
export const items = pgTable('items', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  title: text('title').notNull(),
  category: text('category').notNull(),
  size: text('size'),
  condition: text('condition'),
  color: text('color'),
  status: text('status', { enum: ['active', 'taken'] }).notNull().default('active'),
  qrToken: uuid('qr_token').unique(),
  createdAt: timestamp('created_at').defaultNow(),
  createdBy: uuid('created_by').references(() => users.id),
  // NEU (per SHOW-01):
  isShowcase: boolean('is_showcase').notNull().default(false),
});
```

Aus backend/src/modules/items/items.repository.ts (Muster für neue Queries):
```typescript
import { db } from '../../db/client';
import { items } from '../../db/schema';
import { eq, and, ilike, desc } from 'drizzle-orm';
// Muster: db.select().from(items).where(...)
```

Aus mobile/src/screens/visitor/HomeScreen.tsx (wo Showcase-Sektion eingefügt wird):
```typescript
// Zwischen pointsCard und welcomeSection einfügen
// Bestehende Struktur: ScrollView > pointsCard > welcomeSection > logoutButton
```

Aus mobile/src/theme/index.ts:
```typescript
colors.primary = '#27b092', colors.surface = '#FFFFFF', colors.background = '#F9FAFB'
colors.text = '#111827', colors.textSecondary = '#6B7280', colors.border = '#E5E7EB'
fonts.bold, fonts.semiBold, fonts.medium, fonts.regular
spacing.xs=4, spacing.sm=8, spacing.md=16, spacing.lg=24
borderRadius.sm=8, borderRadius.md=12, borderRadius.lg=16
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + Backend (showcase-Spalte, Repository, Router-Endpunkte)</name>
  <files>
    backend/src/db/schema.ts
    backend/src/modules/items/items.repository.ts
    backend/src/modules/items/items.service.ts
    backend/src/modules/items/items.router.ts
    backend/src/modules/items/items.types.ts
  </files>
  <read_first>
    - backend/src/db/schema.ts (aktuelle items-Tabelle)
    - backend/src/modules/items/items.repository.ts (bestehende Queries)
    - backend/src/modules/items/items.service.ts (Service-Muster)
    - backend/src/modules/items/items.router.ts (Router mit requireRole)
    - backend/src/modules/items/items.types.ts (ItemFilters, CreateItemBody)
  </read_first>
  <action>
1. In backend/src/db/schema.ts — boolean importieren und isShowcase-Spalte hinzufügen (per SHOW-01):
```typescript
import { pgTable, uuid, text, timestamp, integer, real, boolean } from 'drizzle-orm/pg-core';
// In items-Tabelle nach 'createdBy':
isShowcase: boolean('is_showcase').notNull().default(false),
```

2. SQL auf Server ausführen (Spalte hinzufügen, existierende Rows bekommen default false):
```bash
ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"
ALTER TABLE items ADD COLUMN IF NOT EXISTS is_showcase BOOLEAN NOT NULL DEFAULT false;
\""
```

3. In items.repository.ts — zwei neue Funktionen hinzufügen:
```typescript
// Showcase-Items abrufen: max 6, nur 'active', neueste zuerst (per SHOW-02)
export async function getShowcaseItems(storeId: string) {
  return db.select({
    id: items.id,
    title: items.title,
    category: items.category,
    size: items.size,
    color: items.color,
    createdAt: items.createdAt,
  })
  .from(items)
  .where(and(
    eq(items.storeId, storeId),
    eq(items.isShowcase, true),
    eq(items.status, 'active'),
  ))
  .orderBy(desc(items.createdAt))
  .limit(6);
}

// Showcase-Status togglen (per SHOW-01)
export async function setShowcase(id: string, storeId: string, isShowcase: boolean) {
  const result = await db.update(items)
    .set({ isShowcase })
    .where(and(eq(items.id, id), eq(items.storeId, storeId)))
    .returning({ id: items.id, isShowcase: items.isShowcase });
  return result[0] ?? null;
}
```

4. In items.service.ts — neue Exports hinzufügen:
```typescript
export { getShowcaseItems, setShowcase } from './items.repository';
```

5. In items.types.ts — ItemFilters erweitern (optional: Showcase-Filter für Volunteer-Liste):
```typescript
// In ItemFilters interface:
isShowcase?: boolean;
```

6. In items.router.ts — zwei neue Endpunkte hinzufügen:

```typescript
// GET /api/items/showcase — öffentlich für Visitor (nur authenticateToken, kein requireRole)
router.get('/showcase', authenticateToken, async (req: Request, res: Response) => {
  const result = await itemsService.getShowcaseItems(req.user!.storeId);
  res.json(result);
});

// PATCH /api/items/:id/showcase — nur Volunteer/Admin
const ShowcaseSchema = z.object({ isShowcase: z.boolean() });

router.patch(
  '/:id/showcase',
  authenticateToken,
  requireRole('volunteer', 'admin'),
  async (req: Request, res: Response) => {
    const { isShowcase } = ShowcaseSchema.parse(req.body);
    const item = await itemsService.setShowcase(req.params.id, req.user!.storeId, isShowcase);
    if (!item) { res.status(404).json({ error: 'Item nicht gefunden' }); return; }
    res.json(item);
  }
);
```

WICHTIG: GET /showcase MUSS vor GET /:id registriert sein, sonst matcht Express "showcase" als :id-Parameter.
  </action>
  <verify>
    <automated>
# Nach git push + rebuild:
TOKEN=$(curl -skL -X POST https://xn--plietsche-plnn-rsb.de/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@test.de","password":"test1234"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['token'])")

# Showcase-Endpunkt testen
curl -skL https://xn--plietsche-plnn-rsb.de/api/items/showcase \
  -H "Authorization: Bearer $TOKEN"
# Erwartung: leeres Array [] oder Array mit Items

# Showcase setzen (braucht gültige Item-ID)
# curl -skL -X PATCH https://xn--plietsche-plnn-rsb.de/api/items/ITEM_ID/showcase \
#   -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
#   -d '{"isShowcase": true}'

cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - items-Tabelle hat is_showcase-Spalte (boolean, default false)
    - GET /api/items/showcase gibt Array zurück (max 6 aktive Showcase-Items)
    - PATCH /api/items/:id/showcase mit {isShowcase: true/false} setzt Flag
    - Visitor-Token kann GET /showcase aufrufen (nur authenticateToken)
    - Volunteer-Token kann PATCH /showcase ausführen
    - TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>
  <done>showcase-Spalte in DB, zwei neue Endpunkte funktionieren, Service + Router aktualisiert</done>
</task>

<task type="auto">
  <name>Task 2: Visitor-Homescreen Showcase-Sektion</name>
  <files>
    mobile/src/api/items.api.ts
    mobile/src/screens/visitor/HomeScreen.tsx
  </files>
  <read_first>
    - mobile/src/screens/visitor/HomeScreen.tsx (bestehende Struktur erweitern)
    - mobile/src/api/items.api.ts (API-Muster — fetchShowcaseItems hinzufügen)
    - mobile/src/theme/index.ts (colors, fonts, spacing, borderRadius)
  </read_first>
  <action>
1. In mobile/src/api/items.api.ts — fetchShowcaseItems hinzufügen:
```typescript
export interface ShowcaseItem {
  id: string;
  title: string;
  category: string;
  size?: string | null;
  color?: string | null;
  createdAt: string;
}

export async function fetchShowcaseItems(): Promise<ShowcaseItem[]> {
  const { data } = await apiClient.get<ShowcaseItem[]>('/api/items/showcase');
  return data;
}
```

2. HomeScreen.tsx — Showcase-Sektion einbauen (per SHOW-02, SHOW-03):

State hinzufügen:
```typescript
const [showcase, setShowcase] = React.useState<ShowcaseItem[]>([]);
const [showcaseLoading, setShowcaseLoading] = React.useState(false);
```

In useEffect (neben loadBalance):
```typescript
useEffect(() => {
  loadBalance();
  // Showcase laden
  setShowcaseLoading(true);
  fetchShowcaseItems()
    .then(setShowcase)
    .catch(() => {}) // Stiller Fehler
    .finally(() => setShowcaseLoading(false));
}, [loadBalance]);
```

Showcase-Sektion JSX (zwischen pointsCard und welcomeSection, nur wenn showcase.length > 0 per SHOW-02):
```tsx
{showcase.length > 0 && (
  <View style={styles.showcaseSection}>
    <Text style={styles.showcaseSectionTitle}>Schau mal rein ✨</Text>
    <Text style={styles.showcaseSectionSubtitle}>Ausgewählte Stücke — komm vorbei!</Text>
    <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.showcaseScroll}>
      {showcase.map((item) => (
        <View key={item.id} style={styles.showcaseCard}>
          <Text style={styles.showcaseCardTitle} numberOfLines={2}>{item.title}</Text>
          <Text style={styles.showcaseCardCategory}>{item.category}</Text>
          {item.size ? <Text style={styles.showcaseCardMeta}>Größe: {item.size}</Text> : null}
          {item.color ? <Text style={styles.showcaseCardMeta}>{item.color}</Text> : null}
        </View>
      ))}
    </ScrollView>
  </View>
)}
```

Styles hinzufügen:
```typescript
showcaseSection: {
  marginBottom: spacing.lg,
},
showcaseSectionTitle: {
  fontSize: 18,
  fontFamily: fonts.bold,
  color: colors.text,
  marginBottom: spacing.xs,
},
showcaseSectionSubtitle: {
  fontSize: 13,
  fontFamily: fonts.regular,
  color: colors.textSecondary,
  marginBottom: spacing.sm,
},
showcaseScroll: {
  marginHorizontal: -spacing.md, // Karten bis zum Rand
  paddingHorizontal: spacing.md,
},
showcaseCard: {
  backgroundColor: colors.surface,
  borderRadius: borderRadius.md,
  padding: spacing.md,
  marginRight: spacing.sm,
  width: 140,
  shadowColor: '#000',
  shadowOffset: { width: 0, height: 2 },
  shadowOpacity: 0.08,
  shadowRadius: 4,
  elevation: 2,
},
showcaseCardTitle: {
  fontSize: 14,
  fontFamily: fonts.semiBold,
  color: colors.text,
  marginBottom: spacing.xs,
},
showcaseCardCategory: {
  fontSize: 12,
  fontFamily: fonts.medium,
  color: colors.primary,
  marginBottom: spacing.xs,
},
showcaseCardMeta: {
  fontSize: 11,
  fontFamily: fonts.regular,
  color: colors.textSecondary,
},
```

Kein "Reservieren"-Button per SHOW-03 — reine Anzeige.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - fetchShowcaseItems in items.api.ts vorhanden und typisiert
    - HomeScreen lädt Showcase-Items beim Mount
    - Showcase-Sektion erscheint nur wenn showcase.length > 0
    - Horizontaler Scroll mit Karten (Titel, Kategorie, Größe, Farbe)
    - Kein Reservieren-Button (per SHOW-03)
    - TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>
  <done>Showcase-Sektion auf Homescreen, API-Client-Methode vorhanden, kein Reservieren</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor→API | Visitor kann Showcase lesen, aber nicht setzen |
| Volunteer→API | Volunteer kann showcase-Flag auf eigenen Store-Items setzen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Elevation | PATCH /api/items/:id/showcase | mitigate | requireRole('volunteer', 'admin') — Visitor erhält 403 |
| T-06-02-02 | Tampering | PATCH /api/items/:id/showcase | mitigate | where storeId = req.user.storeId — nur eigener Store |
| T-06-02-03 | Information | GET /api/items/showcase | accept | Kein PII — nur Titel, Kategorie, Größe, Farbe; öffentlich sichtbarer Schaufenster-Inhalt |
</threat_model>

<verification>
Backend:
- `curl -skL https://xn--plietsche-plnn-rsb.de/api/items/showcase -H "Authorization: Bearer $TOKEN"` → 200 Array
- `curl -skL -X PATCH .../api/items/ITEM_ID/showcase -H ... -d '{"isShowcase":true}'` → 200 { id, isShowcase: true }
- Visitor-Token bei PATCH → 403

Mobile:
- npx tsc --noEmit ohne Fehler
- HomeScreen zeigt bei leerer Showcase-Liste keine Showcase-Sektion
- Bei 1+ Showcase-Items: horizontale Karten-Liste sichtbar
</verification>

<success_criteria>
1. is_showcase-Spalte in PostgreSQL vorhanden (ALTER TABLE ausgeführt)
2. GET /api/items/showcase gibt max 6 aktive Showcase-Items zurück
3. PATCH /api/items/:id/showcase setzt Flag (Volunteer-geschützt)
4. HomeScreen zeigt Showcase-Sektion (nur wenn Items vorhanden)
5. Kein Reservieren-Button — reine Anzeige
6. TypeScript: npx tsc --noEmit ohne Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-engagement-features/06-02-SUMMARY.md` erstellen
</output>
