---
phase: 06-engagement-features
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/badges/badges.types.ts
  - backend/src/modules/badges/badges.repository.ts
  - backend/src/modules/badges/badges.service.ts
  - backend/src/modules/badges/badges.router.ts
  - backend/src/app.ts
  - mobile/src/api/badges.api.ts
  - mobile/src/screens/visitor/HomeScreen.tsx
  - mobile/src/screens/admin/BadgeLevelsScreen.tsx
  - mobile/src/navigation/AppNavigator.tsx
autonomous: true
requirements:
  - BADGE-01
  - BADGE-02
  - BADGE-03
  - BADGE-04

must_haves:
  truths:
    - "badge_levels-Tabelle enthält Default-Stufen: Neuling(0), Entdecker(25), Stammgast(50), Plietsch-Kenner(100), Plietsch-Profi(200)"
    - "GET /api/badges/my gibt aktuelles Level + Fortschritt zum nächsten zurück"
    - "Admin kann Stufen verwalten (GET/POST/DELETE /api/badges/levels)"
    - "Visitor-Homescreen zeigt Badge-Emoji + Level-Name neben Punktestand"
    - "Fortschrittsbalken zeigt Fortschritt zum nächsten Level (in %)"
  artifacts:
    - path: "backend/src/db/schema.ts"
      provides: "badge_levels-Tabelle (id, store_id, name, min_points, sort_order)"
      contains: "export const badgeLevels"
    - path: "backend/src/modules/badges/badges.router.ts"
      provides: "GET /api/badges/my, GET/POST/DELETE /api/badges/levels"
      exports: ["default router"]
    - path: "mobile/src/screens/visitor/HomeScreen.tsx"
      provides: "Badge-Anzeige in pointsCard + Fortschrittsbalken"
      contains: "badgeLevel"
    - path: "mobile/src/screens/admin/BadgeLevelsScreen.tsx"
      provides: "Admin-Screen für Level-Verwaltung"
  key_links:
    - from: "HomeScreen.tsx"
      to: "/api/badges/my"
      via: "fetchMyBadge in badges.api.ts"
      pattern: "fetchMyBadge"
    - from: "badges.repository"
      to: "users.plietschPoints"
      via: "SELECT plietschPoints WHERE userId = req.user.sub"
      pattern: "getBalance.*userId"
---

<objective>
Badge/Achievement-System: Tabelle mit konfigurierbaren Level-Stufen, Endpunkt für aktuelles Level + Fortschritt, Admin-Verwaltung, Homescreen-Anzeige.

Purpose: Besucher sehen ihren Fortschritt und haben einen Anreiz, mehr PlietschPunkte zu sammeln.
Output: badge_levels-Tabelle mit Defaults, /api/badges/my + /api/badges/levels Endpunkte, Badge-Anzeige mit Fortschrittsbalken im Homescreen, Admin-Verwaltungsscreen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-engagement-features/06-01-SUMMARY.md
@.planning/phases/06-engagement-features/06-02-SUMMARY.md

KRITISCHE CONSTRAINTS:
- Docker NICHT lokal verfügbar
- Schema-Änderungen per: ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c '...'"
- Backend rebuild: ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn && git pull && cd backend && docker build -t plietsche-backend:latest . && docker stop plietsche-backend && docker rm plietsche-backend" dann Container neu starten
- Testen: curl -skL https://xn--plietsche-plnn-rsb.de/api/...
- Store UUID: 6a0fb86e-d795-4fd3-87fa-a21143f830b3
- TypeScript prüfen: cd mobile && npx tsc --noEmit

<interfaces>
Aus backend/src/db/schema.ts (neu hinzufügen):
```typescript
export const badgeLevels = pgTable('badge_levels', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  name: text('name').notNull(),        // z.B. "Stammgast"
  emoji: text('emoji').notNull().default('🏅'),
  minPoints: integer('min_points').notNull(),
  sortOrder: integer('sort_order').notNull().default(0),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Aus backend/src/modules/points/points.repository.ts (Punkte abrufen):
```typescript
export async function getBalance(userId: string): Promise<number>
// Wird wiederverwendet in badges.repository
```

Badge-Berechnung (in badges.repository.ts):
```typescript
// Sortiert nach minPoints ASC
// Aktuelles Level = letztes Level dessen minPoints <= userPoints
// Nächstes Level = erstes Level dessen minPoints > userPoints
// Fortschritt = (userPoints - currentLevel.minPoints) / (nextLevel.minPoints - currentLevel.minPoints)
// Falls kein nächstes Level: progress = 1.0 (Maximum erreicht)
```

Aus mobile/src/screens/visitor/HomeScreen.tsx (bestehende pointsCard erweitern):
```typescript
// pointsCard ist LinearGradient mit pointsLabel + pointsValue + pointsSubtitle
// Badge-Name + Emoji UNTER pointsSubtitle einfügen
// Fortschrittsbalken UNTER dem Badge-Namen (View mit background rgba(255,255,255,0.3))
```

Aus mobile/src/theme/index.ts:
```typescript
colors.white = '#FFFFFF', colors.primary = '#27b092'
fonts.bold, fonts.semiBold, fonts.regular
spacing.xs=4, spacing.sm=8, spacing.md=16
borderRadius.full=999
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + Backend (badge_levels, Repository, Service, Router)</name>
  <files>
    backend/src/db/schema.ts
    backend/src/modules/badges/badges.types.ts
    backend/src/modules/badges/badges.repository.ts
    backend/src/modules/badges/badges.service.ts
    backend/src/modules/badges/badges.router.ts
    backend/src/app.ts
  </files>
  <read_first>
    - backend/src/db/schema.ts (aktuelle Struktur)
    - backend/src/modules/points/points.repository.ts (getBalance)
    - backend/src/modules/campaigns/campaigns.router.ts (Muster aus Plan 06-01)
    - backend/src/app.ts (Router einhängen)
  </read_first>
  <action>
1. In backend/src/db/schema.ts — badgeLevels-Tabelle hinzufügen (per BADGE-01, BADGE-03):
```typescript
export const badgeLevels = pgTable('badge_levels', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  name: text('name').notNull(),
  emoji: text('emoji').notNull().default('🏅'),
  minPoints: integer('min_points').notNull(),
  sortOrder: integer('sort_order').notNull().default(0),
  createdAt: timestamp('created_at').defaultNow(),
});
```

2. SQL auf Server ausführen (Tabelle + Default-Werte für Store):
```bash
ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"
CREATE TABLE IF NOT EXISTS badge_levels (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  store_id UUID NOT NULL REFERENCES stores(id),
  name TEXT NOT NULL,
  emoji TEXT NOT NULL DEFAULT '🏅',
  min_points INTEGER NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Default-Stufen für bestehenden Store einfügen (per BADGE-01)
INSERT INTO badge_levels (store_id, name, emoji, min_points, sort_order)
VALUES
  ('6a0fb86e-d795-4fd3-87fa-a21143f830b3', 'Neuling', '🌱', 0, 0),
  ('6a0fb86e-d795-4fd3-87fa-a21143f830b3', 'Entdecker', '🔍', 25, 1),
  ('6a0fb86e-d795-4fd3-87fa-a21143f830b3', 'Stammgast', '⭐', 50, 2),
  ('6a0fb86e-d795-4fd3-87fa-a21143f830b3', 'Plietsch-Kenner', '🎯', 100, 3),
  ('6a0fb86e-d795-4fd3-87fa-a21143f830b3', 'Plietsch-Profi', '🏆', 200, 4)
ON CONFLICT DO NOTHING;
\""
```

3. badges.types.ts erstellen:
```typescript
export interface BadgeLevel {
  id: string;
  storeId: string;
  name: string;
  emoji: string;
  minPoints: number;
  sortOrder: number;
}

export interface BadgeProgress {
  currentLevel: { name: string; emoji: string; minPoints: number } | null;
  nextLevel: { name: string; emoji: string; minPoints: number } | null;
  progressPercent: number; // 0-100
  pointsToNext: number | null; // null wenn max Level erreicht
  currentPoints: number;
}
```

4. badges.repository.ts erstellen:
```typescript
import { db } from '../../db/client';
import { badgeLevels, users } from '../../db/schema';
import { eq, asc } from 'drizzle-orm';
import { getBalance } from '../points/points.repository';
import type { BadgeProgress } from './badges.types';

export async function getLevels(storeId: string) {
  return db.select().from(badgeLevels)
    .where(eq(badgeLevels.storeId, storeId))
    .orderBy(asc(badgeLevels.sortOrder), asc(badgeLevels.minPoints));
}

export async function addLevel(storeId: string, data: { name: string; emoji: string; minPoints: number; sortOrder: number }) {
  const result = await db.insert(badgeLevels).values({ storeId, ...data }).returning();
  return result[0];
}

export async function deleteLevel(id: string, storeId: string) {
  await db.delete(badgeLevels)
    .where(and(eq(badgeLevels.id, id), eq(badgeLevels.storeId, storeId)));
}

export async function getBadgeProgress(userId: string, storeId: string): Promise<BadgeProgress> {
  const currentPoints = await getBalance(userId);
  const levels = await getLevels(storeId);

  if (levels.length === 0) {
    return { currentLevel: null, nextLevel: null, progressPercent: 0, pointsToNext: null, currentPoints };
  }

  // Aktuelles Level: letztes Level dessen minPoints <= currentPoints
  const eligible = levels.filter((l) => l.minPoints <= currentPoints);
  const currentLevel = eligible.length > 0 ? eligible[eligible.length - 1] : null;

  // Nächstes Level: erstes Level dessen minPoints > currentPoints
  const above = levels.filter((l) => l.minPoints > currentPoints);
  const nextLevel = above.length > 0 ? above[0] : null;

  let progressPercent = 0;
  let pointsToNext: number | null = null;

  if (currentLevel && nextLevel) {
    const range = nextLevel.minPoints - currentLevel.minPoints;
    const progress = currentPoints - currentLevel.minPoints;
    progressPercent = Math.min(100, Math.round((progress / range) * 100));
    pointsToNext = nextLevel.minPoints - currentPoints;
  } else if (currentLevel && !nextLevel) {
    progressPercent = 100; // Max Level erreicht
    pointsToNext = null;
  } else if (!currentLevel && nextLevel) {
    // Unter dem ersten Level (sollte nicht passieren wenn Neuling bei 0)
    progressPercent = 0;
    pointsToNext = nextLevel.minPoints - currentPoints;
  }

  return {
    currentLevel: currentLevel ? { name: currentLevel.name, emoji: currentLevel.emoji, minPoints: currentLevel.minPoints } : null,
    nextLevel: nextLevel ? { name: nextLevel.name, emoji: nextLevel.emoji, minPoints: nextLevel.minPoints } : null,
    progressPercent,
    pointsToNext,
    currentPoints,
  };
}
```

Importiere `and` aus drizzle-orm in badges.repository.ts.

5. badges.service.ts erstellen:
```typescript
import * as repo from './badges.repository';
export const getLevels = (storeId: string) => repo.getLevels(storeId);
export const addLevel = (storeId: string, data: Parameters<typeof repo.addLevel>[1]) => repo.addLevel(storeId, data);
export const deleteLevel = (id: string, storeId: string) => repo.deleteLevel(id, storeId);
export const getBadgeProgress = (userId: string, storeId: string) => repo.getBadgeProgress(userId, storeId);
```

6. badges.router.ts erstellen:
```typescript
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
import * as badgesService from './badges.service';

const router = Router();

const AddLevelSchema = z.object({
  name: z.string().min(1).max(100),
  emoji: z.string().min(1).max(10).default('🏅'),
  minPoints: z.number().int().min(0),
  sortOrder: z.number().int().min(0).default(0),
});

// GET /api/badges/my — aktuelles Level + Fortschritt (Visitor)
router.get('/my', authenticateToken, async (req: Request, res: Response) => {
  const progress = await badgesService.getBadgeProgress(req.user!.sub, req.user!.storeId);
  res.json(progress);
});

// GET /api/badges/levels — alle Level des Stores (Admin/Volunteer)
router.get('/levels', authenticateToken, requireRole('volunteer', 'admin'), async (req: Request, res: Response) => {
  const levels = await badgesService.getLevels(req.user!.storeId);
  res.json(levels);
});

// POST /api/badges/levels — neue Stufe hinzufügen (Admin)
router.post('/levels', authenticateToken, requireRole('admin'), async (req: Request, res: Response) => {
  const body = AddLevelSchema.parse(req.body);
  const level = await badgesService.addLevel(req.user!.storeId, body);
  res.status(201).json(level);
});

// DELETE /api/badges/levels/:id — Stufe löschen (Admin)
router.delete('/levels/:id', authenticateToken, requireRole('admin'), async (req: Request, res: Response) => {
  await badgesService.deleteLevel(req.params.id, req.user!.storeId);
  res.status(204).send();
});

export default router;
```

7. In backend/src/app.ts: Router einhängen:
```typescript
import badgesRouter from './modules/badges/badges.router';
app.use('/api/badges', badgesRouter);
```
  </action>
  <verify>
    <automated>
# Nach git push + rebuild:
TOKEN=$(curl -skL -X POST https://xn--plietsche-plnn-rsb.de/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"visitor@test.de","password":"test1234"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['token'])" 2>/dev/null || echo "")

curl -skL https://xn--plietsche-plnn-rsb.de/api/badges/my \
  -H "Authorization: Bearer $TOKEN"
# Erwartung: { currentLevel: {name, emoji, minPoints}, nextLevel: ..., progressPercent, pointsToNext, currentPoints }

cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit
    </automated>
  </verify>
  <acceptance_criteria>
    - badge_levels-Tabelle vorhanden mit 5 Default-Stufen für Store-UUID
    - GET /api/badges/my gibt BadgeProgress-Objekt zurück (currentLevel, nextLevel, progressPercent, pointsToNext, currentPoints)
    - GET /api/badges/levels listet alle Level (Volunteer/Admin)
    - POST /api/badges/levels erstellt neue Stufe (Admin)
    - DELETE /api/badges/levels/:id löscht Stufe (Admin)
    - TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>
  <done>badge_levels-Tabelle mit Defaults, alle 4 Endpunkte funktionieren, Router eingehängt</done>
</task>

<task type="auto">
  <name>Task 2: Homescreen Badge-Anzeige + Admin BadgeLevels-Screen</name>
  <files>
    mobile/src/api/badges.api.ts
    mobile/src/screens/visitor/HomeScreen.tsx
    mobile/src/screens/admin/BadgeLevelsScreen.tsx
    mobile/src/navigation/AppNavigator.tsx
  </files>
  <read_first>
    - mobile/src/screens/visitor/HomeScreen.tsx (pointsCard erweitern — bestehende Struktur)
    - mobile/src/navigation/AppNavigator.tsx (VolunteerTabs + Stack — Kampagnen-Tab aus Plan 06-01 ist bereits drin)
    - mobile/src/screens/admin/CampaignListScreen.tsx (falls vorhanden: Muster für Admin-Listen-Screen)
    - mobile/src/theme/index.ts (colors, fonts, spacing)
  </read_first>
  <action>
1. mobile/src/api/badges.api.ts erstellen:
```typescript
import { apiClient } from './client';

export interface BadgeProgressResponse {
  currentLevel: { name: string; emoji: string; minPoints: number } | null;
  nextLevel: { name: string; emoji: string; minPoints: number } | null;
  progressPercent: number;
  pointsToNext: number | null;
  currentPoints: number;
}

export interface BadgeLevel {
  id: string;
  storeId: string;
  name: string;
  emoji: string;
  minPoints: number;
  sortOrder: number;
}

export async function fetchMyBadge(): Promise<BadgeProgressResponse> {
  const { data } = await apiClient.get<BadgeProgressResponse>('/api/badges/my');
  return data;
}

export async function fetchBadgeLevels(): Promise<BadgeLevel[]> {
  const { data } = await apiClient.get<BadgeLevel[]>('/api/badges/levels');
  return data;
}

export async function createBadgeLevel(body: { name: string; emoji: string; minPoints: number; sortOrder: number }): Promise<BadgeLevel> {
  const { data } = await apiClient.post<BadgeLevel>('/api/badges/levels', body);
  return data;
}

export async function deleteBadgeLevel(id: string): Promise<void> {
  await apiClient.delete(`/api/badges/levels/${id}`);
}
```

2. HomeScreen.tsx — Badge-Anzeige in pointsCard + Fortschrittsbalken (per BADGE-02, BADGE-04):

State hinzufügen:
```typescript
const [badge, setBadge] = React.useState<BadgeProgressResponse | null>(null);
```

useEffect erweitern:
```typescript
fetchMyBadge()
  .then(setBadge)
  .catch(() => {}); // Stiller Fehler
```

In pointsCard (LinearGradient): NACH pointsSubtitle einfügen:
```tsx
{badge?.currentLevel && (
  <>
    <Text style={styles.badgeLabel}>
      {badge.currentLevel.emoji} {badge.currentLevel.name}
    </Text>
    {badge.nextLevel && (
      <View style={styles.progressContainer}>
        <View style={[styles.progressBar, { width: `${badge.progressPercent}%` as any }]} />
      </View>
    )}
    {badge.pointsToNext != null && (
      <Text style={styles.pointsToNextLabel}>
        Noch {badge.pointsToNext} Punkte bis {badge.nextLevel?.emoji} {badge.nextLevel?.name}
      </Text>
    )}
    {!badge.nextLevel && (
      <Text style={styles.pointsToNextLabel}>Maximales Level erreicht 🎉</Text>
    )}
  </>
)}
```

Styles hinzufügen:
```typescript
badgeLabel: {
  fontSize: 16,
  fontFamily: fonts.semiBold,
  color: colors.white,
  marginTop: spacing.sm,
},
progressContainer: {
  width: '100%',
  height: 6,
  backgroundColor: 'rgba(255,255,255,0.3)',
  borderRadius: borderRadius.full,
  marginTop: spacing.xs,
  overflow: 'hidden',
},
progressBar: {
  height: 6,
  backgroundColor: colors.white,
  borderRadius: borderRadius.full,
},
pointsToNextLabel: {
  fontSize: 11,
  fontFamily: fonts.regular,
  color: 'rgba(255,255,255,0.7)',
  marginTop: spacing.xs,
},
```

3. BadgeLevelsScreen.tsx erstellen (per BADGE-03 — Admin-konfigurierbar):
- FlatList aller Level (Emoji + Name + min_points)
- Sortiert nach minPoints ASC
- Löschen-Button pro Level (Alert zur Bestätigung)
- Formular am Ende: Emoji (TextInput), Name (TextInput), Mindestpunkte (TextInput numeric), "Stufe hinzufügen"-Button
- Leere State: "Keine Stufen definiert"
- Styling konsistent mit anderen Admin-Screens

4. AppNavigator.tsx — BadgeLevelsScreen im Volunteer-Stack hinzufügen:
- Import: BadgeLevelsScreen
- In AuthenticatedStack: neuen Stack.Screen "BadgeLevels" eintragen (headerShown: true, title: 'Badge-Stufen')
- Im VolunteerTabs: Tab "Badges" → BadgeLevelsScreen hinzufügen

Reihenfolge VolunteerTabs nach Änderungen aus Plan 06-01 und 06-03:
Dashboard | Kleidung | Neu anlegen | Kampagnen | Badges | Store-Info
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - badges.api.ts mit fetchMyBadge, fetchBadgeLevels, createBadgeLevel, deleteBadgeLevel
    - HomeScreen zeigt Badge-Name + Emoji in pointsCard (nur wenn currentLevel vorhanden)
    - Fortschrittsbalken in pointsCard (nur wenn nextLevel vorhanden)
    - "Noch X Punkte bis Level Y" Text (null wenn max Level)
    - BadgeLevelsScreen mit Liste + Hinzufügen-Formular
    - AppNavigator registriert BadgeLevelsScreen
    - TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>
  <done>Badge-Anzeige im Homescreen, Fortschrittsbalken, Admin-Screen für Level-Verwaltung</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor→API | Visitor kann GET /api/badges/my aufrufen (eigene Punkte/Badge) |
| Admin→API | Admin kann Level hinzufügen und löschen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Elevation | POST /api/badges/levels | mitigate | requireRole('admin') — Volunteer und Visitor erhalten 403 |
| T-06-03-02 | Elevation | DELETE /api/badges/levels/:id | mitigate | requireRole('admin') + storeId-Check |
| T-06-03-03 | Information | GET /api/badges/my | accept | Nur eigene Punkte (req.user.sub) — kein Zugriff auf andere Nutzer |
| T-06-03-04 | Tampering | getBadgeProgress userId | mitigate | userId = req.user.sub (aus JWT) — nicht aus Request-Body |
</threat_model>

<verification>
Backend:
- `curl .../api/badges/my -H "Authorization: Bearer $VISITOR_TOKEN"` → BadgeProgress-Objekt
- `curl .../api/badges/levels -H "Authorization: Bearer $ADMIN_TOKEN"` → Array mit 5 Default-Levels
- Visitor-Token bei POST /api/badges/levels → 403

Mobile:
- npx tsc --noEmit ohne Fehler
- Homescreen: pointsCard zeigt Badge-Emoji + Name + Fortschrittsbalken
- Admin-Tab "Badges" zeigt BadgeLevelsScreen mit Liste der Level
</verification>

<success_criteria>
1. badge_levels-Tabelle in PostgreSQL mit 5 Default-Stufen
2. GET /api/badges/my gibt BadgeProgress zurück (currentLevel, nextLevel, progressPercent, pointsToNext)
3. Admin kann Level über /api/badges/levels POST/DELETE verwalten
4. Homescreen zeigt Badge-Name + Emoji + Fortschrittsbalken in pointsCard
5. "Noch X Punkte bis Level Y" Text korrekt berechnet
6. BadgeLevelsScreen im Admin-Tab erreichbar
7. TypeScript: npx tsc --noEmit ohne Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-engagement-features/06-03-SUMMARY.md` erstellen
</output>
