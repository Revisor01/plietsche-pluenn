---
phase: 08-icon-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - mobile/src/screens/visitor/HomeScreen.tsx
  - mobile/src/screens/visitor/CheckInScreen.tsx
  - mobile/src/api/badges.api.ts
  - backend/src/db/schema.ts
  - backend/src/modules/badges/badges.types.ts
  - backend/src/modules/badges/badges.router.ts
  - backend/src/modules/badges/badges.repository.ts
  - backend/src/modules/badges/badges.service.ts
  - mobile/src/screens/admin/BadgeLevelsScreen.tsx
autonomous: true
requirements:
  - ICON-02
  - ICON-04

must_haves:
  truths:
    - "Kein einziges Emoji-Zeichen in allen .tsx und .ts Dateien unter mobile/src/ und backend/src/"
    - "Das DB-Feld 'emoji' in badge_levels ist auf 'icon_name' migriert"
    - "HomeScreen zeigt Font Awesome Icon anstelle von badge.currentLevel.emoji"
    - "BadgeLevelsScreen zeigt Font Awesome Icons anstelle von item.emoji"
    - "CheckInScreen Erfolgs-Screen zeigt Font Awesome check-circle statt Text-Checkmark"
    - "TypeScript kompiliert ohne Fehler (cd mobile && npx tsc --noEmit, cd backend && npx tsc --noEmit)"
  artifacts:
    - path: "mobile/src/screens/visitor/HomeScreen.tsx"
      provides: "Badge-Anzeige mit FontAwesome5 Icon statt Emoji"
      contains: "FontAwesome5"
    - path: "mobile/src/screens/visitor/CheckInScreen.tsx"
      provides: "Erfolgs-Icon als FontAwesome5 check-circle"
      contains: "FontAwesome5"
    - path: "mobile/src/screens/admin/BadgeLevelsScreen.tsx"
      provides: "Badge-Icon-Picker als Font Awesome Dropdown, keine Emoji-Inputs"
      contains: "FontAwesome5"
    - path: "backend/src/db/schema.ts"
      provides: "badge_levels.icon_name statt badge_levels.emoji"
      contains: "icon_name"
    - path: "backend/src/modules/badges/badges.types.ts"
      provides: "BadgeLevel.iconName statt BadgeLevel.emoji"
      contains: "iconName"
  key_links:
    - from: "mobile/src/screens/visitor/HomeScreen.tsx"
      to: "badge.currentLevel.iconName"
      via: "FontAwesome5 Icon-Komponente"
      pattern: "iconName"
    - from: "backend/src/modules/badges/badges.router.ts"
      to: "badges.service"
      via: "iconName-Feld in AddLevelSchema"
      pattern: "iconName"
---

<objective>
Alle verbleibenden Emojis aus Screens entfernen und das Badge-Icon-System von Emoji-String auf Font Awesome Icon-Name migrieren.

Purpose: ICON-02 und ICON-04 — vollstaendige Emoji-Freiheit in der gesamten App. Badge-Icons nutzen Font Awesome Namen statt Emoji-Zeichen.
Output: Emoji-freie App, migriertes Backend-Schema, Font Awesome Icons ueberall.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mobile/src/screens/visitor/HomeScreen.tsx
@mobile/src/screens/visitor/CheckInScreen.tsx
@mobile/src/screens/admin/BadgeLevelsScreen.tsx
@mobile/src/api/badges.api.ts
@backend/src/db/schema.ts
@backend/src/modules/badges/badges.types.ts
@backend/src/modules/badges/badges.router.ts
@mobile/src/theme/index.ts

<interfaces>
<!-- Bestehende Badge-Typen, werden in diesem Plan umgebaut -->

Aus mobile/src/api/badges.api.ts (VOR Aenderung):
```typescript
export interface BadgeProgressResponse {
  currentLevel: { name: string; emoji: string; minPoints: number } | null;
  nextLevel: { name: string; emoji: string; minPoints: number } | null;
  progressPercent: number;
  pointsToNext: number | null;
  currentPoints: number;
}

export interface BadgeLevel {
  id: string;
  storeId: string;
  name: string;
  emoji: string;
  minPoints: number;
  sortOrder: number;
}
```

Nach Plan 02 soll `emoji` durch `iconName` ersetzt sein.

Aus backend/src/modules/badges/badges.types.ts (VOR Aenderung):
```typescript
export interface BadgeLevel {
  id: string; storeId: string; name: string;
  emoji: string; minPoints: number; sortOrder: number;
}
```

Aus backend/src/db/schema.ts (VOR Aenderung):
```typescript
export const badgeLevels = pgTable('badge_levels', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  name: text('name').notNull(),
  emoji: text('emoji').notNull().default('🏅'),
  minPoints: integer('min_points').notNull(),
  sortOrder: integer('sort_order').notNull().default(0),
  createdAt: timestamp('created_at').defaultNow(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend emoji -> iconName migrieren (Schema, Types, Router, Repository, Service)</name>
  <files>
    backend/src/db/schema.ts,
    backend/src/modules/badges/badges.types.ts,
    backend/src/modules/badges/badges.router.ts,
    backend/src/modules/badges/badges.repository.ts,
    backend/src/modules/badges/badges.service.ts
  </files>
  <action>
    Lese alle 5 Dateien zuerst, dann aendere gezielt.

    **backend/src/db/schema.ts:**
    - Zeile `emoji: text('emoji').notNull().default('🏅'),` umbenennen zu:
      `iconName: text('icon_name').notNull().default('medal'),`
    - DB-Spaltenname ist 'icon_name' (snake_case), TypeScript-Property ist 'iconName' (camelCase)
    - Kein Emoji als Default-Wert

    **backend/src/modules/badges/badges.types.ts:**
    - Alle `emoji: string` Felder in beiden Interfaces zu `iconName: string` umbenennen

    **backend/src/modules/badges/badges.router.ts:**
    - `emoji: z.string().min(1).max(10).default('🏅')` -> `iconName: z.string().min(1).max(50).default('medal')`
    - Alle weiteren `emoji`-Referenzen auf `iconName` umbenennen

    **backend/src/modules/badges/badges.repository.ts:**
    - Lese die Datei und ersetze alle `emoji`-Referenzen mit `iconName`
    - Falls Drizzle ORM `.emoji`-Property referenziert: auf `.iconName` aendern

    **backend/src/modules/badges/badges.service.ts:**
    - Lese die Datei und ersetze alle `emoji`-Referenzen mit `iconName`

    WICHTIG: Drizzle braucht eine Migration. Pruefe ob eine `drizzle.config.ts` existiert.
    Falls ja: `cd backend && npx drizzle-kit generate` ausfuehren um Migration zu erzeugen.
    Falls keine Migration-Config vorhanden: notiere im Summary dass manuelles ALTER TABLE noetig ist:
    `ALTER TABLE badge_levels RENAME COLUMN emoji TO icon_name;`
    und `ALTER TABLE badge_levels ALTER COLUMN icon_name SET DEFAULT 'medal';`

    NICHT: Das `emoji`-Feld parallel behalten. Sauberer Schnitt.
    NICHT: Emoji-Zeichen als Default-Werte.

    Zum Schluss: `cd backend && npx tsc --noEmit` — muss 0 Fehler geben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - `grep -rn "emoji" backend/src/modules/badges/` gibt keine Treffer ausser Kommentaren
    - `grep "icon_name\|iconName" backend/src/db/schema.ts` zeigt die neue Spalte
    - `cd backend && npx tsc --noEmit` gibt 0 Fehler
    - Kein Emoji-Zeichen in backend/src/ (ausser node_modules): `grep -rn "[🏅🎉✨]" backend/src/` gibt nichts
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile Screens und API-Client auf iconName umstellen</name>
  <files>
    mobile/src/api/badges.api.ts,
    mobile/src/screens/visitor/HomeScreen.tsx,
    mobile/src/screens/visitor/CheckInScreen.tsx,
    mobile/src/screens/admin/BadgeLevelsScreen.tsx
  </files>
  <action>
    Lese alle 4 Dateien zuerst, dann aendere gezielt.

    **mobile/src/api/badges.api.ts:**
    - `emoji: string` in BadgeProgressResponse und BadgeLevel zu `iconName: string` umbenennen
    - `createBadgeLevel`-Parameter: `emoji: string` -> `iconName: string`

    **mobile/src/screens/visitor/HomeScreen.tsx:**

    Importiere Icon am Anfang:
    ```typescript
    import Icon from 'react-native-vector-icons/FontAwesome5';
    ```

    Aenderungen (alle Emojis entfernen):

    1. Zeile 45 `{badge.currentLevel.emoji} {badge.currentLevel.name}` ersetzen durch:
       ```tsx
       <Icon name={badge.currentLevel.iconName} solid size={18} color={colors.white} style={{ marginRight: 6 }} />
       {badge.currentLevel.name}
       ```
       Dazu muss `badgeLabel`-Style auf `flexDirection: 'row', alignItems: 'center'` geaendert werden.

    2. Zeile 54 `{badge.nextLevel?.emoji} {badge.nextLevel?.name}` ersetzen durch:
       ```tsx
       {badge.nextLevel?.iconName ? (
         <Icon name={badge.nextLevel.iconName} solid size={14} color="rgba(255,255,255,0.7)" style={{ marginRight: 4 }} />
       ) : null}
       {badge.nextLevel?.name}
       ```

    3. Zeile 58-59 `Maximales Level erreicht 🎉` ersetzen durch:
       `Maximales Level erreicht` (kein Emoji, kein Icon hier — der Text ist eindeutig genug)

    4. Zeile 66 `Schau mal rein ✨` ersetzen durch:
       ```tsx
       <View style={{ flexDirection: 'row', alignItems: 'center', marginBottom: spacing.xs }}>
         <Icon name="eye" size={18} color={colors.text} style={{ marginRight: spacing.xs }} />
         <Text style={styles.showcaseSectionTitle}>Schau mal rein</Text>
       </View>
       ```
       Dafuer den bestehenden Text-Render der showcaseSectionTitle entfernen und durch obiges View ersetzen.

    **mobile/src/screens/visitor/CheckInScreen.tsx:**

    1. Importiere Icon:
       ```typescript
       import Icon from 'react-native-vector-icons/FontAwesome5';
       ```

    2. Zeile 165 `<Text style={styles.successIcon}>✓</Text>` ersetzen durch:
       ```tsx
       <Icon name="check-circle" solid size={64} color={colors.success} style={{ marginBottom: spacing.sm }} />
       ```
       Den `successIcon`-Style aus StyleSheet entfernen.

    **mobile/src/screens/admin/BadgeLevelsScreen.tsx:**

    Das Formular nutzt aktuell ein freies Emoji-Eingabefeld fuer das Icon.
    Dieses komplett umbauen auf ein Icon-Name-Textfeld:

    1. State-Umbenennung: `newEmoji` -> `newIconName`, Initialwert `'medal'` statt `'🏅'`
    2. Alle `setNewEmoji`/`newEmoji` auf `setNewIconName`/`newIconName` umbenennen
    3. Die TextInput-Komponente fuer Emoji (style: inputEmoji) ersetzen durch:
       ```tsx
       <View style={styles.iconPreview}>
         <Icon name={newIconName || 'medal'} solid size={24} color={colors.primary} />
       </View>
       ```
       (Kein TextInput mehr fuer das Icon-Feld — der Admin gibt einen Icon-Name als Text ein,
       oder wir zeigen einen Preview. Behalte aber das Name-TextInput und fuege ein
       readonly Preview-View hinzu.)
       
       Konkret: Ersetze den emoji-TextInput durch ein normales TextInput fuer den Icon-Namen:
       ```tsx
       <TextInput
         style={[styles.input, styles.inputIconName]}
         value={newIconName}
         onChangeText={setNewIconName}
         placeholder="Icon-Name (z.B. medal, star, trophy)"
         maxLength={50}
         autoCapitalize="none"
       />
       ```
       Und davor ein Preview-Icon:
       ```tsx
       <View style={styles.iconPreviewBox}>
         <Icon name={newIconName || 'medal'} solid size={24} color={colors.primary} />
       </View>
       ```
       Style iconPreviewBox: `{ width: 48, height: 48, justifyContent: 'center', alignItems: 'center', backgroundColor: colors.background, borderRadius: borderRadius.sm, borderWidth: 1, borderColor: colors.border }`

    4. In der FlatList renderItem: `<Text style={styles.levelEmoji}>{item.emoji}</Text>` ersetzen durch:
       ```tsx
       <View style={styles.levelIconBox}>
         <Icon name={item.iconName || 'medal'} solid size={24} color={colors.primary} />
       </View>
       ```
       Style levelIconBox: `{ width: 40, height: 40, justifyContent: 'center', alignItems: 'center', marginRight: spacing.sm }`
       Den `levelEmoji`-Style loeschen.

    5. In handleDelete: `"${level.emoji} ${level.name}"` -> `"${level.name}"`
    6. In handleAdd: `emoji: newEmoji.trim() || '🏅'` -> `iconName: newIconName.trim() || 'medal'`
    7. Reset nach handleAdd: `setNewEmoji('🏅')` -> `setNewIconName('medal')`

    WICHTIG: Alle FontAwesome5-Imports hinzufuegen am Anfang der Datei.
    NICHT: Emoji-Zeichen als Placeholder, Default oder Label verwenden.
    NICHT: Den `inputEmoji`-Style behalten (umbenennen oder loeschen).

    Zum Schluss: `cd mobile && npx tsc --noEmit` -- muss 0 Fehler geben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - npx tsc --noEmit gibt 0 Fehler
    - Emoji-Pruefung alle mobilen Quelldateien (MUSS 0 Treffer ergeben):
      `node -e "
        const {execSync} = require('child_process');
        const r = execSync('grep -rn --include=\"*.tsx\" --include=\"*.ts\" -P \"[\\\\x{1F300}-\\\\x{1FAFF}\\\\x{2600}-\\\\x{27BF}\\\\x{FE00}-\\\\x{FEFF}]\" mobile/src/ 2>/dev/null || true').toString();
        console.log(r.trim() || 'OK - keine Emojis gefunden');
      "`
    - `grep -rn "\.emoji" mobile/src/` gibt keine Treffer mehr (ausser Kommentare)
    - `grep -c "iconName" mobile/src/api/badges.api.ts` gibt >= 3
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin -> Badge-Formular | Admin gibt Icon-Namen ein — nicht validierter String, aber kein Code-Execution-Risiko |
| API Response -> Mobile | iconName aus DB wird direkt als Icon-Name an FontAwesome5 uebergeben |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | badge_levels.icon_name | accept | Nur Admin kann Levels anlegen (requireRole('admin')), kein XSS-Risiko in native App |
| T-08-02-02 | Spoofing | FontAwesome5 Icon-Name | accept | Unbekannte Icon-Namen rendern ein leeres Icon — kein Crash, kein Sicherheitsproblem |
| T-08-02-03 | Elevation of Privilege | Badge-API POST | mitigate | requireRole('admin') bereits in badges.router.ts vorhanden — sicherstellen dass es nach Umbenennung erhalten bleibt |
</threat_model>

<verification>
Vollstaendige Emoji-Pruefung (muss 0 Treffer ergeben):
```bash
node -e "
  const {execSync} = require('child_process');
  const patterns = ['mobile/src', 'backend/src'];
  let found = false;
  for (const p of patterns) {
    const r = execSync('grep -rn --include=\"*.tsx\" --include=\"*.ts\" -P \"[\\x{1F300}-\\x{1FAFF}\\x{2600}-\\x{27BF}\\x{FE00}-\\x{FEFF}\\x{1F004}-\\x{1F0CF}]\" ' + p + ' 2>/dev/null || true').toString().trim();
    if (r) { console.log('EMOJI GEFUNDEN in ' + p + ':', r); found = true; }
  }
  if (!found) console.log('OK - keine Emojis in mobile/src und backend/src');
"
```

Zusaetzlich spezifische Literal-Pruefung (🎉 ✨ 🏅):
```bash
grep -rn --include="*.tsx" --include="*.ts" "🎉\|✨\|🏅\|✓" mobile/src/ backend/src/ 2>/dev/null || echo "Sauber"
```

TypeScript:
```bash
cd mobile && npx tsc --noEmit && echo "Mobile OK"
cd ../backend && npx tsc --noEmit && echo "Backend OK"
```
</verification>

<success_criteria>
- Alle Literal-Emojis (🎉 ✨ 🏅 ✓) aus mobile/src/ und backend/src/ entfernt
- badge_levels DB-Feld heisst icon_name, TypeScript-Property heisst iconName
- HomeScreen zeigt FontAwesome5 Icon fuer Badge-Level statt Emoji-Text
- CheckInScreen Erfolg zeigt fa-check-circle Icon statt Text-Checkmark
- BadgeLevelsScreen hat Icon-Name-TextInput mit Preview statt Emoji-Eingabefeld
- Kein einziger emoji-referenzierender Code in mobile/src und backend/src ausser node_modules
- cd mobile && npx tsc --noEmit: 0 Fehler
- cd backend && npx tsc --noEmit: 0 Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08-icon-cleanup/08-02-SUMMARY.md` erstellen.
Wenn DB-Migration nicht automatisch erzeugt wurde, SQL-Befehle im Summary vermerken.
</output>
