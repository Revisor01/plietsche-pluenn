---
phase: 10-push-benachrichtigungen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/push/push.service.ts
  - backend/src/modules/push/push.repository.ts
  - backend/src/modules/push/push.router.ts
  - backend/src/modules/push/push.types.ts
  - backend/src/app.ts
  - mobile/src/store/authStore.ts
  - mobile/src/services/pushService.ts
autonomous: false
requirements:
  - PUSH-01
  - PUSH-02

must_haves:
  truths:
    - "device_tokens Tabelle existiert in der DB mit user_id, store_id, token, platform, push_enabled"
    - "Backend PushService kann Firebase Admin SDK initialisieren und eine Nachricht an einen Token senden"
    - "POST /api/push/token registriert einen Device-Token fuer den eingeloggten User"
    - "DELETE /api/push/token entfernt den Device-Token beim Logout"
    - "Mobile: Nach Login wird FCM-Token ermittelt und an Backend gesendet"
    - "Mobile: Beim Logout wird DELETE /api/push/token aufgerufen"
  artifacts:
    - path: "backend/src/modules/push/push.service.ts"
      provides: "sendToStore(storeId, title, body) und sendToUser(userId, title, body)"
      exports: ["sendToStore", "sendToUser", "sendToTokens"]
    - path: "backend/src/modules/push/push.repository.ts"
      provides: "CRUD fuer device_tokens Tabelle"
      exports: ["insertToken", "deleteToken", "findTokensByStore", "findTokenByUser", "setUserPushEnabled"]
    - path: "backend/src/modules/push/push.router.ts"
      provides: "POST /token und DELETE /token Endpoints"
    - path: "mobile/src/services/pushService.ts"
      provides: "requestPermission(), getToken(), registerToken(apiToken), unregisterToken(apiToken)"
    - path: "mobile/src/store/authStore.ts"
      provides: "login ruft registerToken auf, logout ruft unregisterToken auf"
  key_links:
    - from: "mobile/src/services/pushService.ts"
      to: "POST /api/push/token"
      via: "axios POST mit FCM-Token + platform"
    - from: "backend/src/modules/push/push.service.ts"
      to: "firebase-admin"
      via: "admin.messaging().sendEachForMulticast()"
---

<objective>
Push-Infrastruktur aufbauen: Firebase-Konfiguration, device_tokens DB-Schema, Backend Push-Service und Mobile SDK-Integration.

Purpose: Ohne diese Foundation koennen keine Push-Notifications gesendet werden. Alle weiteren Push-Pläne hängen von diesem Plan ab.
Output: Funktionsfaehige Push-Infrastruktur — Backend kann Nachrichten senden, Mobile registriert Tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-push-benachrichtigungen/10-CONTEXT.md
@backend/src/db/schema.ts
@backend/src/app.ts
@mobile/src/store/authStore.ts
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Firebase-Projekt einrichten und Credentials herunterladen</name>
  <what-built>Dieser Checkpoint ist VORAUSSETZUNG fuer alle weiteren Tasks. Firebase-Admin SDK benoetigt einen Service Account Key, und die Mobile-App benoetigt Plattform-spezifische Config-Dateien.</what-built>
  <how-to-verify>
    Schritte im Firebase Console (https://console.firebase.google.com):

    1. Neues Firebase-Projekt erstellen: "plietsche-pluenn" (oder vorhandenes nutzen)

    2. Im Projekt: Project Settings -> General
       - "Add app" -> iOS App
         - Apple Bundle ID: com.plietschepluenn (aus mobile/ios/PlietschePluenn.xcodeproj pruefen)
         - App registrieren -> GoogleService-Info.plist herunterladen
         - Datei ablegen: mobile/ios/PlietschePluenn/GoogleService-Info.plist
       - "Add app" -> Android App
         - Android package name: com.plietschepluenn (aus mobile/android/app/build.gradle pruefen)
         - App registrieren -> google-services.json herunterladen
         - Datei ablegen: mobile/android/app/google-services.json

    3. Im Projekt: Project Settings -> Service Accounts
       - "Generate new private key" -> JSON herunterladen
       - Datei ablegen: backend/firebase-service-account.json
       - WICHTIG: Diese Datei NICHT in Git committen (in .gitignore eintragen)

    4. Im Projekt: Project Settings -> Cloud Messaging
       - Sicherstellen dass Cloud Messaging API aktiviert ist
       - Fuer iOS: APNs Authentication Key hochladen
         - Apple Developer Portal -> Certificates, Identifiers & Profiles -> Keys
         - Neuen Key erstellen mit "Apple Push Notifications service (APNs)" Berechtigung
         - Key-Datei (.p8) + Key ID + Team ID notieren
         - In Firebase: Project Settings -> Cloud Messaging -> Apple app configuration -> APNs Authentication Key hochladen

    Nach Abschluss: Alle 3 Dateien vorhanden?
    - mobile/ios/PlietschePluenn/GoogleService-Info.plist: ja/nein
    - mobile/android/app/google-services.json: ja/nein
    - backend/firebase-service-account.json: ja/nein
  </how-to-verify>
  <resume-signal>Schreib "bereit" wenn alle drei Dateien heruntergeladen und abgelegt sind, oder beschreibe Probleme.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Backend -- device_tokens Schema, Push-Modul (Service + Repository + Router)</name>
  <files>
    backend/src/db/schema.ts,
    backend/src/modules/push/push.types.ts,
    backend/src/modules/push/push.repository.ts,
    backend/src/modules/push/push.service.ts,
    backend/src/modules/push/push.router.ts,
    backend/src/app.ts,
    backend/.gitignore
  </files>
  <action>
    LESE ZUERST: backend/src/db/schema.ts, backend/src/app.ts, backend/src/modules/campaigns/campaigns.router.ts (als Muster fuer Router-Struktur)

    Schritt 1 — firebase-admin installieren:
    cd backend && npm install firebase-admin

    Schritt 2 — backend/.gitignore pruefen/ergaenzen:
    Sicherstellen dass "firebase-service-account.json" im .gitignore steht. Falls keine .gitignore vorhanden: erstellen mit Eintrag "firebase-service-account.json".

    Schritt 3 — Schema erweitern (backend/src/db/schema.ts):
    Neue Tabelle ans Ende anfuegen:

    ```typescript
    export const deviceTokens = pgTable('device_tokens', {
      id: uuid('id').primaryKey().defaultRandom(),
      userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
      storeId: uuid('store_id').notNull().references(() => stores.id, { onDelete: 'cascade' }),
      token: text('token').notNull(),
      platform: text('platform', { enum: ['ios', 'android'] }).notNull(),
      pushEnabled: boolean('push_enabled').notNull().default(true),
      createdAt: timestamp('created_at').defaultNow(),
      updatedAt: timestamp('updated_at').defaultNow(),
    });
    ```

    Schritt 4 — DB-Migration ausfuehren (auf Server via SSH):
    Die Migration laeuft auf server.godsapp.de. Verbindung: `ssh root@server.godsapp.de`
    Im Container-Verzeichnis des Backends drizzle-kit push ausfuehren:
    `cd /opt/stacks/plietsche-pluenn && docker compose exec backend npx drizzle-kit push`
    (Alternativ: lokale Migration generieren und auf Server deployen -- siehe Deployment-Strategie in STATE.md)

    Schritt 5 — push.types.ts erstellen:
    ```typescript
    export interface RegisterTokenBody {
      token: string;
      platform: 'ios' | 'android';
    }
    ```

    Schritt 6 — push.repository.ts erstellen:
    Import: db, deviceTokens, users aus schema. Drizzle-Queries:
    - insertToken(userId, storeId, token, platform): upsert -- wenn Token fuer diesen User schon existiert, updaten. Verwende `db.insert(deviceTokens).values(...).onConflictDoUpdate(...)` oder delete+insert Pattern da kein eindeutiger Constraint auf userId+token. Einfachste Loesung: erst delete where userId = userId, dann insert (ein User hat immer nur ein aktives Geraet).
    - deleteToken(userId): loescht alle Tokens des Users
    - findTokensByStore(storeId): gibt alle aktiven Tokens (pushEnabled = true) zurueck
    - findTokensByUser(userId): gibt Tokens des Users zurueck
    - setPushEnabled(userId, enabled: boolean): setzt pushEnabled fuer alle Tokens des Users

    Schritt 7 — push.service.ts erstellen:
    Initialisiert firebase-admin mit Service Account aus backend/firebase-service-account.json.
    Singleton-Pattern: `if (!admin.apps.length) { admin.initializeApp({...}) }`.
    Exportiert:
    - sendToTokens(tokens: string[], title: string, body: string): Promise<void>
      Nutzt admin.messaging().sendEachForMulticast({ tokens, notification: { title, body } })
      Filtert invalid tokens aus dem Ergebnis (response.failureCount > 0 -> log, nicht throw)
    - sendToStore(storeId: string, title: string, body: string): Promise<void>
      Laed Tokens via repo.findTokensByStore(storeId), ruft sendToTokens auf
    - sendToUser(userId: string, title: string, body: string): Promise<void>
      Laed Tokens via repo.findTokensByUser(userId), ruft sendToTokens auf

    Schritt 8 — push.router.ts erstellen:
    POST /api/push/token (authenticateToken) — body: RegisterTokenBody (Zod validiert), ruft repo.insertToken(req.user.id, req.user.storeId, body.token, body.platform) auf, antwortet 201.
    DELETE /api/push/token (authenticateToken) — ruft repo.deleteToken(req.user.id) auf, antwortet 204.

    Schritt 9 — app.ts erweitern:
    Import pushRouter, mount: `app.use('/api/push', pushRouter);`

    Schritt 10 — TypeScript pruefen:
    cd backend && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - backend/src/db/schema.ts enthaelt deviceTokens Tabelle
    - backend/src/modules/push/ Verzeichnis mit 4 Dateien existiert
    - app.ts mountet /api/push
    - npx tsc --noEmit ohne Fehler
    - firebase-service-account.json in .gitignore eingetragen
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mobile -- @react-native-firebase/messaging installieren und Token-Registrierung</name>
  <files>
    mobile/package.json,
    mobile/src/services/pushService.ts,
    mobile/src/store/authStore.ts,
    mobile/ios/PlietschePluenn/AppDelegate.swift,
    mobile/ios/Podfile,
    mobile/android/build.gradle,
    mobile/android/app/build.gradle
  </files>
  <action>
    LESE ZUERST: mobile/src/store/authStore.ts, mobile/package.json

    Schritt 1 — Pakete installieren:
    cd mobile && npm install @react-native-firebase/app @react-native-firebase/messaging
    cd mobile/ios && pod install

    Schritt 2 — iOS native Konfiguration (mobile/ios/PlietschePluenn/AppDelegate.swift):
    Lese die aktuelle Datei. Sicherstellen dass `import Firebase` oben steht und in der `application(_:didFinishLaunchingWithOptions:)` Methode `FirebaseApp.configure()` aufgerufen wird -- VOR dem return/super-Aufruf.
    Falls AppDelegate.mm (Objective-C): `#import <Firebase/Firebase.h>` hinzufuegen und `[FIRApp configure];` in `didFinishLaunchingWithOptions`.

    Schritt 3 — Android: google-services Plugin aktivieren:
    mobile/android/build.gradle: in buildscript.dependencies `classpath 'com.google.gms:google-services:4.4.2'` hinzufuegen (falls nicht vorhanden).
    mobile/android/app/build.gradle: am Ende `apply plugin: 'com.google.gms.google-services'` hinzufuegen (falls nicht vorhanden).

    Schritt 4 — pushService.ts erstellen (mobile/src/services/pushService.ts):
    ```typescript
    import messaging from '@react-native-firebase/messaging';
    import { apiClient } from './apiClient'; // oder der bestehende axios-Client

    export async function requestPushPermission(): Promise<boolean> {
      const authStatus = await messaging().requestPermission();
      return (
        authStatus === messaging.AuthorizationStatus.AUTHORIZED ||
        authStatus === messaging.AuthorizationStatus.PROVISIONAL
      );
    }

    export async function registerPushToken(apiToken: string): Promise<void> {
      const granted = await requestPushPermission();
      if (!granted) return;
      const fcmToken = await messaging().getToken();
      if (!fcmToken) return;
      const platform = Platform.OS === 'ios' ? 'ios' : 'android';
      // apiToken = JWT aus authStore
      await axios.post('/api/push/token', { token: fcmToken, platform }, {
        headers: { Authorization: `Bearer ${apiToken}` },
      });
    }

    export async function unregisterPushToken(apiToken: string): Promise<void> {
      await axios.delete('/api/push/token', {
        headers: { Authorization: `Bearer ${apiToken}` },
      });
    }
    ```
    Passe den Import des axios-Clients an den tatsaechlich genutzten Pfad an (lese mobile/src/ Struktur um den bestehenden API-Client zu finden -- z.B. mobile/src/api/ oder mobile/src/services/).

    Schritt 5 — authStore.ts erweitern:
    Die login-Funktion soll nach dem Set-Aufruf registerPushToken(token) aufrufen (async, fire-and-forget -- kein await, kein throw).
    Die logout-Funktion soll VOR dem Set-Aufruf unregisterPushToken aufrufen mit dem aktuellen Token aus dem State (get()-Pattern von Zustand).

    Konkret:
    ```typescript
    // Im Store: get-Pattern fuer logout
    logout: () => {
      const currentToken = get().token;
      if (currentToken) {
        unregisterPushToken(currentToken).catch(() => {}); // fire-and-forget
      }
      set({ token: null, user: null });
    },
    login: (token, user) => {
      set({ token, user });
      registerPushToken(token).catch(() => {}); // fire-and-forget
    },
    ```
    create((set, get) => ...) -- get hinzufuegen falls noch nicht vorhanden.

    Schritt 6 — TypeScript pruefen:
    cd mobile && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>
    - @react-native-firebase/app und @react-native-firebase/messaging in package.json
    - mobile/src/services/pushService.ts existiert mit requestPushPermission, registerPushToken, unregisterPushToken
    - authStore.ts: login ruft registerPushToken auf, logout ruft unregisterPushToken auf
    - iOS AppDelegate konfiguriert FirebaseApp
    - Android build.gradle hat google-services Plugin
    - npx tsc --noEmit ohne Fehler
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile -> POST /api/push/token | Eingehender FCM-Token vom Geraet -- nur authentifizierte User |
| Backend -> Firebase | Server-seitiger Aufruf mit Service Account -- Credentials muessen geschuetzt sein |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | POST /api/push/token | mitigate | authenticateToken Middleware pflichtend -- kein anonymes Token-Register |
| T-10-02 | Tampering | firebase-service-account.json | mitigate | .gitignore Eintrag, Datei nur auf Server vorhanden, nicht in Repository |
| T-10-03 | Information Disclosure | device_tokens Tabelle | mitigate | Nur eigener Token sichtbar (kein Admin-Endpoint der alle Tokens listed) |
| T-10-04 | Denial of Service | sendToTokens massenhaft aufrufen | accept | Intern aufgerufen, kein oeffentlicher Endpoint zum direkten Senden |
| T-10-05 | Elevation of Privilege | Push-Token eines anderen Users registrieren | mitigate | Token wird immer an req.user.id gebunden -- kein userId im Request-Body |
</threat_model>

<verification>
1. Backend TypeScript kompiliert: `cd backend && npx tsc --noEmit`
2. device_tokens Tabelle in DB vorhanden: Via SSH auf Server `psql -c "\dt device_tokens"` oder Drizzle Studio
3. POST /api/push/token mit gueltigem JWT gibt 201 zurueck
4. DELETE /api/push/token mit gueltigem JWT gibt 204 zurueck
5. Mobile TypeScript kompiliert: `cd mobile && npx tsc --noEmit`
6. firebase-service-account.json ist NICHT in git tracked: `git status backend/firebase-service-account.json` muss "ignored" zeigen
</verification>

<success_criteria>
- device_tokens Tabelle in Produktionsdatenbank vorhanden
- Backend Push-Service kann firebase-admin initialisieren ohne Fehler beim Start
- Mobile App fordert Push-Berechtigung nach Login an
- Device-Token wird nach erfolgreichem Login an Backend registriert (HTTP 201)
- Device-Token wird beim Logout geloescht (HTTP 204)
- Kein TypeScript-Fehler in Backend oder Mobile
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/10-push-benachrichtigungen/10-01-SUMMARY.md` erstellen.
</output>
