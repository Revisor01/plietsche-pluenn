---
phase: 10-push-benachrichtigungen
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - backend/src/modules/campaigns/campaigns.service.ts
  - backend/src/modules/items/items.service.ts
  - backend/src/modules/push/streak-reminder.ts
autonomous: true
requirements:
  - PUSH-03
  - PUSH-04
  - PUSH-05

must_haves:
  truths:
    - "Neue Kampagne erstellt -> Push-Nachricht 'Neue Aktion: [Titel]!' geht an alle Store-Besucher"
    - "Schaufenster-Markierung gesetzt -> Push-Nachricht 'Neues Highlight im Laden!' geht an alle Store-Besucher"
    - "Streak-Erinnerung: Backend-Job prueft Freitags welche Besucher diese Woche noch nicht eingecheckt haben und sendet Push"
    - "Streak-Erinnerung-Push-Text: 'Deine Serie bricht bald ab -- komm diese Woche noch vorbei!'"
  artifacts:
    - path: "backend/src/modules/campaigns/campaigns.service.ts"
      provides: "createCampaign ruft sendToStore nach erfolgreicher DB-Erstellung auf"
    - path: "backend/src/modules/items/items.service.ts"
      provides: "setShowcase ruft sendToStore auf wenn isShowcase=true"
    - path: "backend/src/modules/push/streak-reminder.ts"
      provides: "checkAndSendStreakReminders(): prueft weekly_visits und sendet Push an saeumigeBesucher"
      exports: ["checkAndSendStreakReminders", "startStreakReminderScheduler"]
  key_links:
    - from: "backend/src/modules/campaigns/campaigns.service.ts"
      to: "backend/src/modules/push/push.service.ts"
      via: "sendToStore(storeId, 'Neue Aktion', title) nach repo.createCampaign"
    - from: "backend/src/modules/push/streak-reminder.ts"
      to: "weekly_visits Tabelle"
      via: "Drizzle-Query: Besucher die in aktueller Woche keine Eintraege haben"
    - from: "backend/src/index.ts (oder server.ts)"
      to: "startStreakReminderScheduler"
      via: "Import + Aufruf beim Server-Start"
---

<objective>
Automatische Push-Trigger implementieren: Kampagnen, Schaufenster und Streak-Erinnerung.

Purpose: Die drei automatischen Trigger-Events verbinden bestehende Business-Events (Kampagne anlegen, Showcase markieren) mit dem Push-Service aus Plan 01.
Output: Drei Trigger-Mechanismen -- zwei inline in bestehenden Services, einer als eigenstaendiger Scheduler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-push-benachrichtigungen/10-CONTEXT.md
@.planning/phases/10-push-benachrichtigungen/10-01-SUMMARY.md
@backend/src/modules/campaigns/campaigns.service.ts
@backend/src/modules/items/items.service.ts
@backend/src/db/schema.ts
</context>

<interfaces>
<!-- Aus Plan 01 erzeugt -- push.service.ts Exports -->
```typescript
// backend/src/modules/push/push.service.ts
export async function sendToStore(storeId: string, title: string, body: string): Promise<void>;
export async function sendToUser(userId: string, title: string, body: string): Promise<void>;
export async function sendToTokens(tokens: string[], title: string, body: string): Promise<void>;
```

<!-- Schema -- relevant fuer Streak-Check -->
```typescript
// weekly_visits Tabelle (aus schema.ts)
export const weeklyVisits = pgTable('weekly_visits', {
  userId: uuid('user_id').notNull(),
  storeId: uuid('store_id').notNull(),
  weekStart: text('week_start').notNull(), // ISO-Datum, z.B. "2026-04-06"
  visitCount: integer('visit_count').notNull().default(0),
});

// users Tabelle (relevant fuer Rolle-Filter)
export const users = pgTable('users', {
  id: uuid('id').primaryKey(),
  storeId: uuid('store_id').notNull(),
  role: text('role', { enum: ['admin', 'volunteer', 'visitor'] }).notNull(),
  // ...
});

// deviceTokens Tabelle (aus Plan 01)
export const deviceTokens = pgTable('device_tokens', {
  userId: uuid('user_id').notNull(),
  storeId: uuid('store_id').notNull(),
  pushEnabled: boolean('push_enabled').notNull().default(true),
});
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Kampagne-Push und Showcase-Push in bestehende Services einfuegen</name>
  <files>
    backend/src/modules/campaigns/campaigns.service.ts,
    backend/src/modules/items/items.service.ts
  </files>
  <action>
    LESE ZUERST: backend/src/modules/campaigns/campaigns.service.ts, backend/src/modules/items/items.service.ts

    Datei 1 -- campaigns.service.ts:
    createCampaign ist aktuell ein einfacher Delegation-Call zu repo.createCampaign.
    Umwandeln in async Funktion die nach der DB-Erstellung einen Push sendet.

    Neues createCampaign:
    ```typescript
    import * as pushService from '../push/push.service';

    export const createCampaign = async (storeId: string, data: CreateCampaignBody) => {
      const campaign = await repo.createCampaign(storeId, data);
      // Push fire-and-forget -- kein Fehler nach aussen werfen
      pushService.sendToStore(
        storeId,
        'Neue Aktion',
        `Neue Aktion: ${campaign.title}!`
      ).catch((err: unknown) => {
        console.error('[Push] Kampagne-Push fehlgeschlagen:', err);
      });
      return campaign;
    };
    ```

    Datei 2 -- items.service.ts:
    setShowcase erweitern: nur senden wenn isShowcase true gesetzt wird.
    ```typescript
    import * as pushService from '../push/push.service';

    export async function setShowcase(id: string, storeId: string, isShowcase: boolean) {
      const result = await itemsRepo.setShowcase(id, storeId, isShowcase);
      if (isShowcase) {
        pushService.sendToStore(
          storeId,
          'Neues Highlight',
          'Neues Highlight im Laden!'
        ).catch((err: unknown) => {
          console.error('[Push] Showcase-Push fehlgeschlagen:', err);
        });
      }
      return result;
    }
    ```

    TypeScript pruefen: cd backend && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - campaigns.service.ts: createCampaign ist async und ruft sendToStore fire-and-forget auf
    - items.service.ts: setShowcase ruft sendToStore auf wenn isShowcase=true
    - Kein TypeScript-Fehler
    - Fehler beim Push-Senden werden geloggt aber nicht an den Caller weitergegeben
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Streak-Erinnerung Scheduler implementieren</name>
  <files>
    backend/src/modules/push/streak-reminder.ts,
    backend/src/index.ts
  </files>
  <action>
    LESE ZUERST: backend/src/index.ts (oder server.ts -- suche die Datei die httpServer.listen aufruft), backend/src/db/schema.ts (weeklyVisits + users + deviceTokens)

    Schritt 1 -- node-cron installieren:
    cd backend && npm install node-cron && npm install --save-dev @types/node-cron

    Schritt 2 -- streak-reminder.ts erstellen:

    Logik des Streak-Checks:
    - Freitags (cron: '0 9 * * 5' = jeden Freitag um 9 Uhr)
    - weekStart berechnen: ISO-Datum des letzten Montags (dayjs oder native Date)
    - Query: alle Besucher (role='visitor') des Stores, deren userId NICHT in weekly_visits mit weekStart der aktuellen Woche vorkommt (oder wo visitCount = 0)
    - Diese Besucher erhalten den Push -- aber nur wenn sie einen registrierten Token mit pushEnabled=true haben

    Einfachste Query-Strategie ohne Join-Komplexitaet:
    1. Alle distinct userIds aus deviceTokens WHERE storeId = storeId AND pushEnabled = true laden
    2. Alle userIds aus weekly_visits WHERE weekStart = aktuellerWeekStart AND visitCount > 0 laden (= haben diese Woche besucht)
    3. Differenz bilden: Tokens-UserIds minus besucht-UserIds = saeumigeBesucher
    4. Tokens dieser Users laden und Push senden

    Da Stores-Liste dynamisch: entweder alle Stores iterieren oder einzel-Store-Variante.
    Fuer Phase 10: Alle Stores in einer Schleife abarbeiten.

    ```typescript
    import cron from 'node-cron';
    import { db } from '../../db/client';
    import { deviceTokens, weeklyVisits, stores } from '../../db/schema';
    import { eq, and, inArray } from 'drizzle-orm';
    import { sendToTokens } from './push.service';

    function getCurrentWeekStart(): string {
      const now = new Date();
      const day = now.getDay(); // 0=Sonntag, 1=Montag...
      const diff = now.getDate() - day + (day === 0 ? -6 : 1); // Montag dieser Woche
      const monday = new Date(now.setDate(diff));
      return monday.toISOString().slice(0, 10); // "YYYY-MM-DD"
    }

    export async function checkAndSendStreakReminders(): Promise<void> {
      const weekStart = getCurrentWeekStart();
      const allStores = await db.select({ id: stores.id }).from(stores);

      for (const store of allStores) {
        // Alle aktiven Token-User dieses Stores
        const activeTokenRows = await db
          .select({ userId: deviceTokens.userId, token: deviceTokens.token })
          .from(deviceTokens)
          .where(and(
            eq(deviceTokens.storeId, store.id),
            eq(deviceTokens.pushEnabled, true)
          ));

        if (activeTokenRows.length === 0) continue;

        const activeUserIds = activeTokenRows.map(r => r.userId);

        // User die diese Woche schon besucht haben
        const visitedRows = await db
          .select({ userId: weeklyVisits.userId })
          .from(weeklyVisits)
          .where(and(
            eq(weeklyVisits.storeId, store.id),
            eq(weeklyVisits.weekStart, weekStart),
            inArray(weeklyVisits.userId, activeUserIds)
          ));

        const visitedUserIds = new Set(visitedRows.map(r => r.userId));

        // Token der saeumigen Besucher
        const remindTokens = activeTokenRows
          .filter(r => !visitedUserIds.has(r.userId))
          .map(r => r.token);

        if (remindTokens.length === 0) continue;

        await sendToTokens(
          remindTokens,
          'Plietsche Pluenn',
          'Deine Serie bricht bald ab -- komm diese Woche noch vorbei!'
        ).catch((err: unknown) => {
          console.error(`[Push] Streak-Reminder fuer Store ${store.id} fehlgeschlagen:`, err);
        });

        console.log(`[Push] Streak-Reminder: ${remindTokens.length} Benachrichtigungen fuer Store ${store.id} gesendet`);
      }
    }

    export function startStreakReminderScheduler(): void {
      // Jeden Freitag um 9 Uhr (Server-Zeitzone)
      cron.schedule('0 9 * * 5', () => {
        console.log('[Push] Streak-Reminder Cron gestartet');
        checkAndSendStreakReminders().catch((err: unknown) => {
          console.error('[Push] Streak-Reminder Cron-Fehler:', err);
        });
      });
      console.log('[Push] Streak-Reminder Scheduler registriert (Freitag 9 Uhr)');
    }
    ```

    Schritt 3 -- Scheduler beim Server-Start aktivieren:
    Lese backend/src/index.ts (oder den Einstiegspunkt des Backends).
    startStreakReminderScheduler() aufrufen nach dem Server-Listen-Aufruf:
    ```typescript
    import { startStreakReminderScheduler } from './modules/push/streak-reminder';
    // nach httpServer.listen(...)
    startStreakReminderScheduler();
    ```

    TypeScript pruefen: cd backend && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - backend/src/modules/push/streak-reminder.ts existiert
    - checkAndSendStreakReminders() berechnet weekStart korrekt (Montag der aktuellen Woche)
    - Nur Besucher ohne weekly_visits Eintrag in aktueller Woche erhalten Push
    - startStreakReminderScheduler() wird im Server-Einstiegspunkt aufgerufen
    - node-cron in package.json
    - Kein TypeScript-Fehler
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-Cron -> Firebase | Interner Job, kein User-Input -- niedrige Angriffsoberflaeche |
| Campaign-Create -> Push | Admin-authentifizierter Aufruf, kein User-Input im Push-Text ausser Kampagnen-Titel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | Kampagnen-Titel in Push-Text | accept | Admin erstellt Kampagne -- vertrauenswuerdiger Akteur. Titel bereits durch Zod validiert (max 200 Zeichen) |
| T-10-07 | Denial of Service | Cron sendet massenhaft Pushes | accept | Laeuft einmal woechentlich, maximal so viele Tokens wie Besucher des Stores |
| T-10-08 | Information Disclosure | Push zeigt Streak-Status implizit | accept | Text ist generisch, kein spezifischer Streak-Wert enthalten |
</threat_model>

<verification>
1. Backend TypeScript kompiliert: `cd backend && npx tsc --noEmit`
2. Manuelle Test-Validierung: checkAndSendStreakReminders() direkt aufrufen und im Log pruefen ob "Streak-Reminder Scheduler registriert" erscheint
3. Kampagne erstellen via POST /api/campaigns und in Firebase Console Delivery Reports pruefen
</verification>

<success_criteria>
- campaigns.service.ts: createCampaign loest Push aus -- kein TypeScript-Fehler
- items.service.ts: setShowcase(id, storeId, true) loest Push aus
- streak-reminder.ts: Cron-Schedule korrekt konfiguriert (Freitag 9 Uhr)
- Streak-Reminder sendet nur an Besucher ohne Besuch in aktueller Woche
- Alle drei automatischen Trigger fehlerresistent (catch + log, kein Crash)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/10-push-benachrichtigungen/10-02-SUMMARY.md` erstellen.
</output>
