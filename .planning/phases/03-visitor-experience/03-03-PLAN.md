---
phase: 03-visitor-experience
plan: "03"
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/points/points.router.ts
  - backend/src/modules/points/points.service.ts
  - backend/src/modules/points/points.repository.ts
  - backend/src/modules/checkin/checkin.service.ts
  - backend/src/modules/scan/scan.service.ts
  - backend/src/app.ts
  - mobile/src/screens/visitor/HomeScreen.tsx
  - mobile/src/screens/visitor/PointsHistoryScreen.tsx
  - mobile/src/store/pointsStore.ts
  - mobile/src/api/points.api.ts
autonomous: true
requirements: [PUNKT-01, PUNKT-02, PUNKT-03]

must_haves:
  truths:
    - "HomeScreen zeigt Punktestand prominent oben mit Label 'PlietschPunkte'"
    - "Punktestand aktualisiert sich nach App-Start via GET /api/points/balance"
    - "GET /api/points/history gibt Liste mit Datum + Quelle + Betrag zurück (kein Item-Detail)"
    - "PointsHistoryScreen zeigt Transaktionen als Liste"
    - "store_settings Tabelle speichert konfigurierbare Punktwerte pro Store"
    - "scan.service und checkin.service lesen Punktwerte aus store_settings statt Hardcode"
    - "Admin kann Punktwerte via PATCH /api/stores/settings setzen"
  artifacts:
    - path: backend/src/modules/points/points.router.ts
      provides: GET /api/points/balance + GET /api/points/history
      exports: [default pointsRouter]
    - path: backend/src/modules/points/points.service.ts
      provides: getBalance, getHistory, getStoreSettings
    - path: backend/src/modules/points/points.repository.ts
      provides: getTransactionHistory, getStoreSettings, upsertStoreSettings
    - path: mobile/src/screens/visitor/HomeScreen.tsx
      provides: Prominente Punktestand-Anzeige oben
    - path: mobile/src/screens/visitor/PointsHistoryScreen.tsx
      provides: Transaktionsliste
    - path: mobile/src/store/pointsStore.ts
      provides: Zustand Store für Punkte-Cache
  key_links:
    - from: mobile/src/screens/visitor/HomeScreen.tsx
      to: GET /api/points/balance
      via: points.api.ts
      pattern: "apiClient.get.*api/points/balance"
    - from: scan.service.ts
      to: store_settings table
      via: points.repository.getStoreSettings
      pattern: "getStoreSettings.*storeId"
    - from: checkin.service.ts
      to: store_settings table
      via: points.repository.getStoreSettings
      pattern: "getStoreSettings.*storeId"
---

<objective>
PlietschPunkte-Engine: store_settings Tabelle für konfigurierbare Punkte-Werte pro Store, points-API (Balance + History), HomeScreen mit prominentem Punktestand, PointsHistoryScreen — alles ohne personenbezogene Item-Verknüpfung.

Purpose: Schließt den Gamification-Loop: Besucher sehen ihren Punktestand sofort auf dem Homescreen und eine saubere Historie ohne Datenschutz-Risiko.
Output: points-Backend-Modul, store_settings-Tabelle, HomeScreen mit Punktestand, PointsHistoryScreen mit Transaktionsliste, konfigurierbare Punkte-Werte ersetzen die Hardcodes aus Plan 01/02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-visitor-experience/03-CONTEXT.md
@.planning/phases/03-visitor-experience/03-RESEARCH.md
@.planning/phases/03-visitor-experience/03-01-SUMMARY.md
@.planning/phases/03-visitor-experience/03-02-SUMMARY.md

<interfaces>
<!-- Aus Plan 03-01 und 03-02 -->

Aus backend/src/modules/scan/scan.repository.ts:
```typescript
// awardPoints bereits implementiert — in Plan 03 bleibt der Aufruf gleich,
// nur die Punkte-Anzahl kommt jetzt aus store_settings statt Hardcode
export async function awardPoints(userId, storeId, points, source): Promise<number>
```

Aus backend/src/db/schema.ts (nach 03-01 + 03-02):
```typescript
export const pointTransactions = pgTable('point_transactions', {
  id: uuid, storeId: uuid, userId: uuid,
  // KEIN itemId
  source: text({ enum: ['item_scan', 'checkin', 'manual_items'] }),
  points: integer, createdAt: timestamp
});
export const checkins = pgTable('checkins', {
  id: uuid, storeId: uuid, userId: uuid,
  itemCount: integer, createdAt: timestamp
  // KEINE lat/lng
});
```

Aus CONTEXT.md — Locked Decision (Punkte-Defaults, admin-konfigurierbar):
```
Check-In: 5 Punkte
Item-Scan: 10 Punkte
Pro nicht-digitales Teil: 3 Punkte
```

Aus RESEARCH.md — store_settings Schema:
```typescript
export const storeSettings = pgTable('store_settings', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().unique().references(() => stores.id),
  pointsPerScan: integer('points_per_scan').notNull().default(10),
  pointsPerCheckin: integer('points_per_checkin').notNull().default(5),
  pointsPerItem: integer('points_per_item').notNull().default(3),
  maxItemsPerCheckin: integer('max_items_per_checkin').notNull().default(10),
  updatedAt: timestamp('updated_at').defaultNow(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: store_settings Tabelle + points-Backend-Modul + Hardcodes ersetzen</name>
  <files>
    backend/src/db/schema.ts,
    backend/src/modules/points/points.router.ts,
    backend/src/modules/points/points.service.ts,
    backend/src/modules/points/points.repository.ts,
    backend/src/modules/scan/scan.service.ts,
    backend/src/modules/checkin/checkin.service.ts,
    backend/src/app.ts
  </files>
  <read_first>
    - backend/src/db/schema.ts (aktuelle Tabellen nach 03-01 + 03-02)
    - backend/src/modules/scan/scan.service.ts (POINTS_PER_SCAN Hardcode ersetzen)
    - backend/src/modules/checkin/checkin.service.ts (POINTS_PER_CHECKIN/ITEM Hardcodes ersetzen)
    - backend/src/modules/items/items.router.ts (Router-Pattern)
    - backend/src/app.ts (Einbinden-Pattern)
  </read_first>
  <behavior>
    - GET /api/points/balance → { points: number } (eigener Punktestand aus users.plietschPoints)
    - GET /api/points/history → { transactions: Array<{ id, source, points, createdAt }> } (kein itemId, kein itemTitle)
    - PATCH /api/stores/settings (admin) → store_settings aktualisieren
    - scan.service liest pointsPerScan aus store_settings (Default 10)
    - checkin.service liest pointsPerCheckin + pointsPerItem + maxItemsPerCheckin aus store_settings
  </behavior>
  <action>
    1. schema.ts: storeSettings-Tabelle hinzufügen:
    ```typescript
    export const storeSettings = pgTable('store_settings', {
      id: uuid('id').primaryKey().defaultRandom(),
      storeId: uuid('store_id').notNull().unique().references(() => stores.id),
      pointsPerScan: integer('points_per_scan').notNull().default(10),
      pointsPerCheckin: integer('points_per_checkin').notNull().default(5),
      pointsPerItem: integer('points_per_item').notNull().default(3),
      maxItemsPerCheckin: integer('max_items_per_checkin').notNull().default(10),
      updatedAt: timestamp('updated_at').defaultNow(),
    });
    ```

    2. SQL-Migration auf Server:
    ```bash
    ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"CREATE TABLE IF NOT EXISTS store_settings (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), store_id uuid NOT NULL UNIQUE REFERENCES stores(id), points_per_scan integer NOT NULL DEFAULT 10, points_per_checkin integer NOT NULL DEFAULT 5, points_per_item integer NOT NULL DEFAULT 3, max_items_per_checkin integer NOT NULL DEFAULT 10, updated_at timestamp DEFAULT now());\""
    ```

    Standard-Settings für bestehenden Store anlegen:
    ```bash
    ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"INSERT INTO store_settings (store_id) VALUES ('6a0fb86e-d795-4fd3-87fa-a21143f830b3') ON CONFLICT (store_id) DO NOTHING;\""
    ```

    3. points.repository.ts erstellen:
    ```typescript
    import { db } from '../../db/client';
    import { users, pointTransactions, storeSettings } from '../../db/schema';
    import { eq, desc } from 'drizzle-orm';

    export async function getBalance(userId: string): Promise<number> {
      const result = await db.select({ plietschPoints: users.plietschPoints })
        .from(users).where(eq(users.id, userId)).limit(1);
      return result[0]?.plietschPoints ?? 0;
    }

    export async function getTransactionHistory(userId: string, storeId: string, limit = 50) {
      return db.select({
        id: pointTransactions.id,
        source: pointTransactions.source,
        points: pointTransactions.points,
        createdAt: pointTransactions.createdAt,
      })
      .from(pointTransactions)
      .where(eq(pointTransactions.userId, userId))
      .orderBy(desc(pointTransactions.createdAt))
      .limit(limit);
      // Kein itemId im SELECT — PUNKT-03 Locked Decision
    }

    export async function getStoreSettings(storeId: string) {
      const result = await db.select().from(storeSettings)
        .where(eq(storeSettings.storeId, storeId)).limit(1);
      // Fallback auf Defaults wenn kein Eintrag vorhanden
      return result[0] ?? {
        pointsPerScan: 10,
        pointsPerCheckin: 5,
        pointsPerItem: 3,
        maxItemsPerCheckin: 10,
      };
    }

    export async function upsertStoreSettings(storeId: string, data: {
      pointsPerScan?: number;
      pointsPerCheckin?: number;
      pointsPerItem?: number;
      maxItemsPerCheckin?: number;
    }) {
      const { sql } = await import('drizzle-orm');
      await db.insert(storeSettings)
        .values({ storeId, ...data })
        .onConflictDoUpdate({
          target: storeSettings.storeId,
          set: { ...data, updatedAt: sql`now()` },
        });
    }
    ```

    4. points.service.ts erstellen:
    ```typescript
    import * as pointsRepo from './points.repository';

    export async function getBalance(userId: string) {
      const points = await pointsRepo.getBalance(userId);
      return { points };
    }

    export async function getHistory(userId: string, storeId: string) {
      const transactions = await pointsRepo.getTransactionHistory(userId, storeId);
      return { transactions };
    }

    export { getStoreSettings, upsertStoreSettings } from './points.repository';
    ```

    5. points.router.ts erstellen:
    ```typescript
    import { Router, type Request, type Response } from 'express';
    import { z } from 'zod';
    import { authenticateToken } from '../../middleware/auth';
    import { requireRole } from '../../middleware/requireRole';
    import * as pointsService from './points.service';
    import { upsertStoreSettings } from './points.repository';

    const router = Router();

    const PatchSettingsSchema = z.object({
      pointsPerScan: z.number().int().min(0).max(1000).optional(),
      pointsPerCheckin: z.number().int().min(0).max(1000).optional(),
      pointsPerItem: z.number().int().min(0).max(100).optional(),
      maxItemsPerCheckin: z.number().int().min(1).max(100).optional(),
    });

    router.get('/balance', authenticateToken, async (req: Request, res: Response) => {
      const result = await pointsService.getBalance(req.user!.id);
      res.json(result);
    });

    router.get('/history', authenticateToken, async (req: Request, res: Response) => {
      const result = await pointsService.getHistory(req.user!.id, req.user!.storeId);
      res.json(result);
    });

    router.patch(
      '/settings',
      authenticateToken,
      requireRole('admin'),
      async (req: Request, res: Response) => {
        const data = PatchSettingsSchema.parse(req.body);
        await upsertStoreSettings(req.user!.storeId, data);
        res.json({ ok: true });
      }
    );

    export default router;
    ```

    6. scan.service.ts aktualisieren — POINTS_PER_SCAN Hardcode ersetzen:
    ```typescript
    // Import hinzufügen:
    import { getStoreSettings } from '../points/points.repository';

    // In scanItemQr():
    const settings = await getStoreSettings(storeId);
    const pointsPerScan = settings.pointsPerScan;
    // statt: const POINTS_PER_SCAN = 10;
    ```

    7. checkin.service.ts aktualisieren — alle Hardcodes ersetzen:
    ```typescript
    // Import hinzufügen:
    import { getStoreSettings } from '../points/points.repository';

    // In processCheckin() nach store-Lookup:
    const settings = await getStoreSettings(storeId);
    const basePoints = settings.pointsPerCheckin;
    const itemPoints = Math.min(itemCount, settings.maxItemsPerCheckin) * settings.pointsPerItem;
    // Stepper-Max-Wert kommt jetzt aus settings.maxItemsPerCheckin (für spätere API-Erweiterung)
    ```

    8. app.ts: pointsRouter einbinden:
    ```typescript
    import pointsRouter from './modules/points/points.router';
    app.use('/api/points', pointsRouter);
    ```

    9. Backend deployen:
    ```bash
    cd /Users/simonluthe/Documents/plietsche-pluenn && git add -A && git commit -m "feat(03-03): store_settings + points-Modul + konfigurierbare Punkte-Werte" && git push
    ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn && git pull && cd backend && docker build -t plietsche-backend:latest . && docker restart plietsche-backend"
    ```
  </action>
  <verify>
    <automated>
      # TypeScript-Check:
      cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit

      # Nach Deployment:
      VISITOR_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login -H 'Content-Type: application/json' -d '{"email":"visitor@test.de","password":"test123"}' | jq -r '.token')

      # Balance-Endpoint:
      curl -s https://plietsche-pluenn.godsapp.de/api/points/balance -H "Authorization: Bearer $VISITOR_TOKEN" | jq .
      # Erwartet: { "points": <number> }

      # History-Endpoint (keine item_id im Response):
      curl -s https://plietsche-pluenn.godsapp.de/api/points/history -H "Authorization: Bearer $VISITOR_TOKEN" | jq '.transactions[0] | keys'
      # Erwartet: ["createdAt", "id", "points", "source"] — KEIN "itemId"

      # store_settings existiert:
      ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c 'SELECT * FROM store_settings;'" | grep -c "6a0fb86e" && echo "store_settings: OK"

      # Admin kann Settings setzen:
      ADMIN_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login -H 'Content-Type: application/json' -d '{"email":"admin@test.de","password":"test123"}' | jq -r '.token')
      curl -s -o /dev/null -w "%{http_code}" -X PATCH https://plietsche-pluenn.godsapp.de/api/points/settings -H "Authorization: Bearer $ADMIN_TOKEN" -H 'Content-Type: application/json' -d '{"pointsPerScan":10,"pointsPerCheckin":5,"pointsPerItem":3}'
      # Erwartet: 200
    </automated>
  </verify>
  <acceptance_criteria>
    - GET /api/points/balance → { points: number }
    - GET /api/points/history → { transactions: [...] } ohne itemId-Feld
    - PATCH /api/points/settings (admin) → 200
    - store_settings-Tabelle auf Server erstellt mit Default-Eintrag für Store-UUID
    - scan.service und checkin.service lesen Werte aus store_settings
    - npx tsc --noEmit EXIT 0
  </acceptance_criteria>
  <done>points-Backend deployed, Hardcodes ersetzt, store_settings konfigurierbar</done>
</task>

<task type="auto">
  <name>Task 2: HomeScreen Punktestand + PointsHistoryScreen Mobile</name>
  <files>
    mobile/src/screens/visitor/HomeScreen.tsx,
    mobile/src/screens/visitor/PointsHistoryScreen.tsx,
    mobile/src/store/pointsStore.ts,
    mobile/src/api/points.api.ts
  </files>
  <read_first>
    - mobile/src/screens/visitor/HomeScreen.tsx (Platzhalter aus Plan 03-01)
    - mobile/src/screens/visitor/PointsHistoryScreen.tsx (Platzhalter aus Plan 03-01)
    - mobile/src/store/authStore.ts (Zustand-Store-Pattern)
    - mobile/src/screens/items/ItemListScreen.tsx (FlatList-Pattern für Historie)
  </read_first>
  <action>
    1. points.api.ts erstellen:
    ```typescript
    import apiClient from './client';

    export interface PointTransaction {
      id: string;
      source: 'item_scan' | 'checkin' | 'manual_items';
      points: number;
      createdAt: string;
    }

    export async function fetchBalance(): Promise<{ points: number }> {
      const res = await apiClient.get('/points/balance');
      return res.data;
    }

    export async function fetchHistory(): Promise<{ transactions: PointTransaction[] }> {
      const res = await apiClient.get('/points/history');
      return res.data;
    }
    ```

    2. pointsStore.ts erstellen (Zustand-Pattern aus authStore):
    ```typescript
    import { create } from 'zustand';
    import { fetchBalance } from '../api/points.api';

    interface PointsState {
      balance: number;
      isLoading: boolean;
      loadBalance: () => Promise<void>;
    }

    export const usePointsStore = create<PointsState>((set) => ({
      balance: 0,
      isLoading: false,
      loadBalance: async () => {
        set({ isLoading: true });
        try {
          const { points } = await fetchBalance();
          set({ balance: points });
        } catch {
          // Stiller Fehler — Balance bleibt bei 0
        } finally {
          set({ isLoading: false });
        }
      },
    }));
    ```

    3. HomeScreen.tsx implementieren — Punktestand prominent oben (per PUNKT-01 Locked Decision):
    ```typescript
    import React, { useEffect } from 'react';
    import { View, Text, StyleSheet, TouchableOpacity, ScrollView } from 'react-native';
    import { usePointsStore } from '../../store/pointsStore';
    import { useAuthStore } from '../../store/authStore';

    export default function HomeScreen() {
      const { balance, isLoading, loadBalance } = usePointsStore();
      const user = useAuthStore((s) => s.user);

      useEffect(() => {
        loadBalance();
      }, [loadBalance]);

      return (
        <ScrollView style={styles.container} contentContainerStyle={styles.content}>
          {/* Prominente Punktestand-Anzeige — Locked Decision PUNKT-01 */}
          <View style={styles.pointsCard}>
            <Text style={styles.pointsLabel}>PlietschPunkte</Text>
            <Text style={styles.pointsValue}>{isLoading ? '...' : balance}</Text>
            <Text style={styles.pointsSubtitle}>Dein aktueller Stand</Text>
          </View>

          {/* Begrüßung */}
          <View style={styles.welcomeSection}>
            <Text style={styles.welcomeTitle}>Willkommen{user?.username ? `, ${user.username}` : ''}!</Text>
            <Text style={styles.welcomeText}>
              Scanne den QR-Code an Kleidungsstücken oder checke ein, um PlietschPunkte zu sammeln.
            </Text>
          </View>
        </ScrollView>
      );
    }

    const styles = StyleSheet.create({
      container: { flex: 1, backgroundColor: '#F9FAFB' },
      content: { padding: 16 },
      pointsCard: {
        backgroundColor: '#2563EB',
        borderRadius: 16,
        padding: 24,
        alignItems: 'center',
        marginBottom: 24,
        shadowColor: '#2563EB',
        shadowOffset: { width: 0, height: 4 },
        shadowOpacity: 0.3,
        shadowRadius: 8,
        elevation: 6,
      },
      pointsLabel: { fontSize: 14, color: '#BFDBFE', fontWeight: '600', letterSpacing: 1, textTransform: 'uppercase' },
      pointsValue: { fontSize: 72, fontWeight: '800', color: '#FFFFFF', marginVertical: 4 },
      pointsSubtitle: { fontSize: 13, color: '#93C5FD' },
      welcomeSection: { backgroundColor: '#FFFFFF', borderRadius: 12, padding: 16 },
      welcomeTitle: { fontSize: 18, fontWeight: '700', color: '#111827', marginBottom: 8 },
      welcomeText: { fontSize: 14, color: '#6B7280', lineHeight: 20 },
    });
    ```

    4. PointsHistoryScreen.tsx implementieren (Locked Decision: Datum + Quelle + Betrag, kein Item-Detail):
    ```typescript
    import React, { useState, useEffect } from 'react';
    import { View, Text, FlatList, StyleSheet, ActivityIndicator, RefreshControl } from 'react-native';
    import { fetchHistory, type PointTransaction } from '../../api/points.api';
    import { useAuthStore } from '../../store/authStore';

    const SOURCE_LABELS: Record<string, string> = {
      item_scan: 'QR-Scan',
      checkin: 'Check-In',
      manual_items: 'Teile mitgenommen',
    };

    export default function PointsHistoryScreen() {
      const [transactions, setTransactions] = useState<PointTransaction[]>([]);
      const [isLoading, setIsLoading] = useState(false);
      const [isRefreshing, setIsRefreshing] = useState(false);

      const load = async (refreshing = false) => {
        refreshing ? setIsRefreshing(true) : setIsLoading(true);
        try {
          const { transactions: txs } = await fetchHistory();
          setTransactions(txs);
        } catch { /* stille Fehlerbehandlung */ }
        finally { setIsLoading(false); setIsRefreshing(false); }
      };

      useEffect(() => { load(); }, []);

      const renderItem = ({ item }: { item: PointTransaction }) => (
        <View style={styles.row}>
          <View style={styles.rowLeft}>
            <Text style={styles.source}>{SOURCE_LABELS[item.source] ?? item.source}</Text>
            <Text style={styles.date}>{new Date(item.createdAt).toLocaleDateString('de-DE')}</Text>
          </View>
          <Text style={styles.points}>+{item.points}</Text>
        </View>
      );

      return (
        <View style={styles.container}>
          <Text style={styles.header}>Meine Punkte-Historie</Text>
          {isLoading ? (
            <ActivityIndicator style={styles.loader} />
          ) : (
            <FlatList
              data={transactions}
              keyExtractor={(t) => t.id}
              renderItem={renderItem}
              refreshControl={<RefreshControl refreshing={isRefreshing} onRefresh={() => load(true)} />}
              ListEmptyComponent={<Text style={styles.empty}>Noch keine Aktivitäten</Text>}
              contentContainerStyle={transactions.length === 0 ? styles.emptyContainer : undefined}
            />
          )}
        </View>
      );
    }

    const styles = StyleSheet.create({
      container: { flex: 1, backgroundColor: '#F9FAFB' },
      header: { fontSize: 18, fontWeight: '700', color: '#111827', padding: 16 },
      row: {
        flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center',
        padding: 16, borderBottomWidth: 1, borderBottomColor: '#F3F4F6', backgroundColor: '#FFFFFF',
      },
      rowLeft: { flex: 1 },
      source: { fontSize: 15, fontWeight: '600', color: '#111827' },
      date: { fontSize: 13, color: '#9CA3AF', marginTop: 2 },
      points: { fontSize: 18, fontWeight: '700', color: '#2563EB' },
      loader: { marginTop: 40 },
      empty: { textAlign: 'center', color: '#6B7280', fontSize: 16 },
      emptyContainer: { flexGrow: 1, justifyContent: 'center', alignItems: 'center', padding: 40 },
    });
    ```

    5. Nach Scan und Check-In: Balance-Refresh auslösen
    In ScanScreen.tsx und CheckInScreen.tsx nach erfolgreichem API-Call:
    ```typescript
    import { usePointsStore } from '../../store/pointsStore';
    // ...
    const loadBalance = usePointsStore((s) => s.loadBalance);
    // Nach Toast/Erfolg:
    loadBalance();
    ```

    6. Finales Mobile-Commit:
    ```bash
    cd /Users/simonluthe/Documents/plietsche-pluenn && git add -A && git commit -m "feat(03-03): HomeScreen Punktestand + PointsHistoryScreen + pointsStore" && git push
    ```
  </action>
  <verify>
    <automated>
      # TypeScript-Check:
      cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit

      # Datei-Existenz:
      test -f /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/HomeScreen.tsx && echo "HomeScreen: OK"
      test -f /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/PointsHistoryScreen.tsx && echo "PointsHistoryScreen: OK"
      test -f /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/store/pointsStore.ts && echo "pointsStore: OK"

      # Prominente Punktestand-Anzeige im HomeScreen:
      grep -c "PlietschPunkte" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/HomeScreen.tsx && echo "Label vorhanden: OK"
      grep -c "pointsValue\|balance" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/HomeScreen.tsx

      # PUNKT-03: Keine itemId in History-Response-Typen:
      grep -v "itemId\|item_id" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/api/points.api.ts && echo "Kein itemId in API-Types: OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - HomeScreen zeigt Punktestand-Zahl prominent auf blauem Card oben mit Label "PlietschPunkte"
    - Punktestand wird beim Screen-Mount via GET /api/points/balance geladen
    - PointsHistoryScreen zeigt Transaktionen mit Datum + Quelle-Label + Punktbetrag
    - PointTransaction-Interface hat kein itemId-Feld
    - pointsStore hat loadBalance-Funktion die nach Scan/CheckIn aufgerufen werden kann
    - npx tsc --noEmit EXIT 0
  </acceptance_criteria>
  <done>Gamification-Loop vollständig: Punkte sammeln → HomeScreen zeigt Stand → Historie ohne Item-Tracking</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Vollständige Phase 3 Visitor Experience:
    - POST /api/scan: Item-QR → taken + 10 PlietschPunkte (per store_settings)
    - POST /api/checkin: HMAC-Door-QR + GPS 150m + Stepper 0–10 Teile → Punkte
    - GET /api/points/balance + GET /api/points/history
    - ScanScreen mit react-native-vision-camera + Toast-Feedback
    - CheckInScreen: GPS → Tür-QR-Scan → Stepper → Punkte
    - HomeScreen: Prominenter Punktestand oben
    - PointsHistoryScreen: Transaktionsliste ohne Item-Details
  </what-built>
  <how-to-verify>
    1. App auf iOS starten, als Visitor einloggen
    2. HomeScreen zeigt "PlietschPunkte" Label + Zahl (sollte 0 oder vorhandener Wert sein)
    3. Scan-Tab öffnen → Kamera öffnet sich mit QR-Rahmen-Overlay
    4. QR-Code eines aktiven Items scannen → Toast erscheint 2 Sekunden mit Titel + "+10 PlietschPunkte"
    5. HomeScreen: Punktestand auf 10 erhöht
    6. Punkte-Tab öffnen → 1 Transaktion sichtbar: "QR-Scan" + Datum + "+10"
    7. Check-In-Tab öffnen → GPS-Freigabe-Screen erscheint
    8. GPS freigeben → Tür-QR-Scan-Screen (QR von GET /api/checkin/door-qr scannen oder Tür-QR)
    9. Nach Scan: Stepper erscheint, 0–10 Teile einstellbar
    10. Einchecken → Erfolgs-Screen mit Punkteübersicht

    Backend-Tests:
    - curl -sk https://plietsche-pluenn.godsapp.de/api/points/history (mit Visitor-Token) → transactions ohne itemId
    - curl -sk https://plietsche-pluenn.godsapp.de/api/checkin/door-qr (mit Admin-Token) → PNG
  </how-to-verify>
  <resume-signal>Tippe "approved" wenn alles funktioniert, oder beschreibe Issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor→GET /api/points/balance | Eigene Punkte — nur eigene userId aus JWT |
| Admin→PATCH /api/points/settings | Punkte-Konfiguration pro Store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information Disclosure | GET /api/points/history | mitigate | Query filtert auf req.user.id — kein Zugriff auf fremde Transaktionen; kein itemId im SELECT |
| T-03-13 | Tampering | PATCH /api/points/settings | mitigate | requireRole('admin') — Besucher können Punkte-Werte nicht manipulieren |
| T-03-14 | Information Disclosure | point_transactions ohne itemId | mitigate | PointTransaction-Interface hat kein itemId-Feld — Locked Decision PUNKT-03 by design |
| T-03-15 | Elevation of Privilege | getStoreSettings Default-Fallback | accept | Fallback auf Defaults ist sicher (konservative Werte, kein privilegierter Zugriff) |
</threat_model>

<verification>
Nach Abschluss aller Tasks:
1. `npx tsc --noEmit` in backend/ — EXIT 0
2. `npx tsc --noEmit` in mobile/ — EXIT 0
3. GET /api/points/balance → { points: number }
4. GET /api/points/history → transactions ohne itemId
5. PATCH /api/points/settings (admin) → 200
6. store_settings-Tabelle mit Default-Eintrag für Store-UUID
7. HomeScreen zeigt Punktestand prominent mit "PlietschPunkte" Label
8. Checkpoint:human-verify bestätigt vollständigen Flow
</verification>

<success_criteria>
- PUNKT-01: Prominente Punktestand-Anzeige auf HomeScreen (blauer Card, große Zahl)
- PUNKT-02: Konfigurierbare Punkte-Werte via store_settings (Default: Scan=10, CheckIn=5, Part=3)
- PUNKT-03: point_transactions hat kein itemId — kein persistenter User↔Item-Link
- Alle 10 Phase-3-Requirements (SCAN-01–03, CHKIN-01–04, PUNKT-01–03) erfüllt
</success_criteria>

<output>
Nach Abschluss: .planning/phases/03-visitor-experience/03-03-SUMMARY.md erstellen mit:
- Erledigten Tasks
- Verifikations-Ergebnis aus Checkpoint
- Abweichungen vom Plan
- Phase-3-Status: COMPLETE
</output>
