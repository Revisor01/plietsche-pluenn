---
phase: 03-visitor-experience
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/checkin/checkin.router.ts
  - backend/src/modules/checkin/checkin.service.ts
  - backend/src/modules/checkin/checkin.repository.ts
  - backend/src/app.ts
  - backend/.env
  - mobile/src/screens/visitor/CheckInScreen.tsx
  - mobile/src/api/checkin.api.ts
autonomous: true
requirements: [CHKIN-01, CHKIN-02, CHKIN-03, CHKIN-04]
user_setup:
  - service: backend-env
    why: "DOOR_QR_SECRET für HMAC-Signierung des Tür-QR-Codes"
    env_vars:
      - name: DOOR_QR_SECRET
        source: "Selbst generieren: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\" — dann in backend/.env und Portainer Stack eintragen"

must_haves:
  truths:
    - "POST /api/checkin mit gültigem HMAC-Door-Token + GPS innerhalb 150m → 200 + Punkte"
    - "POST /api/checkin mit ungültigem Door-Token → 403"
    - "POST /api/checkin mit GPS > 150m vom Store → 403"
    - "POST /api/checkin innerhalb von 12h nach letztem Check-In → 409"
    - "GPS-Koordinaten werden NICHT in der DB gespeichert"
    - "Door-QR rotiert wöchentlich — aktueller und vorheriger Wochen-Bucket sind gültig"
    - "GET /api/checkin/door-qr (admin) → PNG mit aktuellem HMAC-Token"
    - "CheckInScreen: GPS anfordern, QR scannen, dann Stepper 0–10 Teile"
    - "Store lat/lng via PATCH /api/stores/info setzbar (admin)"
  artifacts:
    - path: backend/src/modules/checkin/checkin.router.ts
      provides: POST /api/checkin + GET /api/checkin/door-qr
      exports: [default checkinRouter]
    - path: backend/src/modules/checkin/checkin.service.ts
      provides: HMAC-Verifikation, Haversine-Check, Punkte-Vergabe
    - path: backend/src/modules/checkin/checkin.repository.ts
      provides: insertCheckin, lastCheckinByUser
    - path: mobile/src/screens/visitor/CheckInScreen.tsx
      provides: Tür-QR-Scan + GPS-Check + Stepper-UI
  key_links:
    - from: checkin.service.ts
      to: stores table
      via: checkin.repository.findStoreById
      pattern: "store.lat.*store.lng"
    - from: checkin.service.ts
      to: pointTransactions table
      via: scan.repository.awardPoints (shared)
      pattern: "awardPoints.*checkin"
    - from: CheckInScreen.tsx
      to: POST /api/checkin
      via: checkin.api.ts
      pattern: "apiClient.post.*api/checkin"
---

<objective>
Check-In-Flow: HMAC-gesicherter Tür-QR-Code + serverseitige GPS-Radius-Prüfung (150m, Haversine) + "Wie viele Teile?" Stepper. GPS-Koordinaten werden validiert aber NICHT persistiert (DSGVO). Wöchentliche HMAC-Rotation mit Toleranz für Wochenwechsel.

Purpose: Verifiziert physische Vor-Ort-Präsenz ohne Personen-Tracking. Der Tür-QR kann nicht remote gefälscht werden.
Output: checkin-Backend-Modul, Admin-Endpoint für Door-QR PNG, CheckInScreen mit vollständigem Flow, Store-Koordinaten via PATCH setzbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-visitor-experience/03-CONTEXT.md
@.planning/phases/03-visitor-experience/03-RESEARCH.md
@.planning/phases/03-visitor-experience/03-01-SUMMARY.md

<interfaces>
<!-- Aus Phase 03-01 (scan.repository.awardPoints wird wiederverwendet) -->

Aus backend/src/modules/scan/scan.repository.ts (awardPoints wiederverwenden):
```typescript
export async function awardPoints(
  userId: string,
  storeId: string,
  points: number,
  source: 'item_scan' | 'checkin' | 'manual_items'
): Promise<number>
// gibt totalPoints zurück
```

Aus backend/src/db/schema.ts (nach Plan 03-01):
```typescript
export const pointTransactions = pgTable('point_transactions', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  userId: uuid('user_id').notNull().references(() => users.id),
  // KEIN itemId
  source: text('source', { enum: ['item_scan', 'checkin', 'manual_items'] }).notNull(),
  points: integer('points').notNull(),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Aus RESEARCH.md — HMAC-Pattern (node:crypto built-in):
```typescript
import crypto from 'node:crypto';
const WEEK_MS = 7 * 24 * 60 * 60 * 1000;
function currentWeekBucket(): number { return Math.floor(Date.now() / WEEK_MS); }
function generateDoorToken(secret: string, storeId: string, bucket?: number): string {
  const b = bucket ?? currentWeekBucket();
  return crypto.createHmac('sha256', secret).update(`${storeId}:${b}`).digest('hex').substring(0, 32);
}
function verifyDoorToken(token: string, secret: string, storeId: string): boolean {
  const current = currentWeekBucket();
  return token === generateDoorToken(secret, storeId, current) ||
         token === generateDoorToken(secret, storeId, current - 1);
}
```

Aus RESEARCH.md — Haversine-Pattern:
```typescript
function haversineMeters(lat1: number, lon1: number, lat2: number, lon2: number): number {
  const R = 6371000;
  const toRad = (d: number) => (d * Math.PI) / 180;
  const dLat = toRad(lat2 - lat1); const dLon = toRad(lon2 - lon1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
}
```

Aus mobile/src/navigation/AppNavigator.tsx (nach Plan 03-01):
```typescript
// CheckInScreen ist bereits als Platzhalter eingebunden unter Tab "CheckIn"
// Dieser Plan implementiert den echten Screen
```

Store UUID (Test): 6a0fb86e-d795-4fd3-87fa-a21143f830b3
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checkin-Backend-Modul + Door-QR-Admin-Endpoint + Store-Koordinaten PATCH</name>
  <files>
    backend/src/db/schema.ts,
    backend/src/modules/checkin/checkin.router.ts,
    backend/src/modules/checkin/checkin.service.ts,
    backend/src/modules/checkin/checkin.repository.ts,
    backend/src/modules/stores/stores.router.ts,
    backend/src/app.ts,
    backend/.env
  </files>
  <read_first>
    - backend/src/db/schema.ts (aktuelle Tabellen inkl. stores mit lat/lng)
    - backend/src/modules/scan/scan.repository.ts (awardPoints wiederverwenden)
    - backend/src/modules/items/items.router.ts (Router-Pattern)
    - backend/src/app.ts (Einbinden-Pattern)
    - backend/.env (existierende Env-Variablen)
    - backend/src/modules/stores/stores.router.ts (existierender Store-Router, falls vorhanden)
  </read_first>
  <behavior>
    - POST /api/checkin: { doorToken: string, lat: number, lng: number, itemCount: number (0-10) }
      - Ungültiger doorToken → 403 { error: "Ungültiger QR-Code" }
      - GPS > 150m → 403 { error: "Zu weit vom Laden entfernt" }
      - Store hat kein lat/lng → 422 { error: "Store-Koordinaten nicht konfiguriert" }
      - Check-In innerhalb 12h → 409 { error: "Bereits heute eingecheckt" }
      - Erfolg: INSERT checkins (ohne GPS), awardPoints für checkin + itemCount*pointsPerItem
      - Response: { points: number, itemPoints: number, totalPoints: number }
    - GET /api/checkin/door-qr (admin only) → PNG-QR-Code mit aktuellem HMAC-Token
    - PATCH /api/stores/info: lat + lng Felder hinzufügen (admin only)
    - DOOR_QR_SECRET in backend/.env + HMAC-Generierung via node:crypto
  </behavior>
  <action>
    1. schema.ts: checkins-Tabelle hinzufügen (KEINE lat/lng-Spalten — DSGVO):
    ```typescript
    export const checkins = pgTable('checkins', {
      id: uuid('id').primaryKey().defaultRandom(),
      storeId: uuid('store_id').notNull().references(() => stores.id),
      userId: uuid('user_id').notNull().references(() => users.id),
      itemCount: integer('item_count').notNull().default(0),
      // KEINE lat/lng — GPS nur validiert, nie persistiert (per CHKIN-01, Locked Decision)
      createdAt: timestamp('created_at').defaultNow(),
    });
    ```

    2. SQL-Migration auf Server:
    ```bash
    ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"CREATE TABLE IF NOT EXISTS checkins (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), store_id uuid NOT NULL REFERENCES stores(id), user_id uuid NOT NULL REFERENCES users(id), item_count integer NOT NULL DEFAULT 0, created_at timestamp DEFAULT now());\""
    ```

    3. DOOR_QR_SECRET generieren und in backend/.env eintragen:
    ```bash
    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
    ```
    In backend/.env hinzufügen: `DOOR_QR_SECRET=<generierter-hex-string>`
    WICHTIG: Diesen Secret auch im Portainer Stack (Stack #261) als Env-Variable eintragen!

    4. checkin.repository.ts erstellen:
    ```typescript
    import { db } from '../../db/client';
    import { checkins, stores } from '../../db/schema';
    import { eq, and, desc } from 'drizzle-orm';

    export async function findStoreById(storeId: string) {
      const result = await db.select().from(stores).where(eq(stores.id, storeId)).limit(1);
      return result[0] ?? null;
    }

    export async function lastCheckinByUser(userId: string, storeId: string) {
      const result = await db.select().from(checkins)
        .where(and(eq(checkins.userId, userId), eq(checkins.storeId, storeId)))
        .orderBy(desc(checkins.createdAt)).limit(1);
      return result[0] ?? null;
    }

    export async function insertCheckin(data: { userId: string; storeId: string; itemCount: number }) {
      await db.insert(checkins).values(data);
    }
    ```

    5. checkin.service.ts erstellen mit HMAC + Haversine (exakt aus RESEARCH.md übernehmen):
    ```typescript
    import crypto from 'node:crypto';
    import * as checkinRepo from './checkin.repository';
    import { awardPoints } from '../scan/scan.repository';

    const WEEK_MS = 7 * 24 * 60 * 60 * 1000;
    const POINTS_PER_CHECKIN = 5;   // In Plan 03 durch store_settings ersetzt
    const POINTS_PER_ITEM = 3;       // In Plan 03 durch store_settings ersetzt

    function currentWeekBucket(): number { return Math.floor(Date.now() / WEEK_MS); }

    function generateDoorToken(secret: string, storeId: string, bucket?: number): string {
      const b = bucket ?? currentWeekBucket();
      return crypto.createHmac('sha256', secret).update(`${storeId}:${b}`).digest('hex').substring(0, 32);
    }

    export function verifyDoorToken(token: string, secret: string, storeId: string): boolean {
      const current = currentWeekBucket();
      return token === generateDoorToken(secret, storeId, current) ||
             token === generateDoorToken(secret, storeId, current - 1);
    }

    export function getCurrentDoorToken(storeId: string): string {
      const secret = process.env.DOOR_QR_SECRET;
      if (!secret) throw new Error('DOOR_QR_SECRET nicht konfiguriert');
      return generateDoorToken(secret, storeId);
    }

    function haversineMeters(lat1: number, lon1: number, lat2: number, lon2: number): number {
      const R = 6371000;
      const toRad = (d: number) => (d * Math.PI) / 180;
      const dLat = toRad(lat2 - lat1); const dLon = toRad(lon2 - lon1);
      const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLon/2)**2;
      return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
    }

    export async function processCheckin(
      userId: string,
      storeId: string,
      doorToken: string,
      coords: { lat: number; lng: number },
      itemCount: number
    ) {
      const secret = process.env.DOOR_QR_SECRET;
      if (!secret) throw Object.assign(new Error('Server-Konfiguration fehlt'), { statusCode: 500 });

      const store = await checkinRepo.findStoreById(storeId);
      if (!store) throw Object.assign(new Error('Store nicht gefunden'), { statusCode: 404 });
      if (!store.lat || !store.lng) throw Object.assign(new Error('Store-Koordinaten nicht konfiguriert'), { statusCode: 422 });

      const dist = haversineMeters(coords.lat, coords.lng, store.lat, store.lng);
      const radius = store.checkinRadiusMeters ?? 150;
      if (dist > radius) throw Object.assign(new Error('Zu weit vom Laden entfernt'), { statusCode: 403 });

      if (!verifyDoorToken(doorToken, secret, storeId)) {
        throw Object.assign(new Error('Ungültiger QR-Code'), { statusCode: 403 });
      }

      const last = await checkinRepo.lastCheckinByUser(userId, storeId);
      if (last && Date.now() - last.createdAt.getTime() < 12 * 3600 * 1000) {
        throw Object.assign(new Error('Bereits heute eingecheckt'), { statusCode: 409 });
      }

      await checkinRepo.insertCheckin({ userId, storeId, itemCount });

      // Punkte: Basis-Check-In + pro Teil
      const basePoints = POINTS_PER_CHECKIN;
      const itemPoints = itemCount * POINTS_PER_ITEM;
      const totalPoints = await awardPoints(userId, storeId, basePoints + itemPoints, 'checkin');

      return { points: basePoints, itemPoints, totalPoints };
    }
    ```

    6. checkin.router.ts erstellen:
    ```typescript
    import { Router, type Request, type Response } from 'express';
    import { z } from 'zod';
    import QRCode from 'qrcode';
    import { authenticateToken } from '../../middleware/auth';
    import { requireRole } from '../../middleware/requireRole';
    import * as checkinService from './checkin.service';

    const router = Router();

    const CheckInSchema = z.object({
      doorToken: z.string().min(1),
      lat: z.number().min(-90).max(90),
      lng: z.number().min(-180).max(180),
      itemCount: z.number().int().min(0).max(10),
    });

    // POST /api/checkin — alle authentifizierten Rollen
    router.post('/', authenticateToken, async (req: Request, res: Response) => {
      const { doorToken, lat, lng, itemCount } = CheckInSchema.parse(req.body);
      const result = await checkinService.processCheckin(
        req.user!.id, req.user!.storeId, doorToken, { lat, lng }, itemCount
      );
      res.json(result);
    });

    // GET /api/checkin/door-qr — Admin-only, gibt aktuellen HMAC-Token als PNG zurück
    router.get(
      '/door-qr',
      authenticateToken,
      requireRole('admin'),
      async (req: Request, res: Response) => {
        const token = checkinService.getCurrentDoorToken(req.user!.storeId);
        const png = await QRCode.toBuffer(token, { type: 'png', width: 400, margin: 2 });
        res.set('Content-Type', 'image/png');
        res.set('Content-Disposition', 'attachment; filename="door-qr.png"');
        res.send(png);
      }
    );

    export default router;
    ```

    7. Store-Router PATCH /api/stores/info erweitern (lat/lng hinzufügen):
    Existierenden stores.router.ts lesen — das PATCH-Schema um lat und lng erweitern:
    ```typescript
    const PatchStoreSchema = z.object({
      name: z.string().optional(),
      address: z.string().optional(),
      description: z.string().optional(),
      openingHours: z.string().optional(),
      lat: z.number().min(-90).max(90).optional(),   // NEU
      lng: z.number().min(-180).max(180).optional(),  // NEU
      checkinRadiusMeters: z.number().int().min(10).max(2000).optional(), // NEU
    });
    // Im Store-Repository: lat + lng in UPDATE einschließen
    ```

    8. app.ts: checkinRouter einbinden:
    ```typescript
    import checkinRouter from './modules/checkin/checkin.router';
    app.use('/api/checkin', checkinRouter);
    ```

    9. Backend deployen:
    ```bash
    cd /Users/simonluthe/Documents/plietsche-pluenn && git add -A && git commit -m "feat(03-02): checkin-Backend HMAC + GPS + door-qr Admin-Endpoint" && git push
    ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn && git pull && cd backend && docker build -t plietsche-backend:latest . && docker restart plietsche-backend"
    ```

    10. Store-Koordinaten für Test setzen:
    ```bash
    ADMIN_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login -H 'Content-Type: application/json' -d '{"email":"admin@test.de","password":"test123"}' | jq -r '.token')
    curl -s -X PATCH https://plietsche-pluenn.godsapp.de/api/stores/info -H "Authorization: Bearer $ADMIN_TOKEN" -H 'Content-Type: application/json' -d '{"lat":54.1266,"lng":8.8574,"checkinRadiusMeters":150}'
    ```
  </action>
  <verify>
    <automated>
      # TypeScript-Check:
      cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit

      # Nach Deployment — Door-QR Admin Endpoint:
      ADMIN_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login -H 'Content-Type: application/json' -d '{"email":"admin@test.de","password":"test123"}' | jq -r '.token')
      curl -s -o /dev/null -w "%{http_code}" https://plietsche-pluenn.godsapp.de/api/checkin/door-qr -H "Authorization: Bearer $ADMIN_TOKEN"
      # Erwartet: 200

      # HMAC-Check — Visitor mit falschem Token → 403:
      VISITOR_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login -H 'Content-Type: application/json' -d '{"email":"visitor@test.de","password":"test123"}' | jq -r '.token')
      curl -s -o /dev/null -w "%{http_code}" -X POST https://plietsche-pluenn.godsapp.de/api/checkin -H "Authorization: Bearer $VISITOR_TOKEN" -H 'Content-Type: application/json' -d '{"doorToken":"ungueltig","lat":54.1266,"lng":8.8574,"itemCount":2}'
      # Erwartet: 403

      # GPS-Check — weit weg → 403:
      DOOR_TOKEN=$(curl -s https://plietsche-pluenn.godsapp.de/api/checkin/door-qr -H "Authorization: Bearer $ADMIN_TOKEN" | xxd -p | head -1 || echo "png-check")
      # GPS-Distanz-Test: lat 0/lng 0 = weit entfernt
      curl -s -o /dev/null -w "%{http_code}" -X POST https://plietsche-pluenn.godsapp.de/api/checkin -H "Authorization: Bearer $VISITOR_TOKEN" -H 'Content-Type: application/json' -d '{"doorToken":"anytoken","lat":0,"lng":0,"itemCount":1}'
      # Erwartet: 403

      # Checkins-Tabelle hat keine lat/lng:
      ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c '\d checkins'" | grep -v "lat\|lng" && echo "Keine GPS-Spalten: OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - checkins-Tabelle existiert in DB ohne lat/lng-Spalten
    - GET /api/checkin/door-qr (admin) → 200 + PNG
    - POST /api/checkin mit falschem doorToken → 403
    - POST /api/checkin mit GPS > 150m → 403
    - POST /api/checkin doppelt innerhalb 12h → 409
    - PATCH /api/stores/info akzeptiert lat/lng/checkinRadiusMeters
    - DOOR_QR_SECRET in backend/.env und Portainer Stack eingetragen
    - npx tsc --noEmit EXIT 0
  </acceptance_criteria>
  <done>checkin-Backend deployed, HMAC-Rotation funktioniert, GPS-Check server-seitig ohne Persistenz</done>
</task>

<task type="auto">
  <name>Task 2: CheckInScreen Mobile — QR-Scan + GPS + Stepper</name>
  <files>
    mobile/src/screens/visitor/CheckInScreen.tsx,
    mobile/src/api/checkin.api.ts
  </files>
  <read_first>
    - mobile/src/screens/visitor/ScanScreen.tsx (QR-Scanner-Pattern aus Plan 03-01)
    - mobile/src/navigation/AppNavigator.tsx (CheckInScreen ist bereits als Platzhalter eingebunden)
    - mobile/src/store/authStore.ts (für storeId)
  </read_first>
  <action>
    1. checkin.api.ts erstellen:
    ```typescript
    import apiClient from './client';

    export interface CheckInResult {
      points: number;
      itemPoints: number;
      totalPoints: number;
    }

    export async function submitCheckin(
      doorToken: string,
      lat: number,
      lng: number,
      itemCount: number
    ): Promise<CheckInResult> {
      const res = await apiClient.post('/checkin', { doorToken, lat, lng, itemCount });
      return res.data;
    }
    ```

    2. CheckInScreen.tsx implementieren (3-Schritt-Flow):
    - Schritt 1: GPS anfordern (react-native-geolocation-service, bereits installiert)
    - Schritt 2: Tür-QR scannen (useCodeScanner, Pattern aus ScanScreen)
    - Schritt 3: Stepper "Wie viele Teile?" (0–10, + / - Buttons, per CHKIN-02 Locked Decision)
    - Erfolg: Ergebnis-Screen mit Punkteübersicht

    ```typescript
    import React, { useState } from 'react';
    import { View, Text, StyleSheet, TouchableOpacity, Platform } from 'react-native';
    import Geolocation from 'react-native-geolocation-service';
    import { Camera, useCameraDevice, useCodeScanner } from 'react-native-vision-camera';
    import { submitCheckin } from '../../api/checkin.api';

    type Step = 'gps' | 'scan' | 'stepper' | 'success' | 'error';

    export default function CheckInScreen() {
      const [step, setStep] = useState<Step>('gps');
      const [coords, setCoords] = useState<{ lat: number; lng: number } | null>(null);
      const [doorToken, setDoorToken] = useState<string | null>(null);
      const [itemCount, setItemCount] = useState(0);
      const [result, setResult] = useState<{ points: number; itemPoints: number; totalPoints: number } | null>(null);
      const [error, setError] = useState<string | null>(null);
      const [isScanning, setIsScanning] = useState(true);

      const device = useCameraDevice('back');

      // Schritt 1: GPS anfordern
      const requestGps = async () => {
        const status = await Geolocation.requestAuthorization('whenInUse');
        if (status !== 'granted') {
          setError('GPS-Zugriff wird für den Check-In benötigt.');
          setStep('error');
          return;
        }
        Geolocation.getCurrentPosition(
          (pos) => {
            setCoords({ lat: pos.coords.latitude, lng: pos.coords.longitude });
            setStep('scan');
          },
          (err) => { setError('GPS konnte nicht ermittelt werden. Bitte Standort aktivieren.'); setStep('error'); },
          { enableHighAccuracy: true, timeout: 10000 }
        );
      };

      // Schritt 2: QR-Scan
      const codeScanner = useCodeScanner({
        codeTypes: ['qr'],
        onCodeScanned: (codes) => {
          if (!isScanning || codes.length === 0 || step !== 'scan') return;
          setIsScanning(false);
          const token = codes[0].value;
          if (!token) { setIsScanning(true); return; }
          setDoorToken(token);
          setStep('stepper');
        },
      });

      // Schritt 3: Stepper + Absenden
      const handleSubmit = async () => {
        if (!coords || !doorToken) return;
        try {
          const res = await submitCheckin(doorToken, coords.lat, coords.lng, itemCount);
          setResult(res);
          setStep('success');
        } catch (err: any) {
          setError(err?.response?.data?.error ?? 'Check-In fehlgeschlagen');
          setStep('error');
        }
      };

      // Schritt 1: GPS
      if (step === 'gps') return (
        <View style={styles.center}>
          <Text style={styles.title}>Check-In</Text>
          <Text style={styles.subtitle}>Für den Check-In wird dein Standort einmalig geprüft — nicht gespeichert.</Text>
          <TouchableOpacity style={styles.btn} onPress={requestGps}>
            <Text style={styles.btnText}>Standort freigeben & Scannen</Text>
          </TouchableOpacity>
        </View>
      );

      // Schritt 2: QR-Scan
      if (step === 'scan') return (
        <View style={styles.container}>
          {device && (
            <Camera style={StyleSheet.absoluteFill} device={device} isActive={isScanning} codeScanner={codeScanner} />
          )}
          <View style={styles.overlay}>
            <View style={styles.scanFrame} />
            <Text style={styles.hint}>QR-Code an der Ladentür scannen</Text>
          </View>
        </View>
      );

      // Schritt 3: Stepper
      if (step === 'stepper') return (
        <View style={styles.center}>
          <Text style={styles.title}>Wie viele Teile?</Text>
          <Text style={styles.subtitle}>Nicht-digitale Teile die du mitnimmst (ohne QR-Scan)</Text>
          <View style={styles.stepper}>
            <TouchableOpacity style={styles.stepBtn} onPress={() => setItemCount(Math.max(0, itemCount - 1))}>
              <Text style={styles.stepBtnText}>−</Text>
            </TouchableOpacity>
            <Text style={styles.stepCount}>{itemCount}</Text>
            <TouchableOpacity style={styles.stepBtn} onPress={() => setItemCount(Math.min(10, itemCount + 1))}>
              <Text style={styles.stepBtnText}>+</Text>
            </TouchableOpacity>
          </View>
          <TouchableOpacity style={styles.btn} onPress={handleSubmit}>
            <Text style={styles.btnText}>Einchecken</Text>
          </TouchableOpacity>
        </View>
      );

      // Schritt 4: Erfolg
      if (step === 'success' && result) return (
        <View style={styles.center}>
          <Text style={styles.successIcon}>✓</Text>
          <Text style={styles.title}>Eingecheckt!</Text>
          <Text style={styles.points}>+{result.points + result.itemPoints} PlietschPunkte</Text>
          <Text style={styles.subtitle}>Gesamt: {result.totalPoints} Punkte</Text>
          <TouchableOpacity style={styles.btn} onPress={() => { setStep('gps'); setItemCount(0); setDoorToken(null); setIsScanning(true); }}>
            <Text style={styles.btnText}>Fertig</Text>
          </TouchableOpacity>
        </View>
      );

      // Fehler
      return (
        <View style={styles.center}>
          <Text style={styles.errorText}>{error}</Text>
          <TouchableOpacity style={styles.btn} onPress={() => { setError(null); setStep('gps'); setIsScanning(true); }}>
            <Text style={styles.btnText}>Nochmal versuchen</Text>
          </TouchableOpacity>
        </View>
      );
    }

    const styles = StyleSheet.create({
      container: { flex: 1, backgroundColor: '#000' },
      center: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: 24, backgroundColor: '#F9FAFB' },
      title: { fontSize: 24, fontWeight: '700', color: '#111827', marginBottom: 8, textAlign: 'center' },
      subtitle: { fontSize: 14, color: '#6B7280', textAlign: 'center', marginBottom: 24 },
      btn: { backgroundColor: '#2563EB', paddingHorizontal: 32, paddingVertical: 14, borderRadius: 10, marginTop: 16 },
      btnText: { color: '#FFFFFF', fontSize: 16, fontWeight: '600' },
      stepper: { flexDirection: 'row', alignItems: 'center', marginVertical: 24 },
      stepBtn: { width: 48, height: 48, borderRadius: 24, backgroundColor: '#E5E7EB', justifyContent: 'center', alignItems: 'center' },
      stepBtnText: { fontSize: 24, color: '#111827', fontWeight: '600' },
      stepCount: { fontSize: 48, fontWeight: '700', color: '#111827', marginHorizontal: 32 },
      overlay: { flex: 1, justifyContent: 'center', alignItems: 'center' },
      scanFrame: { width: 240, height: 240, borderWidth: 2, borderColor: '#FFFFFF', borderRadius: 12 },
      hint: { color: '#FFFFFF', marginTop: 16, fontSize: 14, textAlign: 'center', paddingHorizontal: 32 },
      successIcon: { fontSize: 64, color: '#10B981', marginBottom: 8 },
      points: { fontSize: 32, fontWeight: '800', color: '#2563EB', marginVertical: 8 },
      errorText: { fontSize: 16, color: '#EF4444', textAlign: 'center', marginBottom: 16 },
    });
    ```
  </action>
  <verify>
    <automated>
      # TypeScript-Check:
      cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit

      # Datei-Existenz:
      test -f /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/CheckInScreen.tsx && echo "CheckInScreen: OK"
      test -f /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/api/checkin.api.ts && echo "checkin.api: OK"

      # Stepper-Pattern:
      grep -c "Math.max(0" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/CheckInScreen.tsx
      grep -c "Math.min(10" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/CheckInScreen.tsx

      # GPS keine Persistenz-Hinweise:
      grep -v "insert\|INSERT\|save\|persist" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/CheckInScreen.tsx | grep -c "coords" || true
    </automated>
  </verify>
  <acceptance_criteria>
    - CheckInScreen hat 3-Schritt-Flow: GPS → QR-Scan → Stepper
    - Stepper geht von 0 bis maximal 10 (Math.min/Math.max)
    - GPS-Koordinaten werden nicht gespeichert (nur als State, nicht in API ohne Weitergabe)
    - Erfolgs-Screen zeigt Punkte-Aufschlüsselung
    - npx tsc --noEmit EXIT 0
  </acceptance_criteria>
  <done>CheckInScreen vollständig implementiert, 3-Schritt-Flow funktioniert, keine GPS-Persistenz</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor→POST /api/checkin | doorToken + GPS-Koordinaten kommen vom Client |
| Admin→GET /api/checkin/door-qr | DOOR_QR_SECRET liegt auf dem Server, nie im Client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Spoofing | doorToken HMAC-Verifikation | mitigate | verifyDoorToken prüft aktuellen + vorherigen Wochen-Bucket; node:crypto HMAC-SHA256 nicht umkehrbar |
| T-03-07 | Tampering | GPS-Koordinaten vom Client | accept | Client kann GPS fälschen — physische Anwesenheit ist Social-Trust-Modell; Anti-Cheat nicht in Scope |
| T-03-08 | Information Disclosure | GPS-Koordinaten in DB | mitigate | checkins-Tabelle hat KEINE lat/lng-Spalten (Locked Decision CHKIN-01); GPS wird nur validiert |
| T-03-09 | Denial of Service | Check-In-Flooding | mitigate | lastCheckinByUser-Check: max 1 Check-In pro 12h pro User+Store |
| T-03-10 | Elevation of Privilege | GET /api/checkin/door-qr | mitigate | requireRole('admin') — nur Admins sehen den aktuellen HMAC-Token |
| T-03-11 | Repudiation | DOOR_QR_SECRET-Rotation | accept | Secret liegt nur in Env-Variablen, nicht in DB — manuelles Rotieren via Portainer Stack |
</threat_model>

<verification>
Nach Abschluss beider Tasks:
1. `npx tsc --noEmit` im backend/ — EXIT 0
2. `npx tsc --noEmit` im mobile/ — EXIT 0
3. GET /api/checkin/door-qr (admin) → 200 PNG
4. POST /api/checkin mit falschem Token → 403
5. checkins-Tabelle in DB ohne lat/lng-Spalten: `\d checkins` zeigt kein lat/lng
6. PATCH /api/stores/info mit lat/lng → Store-Koordinaten gesetzt
</verification>

<success_criteria>
- CHKIN-01: GPS-Radius-Check server-seitig, keine Persistenz
- CHKIN-02: Stepper 0–10 nach QR-Scan in CheckInScreen
- CHKIN-03: Punkte für Check-In + pro Teil (5 + itemCount * 3)
- CHKIN-04: HMAC wöchentlich rotierend, current + prev Bucket akzeptiert
- Store-Koordinaten via PATCH /api/stores/info setzbar
- Door-QR Admin-Endpoint vorhanden
</success_criteria>

<output>
Nach Abschluss: .planning/phases/03-visitor-experience/03-02-SUMMARY.md erstellen mit:
- Erledigten Tasks
- DOOR_QR_SECRET-Hinweis (Portainer Stack aktualisiert?)
- SQL-Migrationen
- Getestete Endpoints
- Abweichungen vom Plan
</output>
