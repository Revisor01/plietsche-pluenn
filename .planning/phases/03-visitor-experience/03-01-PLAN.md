---
phase: 03-visitor-experience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/scan/scan.router.ts
  - backend/src/modules/scan/scan.service.ts
  - backend/src/modules/scan/scan.repository.ts
  - backend/src/app.ts
  - mobile/package.json
  - mobile/src/screens/visitor/ScanScreen.tsx
  - mobile/src/api/scan.api.ts
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03]

must_haves:
  truths:
    - "POST /api/scan mit gültigem qrToken markiert Item als 'taken' und gibt Punkte zurück"
    - "POST /api/scan mit bereits 'taken' Item gibt 409 zurück"
    - "ScanScreen öffnet Kamera und scannt QR-Codes via react-native-vision-camera"
    - "Erfolgreich gescannter QR zeigt Toast mit Item-Titel und +X Punkte für 2 Sekunden"
    - "Unbekannter QR-Code zeigt Fehler inline im Scan-Screen"
    - "point_transactions hat keine item_id-Spalte (kein User↔Item-Link)"
  artifacts:
    - path: backend/src/modules/scan/scan.router.ts
      provides: POST /api/scan endpoint
      exports: [default scanRouter]
    - path: backend/src/modules/scan/scan.service.ts
      provides: Item-Checkout-Logik mit Punkte-Vergabe
    - path: backend/src/modules/scan/scan.repository.ts
      provides: findItemByQrToken, markItemTaken
    - path: mobile/src/screens/visitor/ScanScreen.tsx
      provides: QR-Scanner UI mit Toast-Feedback
    - path: mobile/src/api/scan.api.ts
      provides: scanItem API-Call
  key_links:
    - from: mobile/src/screens/visitor/ScanScreen.tsx
      to: POST /api/scan
      via: scan.api.ts
      pattern: "apiClient.post.*api/scan"
    - from: scan.service.ts
      to: items table
      via: scan.repository.markItemTaken
      pattern: "status.*taken"
    - from: scan.service.ts
      to: point_transactions table
      via: points repository (oder direkt per db.insert)
      pattern: "insert.*point_transactions"
---

<objective>
Item-QR-Scan Backend + Mobile Screen: Besucher scannen QR-Codes an Kleidungsstücken, das Item wird als "mitgenommen" markiert, PlietschPunkte werden gutgeschrieben — ohne persistenten User-Item-Link.

Purpose: Kerninteraktion des Stores — QR-Scan schließt den Loop zwischen physischem Kleidungsstück und digitalem Punktesystem.
Output: Backend scan-Modul (Router/Service/Repository), Mobile ScanScreen mit react-native-vision-camera, react-native-toast-message installiert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visitor-experience/03-CONTEXT.md
@.planning/phases/03-visitor-experience/03-RESEARCH.md

<interfaces>
<!-- Bestehende Patterns aus Phase 2 — direkt übernehmen -->

Aus backend/src/db/schema.ts:
```typescript
// Bestehende Tabellen (nicht anfassen):
export const stores = pgTable('stores', {
  id: uuid('id').primaryKey().defaultRandom(),
  lat: real('lat'),
  lng: real('lng'),
  checkinRadiusMeters: integer('checkin_radius_meters').notNull().default(200),
  // ...
});

export const users = pgTable('users', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  plietschPoints: integer('plietsch_points').notNull().default(0),
  // ...
});

export const items = pgTable('items', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id').notNull().references(() => stores.id),
  title: text('title').notNull(),
  qrToken: uuid('qr_token').unique(),
  status: text('status', { enum: ['active', 'taken'] }).notNull().default('active'),
  // ...
});
```

Aus backend/src/modules/items/items.router.ts (Pattern):
```typescript
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
// Express 5: kein try/catch nötig, async errors propagieren automatisch
router.post('/', authenticateToken, requireRole(...), async (req, res) => { ... });
```

Aus backend/src/app.ts (Einbinden-Pattern):
```typescript
import itemsRouter from './modules/items/items.router';
app.use('/api/items', itemsRouter);
// Analog: app.use('/api/scan', scanRouter);
```

Aus mobile/src/api/items.api.ts (Pattern):
```typescript
import apiClient from './client';
export async function someCall(data: ...) {
  const res = await apiClient.post('/items', data);
  return res.data;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scan-Modul Backend + Schema-Ergänzung</name>
  <files>
    backend/src/db/schema.ts,
    backend/src/modules/scan/scan.router.ts,
    backend/src/modules/scan/scan.service.ts,
    backend/src/modules/scan/scan.repository.ts,
    backend/src/app.ts
  </files>
  <read_first>
    - backend/src/db/schema.ts (aktuelle Tabellen)
    - backend/src/modules/items/items.router.ts (Router-Pattern)
    - backend/src/modules/items/items.service.ts (Service-Pattern)
    - backend/src/modules/items/items.repository.ts (Repository-Pattern)
    - backend/src/app.ts (Einbinden-Pattern)
  </read_first>
  <behavior>
    - POST /api/scan mit { qrToken: string, lat: number, lng: number } (lat/lng optional für spätere Verifikation)
    - Unbekannter qrToken → 404 { error: "Unbekannter QR-Code" }
    - Item bereits taken → 409 { error: "Dieses Teil wurde bereits mitgenommen" }
    - Item gehört zum User (created_by check wenn vorhanden) → 403 { error: "Du hast dieses Teil eingestellt" }
    - Erfolg: Item status → 'taken', INSERT point_transactions (source: 'item_scan', kein item_id), users.plietschPoints += pointsPerScan, Response: { points: number, title: string, totalPoints: number }
    - pointsPerScan: hartkodiert 10 in dieser Phase (store_settings kommt in Plan 03)
  </behavior>
  <action>
    1. schema.ts erweitern: pointTransactions-Tabelle hinzufügen NACH den bestehenden Tabellen:
    ```typescript
    export const pointTransactions = pgTable('point_transactions', {
      id: uuid('id').primaryKey().defaultRandom(),
      storeId: uuid('store_id').notNull().references(() => stores.id),
      userId: uuid('user_id').notNull().references(() => users.id),
      // KEIN itemId — DSGVO: kein User↔Item-Link (per SCAN-02, PUNKT-03)
      source: text('source', { enum: ['item_scan', 'checkin', 'manual_items'] }).notNull(),
      points: integer('points').notNull(),
      createdAt: timestamp('created_at').defaultNow(),
    });
    ```

    2. SQL-Migration auf Server ausführen (Docker nicht lokal verfügbar):
    ```bash
    ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"CREATE TABLE IF NOT EXISTS point_transactions (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), store_id uuid NOT NULL REFERENCES stores(id), user_id uuid NOT NULL REFERENCES users(id), source text NOT NULL CHECK (source IN ('item_scan', 'checkin', 'manual_items')), points integer NOT NULL, created_at timestamp DEFAULT now());\""
    ```
    Zusätzlich created_by zu items hinzufügen (für "Du hast dieses Teil eingestellt"-Check):
    ```bash
    ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"ALTER TABLE items ADD COLUMN IF NOT EXISTS created_by uuid REFERENCES users(id);\""
    ```

    3. scan.repository.ts erstellen:
    ```typescript
    import { db } from '../../db/client';
    import { items, users, pointTransactions } from '../../db/schema';
    import { eq, and } from 'drizzle-orm';

    export async function findItemByQrToken(qrToken: string, storeId: string) {
      const result = await db.select().from(items)
        .where(and(eq(items.qrToken, qrToken), eq(items.storeId, storeId)))
        .limit(1);
      return result[0] ?? null;
    }

    export async function markItemTaken(itemId: string) {
      await db.update(items).set({ status: 'taken' }).where(eq(items.id, itemId));
    }

    export async function awardPoints(userId: string, storeId: string, points: number, source: 'item_scan' | 'checkin' | 'manual_items') {
      await db.insert(pointTransactions).values({ userId, storeId, points, source });
      const result = await db.update(users)
        .set({ plietschPoints: db.$count ? undefined : undefined }) // raw increment
        .where(eq(users.id, userId))
        .returning({ plietschPoints: users.plietschPoints });
      // Drizzle increment via sql helper:
      const { sql } = await import('drizzle-orm');
      await db.update(users)
        .set({ plietschPoints: sql`${users.plietschPoints} + ${points}` })
        .where(eq(users.id, userId));
      const updated = await db.select({ plietschPoints: users.plietschPoints })
        .from(users).where(eq(users.id, userId)).limit(1);
      return updated[0]?.plietschPoints ?? 0;
    }
    ```

    WICHTIG: awardPoints korrekt mit Drizzle sql-Helper implementieren:
    ```typescript
    import { db } from '../../db/client';
    import { items, users, pointTransactions } from '../../db/schema';
    import { eq, and, sql } from 'drizzle-orm';

    export async function awardPoints(userId: string, storeId: string, points: number, source: 'item_scan' | 'checkin' | 'manual_items'): Promise<number> {
      await db.insert(pointTransactions).values({ userId, storeId, points, source });
      await db.update(users)
        .set({ plietschPoints: sql`${users.plietschPoints} + ${points}` })
        .where(eq(users.id, userId));
      const updated = await db.select({ plietschPoints: users.plietschPoints })
        .from(users).where(eq(users.id, userId)).limit(1);
      return updated[0]?.plietschPoints ?? 0;
    }
    ```

    4. scan.service.ts erstellen:
    ```typescript
    import * as scanRepo from './scan.repository';

    const POINTS_PER_SCAN = 10; // In Plan 03 durch store_settings ersetzt

    export async function scanItemQr(qrToken: string, userId: string, storeId: string) {
      const item = await scanRepo.findItemByQrToken(qrToken, storeId);
      if (!item) throw Object.assign(new Error('Unbekannter QR-Code'), { statusCode: 404 });
      if (item.status === 'taken') throw Object.assign(new Error('Dieses Teil wurde bereits mitgenommen'), { statusCode: 409 });
      if (item.createdBy && item.createdBy === userId) throw Object.assign(new Error('Du hast dieses Teil eingestellt'), { statusCode: 403 });

      await scanRepo.markItemTaken(item.id);
      const totalPoints = await scanRepo.awardPoints(userId, storeId, POINTS_PER_SCAN, 'item_scan');

      return { points: POINTS_PER_SCAN, title: item.title, totalPoints };
    }
    ```

    5. scan.router.ts erstellen (nur visitor/admin dürfen scannen — nicht volunteer):
    ```typescript
    import { Router, type Request, type Response } from 'express';
    import { z } from 'zod';
    import { authenticateToken } from '../../middleware/auth';
    import * as scanService from './scan.service';

    const router = Router();

    const ScanSchema = z.object({
      qrToken: z.string().uuid(),
    });

    router.post('/', authenticateToken, async (req: Request, res: Response) => {
      const { qrToken } = ScanSchema.parse(req.body);
      const result = await scanService.scanItemQr(qrToken, req.user!.id, req.user!.storeId);
      res.json(result);
    });

    export default router;
    ```

    6. app.ts: scanRouter einbinden (analog zu itemsRouter):
    ```typescript
    import scanRouter from './modules/scan/scan.router';
    app.use('/api/scan', scanRouter);
    ```

    7. Backend auf Server deployen:
    ```bash
    cd /Users/simonluthe/Documents/plietsche-pluenn && git add -A && git commit -m "feat(03-01): scan-Modul Backend + point_transactions Schema" && git push
    ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn && git pull && cd backend && docker build -t plietsche-backend:latest . && docker restart plietsche-backend"
    ```
  </action>
  <verify>
    <automated>
      # TypeScript-Check lokal:
      cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit

      # Endpoint-Tests (nach Deployment):
      # 1. Visitor-Token holen:
      VISITOR_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login -H 'Content-Type: application/json' -d '{"email":"visitor@test.de","password":"test123"}' | jq -r '.token')
      # 2. Aktives Item-QR-Token holen:
      VOLUNTEER_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login -H 'Content-Type: application/json' -d '{"email":"volunteer@test.de","password":"test123"}' | jq -r '.token')
      QR_TOKEN=$(curl -s https://plietsche-pluenn.godsapp.de/api/items?status=active -H "Authorization: Bearer $VOLUNTEER_TOKEN" | jq -r '.items[0].qrToken')
      # 3. Scan → 200 + points:
      curl -s -X POST https://plietsche-pluenn.godsapp.de/api/scan -H "Authorization: Bearer $VISITOR_TOKEN" -H 'Content-Type: application/json' -d "{\"qrToken\":\"$QR_TOKEN\"}" | jq .
      # Erwartet: { "points": 10, "title": "...", "totalPoints": 10 }
      # 4. Doppel-Scan → 409:
      curl -s -o /dev/null -w "%{http_code}" -X POST https://plietsche-pluenn.godsapp.de/api/scan -H "Authorization: Bearer $VISITOR_TOKEN" -H 'Content-Type: application/json' -d "{\"qrToken\":\"$QR_TOKEN\"}"
      # Erwartet: 409
    </automated>
  </verify>
  <acceptance_criteria>
    - POST /api/scan mit gültigem qrToken → 200 mit { points: 10, title: string, totalPoints: number }
    - POST /api/scan mit bereits taken Item → 409
    - POST /api/scan mit unbekanntem Token → 404
    - point_transactions Tabelle existiert in DB (kein item_id-Feld)
    - items.created_by Spalte existiert in DB
    - npx tsc --noEmit EXIT 0
  </acceptance_criteria>
  <done>scan-Modul deployed, alle 4 HTTP-Status-Codes korrekt, TypeScript sauber</done>
</task>

<task type="auto">
  <name>Task 2: ScanScreen Mobile mit react-native-vision-camera + Toast</name>
  <files>
    mobile/package.json,
    mobile/src/screens/visitor/ScanScreen.tsx,
    mobile/src/api/scan.api.ts,
    mobile/src/navigation/AppNavigator.tsx
  </files>
  <read_first>
    - mobile/src/navigation/AppNavigator.tsx (bestehende Navigation)
    - mobile/src/screens/items/ItemListScreen.tsx (Screen-Pattern)
    - mobile/src/api/items.api.ts (API-Client-Pattern, falls vorhanden)
    - mobile/src/store/authStore.ts (für user.role)
  </read_first>
  <action>
    1. react-native-toast-message installieren (per RESEARCH.md: 2.3.3, New Architecture kompatibel):
    ```bash
    cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npm install react-native-toast-message
    ```

    2. scan.api.ts erstellen:
    ```typescript
    import apiClient from './client';

    export interface ScanResult {
      points: number;
      title: string;
      totalPoints: number;
    }

    export async function scanItem(qrToken: string): Promise<ScanResult> {
      const res = await apiClient.post('/scan', { qrToken });
      return res.data;
    }
    ```

    3. ScanScreen.tsx erstellen (mobile/src/screens/visitor/ScanScreen.tsx):
    ```typescript
    import React, { useState } from 'react';
    import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';
    import { Camera, useCameraDevice, useCodeScanner } from 'react-native-vision-camera';
    import Toast from 'react-native-toast-message';
    import { scanItem } from '../../api/scan.api';

    export default function ScanScreen() {
      const device = useCameraDevice('back');
      const [isScanning, setIsScanning] = useState(true);
      const [error, setError] = useState<string | null>(null);
      const [hasPermission, setHasPermission] = useState<boolean | null>(null);

      // Kamera-Permission beim Mount prüfen
      React.useEffect(() => {
        Camera.requestCameraPermission().then((status) => {
          setHasPermission(status === 'granted');
        });
      }, []);

      const codeScanner = useCodeScanner({
        codeTypes: ['qr'],
        onCodeScanned: async (codes) => {
          if (!isScanning || codes.length === 0) return;
          setIsScanning(false);
          setError(null);
          const token = codes[0].value;
          if (!token) { setIsScanning(true); return; }
          try {
            const result = await scanItem(token);
            Toast.show({
              type: 'success',
              text1: result.title,
              text2: `+${result.points} PlietschPunkte`,
              visibilityTime: 2000,
              onHide: () => setIsScanning(true),
            });
          } catch (err: any) {
            const msg = err?.response?.data?.error ?? 'Fehler beim Scannen';
            setError(msg);
            setTimeout(() => { setError(null); setIsScanning(true); }, 3000);
          }
        },
      });

      if (hasPermission === null) return <View style={styles.center}><Text>Kamera wird vorbereitet...</Text></View>;
      if (!hasPermission) return (
        <View style={styles.center}>
          <Text style={styles.permissionText}>Kamera-Zugriff benötigt</Text>
          <TouchableOpacity onPress={() => Camera.requestCameraPermission().then(s => setHasPermission(s === 'granted'))}>
            <Text style={styles.permissionBtn}>Erlauben</Text>
          </TouchableOpacity>
        </View>
      );
      if (!device) return <View style={styles.center}><Text>Keine Kamera gefunden</Text></View>;

      return (
        <View style={styles.container}>
          <Camera
            style={StyleSheet.absoluteFill}
            device={device}
            isActive={isScanning}
            codeScanner={codeScanner}
          />
          {/* QR-Rahmen Overlay */}
          <View style={styles.overlay}>
            <View style={styles.scanFrame} />
            <Text style={styles.hint}>QR-Code auf Kleidungsstück scannen</Text>
          </View>
          {/* Inline Fehleranzeige */}
          {error && (
            <View style={styles.errorBanner}>
              <Text style={styles.errorText}>{error}</Text>
            </View>
          )}
        </View>
      );
    }

    const styles = StyleSheet.create({
      container: { flex: 1, backgroundColor: '#000' },
      center: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#F9FAFB' },
      overlay: { flex: 1, justifyContent: 'center', alignItems: 'center' },
      scanFrame: {
        width: 240, height: 240,
        borderWidth: 2, borderColor: '#FFFFFF',
        borderRadius: 12, backgroundColor: 'transparent',
      },
      hint: { color: '#FFFFFF', marginTop: 16, fontSize: 14, textAlign: 'center', paddingHorizontal: 32 },
      errorBanner: {
        position: 'absolute', bottom: 80, left: 16, right: 16,
        backgroundColor: '#EF4444', borderRadius: 8, padding: 12,
      },
      errorText: { color: '#FFFFFF', textAlign: 'center', fontSize: 14, fontWeight: '600' },
      permissionText: { fontSize: 16, color: '#374151', marginBottom: 12 },
      permissionBtn: { fontSize: 16, color: '#2563EB', fontWeight: '600' },
    });
    ```

    4. AppNavigator.tsx erweitern: role-basierte Navigation + VisitorTabs + Toast-Provider:
    - `useAuthStore((s) => s.user?.role)` auslesen
    - VisitorTabs mit Home, Scan, CheckIn, Punkte (CheckIn und Punkte als Platzhalter-Screens für diese Phase)
    - Toast-Provider in AppNavigator einbinden (MUSS außerhalb von NavigationContainer stehen, aber innerhalb der Root-View)
    - Beispiel-Struktur:
    ```typescript
    import Toast from 'react-native-toast-message';

    // In AppNavigator return:
    return (
      <>
        <NavigationContainer>
          {/* ... */}
        </NavigationContainer>
        <Toast />
      </>
    );
    ```

    Platzhalter-Screens CheckInScreen und PointsHistoryScreen als minimale Komponenten anlegen
    (mobile/src/screens/visitor/CheckInScreen.tsx, PointsHistoryScreen.tsx) — werden in Plan 02/03 befüllt:
    ```typescript
    import React from 'react';
    import { View, Text } from 'react-native';
    export default function CheckInScreen() {
      return <View style={{flex:1,justifyContent:'center',alignItems:'center'}}><Text>Check-In (kommt bald)</Text></View>;
    }
    ```

    HomeScreen.tsx Platzhalter anlegen (mobile/src/screens/visitor/HomeScreen.tsx) — wird in Plan 03 befüllt.

    5. iOS-Build-Prüfung lokal:
    ```bash
    cd /Users/simonluthe/Documents/plietsche-pluenn/mobile/ios && pod install
    ```
  </action>
  <verify>
    <automated>
      # TypeScript-Check:
      cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit

      # Datei-Existenz-Check:
      test -f /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/screens/visitor/ScanScreen.tsx && echo "ScanScreen: OK"
      test -f /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/api/scan.api.ts && echo "scan.api: OK"
      grep -r "react-native-toast-message" /Users/simonluthe/Documents/plietsche-pluenn/mobile/package.json && echo "Toast-Package: OK"
      grep -r "VisitorTabs" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/navigation/AppNavigator.tsx && echo "VisitorTabs: OK"
      grep -r "user?.role" /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/navigation/AppNavigator.tsx && echo "role-Switch: OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - ScanScreen.tsx existiert mit useCodeScanner und Camera aus react-native-vision-camera
    - Toast wird bei Erfolg mit title + "+X PlietschPunkte" angezeigt
    - Fehler erscheinen inline (errorBanner), nicht als Toast
    - AppNavigator hat VisitorTabs (role === 'visitor') und VolunteerTabs (role !== 'visitor')
    - Toast-Provider ist in AppNavigator eingebunden
    - npx tsc --noEmit EXIT 0
    - pod install läuft durch (iOS)
  </acceptance_criteria>
  <done>ScanScreen deployed-fähig, Navigation role-aware, Toast-Feedback korrekt verdrahtet</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor→POST /api/scan | qrToken kommt vom Client — muss gegen DB validiert werden |
| Punkte-Gutschrift | Jeder authentifizierte User kann scannen — Rate-Limiting nicht in Phase 3 (folgt in 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | POST /api/scan qrToken | mitigate | Zod .uuid()-Validierung am Router-Eingang; qrToken in DB gegen storeId validiert |
| T-03-02 | Repudiation | point_transactions insert | accept | Transaktion hat userId + timestamp — ausreichend für Audit, kein PII-Überschuss |
| T-03-03 | Information Disclosure | point_transactions ohne item_id | mitigate | Bewusst kein item_id — DSGVO-Locked-Decision SCAN-02/PUNKT-03 — kein User↔Item-Link möglich |
| T-03-04 | Elevation of Privilege | scan.router ohne requireRole | accept | Alle authentifizierten Rollen dürfen scannen — visitor/volunteer/admin; keine privilegierte Aktion |
| T-03-05 | Denial of Service | Massen-Scans pumpen Punkte | accept | Rate-Limiting in Phase 4; Scope für v1 akzeptabel (physische Präsenz erforderlich) |
</threat_model>

<verification>
Nach Abschluss beider Tasks:
1. `npx tsc --noEmit` im backend/ — EXIT 0
2. `npx tsc --noEmit` im mobile/ — EXIT 0
3. POST /api/scan mit gültigem Token → 200 + { points, title, totalPoints }
4. POST /api/scan mit doppeltem Token → 409
5. ScanScreen in iOS Simulator: Kamera öffnet, QR-Scan löst API-Call aus
</verification>

<success_criteria>
- SCAN-01: Item-Status wechselt zu 'taken' nach POST /api/scan
- SCAN-02: point_transactions hat keinen item_id-Eintrag nach Scan
- SCAN-03: ScanScreen nutzt useCodeScanner von react-native-vision-camera
- Items-created_by-Migration auf Server ausgeführt
- point_transactions-Tabelle auf Server erstellt
</success_criteria>

<output>
Nach Abschluss: .planning/phases/03-visitor-experience/03-01-SUMMARY.md erstellen mit:
- Erledigten Tasks
- SQL-Migrationen die ausgeführt wurden
- Abweichungen vom Plan
- Deployments
</output>
