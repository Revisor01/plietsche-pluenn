---
phase: 02-volunteer-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/db/schema.ts
  - backend/src/modules/items/items.types.ts
  - backend/src/modules/items/items.repository.ts
  - backend/src/modules/items/items.service.ts
  - backend/src/modules/items/items.router.ts
  - backend/src/app.ts
  - backend/package.json
autonomous: true
requirements:
  - ITEM-01
  - ITEM-02
  - ITEM-03

must_haves:
  truths:
    - "POST /api/items erstellt Item und gibt es mit id + qrToken zurück (HTTP 201)"
    - "GET /api/items?category=X&status=Y&search=Z gibt gefilterte Items zurück, neueste zuerst"
    - "GET /api/items/:id/qr gibt PNG-Binary zurück (Content-Type: image/png)"
    - "Items-Queries filtern immer nach storeId (kein Cross-Tenant-Datenleck)"
    - "condition-Spalte in DB ist freier Text (kein Enum-Constraint)"
  artifacts:
    - path: "backend/src/modules/items/items.types.ts"
      provides: "CreateItemBody, ItemRow, ItemFilters Interfaces"
      exports: ["CreateItemBody", "ItemRow", "ItemFilters"]
    - path: "backend/src/modules/items/items.repository.ts"
      provides: "DB-Zugriff via Drizzle mit storeId-Filter"
      exports: ["createItem", "findItems", "findItemById"]
    - path: "backend/src/modules/items/items.service.ts"
      provides: "Business-Logik + QR-Token UUID-Generierung + QR-PNG via qrcode"
      exports: ["createItem", "listItems", "getItemQrPng"]
    - path: "backend/src/modules/items/items.router.ts"
      provides: "POST /api/items, GET /api/items, GET /api/items/:id/qr"
    - path: "backend/src/db/schema.ts"
      provides: "Schema mit condition als text (kein Enum), stores mit description + openingHours"
      contains: "condition: text('condition')"
  key_links:
    - from: "items.router.ts"
      to: "items.service.ts"
      via: "function import"
      pattern: "import.*itemsService"
    - from: "items.service.ts"
      to: "qrcode npm"
      via: "QRCode.toBuffer"
      pattern: "QRCode.toBuffer"
    - from: "items.repository.ts"
      to: "schema.ts items table"
      via: "Drizzle eq(items.storeId, storeId)"
      pattern: "eq\\(items\\.storeId"
    - from: "backend/src/app.ts"
      to: "items.router.ts"
      via: "app.use('/api/items', ...)"
      pattern: "api/items"
---

<objective>
Schema migrieren (condition-Enum entfernen, stores erweitern), qrcode npm installieren, Backend items-Modul (Router/Service/Repository) mit QR-Generierung aufbauen.

Purpose: Ehrenamtliche können Kleidungsstücke anlegen — das ist der Kern-Workflow von Phase 2. Alle nachfolgenden Pläne (Mobile-Screens, Store-Info) hängen davon ab, dass diese API funktioniert.
Output: POST /api/items (Anlegen + QR-Token), GET /api/items (gefilterte Liste), GET /api/items/:id/qr (PNG-Download) — alle geschützt durch authenticateToken + requireRole.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Existierende Interfaces die der Executor direkt nutzen muss — keine Codebase-Exploration nötig -->

Aus backend/src/modules/auth/auth.router.ts (Muster-Router):
```typescript
// Express 5: async Fehler propagieren automatisch — kein try/catch nötig
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
router.post('/', authenticateToken, requireRole('volunteer', 'admin'), async (req, res) => {
  const body = CreateItemSchema.parse(req.body);
  // ...
});
```

Aus backend/src/middleware/auth.ts (req.user):
```typescript
// req.user wird von authenticateToken gesetzt:
interface AuthUser {
  sub: string;   // userId
  storeId: string;
  role: 'admin' | 'volunteer' | 'visitor';
}
```

Aus backend/src/db/schema.ts (aktuelle items-Tabelle — MUSS GEÄNDERT WERDEN):
```typescript
// IST (condition als Enum — MUSS entfernt werden):
condition: text('condition', { enum: ['neu', 'gut', 'okay'] }),

// SOLL (Freitext):
condition: text('condition'),

// stores-Tabelle — MUSS ERWEITERT WERDEN um:
description: text('description'),
openingHours: text('opening_hours'),
```

Aus backend/src/db/client.ts:
```typescript
export { db } from './client';
// db ist eine Drizzle PostgreSQL-Instanz
```

Drizzle-Importmuster (wie in auth.repository.ts):
```typescript
import { db } from '../../db/client';
import { items } from '../../db/schema';
import { eq, and, ilike, desc } from 'drizzle-orm';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema-Migration + qrcode installieren + items/stores Typen</name>
  <files>
    backend/src/db/schema.ts,
    backend/src/modules/items/items.types.ts,
    backend/package.json
  </files>
  <read_first>
    - backend/src/db/schema.ts (aktuelles Schema — vollständig lesen)
    - backend/package.json (für npm install Kontext)
    - backend/src/modules/auth/auth.types.ts (Muster für types.ts)
  </read_first>
  <behavior>
    - schema.ts: condition-Spalte von `text('condition', { enum: ['neu', 'gut', 'okay'] })` zu `text('condition')` ändern
    - schema.ts: stores-Tabelle um `description: text('description')` und `openingHours: text('opening_hours')` erweitern
    - items.types.ts erstellen mit: CreateItemBody, ItemRow, ItemFilters Interfaces
    - qrcode + @types/qrcode installieren: `cd backend && npm install qrcode && npm install --save-dev @types/qrcode`
    - TypeScript-Check: `cd backend && npx tsc --noEmit` muss EXIT 0 liefern
  </behavior>
  <action>
**Schema-Änderungen in backend/src/db/schema.ts:**

1. condition-Zeile ersetzen:
   ALT: `condition: text('condition', { enum: ['neu', 'gut', 'okay'] }),`
   NEU: `condition: text('condition'),`

2. stores-Tabelle — nach `checkinRadiusMeters`-Zeile einfügen:
   ```typescript
   description: text('description'),
   openingHours: text('opening_hours'),
   ```

**Neue Datei backend/src/modules/items/items.types.ts:**
```typescript
export interface CreateItemBody {
  title: string;
  category: string;
  size?: string;
  condition?: string;
  color?: string;
}

export interface ItemRow {
  id: string;
  storeId: string;
  title: string;
  category: string;
  size: string | null;
  condition: string | null;
  color: string | null;
  status: 'active' | 'taken';
  qrToken: string | null;
  createdAt: Date | null;
}

export interface ItemFilters {
  category?: string;
  status?: 'active' | 'taken';
  search?: string;
  page?: number;
  limit?: number;
}
```

**qrcode installieren:**
```bash
cd backend && npm install qrcode && npm install --save-dev @types/qrcode
```

**Server-SQL-Migration (nach Code-Deployment auf Server ausführen):**
```sql
-- Enum-Constraint von condition entfernen
ALTER TABLE items ALTER COLUMN condition TYPE text;
-- stores erweitern
ALTER TABLE stores ADD COLUMN IF NOT EXISTS description text;
ALTER TABLE stores ADD COLUMN IF NOT EXISTS opening_hours text;
```
Diese SQL-Befehle werden in Task 3 (Deployment-Vorbereitung) über SSH ausgeführt:
```bash
ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"ALTER TABLE items ALTER COLUMN condition TYPE text; ALTER TABLE stores ADD COLUMN IF NOT EXISTS description text; ALTER TABLE stores ADD COLUMN IF NOT EXISTS opening_hours text;\""
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <done>
    - schema.ts: condition ist `text('condition')` ohne enum-Parameter
    - schema.ts: stores hat description + openingHours Spalten
    - items.types.ts existiert mit CreateItemBody, ItemRow, ItemFilters
    - package.json hat qrcode in dependencies, @types/qrcode in devDependencies
    - npx tsc --noEmit EXIT 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: items Repository + Service + Router</name>
  <files>
    backend/src/modules/items/items.repository.ts,
    backend/src/modules/items/items.service.ts,
    backend/src/modules/items/items.router.ts,
    backend/src/app.ts
  </files>
  <read_first>
    - backend/src/modules/auth/auth.repository.ts (Muster: Drizzle-Queries)
    - backend/src/modules/auth/auth.service.ts (Muster: Service-Layer)
    - backend/src/modules/auth/auth.router.ts (Muster: Router mit Zod + Middleware)
    - backend/src/app.ts (wo authRouter eingebunden ist — gleiche Stelle für itemsRouter)
    - backend/src/modules/items/items.types.ts (soeben erstellt — lesen für korrekte Typen)
  </read_first>
  <behavior>
    - Repository: createItem, findItems (mit storeId-Filter + Category/Status/Search + Pagination), findItemById
    - Service: createItem (UUID-Token generieren, QR-PNG via QRCode.toBuffer, Item speichern), listItems, getItemQrPng
    - Router: POST / (volunteer+admin), GET / (volunteer+admin), GET /:id/qr (volunteer+admin)
    - app.ts: itemsRouter unter /api/items einbinden
    - Jede DB-Query hat zwingend eq(items.storeId, storeId) — kein Cross-Tenant
    - TypeScript-Check: npx tsc --noEmit EXIT 0
  </behavior>
  <action>
**backend/src/modules/items/items.repository.ts:**
```typescript
import { db } from '../../db/client';
import { items } from '../../db/schema';
import { eq, and, ilike, desc } from 'drizzle-orm';
import type { CreateItemBody, ItemFilters } from './items.types';

export async function createItem(data: CreateItemBody & { storeId: string; qrToken: string }) {
  const result = await db
    .insert(items)
    .values({
      storeId: data.storeId,
      title: data.title,
      category: data.category,
      size: data.size ?? null,
      condition: data.condition ?? null,
      color: data.color ?? null,
      status: 'active',
      qrToken: data.qrToken,
    })
    .returning();
  return result[0];
}

export async function findItems(storeId: string, filters: ItemFilters) {
  const limit = filters.limit ?? 20;
  const page = filters.page ?? 0;
  const conditions = [eq(items.storeId, storeId)];
  if (filters.category) conditions.push(eq(items.category, filters.category));
  if (filters.status) conditions.push(eq(items.status, filters.status));
  if (filters.search) conditions.push(ilike(items.title, `%${filters.search}%`));

  const rows = await db
    .select()
    .from(items)
    .where(and(...conditions))
    .orderBy(desc(items.createdAt))
    .limit(limit)
    .offset(page * limit);

  return rows;
}

export async function findItemById(id: string, storeId: string) {
  const result = await db
    .select()
    .from(items)
    .where(and(eq(items.id, id), eq(items.storeId, storeId)))
    .limit(1);
  return result[0] ?? null;
}
```

**backend/src/modules/items/items.service.ts:**
```typescript
import crypto from 'crypto';
import QRCode from 'qrcode';
import * as itemsRepo from './items.repository';
import type { CreateItemBody, ItemFilters } from './items.types';

export async function createItem(body: CreateItemBody, storeId: string) {
  const qrToken = crypto.randomUUID();
  const item = await itemsRepo.createItem({ ...body, storeId, qrToken });
  return item;
}

export async function listItems(storeId: string, filters: ItemFilters) {
  const rows = await itemsRepo.findItems(storeId, filters);
  const limit = filters.limit ?? 20;
  return {
    items: rows,
    page: filters.page ?? 0,
    limit,
  };
}

export async function getItemQrPng(itemId: string, storeId: string): Promise<Buffer> {
  const item = await itemsRepo.findItemById(itemId, storeId);
  if (!item) {
    const err = Object.assign(new Error('Item not found'), { statusCode: 404 });
    throw err;
  }
  if (!item.qrToken) {
    const err = Object.assign(new Error('Item has no QR token'), { statusCode: 500 });
    throw err;
  }
  // qrcode.toBuffer returns Buffer in Node.js
  const png = await QRCode.toBuffer(item.qrToken, {
    type: 'png',
    width: 400,
    margin: 2,
    errorCorrectionLevel: 'M',
  });
  return png as Buffer;
}
```

**backend/src/modules/items/items.router.ts:**
```typescript
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
import * as itemsService from './items.service';

const router = Router();

const CreateItemSchema = z.object({
  title: z.string().min(1).max(200),
  category: z.string().min(1),
  size: z.string().optional(),
  condition: z.string().optional(),
  color: z.string().optional(),
});

const ListItemsSchema = z.object({
  category: z.string().optional(),
  status: z.enum(['active', 'taken']).optional(),
  search: z.string().optional(),
  page: z.coerce.number().int().min(0).default(0),
  limit: z.coerce.number().int().min(1).max(100).default(20),
});

// Express 5: async Fehler propagieren automatisch — kein try/catch nötig
router.post(
  '/',
  authenticateToken,
  requireRole('volunteer', 'admin'),
  async (req: Request, res: Response) => {
    const body = CreateItemSchema.parse(req.body);
    const item = await itemsService.createItem(body, req.user!.storeId);
    res.status(201).json(item);
  },
);

router.get(
  '/',
  authenticateToken,
  requireRole('volunteer', 'admin'),
  async (req: Request, res: Response) => {
    const filters = ListItemsSchema.parse(req.query);
    const result = await itemsService.listItems(req.user!.storeId, filters);
    res.json(result);
  },
);

router.get(
  '/:id/qr',
  authenticateToken,
  requireRole('volunteer', 'admin'),
  async (req: Request, res: Response) => {
    const png = await itemsService.getItemQrPng(req.params.id, req.user!.storeId);
    res.set('Content-Type', 'image/png');
    res.set('Content-Disposition', `attachment; filename="qr-${req.params.id}.png"`);
    res.send(png);
  },
);

export default router;
```

**backend/src/app.ts — itemsRouter einbinden:**
Analog zur authRouter-Einbindung, nach der Zeile mit authRouter:
```typescript
import itemsRouter from './modules/items/items.router';
// ...
app.use('/api/items', itemsRouter);
```

**Server-Deployment nach Code-Push:**
```bash
# 1. Code auf GitHub pushen
# 2. Auf Server: Backend neu bauen
ssh root@server.godsapp.de "cd /opt/stacks/plietsche-pluenn/backend && git pull && docker build -t plietsche-backend:latest . && docker restart plietsche-backend"
# 3. Schema-Migration ausführen
ssh root@server.godsapp.de "docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c \"ALTER TABLE items ALTER COLUMN condition TYPE text; ALTER TABLE stores ADD COLUMN IF NOT EXISTS description text; ALTER TABLE stores ADD COLUMN IF NOT EXISTS opening_hours text;\""
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <done>
    - items.repository.ts: createItem, findItems (mit storeId-Pflichtfilter), findItemById
    - items.service.ts: createItem (UUID-Token + QRCode.toBuffer), listItems, getItemQrPng
    - items.router.ts: POST /, GET /, GET /:id/qr — alle mit authenticateToken + requireRole
    - app.ts: `app.use('/api/items', itemsRouter)` vorhanden
    - grep "eq(items.storeId" backend/src/modules/items/items.repository.ts gibt Treffer
    - npx tsc --noEmit EXIT 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile App → POST /api/items | Volunteer-Token aus JWT, aber storeId kommt aus req.user (nicht aus Request-Body) |
| GET /api/items/:id/qr | Authentifizierter Endpoint — QR-PNG wird nur für eigenen Store geliefert |
| DB-Query → items-Tabelle | storeId-Filter verhindert Cross-Tenant-Datenleck |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | POST /api/items storeId | mitigate | storeId kommt AUSSCHLIESSLICH aus req.user.storeId (JWT-verified), nie aus req.body — hardcoded im Service-Aufruf |
| T-02-02 | Information Disclosure | GET /api/items Cross-Tenant | mitigate | Repository nimmt storeId als Pflicht-Parameter; eq(items.storeId, storeId) in JEDER Query — grep-verifizierbar |
| T-02-03 | Elevation of Privilege | POST /api/items ohne Rolle | mitigate | requireRole('volunteer', 'admin') nach authenticateToken — visitor-Token gibt 403 |
| T-02-04 | Tampering | condition-Freitext SQL-Injection | mitigate | Drizzle ORM parametrisiert alle Queries automatisch — kein raw SQL |
| T-02-05 | Denial of Service | GET /api/items ohne Limit | accept | Default-Limit 20, Max 100 via Zod-Validierung — ausreichend für v1 single-store |
</threat_model>

<verification>
Nach Server-Deployment (ssh root@server.godsapp.de):

```bash
# 1. Token holen (volunteer-User nötig — zuerst registrieren oder bestehenden nutzen)
TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"volunteer@test.de","password":"test12345"}' | jq -r '.token')

# 2. Item anlegen
curl -s -X POST https://plietsche-pluenn.godsapp.de/api/items \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"Blaue Jeans","category":"Hose","size":"M","condition":"einwandfrei","color":"#0000FF"}' | jq .

# Erwartung: HTTP 201, { id, storeId, title, category, qrToken, status: "active" }

# 3. Items abrufen
curl -s "https://plietsche-pluenn.godsapp.de/api/items?category=Hose" \
  -H "Authorization: Bearer $TOKEN" | jq .

# 4. QR-PNG herunterladen (ITEM_ID aus Schritt 2)
ITEM_ID="<id-aus-schritt-2>"
curl -s "https://plietsche-pluenn.godsapp.de/api/items/$ITEM_ID/qr" \
  -H "Authorization: Bearer $TOKEN" \
  -o /tmp/test-qr.png && file /tmp/test-qr.png | grep PNG

# 5. Cross-Tenant-Test: condition als Freitext
curl -s -X POST https://plietsche-pluenn.godsapp.de/api/items \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"Test","category":"Oberteil","condition":"kleine Flecken"}' | jq .id
# Muss HTTP 201 liefern (kein Enum-Fehler mehr)
```
</verification>

<success_criteria>
- POST /api/items mit volunteer-Token → HTTP 201 mit qrToken-UUID im Response
- GET /api/items?category=Hose filtert korrekt, gibt neueste zuerst
- GET /api/items/:id/qr → Content-Type: image/png, valide PNG-Datei
- condition: "kleine Flecken" wird akzeptiert (kein Enum-Fehler)
- npx tsc --noEmit EXIT 0 lokal
- grep "eq(items.storeId" items.repository.ts hat mind. 2 Treffer (createItem + findItems)
</success_criteria>

<output>
Nach Completion: `.planning/phases/02-volunteer-core/02-01-SUMMARY.md` erstellen mit:
- Accomplishments
- Files Created/Modified
- Decisions Made
- Deviations from Plan
- Server-Deployment-Notizen (SQL-Migration ausgeführt?)
- Next Phase Readiness
</output>
