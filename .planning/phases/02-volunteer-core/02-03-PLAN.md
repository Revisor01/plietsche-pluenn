---
phase: 02-volunteer-core
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - backend/src/modules/stores/stores.types.ts
  - backend/src/modules/stores/stores.repository.ts
  - backend/src/modules/stores/stores.service.ts
  - backend/src/modules/stores/stores.router.ts
  - backend/src/app.ts
  - mobile/src/api/store.api.ts
  - mobile/src/screens/store/StoreInfoScreen.tsx
  - mobile/src/navigation/AppNavigator.tsx
autonomous: true
requirements:
  - STORE-01

must_haves:
  truths:
    - "GET /api/stores/info gibt Öffnungszeiten, Adresse und Beschreibung zurück (kein Auth erforderlich)"
    - "PATCH /api/stores/info erlaubt Admin Öffnungszeiten/Adresse/Beschreibung zu ändern"
    - "Store-Info Screen zeigt Öffnungszeiten, Adresse, Beschreibung im App"
    - "AppNavigator StoreInfo-Tab-Placeholder ist durch echten StoreInfoScreen ersetzt"
  artifacts:
    - path: "backend/src/modules/stores/stores.types.ts"
      provides: "StoreInfo, UpdateStoreBody Interfaces"
      exports: ["StoreInfo", "UpdateStoreBody"]
    - path: "backend/src/modules/stores/stores.repository.ts"
      provides: "findStoreById, updateStore via Drizzle"
      exports: ["findStoreById", "updateStore"]
    - path: "backend/src/modules/stores/stores.service.ts"
      provides: "getStoreInfo, updateStoreInfo Business-Logik"
      exports: ["getStoreInfo", "updateStoreInfo"]
    - path: "backend/src/modules/stores/stores.router.ts"
      provides: "GET /api/stores/info (public), PATCH /api/stores/info (admin)"
    - path: "mobile/src/screens/store/StoreInfoScreen.tsx"
      provides: "Store-Info Anzeige mit Öffnungszeiten, Adresse, Beschreibung"
  key_links:
    - from: "stores.router.ts"
      to: "stores.service.ts"
      via: "import * as storesService"
      pattern: "storesService"
    - from: "StoreInfoScreen.tsx"
      to: "store.api.ts getStoreInfo"
      via: "useEffect fetch on mount"
      pattern: "getStoreInfo"
    - from: "AppNavigator.tsx"
      to: "StoreInfoScreen"
      via: "Tab.Screen component={StoreInfoScreen}"
      pattern: "StoreInfoScreen"
---

<objective>
Store-Info Backend-Modul (GET public + PATCH admin) aufbauen und StoreInfoScreen in der App implementieren. AppNavigator Placeholder durch echten Screen ersetzen.

Purpose: Besucher sollen Öffnungszeiten und Adresse des Ladens in der App sehen. Admin kann die Infos pflegen. STORE-01 schließt Phase 2 ab.
Output: GET /api/stores/info (öffentlich), PATCH /api/stores/info (admin-only), StoreInfoScreen in Bottom-Tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-volunteer-core/02-01-SUMMARY.md

<interfaces>
<!-- Existierende Interfaces die der Executor direkt nutzen muss -->

Aus backend/src/db/schema.ts (nach Migration in Plan 01):
```typescript
export const stores = pgTable('stores', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: text('name').notNull(),
  address: text('address'),
  lat: real('lat'),
  lng: real('lng'),
  checkinRadiusMeters: integer('checkin_radius_meters').notNull().default(200),
  description: text('description'),       // NEU — aus Plan 01
  openingHours: text('opening_hours'),    // NEU — aus Plan 01 (Freitext, z.B. "Mo-Fr 10-17 Uhr")
  createdAt: timestamp('created_at').defaultNow(),
});
```

Aus backend/src/middleware/auth.ts:
```typescript
// req.user.storeId ist der Store des eingeloggten Users
// req.user.role: 'admin' | 'volunteer' | 'visitor'
```

Aus backend/src/app.ts (nach Plan 01-Änderungen):
```typescript
// app.use('/api/auth', authRouter);
// app.use('/api/items', itemsRouter);
// Hier wird /api/stores eingebunden
```

Store UUID für Produktion (aus STATE.md):
// storeId: '6a0fb86e-d795-4fd3-87fa-a21143f830b3'
// GET /api/stores/info liest den Store des authentifizierten Users (req.user.storeId)
// Für public GET: storeId kommt als Query-Parameter oder aus dem einzigen Store (v1 single-tenant)

Aus mobile/src/navigation/AppNavigator.tsx (nach Plan 02):
```typescript
// StoreInfo Tab zeigt aktuell StoreInfoPlaceholder
// Plan 03 ersetzt das durch: import StoreInfoScreen from '../screens/store/StoreInfoScreen';
```

Aus mobile/src/api/client.ts:
```typescript
import { apiClient } from './client';
// apiClient.get('/stores/info') — Token wird automatisch injiziert
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stores Backend-Modul (types + repository + service + router)</name>
  <files>
    backend/src/modules/stores/stores.types.ts,
    backend/src/modules/stores/stores.repository.ts,
    backend/src/modules/stores/stores.service.ts,
    backend/src/modules/stores/stores.router.ts,
    backend/src/app.ts
  </files>
  <read_first>
    - backend/src/db/schema.ts (stores-Tabelle mit description + openingHours aus Plan 01)
    - backend/src/modules/items/items.router.ts (Muster: authenticateToken + requireRole)
    - backend/src/modules/auth/auth.repository.ts (Drizzle-Query-Muster)
    - backend/src/app.ts (wo itemsRouter eingebunden ist — gleiche Stelle)
  </read_first>
  <behavior>
    - GET /api/stores/info: Kein Auth nötig (public), gibt name/address/description/openingHours zurück
    - PATCH /api/stores/info: admin-only, aktualisiert address/description/openingHours
    - storeId für GET: aus optionalem Query-Param oder Fallback auf ersten Store (v1 single-tenant)
    - storeId für PATCH: aus req.user.storeId (nur eigener Store)
    - TypeScript-Check: npx tsc --noEmit EXIT 0
  </behavior>
  <action>
**backend/src/modules/stores/stores.types.ts:**
```typescript
export interface StoreInfo {
  id: string;
  name: string;
  address: string | null;
  description: string | null;
  openingHours: string | null;
  lat: number | null;
  lng: number | null;
}

export interface UpdateStoreBody {
  address?: string;
  description?: string;
  openingHours?: string;
}
```

**backend/src/modules/stores/stores.repository.ts:**
```typescript
import { db } from '../../db/client';
import { stores } from '../../db/schema';
import { eq } from 'drizzle-orm';
import type { UpdateStoreBody } from './stores.types';

export async function findStoreById(storeId: string) {
  const result = await db
    .select()
    .from(stores)
    .where(eq(stores.id, storeId))
    .limit(1);
  return result[0] ?? null;
}

export async function findFirstStore() {
  // v1 single-tenant: gibt den ersten Store zurück
  const result = await db.select().from(stores).limit(1);
  return result[0] ?? null;
}

export async function updateStore(storeId: string, data: UpdateStoreBody) {
  const result = await db
    .update(stores)
    .set({
      ...(data.address !== undefined && { address: data.address }),
      ...(data.description !== undefined && { description: data.description }),
      ...(data.openingHours !== undefined && { openingHours: data.openingHours }),
    })
    .where(eq(stores.id, storeId))
    .returning();
  return result[0] ?? null;
}
```

**backend/src/modules/stores/stores.service.ts:**
```typescript
import * as storesRepo from './stores.repository';
import type { UpdateStoreBody, StoreInfo } from './stores.types';

export async function getStoreInfo(storeId?: string): Promise<StoreInfo> {
  const store = storeId
    ? await storesRepo.findStoreById(storeId)
    : await storesRepo.findFirstStore();

  if (!store) {
    const err = Object.assign(new Error('Store not found'), { statusCode: 404 });
    throw err;
  }

  return {
    id: store.id,
    name: store.name,
    address: store.address ?? null,
    description: store.description ?? null,
    openingHours: store.openingHours ?? null,
    lat: store.lat ?? null,
    lng: store.lng ?? null,
  };
}

export async function updateStoreInfo(storeId: string, data: UpdateStoreBody): Promise<StoreInfo> {
  const updated = await storesRepo.updateStore(storeId, data);
  if (!updated) {
    const err = Object.assign(new Error('Store not found'), { statusCode: 404 });
    throw err;
  }
  return {
    id: updated.id,
    name: updated.name,
    address: updated.address ?? null,
    description: updated.description ?? null,
    openingHours: updated.openingHours ?? null,
    lat: updated.lat ?? null,
    lng: updated.lng ?? null,
  };
}
```

**backend/src/modules/stores/stores.router.ts:**
```typescript
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import { authenticateToken } from '../../middleware/auth';
import { requireRole } from '../../middleware/requireRole';
import * as storesService from './stores.service';

const router = Router();

const UpdateStoreSchema = z.object({
  address: z.string().min(1).max(500).optional(),
  description: z.string().max(2000).optional(),
  openingHours: z.string().max(500).optional(),
});

// Public: kein Auth nötig
router.get('/info', async (req: Request, res: Response) => {
  const storeId = typeof req.query.storeId === 'string' ? req.query.storeId : undefined;
  const store = await storesService.getStoreInfo(storeId);
  res.json(store);
});

// Admin-only
router.patch(
  '/info',
  authenticateToken,
  requireRole('admin'),
  async (req: Request, res: Response) => {
    const body = UpdateStoreSchema.parse(req.body);
    const store = await storesService.updateStoreInfo(req.user!.storeId, body);
    res.json(store);
  },
);

export default router;
```

**backend/src/app.ts — storesRouter einbinden:**
Nach der itemsRouter-Zeile:
```typescript
import storesRouter from './modules/stores/stores.router';
// ...
app.use('/api/stores', storesRouter);
```

**TypeScript-Check:**
```bash
cd backend && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <done>
    - stores.types.ts mit StoreInfo + UpdateStoreBody
    - stores.repository.ts: findStoreById, findFirstStore, updateStore
    - stores.service.ts: getStoreInfo, updateStoreInfo
    - stores.router.ts: GET /info (public), PATCH /info (admin)
    - app.ts: `app.use('/api/stores', storesRouter)` vorhanden
    - npx tsc --noEmit EXIT 0
  </done>
</task>

<task type="auto">
  <name>Task 2: StoreInfoScreen + AppNavigator Placeholder ersetzen</name>
  <files>
    mobile/src/api/store.api.ts,
    mobile/src/screens/store/StoreInfoScreen.tsx,
    mobile/src/navigation/AppNavigator.tsx
  </files>
  <read_first>
    - mobile/src/navigation/AppNavigator.tsx (aktueller Stand nach Plan 02 — StoreInfoPlaceholder ersetzen)
    - mobile/src/api/items.api.ts (Muster für API-Client Nutzung)
    - mobile/src/screens/items/ItemListScreen.tsx (Styling-Muster übernehmen)
  </read_first>
  <action>
**mobile/src/api/store.api.ts:**
```typescript
import { apiClient } from './client';

export interface StoreInfo {
  id: string;
  name: string;
  address: string | null;
  description: string | null;
  openingHours: string | null;
  lat: number | null;
  lng: number | null;
}

export async function getStoreInfo(): Promise<StoreInfo> {
  const res = await apiClient.get<StoreInfo>('/stores/info');
  return res.data;
}

export interface UpdateStoreBody {
  address?: string;
  description?: string;
  openingHours?: string;
}

export async function updateStoreInfo(data: UpdateStoreBody): Promise<StoreInfo> {
  const res = await apiClient.patch<StoreInfo>('/stores/info', data);
  return res.data;
}
```

**mobile/src/screens/store/StoreInfoScreen.tsx:**
```typescript
import React, { useState, useEffect } from 'react';
import {
  View, Text, ScrollView, StyleSheet, ActivityIndicator, RefreshControl
} from 'react-native';
import { getStoreInfo, type StoreInfo } from '../../api/store.api';

export default function StoreInfoScreen() {
  const [store, setStore] = useState<StoreInfo | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  const [isRefreshing, setIsRefreshing] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchStore = async (refreshing = false) => {
    if (refreshing) setIsRefreshing(true);
    else setIsLoading(true);
    setError(null);
    try {
      const data = await getStoreInfo();
      setStore(data);
    } catch (err: unknown) {
      const message = err instanceof Error ? err.message : 'Fehler beim Laden';
      setError(message);
    } finally {
      setIsLoading(false);
      setIsRefreshing(false);
    }
  };

  useEffect(() => { fetchStore(); }, []);

  if (isLoading) {
    return (
      <View style={styles.centered}>
        <ActivityIndicator size="large" />
      </View>
    );
  }

  if (error) {
    return (
      <View style={styles.centered}>
        <Text style={styles.errorText}>{error}</Text>
      </View>
    );
  }

  return (
    <ScrollView
      style={styles.container}
      refreshControl={<RefreshControl refreshing={isRefreshing} onRefresh={() => fetchStore(true)} />}
    >
      <View style={styles.header}>
        <Text style={styles.storeName}>{store?.name ?? 'Laden'}</Text>
      </View>

      <View style={styles.section}>
        <Text style={styles.sectionTitle}>Adresse</Text>
        <Text style={styles.sectionContent}>
          {store?.address ?? 'Keine Adresse hinterlegt'}
        </Text>
      </View>

      <View style={styles.section}>
        <Text style={styles.sectionTitle}>Öffnungszeiten</Text>
        <Text style={styles.sectionContent}>
          {store?.openingHours ?? 'Keine Öffnungszeiten hinterlegt'}
        </Text>
      </View>

      {store?.description ? (
        <View style={styles.section}>
          <Text style={styles.sectionTitle}>Über uns</Text>
          <Text style={styles.sectionContent}>{store.description}</Text>
        </View>
      ) : null}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#F9FAFB' },
  centered: { flex: 1, justifyContent: 'center', alignItems: 'center' },
  errorText: { color: '#EF4444', fontSize: 16, textAlign: 'center', padding: 24 },
  header: {
    backgroundColor: '#2563EB', padding: 24, alignItems: 'center',
  },
  storeName: { fontSize: 24, fontWeight: '700', color: '#FFFFFF' },
  section: {
    backgroundColor: '#FFFFFF', marginHorizontal: 12, marginTop: 12,
    borderRadius: 8, padding: 16, borderWidth: 1, borderColor: '#E5E7EB',
  },
  sectionTitle: { fontSize: 13, fontWeight: '600', color: '#6B7280', marginBottom: 6, textTransform: 'uppercase' },
  sectionContent: { fontSize: 16, color: '#111827', lineHeight: 24 },
});
```

**mobile/src/navigation/AppNavigator.tsx — StoreInfoPlaceholder durch echten Screen ersetzen:**

Den Placeholder-Import entfernen und durch echten Screen ersetzen:
```typescript
// ENTFERNEN:
// function StoreInfoPlaceholder() { ... }

// HINZUFÜGEN:
import StoreInfoScreen from '../screens/store/StoreInfoScreen';

// In VolunteerTabs — Tab.Screen für StoreInfo:
// ERSETZEN: component={StoreInfoPlaceholder}
// DURCH: component={StoreInfoScreen}
```

**TypeScript-Check:**
```bash
cd mobile && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <done>
    - store.api.ts: getStoreInfo + updateStoreInfo exportiert
    - StoreInfoScreen.tsx: zeigt name, address, openingHours, description mit Loading-State
    - AppNavigator.tsx: StoreInfoPlaceholder entfernt, StoreInfoScreen importiert und eingebunden
    - grep "StoreInfoPlaceholder" AppNavigator.tsx gibt keinen Treffer
    - grep "StoreInfoScreen" AppNavigator.tsx gibt Treffer
    - npx tsc --noEmit EXIT 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /api/stores/info (public) | Kein Auth — gibt nur Store-Metadaten zurück (keine PII) |
| PATCH /api/stores/info | Admin-only — schreibt nur eigenen Store (req.user.storeId aus JWT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Spoofing | PATCH /api/stores/info storeId | mitigate | storeId kommt ausschließlich aus req.user.storeId (JWT-verified) — kein Query/Body-Parameter |
| T-02-11 | Information Disclosure | GET /api/stores/info public | accept | Store-Infos (Name, Adresse, Öffnungszeiten) sind explizit öffentlich — kein PII, Zweck der App |
| T-02-12 | Elevation of Privilege | PATCH /api/stores/info ohne Admin-Rolle | mitigate | requireRole('admin') — volunteer-Token gibt 403 |
| T-02-13 | Tampering | openingHours/description max-length | mitigate | Zod: description max 2000 Zeichen, openingHours max 500 Zeichen — verhindert DB-Bloat |
</threat_model>

<verification>
Nach Server-Deployment (Backend neu bauen nach Plan 01 + Plan 03):

```bash
# 1. Public GET ohne Token
curl -s https://plietsche-pluenn.godsapp.de/api/stores/info | jq .
# Erwartung: { id, name, address, description, openingHours, lat, lng }

# 2. PATCH als Admin
ADMIN_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@test.de","password":"test12345"}' | jq -r '.token')

curl -s -X PATCH https://plietsche-pluenn.godsapp.de/api/stores/info \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"address":"Musterstraße 1, 12345 Musterstadt","openingHours":"Mo-Fr 10-17 Uhr, Sa 10-14 Uhr","description":"Unser Tausch-Laden für alle!"}' | jq .

# 3. GET nach PATCH — neue Werte sichtbar
curl -s https://plietsche-pluenn.godsapp.de/api/stores/info | jq '.openingHours'
# Erwartung: "Mo-Fr 10-17 Uhr, Sa 10-14 Uhr"

# 4. PATCH als Volunteer → 403
VOLUNTEER_TOKEN=$(curl -s -X POST https://plietsche-pluenn.godsapp.de/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"volunteer@test.de","password":"test12345"}' | jq -r '.token')
curl -s -X PATCH https://plietsche-pluenn.godsapp.de/api/stores/info \
  -H "Authorization: Bearer $VOLUNTEER_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"address":"Hack"}' | jq .status
# Erwartung: 403
```

Mobile: StoreInfoScreen zeigt Öffnungszeiten, Adresse, Beschreibung nach API-Aufruf.
</verification>

<success_criteria>
- GET /api/stores/info ohne Auth → HTTP 200 mit Store-Objekt (name, address, openingHours, description)
- PATCH /api/stores/info mit admin-Token → HTTP 200 mit aktualisierten Werten
- PATCH /api/stores/info mit volunteer-Token → HTTP 403
- StoreInfoScreen zeigt Store-Name, Adresse, Öffnungszeiten aus API
- grep "StoreInfoPlaceholder" AppNavigator.tsx gibt keinen Treffer
- npx tsc --noEmit EXIT 0 in mobile/ und backend/
</success_criteria>

<output>
Nach Completion: `.planning/phases/02-volunteer-core/02-03-SUMMARY.md` erstellen mit:
- Accomplishments
- Files Created/Modified
- Phase 2 Gesamt-Status (STORE-01, ITEM-01..04 alle abgehakt?)
- Server-Deployment-Notizen
- Readiness für Phase 3 (Visitor Experience)
</output>
