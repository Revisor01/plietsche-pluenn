---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/
  - mobile/src/navigation/AppNavigator.tsx
  - mobile/src/screens/auth/LoginScreen.tsx
  - mobile/src/screens/HomeScreen.tsx
  - mobile/src/api/client.ts
  - mobile/ios/PlietschePluenn/Info.plist
  - mobile/android/app/src/main/AndroidManifest.xml
  - mobile/babel.config.js
  - mobile/tsconfig.json
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "React Native bare App startet auf iOS Simulator ohne Metro-Fehler"
    - "App zeigt einen Login-Screen (nicht Expo-Standard-Screen)"
    - "Navigation ist eingerichtet: AppNavigator wechselt zwischen Auth- und App-Stack"
    - "Phase-3-Libraries sind installiert (vision-camera, geolocation-service)"
    - "Keine Expo-Abhängigkeiten in package.json"
  artifacts:
    - path: "mobile/src/navigation/AppNavigator.tsx"
      provides: "Auth-State-basierter Root-Navigator (AuthStack vs. AppStack)"
      exports: ["AppNavigator"]
    - path: "mobile/src/screens/auth/LoginScreen.tsx"
      provides: "Platzhalter-Login-Screen (Phase 3 wird ihn mit echter Auth verbinden)"
    - path: "mobile/src/api/client.ts"
      provides: "Axios-Instanz mit BASE_URL aus env, Token-Injection vorbereitet"
      exports: ["apiClient"]
    - path: "mobile/package.json"
      provides: "Bare React Native 0.85.0, alle Navigation- und Phase-3-Libraries"
  key_links:
    - from: "mobile/src/navigation/AppNavigator.tsx"
      to: "mobile/src/screens/auth/LoginScreen.tsx"
      via: "Stack.Screen name='Login'"
    - from: "mobile/index.js"
      to: "mobile/src/navigation/AppNavigator.tsx"
      via: "AppRegistry.registerComponent"
---

<objective>
React Native bare Projekt von Null aufsetzen — Expo-Projekt ersetzen, New Architecture aktivieren, Navigation-Grundstruktur anlegen, Phase-3-Libraries installieren.

Purpose: Die Mobile-App-Basis steht, sodass Plan 03 Login/Register-Screens darauf aufbauen kann. Phase-3-Libraries (vision-camera, geolocation) werden jetzt installiert, damit spätere Pod-Install-Läufe nicht nötig sind.

Output: Lauffähige React Native 0.85 bare App mit Navigation-Skeleton, iOS Simulator bestätigt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Für Plan 03 bereitgestellte Contracts (werden hier erstellt) -->

Aus mobile/src/api/client.ts (wird erstellt):
```typescript
import axios from 'axios';

export const apiClient = axios.create({
  baseURL: process.env.API_BASE_URL ?? 'http://localhost:3000',
});

// Token-Injection: in Plan 03 wird ein Interceptor hinzugefügt
// apiClient.interceptors.request.use(...)
```

Aus mobile/src/navigation/AppNavigator.tsx (wird erstellt):
```typescript
// isAuthenticated: boolean — kommt aus authStore (Plan 03 implementiert den Store)
// AuthStack enthält: Login, Register
// AppStack enthält: Home (role-basiert in Phase 2 erweitert)
export function AppNavigator(): React.JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expo-Projekt ersetzen — React Native 0.85 bare init</name>
  <files>mobile/ (vollständig ersetzt)</files>
  <read_first>
    - /Users/simonluthe/Documents/plietsche-pluenn/mobile/package.json (zeigt Expo-Abhängigkeiten die entfernt werden)
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Installation" und "Pitfall 1: Expo-Abhängigkeiten")
  </read_first>
  <action>
Das bestehende Expo-Projekt in mobile/ vollständig ersetzen:

1. Altes Verzeichnis löschen und neues bare Projekt initialisieren:
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn
rm -rf mobile
npx react-native@0.85.0 init PlietschePluenn --directory mobile --skip-git-init
```

2. Navigation-Libraries installieren:
```bash
cd mobile
npm install @react-navigation/native@7.2.2 @react-navigation/stack@7.8.9 @react-navigation/bottom-tabs@7.15.9
npm install react-native-screens@4.24.0 react-native-safe-area-context@5.7.0
```

3. Phase-3-Libraries installieren (jetzt installieren, Phase 3 aktiviert sie):
```bash
npm install react-native-vision-camera@4.7.3 react-native-geolocation-service@5.3.1
npm install react-native-reanimated@4.3.0 react-native-worklets@0.8.1 react-native-gesture-handler@2.31.0
```

4. Axios für API-Client installieren:
```bash
npm install axios
```

5. iOS Pods installieren:
```bash
cd ios && pod install && cd ..
```

Wichtig: Keine Dateien aus dem alten Expo-Projekt übernehmen. Nach `npm install` prüfen ob `expo` in package.json vorhanden ist — wenn ja, entfernen mit `npm uninstall expo`.

react-native-reanimated babel-Plugin in babel.config.js eintragen (MUSS letztes Plugin sein):
```javascript
module.exports = {
  presets: ['module:@react-native/babel-preset'],
  plugins: ['react-native-reanimated/plugin'],
};
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && node -e "const p = require('./package.json'); if (p.dependencies.expo) throw new Error('Expo still present'); console.log('OK: no expo, RN version:', p.dependencies['react-native'])"</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/package.json` enthält `"react-native": "0.85.0"` (oder neueste 0.85.x)
    - `mobile/package.json` enthält KEIN `"expo"` in dependencies oder devDependencies
    - `mobile/package.json` enthält `"react-native-vision-camera"`, `"react-native-geolocation-service"`, `"react-native-reanimated"`
    - `mobile/babel.config.js` endet mit `'react-native-reanimated/plugin'` als letztem Plugin-Eintrag
    - `mobile/ios/Pods/` Verzeichnis existiert (pod install erfolgreich)
    - `grep -r "expo" mobile/package.json` gibt keinen Treffer zurück
  </acceptance_criteria>
  <done>Sauberes React Native 0.85 bare Projekt ohne jede Expo-Abhängigkeit, alle Libraries installiert und iOS Pods eingerichtet.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation-Skeleton und App-Struktur anlegen</name>
  <files>
    mobile/src/navigation/AppNavigator.tsx,
    mobile/src/screens/auth/LoginScreen.tsx,
    mobile/src/screens/HomeScreen.tsx,
    mobile/src/api/client.ts,
    mobile/App.tsx,
    mobile/ios/PlietschePluenn/Info.plist,
    mobile/android/app/src/main/AndroidManifest.xml
  </files>
  <read_first>
    - /Users/simonluthe/Documents/plietsche-pluenn/mobile/App.tsx (RN-Standard-App nach init, wird überschrieben)
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: React Navigation auth-basiertes Root-Routing)
  </read_first>
  <action>
Verzeichnisstruktur anlegen und Dateien erstellen:

```bash
mkdir -p mobile/src/navigation mobile/src/screens/auth mobile/src/api
```

**mobile/src/navigation/AppNavigator.tsx** — Auth-State-basierter Root-Navigator:
```typescript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import LoginScreen from '../screens/auth/LoginScreen';
import HomeScreen from '../screens/HomeScreen';

// Plan 03 ersetzt diesen Platzhalter durch einen echten Zustand-Store
const useIsAuthenticated = () => false;

const Stack = createStackNavigator();

export function AppNavigator(): React.JSX.Element {
  const isAuthenticated = useIsAuthenticated();

  return (
    <NavigationContainer>
      <Stack.Navigator screenOptions={{ headerShown: false }}>
        {isAuthenticated ? (
          <Stack.Screen name="Home" component={HomeScreen} />
        ) : (
          <Stack.Screen name="Login" component={LoginScreen} />
        )}
      </Stack.Navigator>
    </NavigationContainer>
  );
}
```

**mobile/src/screens/auth/LoginScreen.tsx** — Platzhalter (Plan 03 implementiert vollständig):
```typescript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function LoginScreen(): React.JSX.Element {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Plietsche Plünn</Text>
      <Text style={styles.subtitle}>Login kommt in Plan 03</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#fff' },
  title: { fontSize: 24, fontWeight: 'bold', marginBottom: 8 },
  subtitle: { fontSize: 14, color: '#666' },
});
```

**mobile/src/screens/HomeScreen.tsx** — Platzhalter:
```typescript
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function HomeScreen(): React.JSX.Element {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Willkommen</Text>
      <Text style={styles.subtitle}>Homescreen — Phase 2+ füllt diesen Screen</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#fff' },
  title: { fontSize: 24, fontWeight: 'bold', marginBottom: 8 },
  subtitle: { fontSize: 14, color: '#666' },
});
```

**mobile/src/api/client.ts** — Axios-Instanz (Token-Interceptor kommt in Plan 03):
```typescript
import axios from 'axios';

const BASE_URL = (process.env.API_BASE_URL as string | undefined) ?? 'http://localhost:3000';

export const apiClient = axios.create({
  baseURL: BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});
```

**mobile/App.tsx** — AppNavigator einbinden:
```typescript
import React from 'react';
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import { AppNavigator } from './src/navigation/AppNavigator';

export default function App(): React.JSX.Element {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <AppNavigator />
    </GestureHandlerRootView>
  );
}
```

**iOS Info.plist** — Camera- und Location-Permissions hinzufügen (für Phase 3 vorbereiten):
Folgende Keys in den bestehenden Info.plist einfügen (innerhalb des `<dict>`):
```xml
<key>NSCameraUsageDescription</key>
<string>Kamera wird benötigt um QR-Codes zu scannen</string>
<key>NSLocationWhenInUseUsageDescription</key>
<string>Standort wird benötigt um deinen Check-In beim Laden zu bestätigen</string>
```

**Android AndroidManifest.xml** — Permissions hinzufügen:
```xml
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && grep -l "AppNavigator" src/navigation/AppNavigator.tsx && grep -l "LoginScreen" src/screens/auth/LoginScreen.tsx && grep -l "apiClient" src/api/client.ts && grep "GestureHandlerRootView" App.tsx && echo "ALL FILES OK"</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/src/navigation/AppNavigator.tsx` existiert und exportiert `AppNavigator` als named export
    - `mobile/src/navigation/AppNavigator.tsx` enthält `NavigationContainer` und `createStackNavigator`
    - `mobile/src/screens/auth/LoginScreen.tsx` existiert mit default export `LoginScreen`
    - `mobile/src/screens/HomeScreen.tsx` existiert mit default export `HomeScreen`
    - `mobile/src/api/client.ts` exportiert `apiClient` (axios.create)
    - `mobile/App.tsx` importiert `GestureHandlerRootView` und `AppNavigator`
    - `mobile/ios/PlietschePluenn/Info.plist` enthält `NSCameraUsageDescription`
    - `mobile/android/app/src/main/AndroidManifest.xml` enthält `android.permission.CAMERA`
    - `npx tsc --noEmit` im mobile/-Verzeichnis läuft ohne TypeScript-Fehler durch
  </acceptance_criteria>
  <done>App startet auf iOS Simulator und zeigt LoginScreen. Navigation-Container ist eingerichtet. API-Client-Instanz ist bereit für Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile App → Backend API | Alle API-Aufrufe gehen über HTTPS; Plan 01 legt apiClient.baseURL fest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | `mobile/src/api/client.ts` baseURL | accept | BASE_URL ist env-konfigurierbar; in Plan 03 wird HTTPS-Pinning optional; kein Prod-Secret in Phase 1 |
| T-01-02 | Information Disclosure | iOS Info.plist Permissions | mitigate | NSCameraUsageDescription und NSLocationWhenInUseUsageDescription erklären Zweck; Permission wird erst in Phase 3 angefragt |
</threat_model>

<verification>
iOS Simulator Build-Test:
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn/mobile
npx react-native run-ios --simulator "iPhone 16 Pro"
```
Erwartetes Ergebnis: App startet, zeigt Plietsche Plünn LoginScreen-Platzhalter, keine roten Fehlerboxen.

TypeScript-Check:
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn/mobile
npx tsc --noEmit
```
</verification>

<success_criteria>
1. `mobile/package.json`: `react-native` = 0.85.x, kein `expo`
2. iOS Simulator zeigt LoginScreen ohne Metro-Fehler
3. `react-native-vision-camera`, `react-native-geolocation-service`, `react-native-reanimated` sind in package.json
4. `AppNavigator` rendert `LoginScreen` wenn `isAuthenticated = false`
5. `mobile/src/api/client.ts` exportiert `apiClient` mit axios.create
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-foundation/01-01-SUMMARY.md` erstellen mit:
- Tatsächlich erstellte Dateien und Verzeichnisstruktur
- React Native Version (exakt)
- Installierte Library-Versionen (aus package.json)
- iOS Simulator Build-Ergebnis
- Abweichungen vom Plan (falls aufgetreten)
</output>
