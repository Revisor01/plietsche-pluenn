---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/app.ts
  - backend/src/server.ts
  - backend/src/config.ts
  - backend/src/db/client.ts
  - backend/src/db/schema.ts
  - backend/src/db/seed.ts
  - backend/drizzle.config.ts
  - backend/.env.example
  - backend/package.json
  - backend/tsconfig.json
  - backend/docker-compose.yml
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "Express 5 Backend antwortet auf GET /api/health mit { status: 'OK' }"
    - "PostgreSQL-Datenbank ist erreichbar (lokal via Docker Compose)"
    - "Drizzle-Schema enthält stores, users, items Tabellen mit store_id"
    - "Drizzle-Migration wurde ausgeführt — Tabellen existieren in DB"
    - "Backend hat Router → Service → Repository Verzeichnisstruktur"
  artifacts:
    - path: "backend/src/db/schema.ts"
      provides: "Drizzle-Schema: stores, users, items (mit store_id, UUID PKs, role enum)"
      exports: ["stores", "users", "items"]
    - path: "backend/src/db/client.ts"
      provides: "Drizzle-Instanz mit pg Pool"
      exports: ["db"]
    - path: "backend/src/app.ts"
      provides: "Express 5 App mit helmet, cors, json-Middleware, Health-Endpoint, globalem Error Handler"
      exports: ["app"]
    - path: "backend/src/config.ts"
      provides: "Typisierte Env-Variablen via zod (DATABASE_URL, JWT_SECRET, PORT, NODE_ENV)"
      exports: ["config"]
    - path: "backend/docker-compose.yml"
      provides: "PostgreSQL 16 Container für lokale Entwicklung"
  key_links:
    - from: "backend/src/db/client.ts"
      to: "PostgreSQL (docker-compose)"
      via: "DATABASE_URL env var → pg Pool"
      pattern: "connectionString.*DATABASE_URL"
    - from: "backend/src/app.ts"
      to: "backend/src/db/client.ts"
      via: "Health-Endpoint importiert db für DB-Check"
---

<objective>
Express 5 Backend mit PostgreSQL + Drizzle ORM aufsetzen — bestehendes Express 4 Monolith (server.js) wird durch eine saubere TypeScript-Modularstruktur ersetzt.

Purpose: Das Backend-Fundament steht, auf dem Plan 03 Auth-Module und alle späteren Feature-Module aufgebaut werden. Drizzle-Schema mit store_id legt Multi-Tenant-Basis von Tag 1.

Output: Laufendes Express 5 Backend, PostgreSQL-Datenbank mit migrierten Tabellen, Health-Endpoint antwortet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Für Plan 03 bereitgestellte Contracts (werden hier erstellt) -->

Aus backend/src/db/schema.ts (wird erstellt):
```typescript
export const stores: PgTable  // id uuid PK, name text NOT NULL, lat/lng/checkin_radius, createdAt
export const users: PgTable   // id uuid PK, storeId uuid FK→stores, username, email, passwordHash, role enum('admin','volunteer','visitor'), plietschPoints int default 0, createdAt
export const items: PgTable   // id uuid PK, storeId uuid FK→stores, title, category, status enum('active','taken') default 'active', createdAt
```

Aus backend/src/db/client.ts (wird erstellt):
```typescript
export const db: DrizzleDB  // drizzle(pool, { schema })
```

Aus backend/src/config.ts (wird erstellt):
```typescript
export const config: {
  databaseUrl: string;
  jwtSecret: string;
  port: number;
  nodeEnv: string;
}
```

Aus backend/src/app.ts (wird erstellt):
```typescript
export default app: Express  // fertig konfiguriert mit Middleware + Health-Route
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend TypeScript-Struktur und Express 5 App einrichten</name>
  <files>
    backend/package.json,
    backend/tsconfig.json,
    backend/src/config.ts,
    backend/src/app.ts,
    backend/src/server.ts,
    backend/.env.example,
    backend/docker-compose.yml
  </files>
  <read_first>
    - /Users/simonluthe/Documents/plietsche-pluenn/backend/package.json (zeigt alte Dependencies — werden ersetzt)
    - /Users/simonluthe/Documents/plietsche-pluenn/backend/server.js (JWT-Flow und Patterns als Referenz, wird NICHT übernommen)
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Express 5 App Setup mit Helmet + Zod Error Handler")
  </read_first>
  <action>
Bestehendes `backend/server.js` bleibt vorerst als Referenz erhalten. Neue TypeScript-Struktur wird in `backend/src/` aufgebaut.

**1. Dependencies aktualisieren — backend/package.json vollständig ersetzen:**
```json
{
  "name": "plietsche-pluenn-backend",
  "version": "1.0.0",
  "description": "Backend for Plietsche Plünn app",
  "main": "dist/server.js",
  "scripts": {
    "dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:seed": "ts-node src/db/seed.ts"
  },
  "dependencies": {
    "express": "5.2.1",
    "pg": "8.20.0",
    "drizzle-orm": "0.45.2",
    "jsonwebtoken": "9.0.3",
    "bcrypt": "6.0.0",
    "cors": "2.8.5",
    "dotenv": "16.5.0",
    "helmet": "8.1.0",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "nodemon": "3.1.10",
    "typescript": "6.0.2",
    "ts-node": "10.9.2",
    "@types/express": "5.0.3",
    "@types/pg": "8.12.1",
    "@types/jsonwebtoken": "9.0.9",
    "@types/bcrypt": "5.0.2",
    "@types/cors": "2.8.17",
    "@types/node": "22.15.3"
  }
}
```

Dann: `cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npm install`

**2. backend/tsconfig.json:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "lib": ["ES2022"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**3. backend/src/config.ts — Typisierte Env-Variablen via zod:**
```typescript
import dotenv from 'dotenv';
import { z } from 'zod';

dotenv.config();

const envSchema = z.object({
  DATABASE_URL: z.string().min(1),
  JWT_SECRET: z.string().min(32),
  PORT: z.coerce.number().default(3000),
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
});

const parsed = envSchema.safeParse(process.env);

if (!parsed.success) {
  console.error('Invalid environment variables:', parsed.error.flatten().fieldErrors);
  process.exit(1);
}

export const config = {
  databaseUrl: parsed.data.DATABASE_URL,
  jwtSecret: parsed.data.JWT_SECRET,
  port: parsed.data.PORT,
  nodeEnv: parsed.data.NODE_ENV,
};
```

**4. backend/src/app.ts — Express 5 App:**
```typescript
import express, { type Request, type Response, type NextFunction } from 'express';
import helmet from 'helmet';
import cors from 'cors';
import { ZodError } from 'zod';

const app = express();

app.use(helmet());
app.use(cors());
app.use(express.json());

app.get('/api/health', (_req: Request, res: Response) => {
  res.json({ status: 'OK', timestamp: new Date().toISOString() });
});

// Platzhalter für Auth-Router — Plan 03 fügt ihn hier ein:
// import authRouter from './modules/auth/auth.router';
// app.use('/api/auth', authRouter);

// Globaler Error Handler (Express 5)
app.use((err: unknown, _req: Request, res: Response, _next: NextFunction) => {
  if (err instanceof ZodError) {
    return res.status(400).json({ error: 'Validation failed', issues: err.issues });
  }
  if (err instanceof Error) {
    console.error(err.stack);
    return res.status(500).json({ error: 'Internal server error' });
  }
  res.status(500).json({ error: 'Unknown error' });
});

export default app;
```

**5. backend/src/server.ts — Entry Point:**
```typescript
import { config } from './config';
import app from './app';

app.listen(config.port, () => {
  console.log(`Server running on port ${config.port} [${config.nodeEnv}]`);
});
```

**6. backend/.env.example:**
```
DATABASE_URL=postgresql://plietsche:plietsche@localhost:5432/plietschepluenn
JWT_SECRET=change-me-to-a-random-32-char-minimum-secret
PORT=3000
NODE_ENV=development
```

**7. backend/docker-compose.yml — PostgreSQL für lokale Entwicklung:**
```yaml
version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: plietsche-postgres
    environment:
      POSTGRES_USER: plietsche
      POSTGRES_PASSWORD: plietsche
      POSTGRES_DB: plietschepluenn
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U plietsche -d plietschepluenn"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
```

Developer muss `.env` aus `.env.example` erstellen und Docker starten: `docker-compose up -d`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `backend/package.json` enthält `"express": "5.2.1"` und `"drizzle-orm": "0.45.2"`
    - `backend/package.json` enthält KEIN `"sqlite3"` mehr
    - `backend/src/config.ts` existiert und importiert `zod`
    - `backend/src/app.ts` existiert und enthält `helmet()` und `/api/health` Route
    - `backend/src/server.ts` existiert und ruft `app.listen` auf
    - `backend/docker-compose.yml` existiert mit `postgres:16-alpine` Image
    - `backend/.env.example` existiert mit `DATABASE_URL`, `JWT_SECRET`, `PORT`
    - `npx tsc --noEmit` im backend/-Verzeichnis produziert keine Fehler
  </acceptance_criteria>
  <done>Express 5 App mit TypeScript-Konfiguration, Zod-Env-Validierung, und Docker-Compose für PostgreSQL ist eingerichtet.</done>
</task>

<task type="auto">
  <name>Task 2: Drizzle-Schema definieren (stores, users, items mit store_id)</name>
  <files>
    backend/src/db/client.ts,
    backend/src/db/schema.ts,
    backend/drizzle.config.ts,
    backend/src/db/seed.ts
  </files>
  <read_first>
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Drizzle Schema mit store_id, Drizzle pg-Client Setup, drizzle.config.ts)
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/research/ARCHITECTURE.md (Abschnitt "Pattern 1: Shared Schema Multi-Tenancy")
  </read_first>
  <action>
Verzeichnisse anlegen:
```bash
mkdir -p backend/src/db backend/src/middleware backend/src/modules/auth
```

**backend/src/db/client.ts — Drizzle + pg Pool:**
```typescript
import { drizzle } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';
import * as schema from './schema';
import { config } from '../config';

const pool = new Pool({
  connectionString: config.databaseUrl,
});

export const db = drizzle(pool, { schema });
```

**backend/src/db/schema.ts — Alle Tabellen mit store_id:**
```typescript
import { pgTable, uuid, text, timestamp, integer, real } from 'drizzle-orm/pg-core';

export const stores = pgTable('stores', {
  id: uuid('id').primaryKey().defaultRandom(),
  name: text('name').notNull(),
  address: text('address'),
  lat: real('lat'),
  lng: real('lng'),
  checkinRadiusMeters: integer('checkin_radius_meters').notNull().default(200),
  createdAt: timestamp('created_at').defaultNow(),
});

export const users = pgTable('users', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id')
    .notNull()
    .references(() => stores.id),
  username: text('username').notNull().unique(),
  email: text('email').notNull().unique(),
  passwordHash: text('password_hash').notNull(),
  role: text('role', { enum: ['admin', 'volunteer', 'visitor'] })
    .notNull()
    .default('visitor'),
  plietschPoints: integer('plietsch_points').notNull().default(0),
  createdAt: timestamp('created_at').defaultNow(),
});

export const items = pgTable('items', {
  id: uuid('id').primaryKey().defaultRandom(),
  storeId: uuid('store_id')
    .notNull()
    .references(() => stores.id),
  title: text('title').notNull(),
  category: text('category').notNull(),
  size: text('size'),
  condition: text('condition', { enum: ['neu', 'gut', 'okay'] }),
  color: text('color'),
  status: text('status', { enum: ['active', 'taken'] })
    .notNull()
    .default('active'),
  qrToken: uuid('qr_token').unique(),
  createdAt: timestamp('created_at').defaultNow(),
});
```

**backend/drizzle.config.ts:**
```typescript
import type { Config } from 'drizzle-kit';
import dotenv from 'dotenv';

dotenv.config();

export default {
  schema: './src/db/schema.ts',
  out: './src/db/migrations',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
} satisfies Config;
```

**backend/src/db/seed.ts — Seed-Daten für Entwicklung:**
```typescript
import { db } from './client';
import { stores, users } from './schema';
import bcrypt from 'bcrypt';

async function seed() {
  console.log('Seeding database...');

  // Einen Store anlegen
  const [store] = await db
    .insert(stores)
    .values({
      name: 'Plietsche Plünn Büsum',
      address: 'Musterstraße 1, 25761 Büsum',
      lat: 54.1234,
      lng: 8.8678,
      checkinRadiusMeters: 200,
    })
    .returning();

  console.log('Store created:', store.id);

  // Admin-User anlegen
  const passwordHash = await bcrypt.hash('admin123', 12);
  const [admin] = await db
    .insert(users)
    .values({
      storeId: store.id,
      username: 'admin',
      email: 'admin@plietschepluenn.de',
      passwordHash,
      role: 'admin',
    })
    .returning();

  console.log('Admin user created:', admin.id);
  console.log('Seeding complete.');
  process.exit(0);
}

seed().catch((err) => {
  console.error('Seed failed:', err);
  process.exit(1);
});
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit 2>&1 | head -20 && grep -c "store_id" src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/db/schema.ts` exportiert `stores`, `users`, `items`
    - `backend/src/db/schema.ts` enthält `store_id` in `users` und `items` Tabellen (`grep -c "store_id" backend/src/db/schema.ts` gibt mindestens 2 zurück)
    - `backend/src/db/schema.ts` enthält `role: text('role', { enum: ['admin', 'volunteer', 'visitor'] })`
    - `backend/src/db/schema.ts` enthält `plietschPoints` in users-Tabelle
    - `backend/src/db/schema.ts` enthält `qrToken` in items-Tabelle (für Phase 2 vorbereitet)
    - `backend/src/db/client.ts` exportiert `db` (drizzle-Instanz)
    - `backend/drizzle.config.ts` referenziert `./src/db/schema.ts` als schema
    - `npx tsc --noEmit` gibt keine Fehler aus
  </acceptance_criteria>
  <done>Drizzle-Schema mit stores, users, items definiert. Alle Tabellen haben store_id als UUID FK. Migration-Config ist eingerichtet.</done>
</task>

<task type="auto" id="schema-push">
  <name>Task 3: [BLOCKING] Drizzle-Migration ausführen und Health-Endpoint prüfen</name>
  <files>
    backend/src/db/migrations/,
    backend/.env
  </files>
  <read_first>
    - /Users/simonluthe/Documents/plietsche-pluenn/backend/drizzle.config.ts (wird in Task 2 erstellt)
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/phases/01-foundation/01-RESEARCH.md (Pitfall 3: Drizzle-Kit Migrations-Flow, Pitfall 5: PostgreSQL-Verbindung in Docker)
  </read_first>
  <action>
**Voraussetzung:** Docker läuft und PostgreSQL ist erreichbar. Falls nötig:
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn/backend
docker-compose up -d
# 5 Sekunden warten bis PostgreSQL bereit ist
docker-compose ps  # postgres sollte "healthy" zeigen
```

**.env aus .env.example erstellen:**
```bash
cp /Users/simonluthe/Documents/plietsche-pluenn/backend/.env.example /Users/simonluthe/Documents/plietsche-pluenn/backend/.env
```
Die Default-Werte in .env.example passen zu docker-compose.yml (user: plietsche, password: plietsche, db: plietschepluenn).

**Drizzle-Migration generieren und ausführen:**
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn/backend
npm run db:generate
npm run db:migrate
```

Erwartete Ausgabe: Migrations-SQL-Dateien in `src/db/migrations/`, Tabellen werden in DB angelegt.

**Backend starten und Health-Endpoint prüfen:**
```bash
npm run dev &
sleep 3
curl -s http://localhost:3000/api/health
```
Erwartete Antwort: `{"status":"OK","timestamp":"..."}`

**Optional: Seed-Daten einspielen:**
```bash
npm run db:seed
```

WICHTIG: `npm run db:push` (direktes Schema-Push ohne Migration-Dateien) NICHT verwenden — laut RESEARCH Pitfall 3 nur `generate + migrate` für nachvollziehbare Migrations-History.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/api/health | grep -o '"status":"OK"'</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/db/migrations/` Verzeichnis existiert mit mindestens einer .sql-Datei
    - Die Migrations-SQL-Datei enthält `CREATE TABLE stores`, `CREATE TABLE users`, `CREATE TABLE items`
    - `curl -s http://localhost:3000/api/health` gibt `{"status":"OK",...}` zurück (HTTP 200)
    - `psql $DATABASE_URL -c "\dt"` (oder docker exec) zeigt `stores`, `users`, `items` Tabellen
    - `grep "store_id" backend/src/db/migrations/*.sql` gibt Treffer zurück (Spalte ist in Migration)
  </acceptance_criteria>
  <done>PostgreSQL-Datenbank hat stores, users, items Tabellen mit store_id. Backend antwortet auf Health-Endpoint. Migrations-Dateien sind in git-tracked Verzeichnis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP Client → Express API | Unvalidierter JSON-Body trifft auf Express-Route |
| Express App → PostgreSQL | Backend-Code als einziger Tenant-Isolation-Enforcer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `backend/src/db/schema.ts` store_id Columns | mitigate | Jede Repository-Methode (Phase 2+) erhält storeId als Pflichtparameter; kein Query ohne WHERE store_id |
| T-02-02 | Information Disclosure | `backend/.env` JWT_SECRET | mitigate | `.env` ist in .gitignore; nur `.env.example` committed; minimale Länge von 32 Zeichen durch zod erzwungen |
| T-02-03 | Denial of Service | Express ohne Rate Limiting | accept | Phase 1 ist Entwicklungsumgebung; Rate Limiting (express-rate-limit) kommt vor Produktion |
| T-02-04 | Spoofing | Docker PostgreSQL ohne Auth-Härtung | accept | Lokal nur; Hetzner-Produktion nutzt Docker-Netz ohne öffentlichem Port-Binding |
</threat_model>

<verification>
Health-Endpoint-Check:
```bash
curl -s http://localhost:3000/api/health
# Erwartete Ausgabe: {"status":"OK","timestamp":"2026-..."}
```

Schema-Verifikation:
```bash
docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c "\dt"
# Erwartete Ausgabe: stores, users, items Tabellen
```

TypeScript-Check:
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit
```
</verification>

<success_criteria>
1. `GET /api/health` → HTTP 200, `{"status":"OK"}`
2. PostgreSQL hat `stores`, `users`, `items` Tabellen (via Migration)
3. Alle Tabellen haben `store_id uuid NOT NULL REFERENCES stores(id)`
4. `npx tsc --noEmit` ohne Fehler
5. `backend/src/db/migrations/` enthält commitbare SQL-Dateien
6. Docker-Compose startet PostgreSQL 16 lokal
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-foundation/01-02-SUMMARY.md` erstellen mit:
- Tatsächlich installierte Package-Versionen
- Drizzle-Migration-Ausgabe
- Health-Endpoint curl-Antwort
- Datenbankstruktur (Tabellen und Spalten)
- Abweichungen vom Plan (falls aufgetreten, insbesondere bcrypt/Node 25 Kompatibilität)
</output>
