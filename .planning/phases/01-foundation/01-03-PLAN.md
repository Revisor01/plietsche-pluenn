---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01-PLAN
  - 01-02-PLAN
files_modified:
  - backend/src/middleware/auth.ts
  - backend/src/middleware/requireRole.ts
  - backend/src/modules/auth/auth.router.ts
  - backend/src/modules/auth/auth.service.ts
  - backend/src/modules/auth/auth.repository.ts
  - backend/src/modules/auth/auth.types.ts
  - backend/src/app.ts
  - mobile/src/screens/auth/LoginScreen.tsx
  - mobile/src/screens/auth/RegisterScreen.tsx
  - mobile/src/store/authStore.ts
  - mobile/src/api/client.ts
  - mobile/src/navigation/AppNavigator.tsx
autonomous: true
requirements:
  - INFRA-04

must_haves:
  truths:
    - "POST /api/auth/register erstellt User mit gehashtem Passwort und gibt JWT zurück"
    - "POST /api/auth/login prüft Credentials und gibt JWT zurück"
    - "JWT-Payload enthält sub (userId), storeId, role"
    - "requireRole-Middleware blockiert Zugriff mit 403 wenn Rolle unzureichend"
    - "Mobile App wechselt nach erfolgreichem Login auf Homescreen"
    - "Mobile App zeigt RegisterScreen als Navigation-Option"
  artifacts:
    - path: "backend/src/middleware/auth.ts"
      provides: "authenticateToken Middleware — verifiziert JWT, setzt req.user"
      exports: ["authenticateToken", "AuthUser"]
    - path: "backend/src/middleware/requireRole.ts"
      provides: "requireRole Guard — prüft req.user.role gegen erlaubte Rollen"
      exports: ["requireRole"]
    - path: "backend/src/modules/auth/auth.router.ts"
      provides: "POST /register und POST /login Endpunkte mit Zod-Validierung"
    - path: "mobile/src/store/authStore.ts"
      provides: "Zustand Store: token, user, login(), logout() Actions"
      exports: ["useAuthStore"]
    - path: "mobile/src/screens/auth/LoginScreen.tsx"
      provides: "Login-Formular mit E-Mail/Passwort, ruft POST /api/auth/login auf"
    - path: "mobile/src/screens/auth/RegisterScreen.tsx"
      provides: "Registrierungs-Formular, ruft POST /api/auth/register auf"
  key_links:
    - from: "mobile/src/screens/auth/LoginScreen.tsx"
      to: "POST /api/auth/login"
      via: "apiClient.post('/api/auth/login', {email, password})"
    - from: "mobile/src/store/authStore.ts"
      to: "mobile/src/navigation/AppNavigator.tsx"
      via: "useAuthStore(s => s.token) — steuert Auth/App-Stack Wechsel"
    - from: "backend/src/middleware/auth.ts"
      to: "backend/src/middleware/requireRole.ts"
      via: "authenticateToken muss vor requireRole in Middleware-Chain stehen"
---

<objective>
JWT-Authentifizierung mit Rollen (admin, volunteer, visitor) implementieren — Backend-Auth-Modul und Mobile-Login/Register-Screens mit Zustand-Store.

Purpose: Ende-zu-Ende Auth-Flow schließt Phase 1 ab: User registriert/loggt sich ein, erhält JWT, App wechselt auf Homescreen. Login→Homescreen Navigation ist der Smoke-Test für das gesamte Foundation-Setup.

Output: Funktionierender Auth-Flow (register → login → JWT → Homescreen), requireRole-Middleware für Phase 2 vorbereitet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Aus Plan 01 und Plan 02 übernommene Contracts -->

Aus backend/src/db/schema.ts (Plan 02):
```typescript
export const stores: PgTable  // id uuid, name, lat, lng, checkinRadiusMeters
export const users: PgTable   // id uuid, storeId uuid FK, username, email, passwordHash, role enum('admin','volunteer','visitor'), plietschPoints int
export const items: PgTable   // id uuid, storeId uuid FK, title, category, status
```

Aus backend/src/db/client.ts (Plan 02):
```typescript
export const db: DrizzleDB
```

Aus backend/src/config.ts (Plan 02):
```typescript
export const config: { databaseUrl: string; jwtSecret: string; port: number; nodeEnv: string; }
```

Aus mobile/src/api/client.ts (Plan 01):
```typescript
export const apiClient: AxiosInstance  // baseURL: http://localhost:3000, timeout: 10000
// Token-Interceptor wird in diesem Plan hinzugefügt
```

Aus mobile/src/navigation/AppNavigator.tsx (Plan 01):
```typescript
// useIsAuthenticated() Platzhalter wird durch useAuthStore ersetzt
// AuthStack und AppStack bleiben bestehen
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend Auth-Modul (Register, Login, Middleware)</name>
  <files>
    backend/src/modules/auth/auth.types.ts,
    backend/src/modules/auth/auth.repository.ts,
    backend/src/modules/auth/auth.service.ts,
    backend/src/modules/auth/auth.router.ts,
    backend/src/middleware/auth.ts,
    backend/src/middleware/requireRole.ts,
    backend/src/app.ts
  </files>
  <read_first>
    - /Users/simonluthe/Documents/plietsche-pluenn/backend/src/db/schema.ts (users-Tabelle, Rollen-Enum)
    - /Users/simonluthe/Documents/plietsche-pluenn/backend/src/config.ts (jwtSecret)
    - /Users/simonluthe/Documents/plietsche-pluenn/backend/src/app.ts (hier wird authRouter eingebunden)
    - /Users/simonluthe/Documents/plietsche-pluenn/backend/server.js (alte JWT/bcrypt-Patterns als Referenz)
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: JWT mit storeId + role im Payload)
  </read_first>
  <action>
Verzeichnis anlegen:
```bash
mkdir -p /Users/simonluthe/Documents/plietsche-pluenn/backend/src/modules/auth
```

**backend/src/modules/auth/auth.types.ts:**
```typescript
export interface AuthUser {
  sub: string;      // userId (UUID)
  storeId: string;  // UUID
  role: 'admin' | 'volunteer' | 'visitor';
}

export interface RegisterBody {
  username: string;
  email: string;
  password: string;
  storeId: string;
}

export interface LoginBody {
  email: string;
  password: string;
}

export interface AuthResponse {
  token: string;
  user: {
    id: string;
    username: string;
    email: string;
    role: 'admin' | 'volunteer' | 'visitor';
    storeId: string;
    plietschPoints: number;
  };
}
```

**backend/src/modules/auth/auth.repository.ts:**
```typescript
import { db } from '../../db/client';
import { users } from '../../db/schema';
import { eq } from 'drizzle-orm';
import type { RegisterBody } from './auth.types';

export async function findUserByEmail(email: string) {
  const result = await db
    .select()
    .from(users)
    .where(eq(users.email, email))
    .limit(1);
  return result[0] ?? null;
}

export async function createUser(data: {
  username: string;
  email: string;
  passwordHash: string;
  storeId: string;
  role?: 'admin' | 'volunteer' | 'visitor';
}) {
  const result = await db
    .insert(users)
    .values({
      username: data.username,
      email: data.email,
      passwordHash: data.passwordHash,
      storeId: data.storeId,
      role: data.role ?? 'visitor',
    })
    .returning();
  return result[0];
}
```

**backend/src/modules/auth/auth.service.ts:**
```typescript
import bcrypt from 'bcrypt';
import jwt from 'jsonwebtoken';
import { config } from '../../config';
import * as authRepo from './auth.repository';
import type { RegisterBody, LoginBody, AuthResponse, AuthUser } from './auth.types';

const BCRYPT_ROUNDS = 12;
const JWT_EXPIRY = '7d';

export async function register(body: RegisterBody): Promise<AuthResponse> {
  const existing = await authRepo.findUserByEmail(body.email);
  if (existing) {
    const err = Object.assign(new Error('Email already registered'), { statusCode: 409 });
    throw err;
  }

  const passwordHash = await bcrypt.hash(body.password, BCRYPT_ROUNDS);
  const user = await authRepo.createUser({
    username: body.username,
    email: body.email,
    passwordHash,
    storeId: body.storeId,
  });

  const payload: AuthUser = { sub: user.id, storeId: user.storeId, role: user.role as AuthUser['role'] };
  const token = jwt.sign(payload, config.jwtSecret, { expiresIn: JWT_EXPIRY });

  return {
    token,
    user: {
      id: user.id,
      username: user.username,
      email: user.email,
      role: user.role as AuthUser['role'],
      storeId: user.storeId,
      plietschPoints: user.plietschPoints ?? 0,
    },
  };
}

export async function login(body: LoginBody): Promise<AuthResponse> {
  const user = await authRepo.findUserByEmail(body.email);
  if (!user) {
    const err = Object.assign(new Error('Invalid credentials'), { statusCode: 401 });
    throw err;
  }

  const passwordMatch = await bcrypt.compare(body.password, user.passwordHash);
  if (!passwordMatch) {
    const err = Object.assign(new Error('Invalid credentials'), { statusCode: 401 });
    throw err;
  }

  const payload: AuthUser = { sub: user.id, storeId: user.storeId, role: user.role as AuthUser['role'] };
  const token = jwt.sign(payload, config.jwtSecret, { expiresIn: JWT_EXPIRY });

  return {
    token,
    user: {
      id: user.id,
      username: user.username,
      email: user.email,
      role: user.role as AuthUser['role'],
      storeId: user.storeId,
      plietschPoints: user.plietschPoints ?? 0,
    },
  };
}
```

**backend/src/modules/auth/auth.router.ts:**
```typescript
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import * as authService from './auth.service';

const router = Router();

const RegisterSchema = z.object({
  username: z.string().min(3).max(50),
  email: z.string().email(),
  password: z.string().min(8),
  storeId: z.string().uuid(),
});

const LoginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(1),
});

// Express 5: async Fehler propagieren automatisch — kein try/catch nötig
router.post('/register', async (req: Request, res: Response) => {
  const body = RegisterSchema.parse(req.body);
  const result = await authService.register(body);
  res.status(201).json(result);
});

router.post('/login', async (req: Request, res: Response) => {
  const body = LoginSchema.parse(req.body);
  const result = await authService.login(body);
  res.json(result);
});

export default router;
```

**backend/src/middleware/auth.ts:**
```typescript
import jwt from 'jsonwebtoken';
import type { Request, Response, NextFunction } from 'express';
import { config } from '../config';
import type { AuthUser } from '../modules/auth/auth.types';

// Erweitert Express Request um user-Feld
declare global {
  namespace Express {
    interface Request {
      user?: AuthUser;
    }
  }
}

export function authenticateToken(req: Request, res: Response, next: NextFunction): void {
  const authHeader = req.headers.authorization;
  const token = authHeader?.startsWith('Bearer ') ? authHeader.slice(7) : null;

  if (!token) {
    res.status(401).json({ error: 'Authorization token required' });
    return;
  }

  // Express 5: jwt.verify wirft bei ungültigem Token → Express fängt automatisch
  const user = jwt.verify(token, config.jwtSecret) as AuthUser;
  req.user = user;
  next();
}
```

**backend/src/middleware/requireRole.ts:**
```typescript
import type { Request, Response, NextFunction } from 'express';
import type { AuthUser } from '../modules/auth/auth.types';

export function requireRole(...roles: AuthUser['role'][]) {
  return (req: Request, res: Response, next: NextFunction): void => {
    const user = req.user;
    if (!user) {
      res.status(401).json({ error: 'Authentication required' });
      return;
    }
    if (!roles.includes(user.role)) {
      res.status(403).json({ error: 'Insufficient permissions', required: roles, actual: user.role });
      return;
    }
    next();
  };
}
```

**backend/src/app.ts aktualisieren** — authRouter einbinden:
Die bestehende app.ts aus Plan 02 lesen und den Auth-Router-Kommentar durch echten Import ersetzen:
```typescript
// Diese Zeile einfügen nach den bestehenden Imports:
import authRouter from './modules/auth/auth.router';

// Diese Zeile nach dem Health-Endpoint einfügen:
app.use('/api/auth', authRouter);

// Den Kommentarblock entfernen:
// // Platzhalter für Auth-Router — Plan 03 fügt ihn hier ein:
// // import authRouter from './modules/auth/auth.router';
// // app.use('/api/auth', authRouter);
```

Auch den globalen Error Handler um statusCode-Weiterleitung erweitern:
```typescript
app.use((err: unknown, _req: Request, res: Response, _next: NextFunction) => {
  if (err instanceof ZodError) {
    return res.status(400).json({ error: 'Validation failed', issues: err.issues });
  }
  if (err instanceof Error) {
    const statusCode = (err as any).statusCode;
    if (typeof statusCode === 'number' && statusCode >= 400 && statusCode < 500) {
      return res.status(statusCode).json({ error: err.message });
    }
    console.error(err.stack);
    return res.status(500).json({ error: 'Internal server error' });
  }
  res.status(500).json({ error: 'Unknown error' });
});
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/modules/auth/auth.types.ts` exportiert `AuthUser` mit `sub`, `storeId`, `role`
    - `backend/src/middleware/auth.ts` exportiert `authenticateToken`
    - `backend/src/middleware/requireRole.ts` exportiert `requireRole`
    - `backend/src/app.ts` enthält `app.use('/api/auth', authRouter)` (kein Kommentar mehr)
    - `grep "storeId" backend/src/modules/auth/auth.types.ts` hat Treffer
    - `grep "requireRole" backend/src/middleware/requireRole.ts` hat Treffer
    - `npx tsc --noEmit` gibt keine Fehler aus
    - `curl -s -X POST http://localhost:3000/api/auth/login -H 'Content-Type: application/json' -d '{"email":"wrong@test.de","password":"wrong"}' | grep -o '"error"'` gibt einen Treffer zurück (Auth-Endpoint antwortet)
  </acceptance_criteria>
  <done>Auth-Modul mit Register, Login, JWT-Generierung und Rollen-Middleware ist vollständig. Alle drei Rollen (admin, volunteer, visitor) werden unterstützt.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile Auth-Store und Login/Register-Screens</name>
  <files>
    mobile/src/store/authStore.ts,
    mobile/src/screens/auth/LoginScreen.tsx,
    mobile/src/screens/auth/RegisterScreen.tsx,
    mobile/src/api/client.ts,
    mobile/src/navigation/AppNavigator.tsx
  </files>
  <read_first>
    - /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/navigation/AppNavigator.tsx (Platzhalter aus Plan 01 — wird aktualisiert)
    - /Users/simonluthe/Documents/plietsche-pluenn/mobile/src/api/client.ts (Axios-Instanz aus Plan 01 — Interceptor wird hinzugefügt)
    - /Users/simonluthe/Documents/plietsche-pluenn/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: React Navigation auth-basiertes Root-Routing)
  </read_first>
  <action>
**Zustand installieren** (State Management für Auth):
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn/mobile
npm install zustand
```

**mobile/src/store/authStore.ts — Zustand Auth Store:**
```typescript
import { create } from 'zustand';

export interface AuthUserState {
  id: string;
  username: string;
  email: string;
  role: 'admin' | 'volunteer' | 'visitor';
  storeId: string;
  plietschPoints: number;
}

interface AuthStore {
  token: string | null;
  user: AuthUserState | null;
  login: (token: string, user: AuthUserState) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthStore>((set) => ({
  token: null,
  user: null,
  login: (token, user) => set({ token, user }),
  logout: () => set({ token: null, user: null }),
}));
```

**mobile/src/api/client.ts aktualisieren** — Token-Interceptor hinzufügen:
Die bestehende client.ts aus Plan 01 lesen und Token-Injection ergänzen:
```typescript
import axios from 'axios';
import { useAuthStore } from '../store/authStore';

const BASE_URL = (process.env.API_BASE_URL as string | undefined) ?? 'http://localhost:3000';

export const apiClient = axios.create({
  baseURL: BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// Token-Interceptor: fügt Bearer-Token aus authStore zu jedem Request hinzu
apiClient.interceptors.request.use((reqConfig) => {
  const token = useAuthStore.getState().token;
  if (token) {
    reqConfig.headers.Authorization = `Bearer ${token}`;
  }
  return reqConfig;
});
```

**mobile/src/screens/auth/LoginScreen.tsx — vollständiger Login-Screen:**
```typescript
import React, { useState } from 'react';
import {
  View, Text, TextInput, TouchableOpacity,
  StyleSheet, ActivityIndicator, Alert,
} from 'react-native';
import type { NativeStackNavigationProp } from '@react-navigation/native-stack';
import { apiClient } from '../../api/client';
import { useAuthStore } from '../../store/authStore';

type Props = {
  navigation: NativeStackNavigationProp<any>;
};

export default function LoginScreen({ navigation }: Props): React.JSX.Element {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const login = useAuthStore((s) => s.login);

  const handleLogin = async () => {
    if (!email || !password) {
      Alert.alert('Fehler', 'Bitte E-Mail und Passwort eingeben');
      return;
    }
    setLoading(true);
    try {
      const { data } = await apiClient.post('/api/auth/login', { email, password });
      login(data.token, data.user);
      // AppNavigator wechselt automatisch zu HomeScreen
    } catch (err: any) {
      const message = err?.response?.data?.error ?? 'Login fehlgeschlagen';
      Alert.alert('Fehler', message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>Plietsche Plünn</Text>
      <TextInput
        style={styles.input}
        placeholder="E-Mail"
        value={email}
        onChangeText={setEmail}
        autoCapitalize="none"
        keyboardType="email-address"
        autoComplete="email"
      />
      <TextInput
        style={styles.input}
        placeholder="Passwort"
        value={password}
        onChangeText={setPassword}
        secureTextEntry
        autoComplete="current-password"
      />
      <TouchableOpacity
        style={[styles.button, loading && styles.buttonDisabled]}
        onPress={handleLogin}
        disabled={loading}
      >
        {loading ? (
          <ActivityIndicator color="#fff" />
        ) : (
          <Text style={styles.buttonText}>Anmelden</Text>
        )}
      </TouchableOpacity>
      <TouchableOpacity onPress={() => navigation.navigate('Register')}>
        <Text style={styles.linkText}>Noch kein Konto? Registrieren</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 24, backgroundColor: '#fff' },
  title: { fontSize: 28, fontWeight: 'bold', marginBottom: 32, textAlign: 'center' },
  input: {
    borderWidth: 1, borderColor: '#ddd', borderRadius: 8,
    padding: 12, marginBottom: 16, fontSize: 16,
  },
  button: {
    backgroundColor: '#2d6a4f', padding: 14, borderRadius: 8,
    alignItems: 'center', marginBottom: 16,
  },
  buttonDisabled: { opacity: 0.6 },
  buttonText: { color: '#fff', fontSize: 16, fontWeight: '600' },
  linkText: { textAlign: 'center', color: '#2d6a4f', fontSize: 14 },
});
```

**mobile/src/screens/auth/RegisterScreen.tsx:**
```typescript
import React, { useState } from 'react';
import {
  View, Text, TextInput, TouchableOpacity,
  StyleSheet, ActivityIndicator, Alert,
} from 'react-native';
import type { NativeStackNavigationProp } from '@react-navigation/native-stack';
import { apiClient } from '../../api/client';
import { useAuthStore } from '../../store/authStore';

// Fester storeId für Phase 1 (Single-Tenant) — wird in Phase 2+ konfigurierbar
// Diese UUID muss mit dem Seed-Store aus db/seed.ts übereinstimmen oder
// wird nach dem ersten db:seed-Lauf aus der DB gelesen und hier hinterlegt.
// Für Phase 1: nach db:seed den Store-ID aus der DB-Ausgabe hier eintragen.
const PHASE1_STORE_ID = process.env.STORE_ID ?? '00000000-0000-0000-0000-000000000000';

type Props = {
  navigation: NativeStackNavigationProp<any>;
};

export default function RegisterScreen({ navigation }: Props): React.JSX.Element {
  const [username, setUsername] = useState('');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const login = useAuthStore((s) => s.login);

  const handleRegister = async () => {
    if (!username || !email || !password) {
      Alert.alert('Fehler', 'Bitte alle Felder ausfüllen');
      return;
    }
    setLoading(true);
    try {
      const { data } = await apiClient.post('/api/auth/register', {
        username, email, password,
        storeId: PHASE1_STORE_ID,
      });
      login(data.token, data.user);
    } catch (err: any) {
      const message = err?.response?.data?.error ?? 'Registrierung fehlgeschlagen';
      Alert.alert('Fehler', message);
    } finally {
      setLoading(false);
    }
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>Registrieren</Text>
      <TextInput
        style={styles.input}
        placeholder="Benutzername (min. 3 Zeichen)"
        value={username}
        onChangeText={setUsername}
        autoCapitalize="none"
        autoComplete="username"
      />
      <TextInput
        style={styles.input}
        placeholder="E-Mail"
        value={email}
        onChangeText={setEmail}
        autoCapitalize="none"
        keyboardType="email-address"
        autoComplete="email"
      />
      <TextInput
        style={styles.input}
        placeholder="Passwort (min. 8 Zeichen)"
        value={password}
        onChangeText={setPassword}
        secureTextEntry
        autoComplete="new-password"
      />
      <TouchableOpacity
        style={[styles.button, loading && styles.buttonDisabled]}
        onPress={handleRegister}
        disabled={loading}
      >
        {loading ? (
          <ActivityIndicator color="#fff" />
        ) : (
          <Text style={styles.buttonText}>Konto erstellen</Text>
        )}
      </TouchableOpacity>
      <TouchableOpacity onPress={() => navigation.goBack()}>
        <Text style={styles.linkText}>Zurück zum Login</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 24, backgroundColor: '#fff' },
  title: { fontSize: 28, fontWeight: 'bold', marginBottom: 32, textAlign: 'center' },
  input: {
    borderWidth: 1, borderColor: '#ddd', borderRadius: 8,
    padding: 12, marginBottom: 16, fontSize: 16,
  },
  button: {
    backgroundColor: '#2d6a4f', padding: 14, borderRadius: 8,
    alignItems: 'center', marginBottom: 16,
  },
  buttonDisabled: { opacity: 0.6 },
  buttonText: { color: '#fff', fontSize: 16, fontWeight: '600' },
  linkText: { textAlign: 'center', color: '#2d6a4f', fontSize: 14 },
});
```

**mobile/src/navigation/AppNavigator.tsx aktualisieren** — Platzhalter durch echten authStore ersetzen:
Die bestehende AppNavigator.tsx aus Plan 01 lesen und useIsAuthenticated-Platzhalter ersetzen:
```typescript
import React from 'react';
import { NavigationContainer } from '@react-navigation/native';
import { createStackNavigator } from '@react-navigation/stack';
import LoginScreen from '../screens/auth/LoginScreen';
import RegisterScreen from '../screens/auth/RegisterScreen';
import HomeScreen from '../screens/HomeScreen';
import { useAuthStore } from '../store/authStore';

const Stack = createStackNavigator();

export function AppNavigator(): React.JSX.Element {
  const token = useAuthStore((s) => s.token);

  return (
    <NavigationContainer>
      <Stack.Navigator screenOptions={{ headerShown: false }}>
        {token ? (
          <Stack.Screen name="Home" component={HomeScreen} />
        ) : (
          <>
            <Stack.Screen name="Login" component={LoginScreen} />
            <Stack.Screen name="Register" component={RegisterScreen} />
          </>
        )}
      </Stack.Navigator>
    </NavigationContainer>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit 2>&1 | head -20 && grep -l "useAuthStore" src/store/authStore.ts src/screens/auth/LoginScreen.tsx src/navigation/AppNavigator.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `mobile/src/store/authStore.ts` existiert und exportiert `useAuthStore`
    - `mobile/src/store/authStore.ts` enthält `login(token, user)` und `logout()` Actions
    - `mobile/src/screens/auth/LoginScreen.tsx` enthält `apiClient.post('/api/auth/login'`
    - `mobile/src/screens/auth/RegisterScreen.tsx` existiert und enthält `apiClient.post('/api/auth/register'`
    - `mobile/src/navigation/AppNavigator.tsx` importiert `useAuthStore` (nicht mehr Platzhalter `useIsAuthenticated`)
    - `mobile/src/navigation/AppNavigator.tsx` enthält `Stack.Screen name="Register"` in AuthStack
    - `mobile/src/api/client.ts` enthält `interceptors.request.use` (Token-Interceptor)
    - `npx tsc --noEmit` gibt keine Fehler aus
    - `grep "useIsAuthenticated" mobile/src/navigation/AppNavigator.tsx` gibt keinen Treffer (Platzhalter entfernt)
  </acceptance_criteria>
  <done>Vollständiger Auth-Flow: Login-Screen ruft Backend auf, JWT wird in Zustand gespeichert, AppNavigator wechselt auf Homescreen. RegisterScreen ist als zweite Auth-Option vorhanden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile LoginScreen → POST /api/auth/login | E-Mail und Passwort aus User-Input, unvalidiert auf Mobile |
| JWT Bearer Token → authenticateToken Middleware | Token aus mobilem Speicher, kann manipuliert werden |
| requireRole Guard → Feature-Routes | Rollenprüfung nur so sicher wie JWT-Signierung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | POST /api/auth/login Brute-Force | accept | Rate Limiting vor Produktion hinzufügen (express-rate-limit); Phase 1 ist Entwicklung |
| T-03-02 | Tampering | JWT ohne Ablaufdatum | mitigate | `expiresIn: '7d'` in auth.service.ts gesetzt; Token läuft ab |
| T-03-03 | Information Disclosure | bcrypt Timing Attack via Error-Message | mitigate | Fehler "Invalid credentials" für sowohl "User nicht gefunden" als auch "Passwort falsch" — gleiche Meldung verhindert User-Enumeration |
| T-03-04 | Elevation of Privilege | `requireRole` ohne `authenticateToken` | mitigate | Dokumentation in middleware/requireRole.ts: "Muss nach authenticateToken in Chain stehen"; Phase 2 verwendet immer beide |
| T-03-05 | Information Disclosure | Zustand token in-memory | accept | Token ist nur im App-Prozess; für Produktion: SecureStore (Expo) oder Keychain (bare RN) in Phase 4 |
</threat_model>

<verification>
Register + Login Ende-zu-Ende Test (Backend muss laufen, DB muss migriert sein):

```bash
# 1. Store aus der DB lesen (für storeId)
docker exec plietsche-postgres psql -U plietsche -d plietschepluenn -c "SELECT id FROM stores LIMIT 1;"

# 2. Neuen User registrieren (STORE_ID ersetzen!)
curl -s -X POST http://localhost:3000/api/auth/register \
  -H 'Content-Type: application/json' \
  -d '{"username":"testuser","email":"test@test.de","password":"passwort123","storeId":"STORE_ID_HIER"}' \
  | jq '.token, .user.role'

# 3. Mit neuem User einloggen
curl -s -X POST http://localhost:3000/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"test@test.de","password":"passwort123"}' \
  | jq '.token'

# 4. requireRole testen — ohne Token muss 401 kommen
curl -s -X GET http://localhost:3000/api/health \
  -H 'Authorization: Bearer ungueltigertoken' \
  | jq '.error'
```

TypeScript-Checks:
```bash
cd /Users/simonluthe/Documents/plietsche-pluenn/backend && npx tsc --noEmit
cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit
```
</verification>

<success_criteria>
1. `POST /api/auth/register` → HTTP 201, `{ token, user: { role: "visitor", storeId, ... } }`
2. `POST /api/auth/login` → HTTP 200, JWT-Token im Response-Body
3. JWT-Payload enthält `sub` (userId), `storeId`, `role`
4. `authenticateToken` mit gültigem Token → 200; mit ungültigem Token → 401
5. `requireRole('volunteer')` mit visitor-Token → 403
6. Mobile App: Login-Screen → Credentials eingeben → Homescreen erscheint
7. `npx tsc --noEmit` ohne Fehler auf Backend und Mobile
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-foundation/01-03-SUMMARY.md` erstellen mit:
- Auth-Endpunkte und curl-Testergebnisse
- JWT-Payload-Struktur (Beispiel dekodiert)
- iOS Simulator Smoke-Test: Login → Homescreen Navigation
- Hinweise zu PHASE1_STORE_ID (wie Developer die echte Store-UUID einträgt)
- Abweichungen vom Plan (falls aufgetreten)
</output>
