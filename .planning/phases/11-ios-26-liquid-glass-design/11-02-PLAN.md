---
phase: 11-ios-26-liquid-glass-design
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - mobile/src/navigation/AppNavigator.tsx
autonomous: true
requirements: [LG-03, LG-04]

must_haves:
  truths:
    - "Tab-Bar ist floating und transluzent (BlurView) auf iOS, semi-transparent auf Android"
    - "Navigation-Header ist transluzent mit Blur auf iOS"
    - "Gradient-Akzentfarbe bleibt sichtbar im Header durch Tint"
    - "Font Awesome Icons bleiben unveraendert"
  artifacts:
    - path: "mobile/src/navigation/AppNavigator.tsx"
      provides: "Glassmorphism Tab-Bar und Header"
      contains: "BlurView"
  key_links:
    - from: "AppNavigator.tsx tabBarStyle"
      to: "@react-native-community/blur"
      via: "tabBarBackground mit BlurView"
      pattern: "tabBarBackground"
    - from: "AppNavigator.tsx headerStyle"
      to: "glass Token"
      via: "headerTransparent + headerBackground"
      pattern: "headerBackground"
---

<objective>
Tab-Bar und Navigation-Header auf iOS 26 Liquid Glass umstellen -- floating Tab-Bar mit Blur, transluzenter Header.

Purpose: Tab-Bar und Header sind die prominentesten Chrome-Elemente. Glassmorphism hier hat den groessten visuellen Impact auf das iOS 26 Feeling.
Output: AppNavigator.tsx mit BlurView-Tab-Bar und Glass-Header fuer alle drei Tab-Navigatoren (Visitor, Admin, Volunteer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@mobile/src/navigation/AppNavigator.tsx
@mobile/src/theme/index.ts

<interfaces>
<!-- Aus 11-01 erstellt. Executor braucht diese direkt. -->
From mobile/src/theme/index.ts (nach Plan 01 erweitert):
```typescript
export const glass = {
  blurType: 'chromeMaterial' as const,
  blurAmount: 20,
  tintColors: ['rgba(255,255,255,0.18)', 'rgba(255,255,255,0.08)'] as const,
  borderColor: 'rgba(255,255,255,0.35)',
  borderWidth: 1,
  androidBackground: 'rgba(255,255,255,0.82)',
  androidBorderColor: 'rgba(0,0,0,0.08)',
  shadowColor: '#000',
  shadowOpacity: 0.12,
  shadowRadius: 12,
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab-Bar auf Glassmorphism umstellen</name>
  <files>mobile/src/navigation/AppNavigator.tsx</files>
  <action>
Lese mobile/src/navigation/AppNavigator.tsx vollstaendig. Dann nehme folgende Aenderungen vor:

1. Import ergaenzen:
```typescript
import { BlurView } from '@react-native-community/blur';
import { Platform, StyleSheet, View } from 'react-native'; // View und StyleSheet ergaenzen falls nicht vorhanden
import { glass } from '../theme'; // glass aus theme importieren
```

2. Das `tabBarStyle`-Objekt in `tabScreenOptions` (die gemeinsame Konstante) austauschen:
```typescript
// ALT:
tabBarStyle: {
  backgroundColor: colors.surface,
  borderTopColor: colors.border,
},

// NEU (floating glass tab bar):
tabBarStyle: Platform.select({
  ios: {
    position: 'absolute',
    bottom: 20,
    left: 20,
    right: 20,
    borderRadius: borderRadius.full,
    backgroundColor: 'transparent',
    borderTopWidth: 0,
    elevation: 0,
    shadowColor: glass.shadowColor,
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.18,
    shadowRadius: 20,
    height: 72,
    paddingBottom: 10,
  },
  android: {
    backgroundColor: glass.androidBackground,
    borderTopColor: glass.androidBorderColor,
    elevation: 8,
  },
}),
tabBarBackground: Platform.OS === 'ios'
  ? () => (
      <BlurView
        blurType={glass.blurType}
        blurAmount={glass.blurAmount}
        style={[StyleSheet.absoluteFillObject, { borderRadius: borderRadius.full }]}
        reducedTransparencyFallbackColor="white"
      />
    )
  : undefined,
```

3. Das GLEICHE gilt fuer die VisitorTabs-Komponente -- sie hat ihr eigenes screenOptions-Objekt (kein Verweis auf tabScreenOptions). Exakt dieselben tabBarStyle + tabBarBackground Aenderungen dort eintragen.

4. borderRadius aus theme importieren falls noch nicht: `import { colors, fonts, borderRadius } from '../theme';` -> `import { colors, fonts, borderRadius, glass } from '../theme';`

Wichtig: Alle drei Tab-Navigatoren (AdminTabs, VolunteerTabs, VisitorTabs) sollen die gleiche Tab-Bar erhalten. AdminTabs und VolunteerTabs nutzen tabScreenOptions -- das eine Update dort deckt beide ab. VisitorTabs hat ein eigenes Objekt -- das muss separat angepasst werden.

TypeScript-Check danach: cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>tabBarStyle nutzt position:absolute + borderRadius:full auf iOS, tabBarBackground liefert BlurView, TypeScript fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: Navigation-Header transluzent mit Gradient-Tint</name>
  <files>mobile/src/navigation/AppNavigator.tsx</files>
  <action>
Lese mobile/src/navigation/AppNavigator.tsx erneut (nach Task 1). Dann passe den Header an:

1. In `tabScreenOptions` die Header-Optionen anpassen:
```typescript
// ALT:
headerStyle: {
  backgroundColor: colors.primary,
},
headerTintColor: colors.white,

// NEU (Glass Header mit Gradient-Tint):
headerTransparent: true,
headerStyle: {
  backgroundColor: 'transparent',
},
headerBackground: () => (
  <View style={StyleSheet.absoluteFillObject}>
    {Platform.OS === 'ios' && (
      <BlurView
        blurType={glass.blurType}
        blurAmount={glass.blurAmount}
        style={StyleSheet.absoluteFillObject}
        reducedTransparencyFallbackColor={colors.primary}
      />
    )}
    <LinearGradient
      colors={['rgba(39,176,146,0.75)', 'rgba(128,180,226,0.65)']}
      start={{ x: 0, y: 0 }}
      end={{ x: 1, y: 0 }}
      style={StyleSheet.absoluteFillObject}
    />
  </View>
),
headerTintColor: colors.white,
```

2. LinearGradient importieren falls nicht bereits importiert:
```typescript
import LinearGradient from 'react-native-linear-gradient';
```

3. Die gleiche headerTransparent + headerBackground Aenderung AUCH in VisitorTabs screenOptions vornehmen (da es ein eigenes Objekt hat).

4. Im AuthenticatedStack (Stack.Navigator) ebenfalls den Header anpassen:
```typescript
screenOptions={{
  headerShown: false,
  headerTransparent: true,
  headerStyle: { backgroundColor: 'transparent' },
  headerBackground: () => (
    <View style={StyleSheet.absoluteFillObject}>
      {Platform.OS === 'ios' && (
        <BlurView
          blurType={glass.blurType}
          blurAmount={glass.blurAmount}
          style={StyleSheet.absoluteFillObject}
        />
      )}
      <LinearGradient
        colors={['rgba(39,176,146,0.75)', 'rgba(128,180,226,0.65)']}
        start={{ x: 0, y: 0 }}
        end={{ x: 1, y: 0 }}
        style={StyleSheet.absoluteFillObject}
      />
    </View>
  ),
  headerTintColor: colors.white,
  headerTitleStyle: { fontFamily: fonts.bold, fontSize: 17 },
}}
```

Wichtig: Die Gradient-Farben sind rgba-Varianten der bestehenden colors.gradientStart (#27b092) und colors.gradientEnd (#80b4e2) mit 0.75 bzw. 0.65 Opacity. Das bewahrt die Markenfarben sichtbar hinter dem Glas.

TypeScript-Check: cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>headerTransparent:true gesetzt, headerBackground liefert BlurView + LinearGradient-Tint, kein TypeScript-Fehler</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Navigation Chrome | Tab-Bar und Header zeigen nur UI-eigene Elemente -- keine User-Daten direkt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Spoofing | headerTransparent | accept | Header ist Teil der App, kein externer Inhalt sichtbar -- Gradient-Tint macht Origin klar erkennbar |
| T-11-04 | Denial of Service | BlurView im Header (immer sichtbar) | mitigate | blurAmount=20 ist moderat; chromeMaterial ist performanter als xLight/dark auf iOS |
</threat_model>

<verification>
- npx tsc --noEmit zeigt 0 Fehler
- tabBarStyle auf iOS hat position:'absolute', bottom:20, borderRadius:full
- tabBarBackground liefert BlurView (iOS) / undefined (Android)
- headerTransparent:true in allen Tab-Navigatoren
- headerBackground liefert BlurView + Gradient-Tint
</verification>

<success_criteria>
- Floating Tab-Bar mit voller Breite, abgerundet, BlurView-Hintergrund auf iOS
- Header mit transluzenter Gradient-Ueberlagerung auf iOS und Android
- Alle drei Tab-Navigatoren (Visitor, Admin, Volunteer) identisch gestaltet
- Keine TypeScript-Fehler
</success_criteria>

<output>
Nach Fertigstellung: .planning/phases/11-ios-26-liquid-glass-design/11-02-SUMMARY.md erstellen
</output>
