---
phase: 11-ios-26-liquid-glass-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/ios/Podfile.lock
  - mobile/src/theme/index.ts
  - mobile/src/components/GlassCard.tsx
autonomous: true
requirements: [LG-01, LG-02]

must_haves:
  truths:
    - "@react-native-community/blur ist installiert und auf iOS verlinkt"
    - "Theme exportiert glass-Token (blur-Staerke, Opacity, Border-Farbe)"
    - "GlassCard-Komponente existiert und rendert BlurView mit Gradient-Tint auf iOS, semi-transparent auf Android"
  artifacts:
    - path: "mobile/src/components/GlassCard.tsx"
      provides: "Wiederverwendbare Glassmorphism-Card-Komponente"
      exports: ["GlassCard", "GlassCardProps"]
    - path: "mobile/src/theme/index.ts"
      provides: "glass-Namespace mit Blur-/Opacity-Werten"
      contains: "glass"
  key_links:
    - from: "mobile/src/components/GlassCard.tsx"
      to: "@react-native-community/blur"
      via: "BlurView import"
      pattern: "BlurView"
    - from: "mobile/src/components/GlassCard.tsx"
      to: "react-native-linear-gradient"
      via: "LinearGradient fuer Tint"
      pattern: "LinearGradient"
---

<objective>
Liquid Glass Basis-Schicht einrichten: Paket installieren, Theme erweitern, GlassCard-Komponente erstellen.

Purpose: Alle nachfolgenden Plaene (Tab-Bar, Screen-Cards) bauen auf dieser Komponente auf. Ohne sie kein Glassmorphism.
Output: Installiertes @react-native-community/blur, erweitertes Theme, fertige GlassCard-Komponente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mobile/src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: @react-native-community/blur installieren und iOS verlinken</name>
  <files>mobile/package.json, mobile/ios/Podfile.lock</files>
  <action>
Fuhre aus: cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npm install @react-native-community/blur

Danach pod install: cd ios && pod install

Wichtig: Der Befehl MUSS im mobile/-Verzeichnis ausgefuehrt werden. Das Paket bringt BlurView fuer iOS (native UIVisualEffectView) und einen reinen View-Fallback fuer Android.

Pruefe nach der Installation, dass @react-native-community/blur in package.json unter dependencies steht.

TypeScript-Check: cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && node -e "require('./node_modules/@react-native-community/blur/package.json'); console.log('OK')"</automated>
  </verify>
  <done>@react-native-community/blur in node_modules vorhanden, keine TypeScript-Fehler</done>
</task>

<task type="auto">
  <name>Task 2: Theme um glass-Namespace erweitern</name>
  <files>mobile/src/theme/index.ts</files>
  <action>
Erweitere mobile/src/theme/index.ts. Lese die Datei zuerst vollstaendig, dann fuege nach dem bestehenden borderRadius-Export folgendes hinzu:

```typescript
export const glass = {
  // iOS: BlurView-Typ fuer @react-native-community/blur
  blurType: 'chromeMaterial' as const,  // passt zu iOS 26 frosted glass
  blurAmount: 20,

  // Tint-Gradient ueber dem Blur (sehr leicht transluzent)
  tintColors: ['rgba(255,255,255,0.18)', 'rgba(255,255,255,0.08)'] as const,

  // Card-Rahmen
  borderColor: 'rgba(255,255,255,0.35)',
  borderWidth: 1,

  // Android-Fallback (kein Blur-Support)
  androidBackground: 'rgba(255,255,255,0.82)',
  androidBorderColor: 'rgba(0,0,0,0.08)',

  // Schatten fuer Glass-Cards
  shadowColor: '#000',
  shadowOpacity: 0.12,
  shadowRadius: 12,
};
```

Die bestehenden exports (colors, fonts, spacing, borderRadius) unveraendert lassen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>glass-Export in theme/index.ts vorhanden, TypeScript compileert fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 3: GlassCard-Komponente erstellen</name>
  <files>mobile/src/components/GlassCard.tsx</files>
  <action>
Erstelle mobile/src/components/GlassCard.tsx -- eine wiederverwendbare Komponente die auf iOS BlurView + LinearGradient-Tint kombiniert, auf Android semi-transparent faellt zurueck:

```typescript
import React from 'react';
import { Platform, StyleSheet, View, ViewStyle } from 'react-native';
import LinearGradient from 'react-native-linear-gradient';
import { BlurView } from '@react-native-community/blur';
import { borderRadius, glass } from '../theme';

export interface GlassCardProps {
  children: React.ReactNode;
  style?: ViewStyle;
  /** Border-Radius -- Standard: borderRadius.lg (16) */
  radius?: number;
  /** Blur-Staerke uebersteuern -- Standard: glass.blurAmount (20) */
  blurAmount?: number;
}

export function GlassCard({ children, style, radius = borderRadius.lg, blurAmount = glass.blurAmount }: GlassCardProps) {
  const containerStyle: ViewStyle = {
    borderRadius: radius,
    overflow: 'hidden',
    borderWidth: glass.borderWidth,
    borderColor: glass.borderColor,
    shadowColor: glass.shadowColor,
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: glass.shadowOpacity,
    shadowRadius: glass.shadowRadius,
    elevation: 4,
  };

  if (Platform.OS === 'android') {
    // Android-Fallback: kein nativer Blur
    return (
      <View style={[containerStyle, { backgroundColor: glass.androidBackground, borderColor: glass.androidBorderColor }, style]}>
        {children}
      </View>
    );
  }

  return (
    <View style={[containerStyle, style]}>
      <BlurView
        blurType={glass.blurType}
        blurAmount={blurAmount}
        style={StyleSheet.absoluteFillObject}
        reducedTransparencyFallbackColor="white"
      />
      <LinearGradient
        colors={[...glass.tintColors]}
        start={{ x: 0, y: 0 }}
        end={{ x: 1, y: 1 }}
        style={StyleSheet.absoluteFillObject}
      />
      <View style={styles.content}>
        {children}
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  content: {
    position: 'relative',
    zIndex: 1,
  },
});
```

Wichtig: Der `content`-View mit position:relative + zIndex:1 stellt sicher dass children UBER dem Blur-Layer gerendert werden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/plietsche-pluenn/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>GlassCard.tsx existiert, exportiert GlassCard und GlassCardProps, TypeScript fehlerfrei</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| iOS Blur | UIVisualEffectView rendert was hinter der View liegt -- keine sensiblen Daten hinter Glass-Cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | BlurView | accept | Hinter Glass-Cards stehen nur App-eigene Views (Gradient-Background), keine fremden Inhalte |
| T-11-02| Denial of Service | BlurView mit hohem blurAmount | mitigate | blurAmount auf max 25 begrenzen; hohe Werte koennen auf alten Geraeten laggen |
</threat_model>

<verification>
- npm install erfolgreich abgeschlossen
- pod install in ios/ ausgefuehrt
- npx tsc --noEmit zeigt 0 Fehler
- GlassCard.tsx exportiert GlassCard und GlassCardProps
- theme/index.ts exportiert glass-Namespace
</verification>

<success_criteria>
- @react-native-community/blur in package.json und node_modules
- glass-Export in theme/index.ts mit allen Token-Werten
- GlassCard-Komponente funktioniert auf iOS (BlurView) und Android (Fallback)
- TypeScript compiliert fehlerfrei
</success_criteria>

<output>
Nach Fertigstellung: .planning/phases/11-ios-26-liquid-glass-design/11-01-SUMMARY.md erstellen
</output>
